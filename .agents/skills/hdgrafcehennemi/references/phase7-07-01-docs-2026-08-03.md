# Phase 7 plan 07-01 docs deliverables + grep-gate table (2026-08-03)

Plan 07-01 was a docs-only plan (type: execute, autonomous, requirements DOCS-01/DOCS-02).
Task 1 (07-AUDIT.md) was committed by the previous executor as `8e286ed`; the continuation
executor (this session) did Tasks 2–3. All commits `docs(07-01): ...` on `main`.

## Deliverable map (where later plans find their contract)

| File | Content | Who depends on it |
|---|---|---|
| `.planning/phases/07-spoiler-safety-hardening/07-AUDIT.md` | Leak-channel inventory: 6 direct (D1–D6) + 22 indirect (I1–I22) rows with real symbols, boundary-plumbing table, `visible_until_order`/`visible_from_order` grep counts per file, gap summary with owning plans, D-01 stack verdict (keep Neo4j/FastAPI/driver/React/Vite/Cytoscape/Google/HttpOnly; reject Memgraph/GQLAlchemy/Next.js/JWT/Redis/second graph DB/frontend rewrite/unrestricted Cypher) | 07-02..07-08 (owning-plan column) |
| `docs/SPOILER-THREAT-MODEL.md` | D-19 shape: per-class enforcement layer / backend query-service / frontend behavior / test coverage / fail-closed rule; D-25 completion gate; 26-row regression matrix mapping classes to real `backend/tests/` files + canonical pytest/vitest invocations | 07-03..07-08 regression work |
| `docs/SPOILER-TERMINOLOGY.md` | Locked vocabulary: `visible_from_order` canonical (D-02, rejected names listed), fail-closed rule + no `coalesce(visible_from_order,1)` (D-03), watched/view/effective progress model + invariant (D-05), publication-order authority / never compare code strings (D-09). **§6 "Central visibility-policy service contract" = the D-04 contract 07-02 implements (`backend/app/spoiler/policy.py`: validate_visibility_order, is_visible, effective_view_order, require_visible_resource, filter_public_metadata, mask_episode_metadata, assert_visibility_invariants)** | 07-02 (implements §6), all later plans (vocabulary) |
| `docs/SPOILER-DEFERRED-DESIGN.md` | D-17/D-18 invariants: Person/ACTED_AS/APPEARS_IN (`episodes_seen_so_far`, never last appearance), reviews (`spoiler_up_to_order`), ratings (watched-only), trivia (`visible_from_order`), recommendations, awards, external wiki, movie-series publication-order note. No placeholder tables/UI | 07-08 regression guards; future milestone |
| `.planning/phases/07-spoiler-safety-hardening/07-01-SUMMARY.md` | Frontmatter + coverage block (`kind: other`, grep commands as `ref`, `human_judgment: false`) + real verification output | 07-02 plan-phase context |

## Grep-gate table (acceptance greps that must pass before commit)

Plan-level: `grep -c "regression matrix" docs/SPOILER-THREAT-MODEL.md` >= 1;
`grep -c "Central visibility-policy service contract" docs/SPOILER-TERMINOLOGY.md` >= 1;
`git diff --check` clean; all four artifacts exist.

| Doc | Token | Min |
|---|---|---|
| SPOILER-THREAT-MODEL.md | `regression matrix` | 1 |
| SPOILER-THREAT-MODEL.md | indirect-class sample `title\|synopsis\|autocomplete\|degree\|citation` | 1 (24 actual) |
| SPOILER-TERMINOLOGY.md | `fails closed` | 1 (2 actual) |
| SPOILER-TERMINOLOGY.md | `visible_from_order` | 3 (11 actual) |
| SPOILER-TERMINOLOGY.md | `def effective_view_order` (literal, in code block) | 1 |
| SPOILER-TERMINOLOGY.md | each of validate_visibility_order / is_visible / require_visible_resource / filter_public_metadata / mask_episode_metadata / assert_visibility_invariants | 1 each |
| SPOILER-DEFERRED-DESIGN.md | `episodes_seen_so_far` | 1 (2 actual) |
| SPOILER-DEFERRED-DESIGN.md | `last appearance` | 1 |

Key: `def effective_view_order` greps hit ONLY because the contract section writes signatures as
literal `def` lines inside a ```python code block. Prose like "the effective_view_order function
returns the min" would fail the gate. Write docs so the acceptance grep lands.

## Execution mechanics that worked

- Read plan `<verify>`/`<acceptance_criteria>` BEFORE writing each doc; write tokens to satisfy them.
- Commit per doc (4 doc commits + 1 SUMMARY commit), not per task: `c81f95b` threat model,
  `dc0aa00` terminology, `8fc6a40` deferred design, `1bac593` contract §6 append, `47fc865` SUMMARY.
- Pre-existing working-tree state (`.planning/STATE.md`, `.planning/config.json` modified; root
  `ROADMAP.md` + `HD_GRAF_CEHENNEMI_CODING_AGENT_SPEC_V2.md` deleted; `.hermes/` untracked) was left
  untouched; every commit staged explicit paths only.
- One fix: `git diff --check` flagged "new blank line at EOF" after the §6 patch append — removed
  the trailing blank line via targeted patch (old_string = last content line + `\n` → last content
  line) and re-checked. Run `git diff --check` after every doc edit.
- No test suites run (docs-only plan); SUMMARY documented that explicitly.
