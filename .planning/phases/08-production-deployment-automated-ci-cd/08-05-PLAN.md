---
phase: 08-production-deployment-automated-ci-cd
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - pyproject.toml
  - uv.lock
  - backend/app/cache/redis_client.py
  - backend/app/services/rate_limit.py
  - backend/app/core/config.py
  - backend/app/main.py
  - backend/app/api/auth.py
  - backend/app/api/chat.py
  - backend/app/api/user_content.py
  - backend/tests/test_rate_limit.py
  - backend/tests/test_auth.py
autonomous: false
requirements: [SEC-03]
user_setup:
  - service: upstash
    why: "Redis-backed rate-limit counters, multi-worker-safe (SEC-03, D-14); Redis database itself was provisioned in the tracer plan (08-01)"
    env_vars:
      - name: REDIS_URL
        source: "Upstash Console -> Redis database -> Details -> rediss:// TLS connection string (already collected in 08-01's user_setup)"

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Login, chat-send (both message endpoints), and content-write endpoints (notes, custom nodes, custom relationships) enforce a rate limit per user/IP (SEC-03, D-13)"
    - "Exceeding the limit returns 429 in the existing sanitized error envelope (too_many_requests code)"
    - "The rate-limit store is Redis-backed and correct under Render's multi-worker deployment, not an in-memory per-process counter (D-14)"
    - "Session storage remains in Neo4j (Neo4jSessionRepository) — not migrated to Redis this phase, per D-17's discretion"
  artifacts:
    - path: "backend/app/cache/redis_client.py"
      provides: "Shared redis.asyncio client singleton, reused by rate limiting here and by the graph-cache plan"
    - path: "backend/app/services/rate_limit.py"
      provides: "fastapi-limiter init + identifier/callback wiring, reusing the existing too_many_requests error code"
  key_links:
    - from: "backend/app/main.py (lifespan)"
      to: "backend/app/services/rate_limit.py::init_rate_limiter"
      via: "await init_rate_limiter(settings.redis_url) alongside the existing database.open() call"
      pattern: "init_rate_limiter"
    - from: "backend/app/api/auth.py, api/chat.py, api/user_content.py"
      to: "backend/app/services/rate_limit.py"
      via: "RateLimiter(...) FastAPI dependency added to each rate-limited route"
      pattern: "RateLimiter"
---

<objective>
Add a shared Redis client and a Redis-backed, multi-worker-safe rate limiter
covering login, chat-send, and content-write endpoints, closing
docs/PROBLEMS.md #6 (no rate limiting, in-memory slot breaks under
`--workers N`). The Redis client this plan creates is deliberately shared
with the later graph-response-cache plan (INFRA-02) rather than each plan
standing up its own connection.

Purpose: Bound LLM cost and abuse exposure with a limiter that stays correct
regardless of how many Render worker processes run.
Output: Redis client singleton, rate-limit dependency wired onto the three
requirement-named route groups, 429 on exceed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-deployment-automated-ci-cd/08-CONTEXT.md
@.planning/phases/08-production-deployment-automated-ci-cd/08-RESEARCH.md
@backend/app/main.py
@backend/app/core/config.py
@backend/app/core/errors.py
@backend/app/api/auth.py
@backend/app/api/chat.py
@backend/app/api/user_content.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify redis and fastapi-limiter package legitimacy before install</name>
  <read_first>.planning/phases/08-production-deployment-automated-ci-cd/08-RESEARCH.md (section: "Package Legitimacy Audit")</read_first>
  <what-built>08-RESEARCH.md's Package Legitimacy Audit flags both `redis` (redis-py) and `fastapi-limiter` as SUS — not because of any actual red flag, but because the automated legitimacy tool could not fetch PyPI download telemetry for either package (an "unknown-downloads" tooling limitation, not a finding about the packages themselves).</what-built>
  <how-to-verify>
    Open https://pypi.org/project/redis/ and confirm it links to
    github.com/redis/redis-py (the official Redis Inc. organization), has a
    long release history, and matches the 8.1.0-era version referenced in
    08-RESEARCH.md. Open https://pypi.org/project/fastapi-limiter/ and
    confirm it links to github.com/long2ice/fastapi-limiter with a
    plausible release history matching the 0.2.0-era version referenced.
  </how-to-verify>
  <acceptance_criteria>
    - Both PyPI project pages resolve to their claimed GitHub source repos
    - Neither package shows signs of a recent takeover, typosquat name, or suspicious maintainer change
  </acceptance_criteria>
  <resume-signal>Type "approved" once both packages are confirmed legitimate on pypi.org, or describe any concern found.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redis client, rate limiter wiring, dependency on rate-limited routes</name>
  <read_first>backend/app/main.py, backend/app/core/config.py, backend/app/core/errors.py, backend/app/api/auth.py, backend/app/api/chat.py, backend/app/api/user_content.py</read_first>
  <precondition>Task 1 approved the redis and fastapi-limiter packages for install; Upstash REDIS_URL from 08-01's user_setup is available.</precondition>
  <files>pyproject.toml, uv.lock, backend/app/cache/redis_client.py, backend/app/services/rate_limit.py, backend/app/core/config.py, backend/app/main.py, backend/app/api/auth.py, backend/app/api/chat.py, backend/app/api/user_content.py</files>
  <behavior>
    - Test: rate_limit_identifier(request), given a request whose request.state carries an authenticated user, returns "user:{id}"; given no user in state, returns "ip:{client.host}".
    - Test: rate_limit_callback(request, response, pexpire) raises an http_error with status 429 and code "too_many_requests" (the existing lowercase code already used at backend/app/api/chat.py and backend/app/core/errors.py's _ERROR_SPECS[429] — not a new invented code).
  </behavior>
  <action>
    Run `uv add redis fastapi-limiter` (updates pyproject.toml and uv.lock).

    Create backend/app/cache/redis_client.py exporting a
    `@lru_cache`-decorated `get_redis() -> Redis` singleton (matching
    `core/config.py::get_settings`'s existing pattern) that constructs
    `redis.asyncio.Redis.from_url(get_settings().redis_url)` — the URL is
    expected to be a `rediss://` TLS Upstash connection string. This module
    is intentionally the ONE shared Redis connection point both this plan
    and the later graph-cache plan import from, avoiding two separate Redis
    clients.

    In backend/app/core/config.py, add `redis_url: str = Field(default="",
    description="Upstash Redis connection string (rediss://...), used for
    rate-limit counters and the graph query response cache. Empty disables
    both — see services/rate_limit.py and cache/redis_client.py.")`.

    Create backend/app/services/rate_limit.py per 08-RESEARCH.md's Code
    Examples section: `rate_limit_identifier(request: Request) -> str`
    (reads `request.state.user` if present, else falls back to
    `request.client.host`), `rate_limit_callback(request, response,
    pexpire) -> None` (raises `http_error(429, "too_many_requests", "Too
    many requests. Please slow down.")` — reusing the exact code already
    used at `backend/app/api/chat.py`'s `_too_many_requests()` and
    `core/errors.py`'s `_ERROR_SPECS[429]`, never inventing a new
    uppercase code), and `async def init_rate_limiter() -> None` calling
    `FastAPILimiter.init(get_redis(), identifier=rate_limit_identifier,
    http_callback=rate_limit_callback)`. Verify the exact
    `FastAPILimiter.init`/`RateLimiter` parameter names against the
    installed version with `python -c "import fastapi_limiter; help(fastapi_limiter)"`
    before finalizing this file — 08-RESEARCH.md flags this library's
    internals as having shifted across recent releases (Assumption A5).

    In backend/app/main.py's `lifespan`, call `await init_rate_limiter()`
    right after `database.open()`, guarded by `if
    get_settings().redis_url:` (an empty `redis_url` — e.g. in a local dev
    environment without Upstash configured — disables rate limiting rather
    than crashing startup).

    Add a `RateLimiter(...)` dependency (concrete times/seconds window per
    route, chosen here as: login 10 requests / 5 minutes per IP; chat-send
    [both `post_message` and `stream_message`] 20 requests / minute per
    user; content-write [notes and custom-node/custom-relationship
    create/update/delete in user_content.py] 30 requests / minute per
    user-or-IP) to: `google_auth` in api/auth.py; `post_message` and
    `stream_message` in api/chat.py; every POST/PATCH/DELETE route in
    api/user_content.py. Where a route has no authenticated user available
    (user_content.py's routes have no `CurrentUserDependency` today — that
    remains Phase 9 scope, not this plan's), `rate_limit_identifier` falls
    back to IP automatically, so no route-level change beyond adding the
    dependency is needed.

    Ensure test app fixtures that build a bare `FastAPI()` + router (not the
    full `backend.app.main.app` with its lifespan) either call
    `FastAPILimiter.init` against a throwaway/mocked Redis in a fixture, or
    override the `RateLimiter` dependency via
    `app.dependency_overrides` to a no-op — otherwise every existing test
    hitting a now-rate-limited route raises an uninitialized-limiter error
    rather than running the route logic. Apply this to
    backend/tests/test_auth.py's existing test app fixture specifically,
    since `google_auth` is now rate-limited.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/test_auth.py backend/tests/test_chat_api.py backend/tests/test_user_content_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - redis and fastapi-limiter are added to pyproject.toml/uv.lock
    - get_redis() is the single shared Redis client constructor, imported by both rate_limit.py and (in the later cache plan) graph_cache.py
    - init_rate_limiter is called from main.py's lifespan, guarded on redis_url being non-empty
    - google_auth, post_message, stream_message, and every user_content.py mutation route carry a RateLimiter dependency
    - rate_limit_callback raises the existing "too_many_requests" code, not a new one
    - Existing tests in test_auth.py, test_chat_api.py, test_user_content_api.py pass with the rate limiter either overridden or initialized against a test double
  </acceptance_criteria>
  <done>A Redis-backed rate limiter is wired into login, chat-send, and content-write routes; existing tests pass with the limiter properly bypassed or initialized in test fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Rate-limit wiring unit tests</name>
  <read_first>backend/app/services/rate_limit.py, backend/app/core/errors.py</read_first>
  <files>backend/tests/test_rate_limit.py</files>
  <action>
    Create backend/tests/test_rate_limit.py testing the two pure functions
    directly (no live Redis needed, per this codebase's "don't hand-roll,
    trust the library" convention — the sliding/fixed-window correctness
    itself is fastapi-limiter's tested responsibility, not this repo's):
    `rate_limit_identifier` given a fake `Request`-like object with
    `request.state.user = {"id": "user:1"}` returns `"user:user:1"`; given
    no `user` attribute on `request.state` (or `None`), returns
    `"ip:{request.client.host}"`. `rate_limit_callback` raises an
    `HTTPException` with `status_code == 429` and
    `exc.detail["code"] == "too_many_requests"` when called (mock/stub the
    `Request`/`Response` arguments minimally — the function only reads
    `request.client.host` inside `rate_limit_identifier`, and
    `rate_limit_callback` doesn't need real request data at all, only that
    it raises correctly).
  </action>
  <verify>
    <automated>uv run pytest backend/tests/test_rate_limit.py -x</automated>
  </verify>
  <acceptance_criteria>
    - test_rate_limit.py exists and covers both rate_limit_identifier branches (user-present, user-absent) and rate_limit_callback's 429/too_many_requests raise
    - No test in this file opens a real network connection to Redis
  </acceptance_criteria>
  <done>backend/tests/test_rate_limit.py exists and passes, covering the rate-limit wiring's pure-function surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Any client <-> login/chat-send/content-write endpoints | Previously unbounded request volume, now counted per user-or-IP in a shared store |
| npm/pip package install <-> production dependency tree | New third-party packages (redis, fastapi-limiter) entering the supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Denial of Service | Unbounded LLM/API request volume, operator pays for all of it (docs/PROBLEMS.md #6) | high | mitigate | Redis-backed RateLimiter dependency on login/chat-send/content-write |
| T-08-05-02 | Denial of Service | In-memory per-process limit multiplied under multiple Render workers | high | mitigate | Shared Redis counter store, correct regardless of worker count (D-14) |
| T-08-05-SC | Tampering | Supply-chain risk on newly installed packages (redis, fastapi-limiter), both SUS-flagged by the automated legitimacy tool | high | mitigate | Blocking human-verify checkpoint (Task 1) confirming both resolve to their official GitHub source repos before install |
| T-08-05-03 | Denial of Service | Anonymous content-write rate limiting falls back to IP, weaker than per-user | low | accept | Documented, acceptable fallback tier — ownership binding on these routes is Phase 9/PROB-01 scope, not this plan's |

</threat_model>

<verification>
Run `uv run pytest -x` for the full backend suite, then confirm
`backend/app/main.py`'s lifespan calls `init_rate_limiter` guarded on
`redis_url`.
</verification>

<success_criteria>
Login, chat-send, and content-write routes are rate-limited through a shared,
Redis-backed, multi-worker-safe store; exceeding the limit returns 429 in the
existing error envelope; the full test suite remains green.
</success_criteria>

<output>
Create `.planning/phases/08-production-deployment-automated-ci-cd/08-05-SUMMARY.md` when done
</output>
