---
phase: 12-post-hardening-remediation-and-code-quality
plan: 09
type: execute
wave: 2
depends_on: [08]
files_modified: [
  "frontend/src/index.css",
  "frontend/src/lib/tokens/graphTokens.ts",
  "frontend/src/lib/nodeTypes.ts",
  "frontend/src/components/graph/graphStylesheet.ts",
  "frontend/src/components/graph/relationshipStyles.ts",
  "frontend/src/components/detail/DetailPanel.tsx",
  "frontend/src/components/detail/tabs/ClaimsTab.tsx",
  "frontend/src/components/detail/tabs/EvidenceTab.tsx"
]
autonomous: true
requirements: [THERMO-P2-06]
estimate: {tokens: 15000, raw_tokens: 15000, tasks: 3, confidence: high}
must_haves:
  truths:
    - "Design tokens for entity nodes, relationship families, claim/evidence badges, and Cytoscape styling are centralized in a single graphTokens.ts module."
    - "Hardcoded hex color constants in DetailPanel, nodeTypes.ts, and GraphCanvas are replaced by semantic token references or CSS custom properties."
    - "Dark theme styling across canvas, drawers, and badges adheres strictly to contrast and token guidelines."
    - "44px minimum touch targets are enforced across interactive UI elements and buttons."
  artifacts:
    - path: "frontend/src/lib/tokens/graphTokens.ts"
      provides: "Authoritative design token palette for graph visualization, entities, and relationships"
    - path: "frontend/src/index.css"
      provides: "CSS theme custom properties for claim and evidence accent colors"
  key_links:
    - from: "frontend/src/components/graph/graphStylesheet.ts"
      to: "frontend/src/lib/tokens/graphTokens.ts"
      via: "Stylesheet consuming centralized graph tokens"
---

<objective>
Harmonize the UI/UX design system and eliminate design token drift:
1. Centralize all visual design tokens into `frontend/src/lib/tokens/graphTokens.ts` (node colors, edge category styles, highlight glows, claim and evidence accent colors).
2. Register `--color-accent-claim` (`#D946EF`) and `--color-accent-evidence` (`#FB923C`) in `frontend/src/index.css` `@theme` block.
3. Migrate `nodeTypes.ts`, `graphStylesheet.ts`, `relationshipStyles.ts`, and `DetailPanel` tabs to consume centralized tokens rather than duplicated ad-hoc hex values.
4. Standardize touch target utilities (`min-h-[44px]`, `min-w-[44px]`) across button components for mobile and touch accessibility.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-CONTEXT.md
@frontend/src/index.css
@frontend/src/lib/nodeTypes.ts
@frontend/src/components/graph/graphStylesheet.ts
@frontend/src/components/graph/relationshipStyles.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: Centralize design tokens in graphTokens.ts and update index.css</name>
  <files>
    frontend/src/lib/tokens/graphTokens.ts,
    frontend/src/index.css
  </files>
  <action>
1. Create `frontend/src/lib/tokens/graphTokens.ts`:
   - Export node type palette: `Character` (`#38BDF8`), `Location` (`#2DD4BF`), `Event` (`#60A5FA`), `Organization` (`#FB7185`), `Object` (`#FBBF24`), `Claim` (`#D946EF`), `Evidence` (`#FB923C`).
   - Export edge family palette: `STRUCTURAL`, `CANONICAL_CLAIM`, `USER_NOTE`, `EXPANSION`, `PATH_HIGHLIGHT`.
   - Export canvas background, selection glow, pulse animations, and opacity tokens.
2. In `frontend/src/index.css`:
   - Add semantic accent tokens to the `@theme` block:
     ```css
     --color-accent-claim: #d946ef;
     --color-accent-evidence: #fb923c;
     ```
  </action>
</task>

<task type="execute">
  <name>Task 2: Migrate styling files to centralized tokens</name>
  <files>
    frontend/src/lib/nodeTypes.ts,
    frontend/src/components/graph/graphStylesheet.ts,
    frontend/src/components/graph/relationshipStyles.ts,
    frontend/src/components/detail/tabs/ClaimsTab.tsx,
    frontend/src/components/detail/tabs/EvidenceTab.tsx
  </files>
  <action>
1. In `frontend/src/lib/nodeTypes.ts`:
   - Replace hardcoded hex codes in `NODE_TYPE_CONFIG` with imports from `graphTokens.ts`.
2. In `frontend/src/components/graph/graphStylesheet.ts` and `relationshipStyles.ts`:
   - Replace raw hex codes (`#1E2740`, `#334155`, `#94A3B8`, `#7C3AED`, etc.) with imported token references.
3. In `ClaimsTab.tsx` and `EvidenceTab.tsx`:
   - Replace local `CLAIM_ACCENT_COLOR` / `EVIDENCE_ACCENT_COLOR` constants with centralized tokens or Tailwind theme classes (`text-accent-claim`, `bg-accent-claim/10`).
  </action>
</task>

<task type="test">
  <name>Task 3: Run Vitest tests and check styling consistency</name>
  <files>frontend/src/components/graph/graphElements.test.ts, frontend/src/components/graph/relationshipStyles.test.ts</files>
  <action>
Run `npx vitest run` in `frontend/` to confirm stylesheet tests, node type tests, and element builders remain 100% green.
  </action>
</task>

</tasks>
