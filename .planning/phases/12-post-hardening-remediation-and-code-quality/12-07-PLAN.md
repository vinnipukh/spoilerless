---
phase: 12-post-hardening-remediation-and-code-quality
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/src/components/detail/DetailPanel.tsx",
  "frontend/src/components/graph/PathFinder.tsx",
  "frontend/src/components/series/SeriesDashboard.tsx",
  "frontend/src/components/detail/RevisionHistoryPanel.tsx",
  "frontend/src/components/chat/ChatPanel.tsx",
  "frontend/src/api/export.ts",
  "frontend/src/types/graph.ts",
  "frontend/src/types/chat.ts",
  "frontend/src/types/changeSet.ts",
  "spoilerless/app/domain/user_content.py",
  "spoilerless/app/repository/user_content.py"
]
autonomous: true
requirements: [
  THERMO-P1-03,
  THERMO-P1-04,
  THERMO-P1-05,
  THERMO-P1-06,
  THERMO-P3-08,
  THERMO-P3-09,
  THERMO-P3-10
]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 4, confidence: high}
must_haves:
  truths:
    - "CreateRelationshipDialog selects the episode with the highest episode_order by default instead of lexicographical UUID comparison."
    - "Note creation supports attaching notes across all supported CustomNodeType labels without 404/409 errors."
    - "TypeScript interfaces in frontend/src/types match backend Pydantic models for relationship_effect, effective_view_order, content_hash, status, and revert_revision_id."
    - "fetchExportMarkdown escapes the seriesId route parameter with encodeURIComponent."
    - "PathFinder renders distinct icons and accessible labels for Clear Path vs Exit Path Finder."
    - "ChatPanel differentiates concurrent generation limit ('busy') from rate limit exceeded ('rate_limited')."
    - "SeriesDashboard synchronizes keyboard arrow navigation with scrollIntoView; DetailPanel and RevisionHistoryPanel restore focus after delete/revert operations."
  artifacts:
    - path: "frontend/src/types/graph.ts"
      provides: "Updated GraphClaim, GraphResponse, and GraphEvidence TypeScript models"
    - path: "frontend/src/components/detail/DetailPanel.tsx"
      provides: "Correct episode order comparison and note target handling"
    - path: "frontend/src/components/graph/PathFinder.tsx"
      provides: "Distinct reset icon for clear action"
  key_links:
    - from: "frontend/src/types/graph.ts"
      to: "spoilerless/app/domain/graph.py"
      via: "Type matching on GraphClaim and GraphResponse"
---

<objective>
Fix critical frontend and full-stack contract bugs:
1. Fix episode order comparison in `CreateRelationshipDialog` (numerical `episode_order` rather than string ID).
2. Fix note creation on non-Character nodes by expanding `NoteTargetType` support in `domain/user_content.py` and `repository/user_content.py`.
3. Synchronize TypeScript interfaces (`graph.ts`, `chat.ts`, `changeSet.ts`) with backend models.
4. Add `encodeURIComponent` to `export.ts`.
5. Fix `PathFinder.tsx` ambiguous duplicate `X` buttons with a distinct `RotateCcw` / reset icon.
6. Fix chat error classification in `ChatPanel.tsx` to distinguish concurrency limits from Redis rate limits.
7. Enhance accessibility: add `scrollIntoView` to `SeriesDashboard.tsx` keyboard navigation and manage focus restoration after deletes/reverts.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-CONTEXT.md
@frontend/src/components/detail/DetailPanel.tsx
@frontend/src/components/graph/PathFinder.tsx
@frontend/src/components/series/SeriesDashboard.tsx
@frontend/src/types/graph.ts
@frontend/src/types/chat.ts
@frontend/src/types/changeSet.ts
@frontend/src/api/export.ts
@spoilerless/app/domain/user_content.py
@spoilerless/app/repository/user_content.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Fix DetailPanel note targets, relationship default episode, and export API encoding</name>
  <files>
    frontend/src/components/detail/DetailPanel.tsx,
    frontend/src/api/export.ts,
    spoilerless/app/domain/user_content.py,
    spoilerless/app/repository/user_content.py
  </files>
  <action>
1. In `frontend/src/components/detail/DetailPanel.tsx` inside `CreateRelationshipDialog`:
   - Replace `const highest = episodes.reduce((a, b) => a.id > b.id ? a : b)` with:
     ```typescript
     const highest = episodes.reduce((a, b) => a.episode_order > b.episode_order ? a : b)
     ```
2. In `frontend/src/api/export.ts`:
   - Wrap `seriesId` in `encodeURIComponent(seriesId)`.
3. In `spoilerless/app/domain/user_content.py` and `spoilerless/app/repository/user_content.py`:
   - Extend `NoteTargetType` enum to include `LOCATION = "Location"`, `EVENT = "Event"`, `ORGANIZATION = "Organization"`, `OBJECT = "Object"` alongside `CHARACTER` and `CLAIM`.
   - Update `NOTE_CREATE_QUERIES` dictionary in `repository/user_content.py` to support matching the respective node labels dynamically or per type.
   - In `DetailPanel.tsx`, map `selectedNode.type` directly to the matching `NoteTargetType` (e.g. `'Character'`, `'Location'`, `'Event'`, `'Organization'`, `'Object'`, `'Claim'`).
  </action>
</task>

<task type="execute">
  <name>Task 2: Synchronize TypeScript interfaces with backend Pydantic models</name>
  <files>
    frontend/src/types/graph.ts,
    frontend/src/types/chat.ts,
    frontend/src/types/changeSet.ts
  </files>
  <action>
1. In `frontend/src/types/graph.ts`:
   - Update `GraphClaim.relationship_effect`: `relationship_effect: string | number | null`
   - Update `GraphResponse`: add `effective_view_order?: number`
   - Update `GraphEvidence`: make `content_hash?: string | null`
2. In `frontend/src/types/chat.ts`:
   - Update `ChatMessage`: add `status?: 'pending' | 'completed' | 'failed'`
3. In `frontend/src/types/changeSet.ts`:
   - Update `ChangeSet`: add `revert_revision_id?: string | null`
  </action>
</task>

<task type="execute">
  <name>Task 3: Fix PathFinder duplicate icons, ChatPanel error messages, and accessibility</name>
  <files>
    frontend/src/components/graph/PathFinder.tsx,
    frontend/src/components/chat/ChatPanel.tsx,
    frontend/src/components/series/SeriesDashboard.tsx,
    frontend/src/components/detail/RevisionHistoryPanel.tsx
  </files>
  <action>
1. In `frontend/src/components/graph/PathFinder.tsx`:
   - Replace the first `<X className="h-4 w-4" />` clear button with `<RotateCcw className="h-4 w-4" />` and explicit title/tooltip "Clear path selection".
   - Keep the second `X` button as "Exit path finder".
2. In `frontend/src/components/chat/ChatPanel.tsx`:
   - Differentiate `status === 429` with `error_code === 'CONCURRENT_GENERATION_LIMIT'` vs generic rate limit (`RATE_LIMIT_EXCEEDED` / `TOO_MANY_REQUESTS`).
   - If rate limited by Redis bucket, display: *"You have exceeded the message rate limit. Please wait a minute before sending another message."*
3. In `frontend/src/components/series/SeriesDashboard.tsx`:
   - Add a `cardRefs` array ref and call `cardRefs.current[activeIndex]?.scrollIntoView({ block: 'nearest' })` inside the keyboard handler when `activeIndex` changes.
4. In `frontend/src/components/detail/RevisionHistoryPanel.tsx` and `DetailPanel.tsx`:
   - After note deletion or revert mutation completes, explicitly refocus the panel container or primary action button instead of dropping focus to `document.body`.
  </action>
</task>

<task type="test">
  <name>Task 4: Verify frontend tests and contract behavior</name>
  <files>frontend/src/components/detail/DetailPanel.test.tsx, frontend/src/components/graph/PathFinder.test.tsx</files>
  <action>
Run `npm run test` (or `npx vitest run`) inside `frontend/` to ensure all existing and updated component tests pass cleanly.
  </action>
</task>

</tasks>
