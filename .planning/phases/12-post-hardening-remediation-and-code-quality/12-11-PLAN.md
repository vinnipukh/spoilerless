---
phase: 12-post-hardening-remediation-and-code-quality
plan: 11
type: execute
wave: 3
depends_on: [02, 06]
files_modified: [
  "spoilerless/app/revisions/__init__.py",
  "spoilerless/app/revisions/repository.py",
  "spoilerless/app/revisions/service.py",
  "spoilerless/app/api/revisions.py",
  "spoilerless/app/graph/candidates.py",
  "spoilerless/app/repository/change_set.py",
  "spoilerless/app/repository/user_content.py",
  "spoilerless/app/api/exceptions.py",
  "spoilerless/tests/test_revisions.py",
  "spoilerless/tests/test_error_handlers.py"
]
autonomous: true
requirements: [THERMO-P3-05]
estimate: {tokens: 16000, raw_tokens: 16000, tasks: 3, confidence: high}
must_haves:
  truths:
    - "The revisions package is a named two-module layout: revisions/repository.py owns RevisionRepository + REVISION_CREATE_QUERY + REVISION_GET_QUERY; revisions/service.py owns revert_revision_work + _REVERT_LABEL_ALLOWLIST + _IMMUTABLE_FIELDS. revisions/__init__.py holds no logic and no re-export shim (all importers updated in this same plan; `grep -rn "spoilerless.app.revisions" spoilerless/app spoilerless/tests` returns only the new submodule paths)."
    - "Every one of the 10 http_error raise sites inside revert_revision_work is converted to a domain exception with a byte-identical envelope (Table A in Task 2); the data layer no longer raises HTTP errors from inside the write transaction."
    - "_SENTINEL_SPECS in api/exceptions.py contains the 5 uniform revision entries (RevisionNotFound 404 RESOURCE_NOT_FOUND, RevisionForbidden 403 FORBIDDEN, RevisionCannotRevertCreate 422 CANNOT_REVERT_CREATE, RevisionCannotRevertCanonical 409 CANNOT_REVERT_CANONICAL, RevisionAlreadyExists 409 RESOURCE_ALREADY_EXISTS) and the revert route keeps exactly ONE explicit catch: RevisionInvalidAction -> http_error(422, \"INVALID_ACTION\", str(exc))."
    - "The revert route body is otherwise unchanged (command build -> execute_write -> model_validate); _REVERT_LABEL_ALLOWLIST and the SEC-GR-014 interpolation guards are untouched."
  artifacts:
    - path: "spoilerless/app/revisions/service.py"
      provides: "Named revision service module with domain exceptions"
      exports: ["revert_revision_work", "RevisionNotFound", "RevisionForbidden", "RevisionCannotRevertCreate", "RevisionCannotRevertCanonical", "RevisionAlreadyExists", "RevisionInvalidAction", "_REVERT_LABEL_ALLOWLIST"]
    - path: "spoilerless/app/revisions/repository.py"
      provides: "Named revision repository module"
      exports: ["RevisionRepository", "REVISION_CREATE_QUERY", "REVISION_GET_QUERY"]
  key_links:
    - from: "spoilerless/app/api/revisions.py"
      to: "spoilerless/app/revisions/service.py"
      via: "Top-level imports of revert_revision_work and RevisionInvalidAction"
      pattern: "from spoilerless.app.revisions.service import"
    - from: "spoilerless/app/api/exceptions.py"
      to: "spoilerless/app/revisions/service.py"
      via: "Sentinel registry entries"
      pattern: "RevisionNotFound"
  prohibitions:
    - "Do not change any revert envelope: status code, error code, or message byte-for-byte across all 10 raise-site conversions (Table A)."
    - "Do not touch _REVERT_LABEL_ALLOWLIST contents or the SEC-GR-014 label-interpolation guards in the DELETED/restore Cypher."
    - "Do not alter route signatures, response models, or the OpenAPI surface (test_openapi_contract.py stays green)."
    - "Security breadcrumb: no OWASP/injection/GDPR item is minted here — the transaction-callback fail-closed behavior (unregistered exception -> 500) is the hard constraint the sentinel audit exists for."
---

<objective>
Name the anonymous `revisions` package and unseam it from HTTP (Candidate C3, 12-RESEARCH.md §3.3): split `revisions/__init__.py` (340 lines holding repository, queries, and the revert business flow) into `revisions/repository.py` + `revisions/service.py`, update all four importers in the same plan (no back-compat shim), and convert the 10 `http_error` calls inside `revert_revision_work` into domain exceptions registered in `api/exceptions.py::_SENTINEL_SPECS` with byte-identical envelopes — so the data layer stops raising HTTP errors from inside the write transaction and the revert route stays a one-liner.
</objective>

<context>
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-RESEARCH.md (Candidate C3, §3.3)
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-CONTEXT.md (THERMO-P3-05)
@spoilerless/app/revisions/__init__.py (FULL FILE — 12-06 Task 3 has already landed its hygiene fixes before this plan executes)
@spoilerless/app/api/exceptions.py
@spoilerless/app/api/revisions.py
@spoilerless/app/graph/candidates.py
@spoilerless/app/repository/change_set.py
@spoilerless/app/repository/user_content.py
@spoilerless/tests/test_revisions.py
@spoilerless/tests/test_error_handlers.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Split revisions/__init__.py into named repository and service modules</name>
  <files>spoilerless/app/revisions/__init__.py, spoilerless/app/revisions/repository.py, spoilerless/app/revisions/service.py, spoilerless/app/api/revisions.py, spoilerless/app/graph/candidates.py, spoilerless/app/repository/change_set.py, spoilerless/app/repository/user_content.py</files>
  <read_first>spoilerless/app/revisions/__init__.py (FULL FILE — 12-06 cleaned it: single CustomNodeType import, single before_snapshot deserialization), then `grep -rn "spoilerless.app.revisions" spoilerless/app spoilerless/tests` to enumerate every importer (expect: api/revisions.py, graph/candidates.py, repository/change_set.py, repository/user_content.py, tests/test_revisions.py)</read_first>
  <action>
1. Create `spoilerless/app/revisions/repository.py` containing: `REVISION_CREATE_QUERY`, `REVISION_GET_QUERY`, and the whole `RevisionRepository` class (static tx-callback methods `_to_json`, `_from_json`, `log_revision`, `take_snapshot`) — moved verbatim from the current `__init__.py`.
2. Create `spoilerless/app/revisions/service.py` containing: `_REVERT_LABEL_ALLOWLIST`, `_IMMUTABLE_FIELDS`, and `revert_revision_work(tx, command)` — moved verbatim. Import `RevisionRepository` from `spoilerless.app.revisions.repository`, `RevisionAction` from `spoilerless.app.domain.revision`. The module must NOT import `spoilerless.app.core.errors` after Task 2 (http_error leaves the data layer).
3. Rewrite `revisions/__init__.py` to contain ONLY the package docstring (e.g. "Named submodules: repository.py (queries + RevisionRepository), service.py (revert business flow + domain exceptions).") — NO re-export shim, NO logic, NO imports of the moved symbols.
4. Update the four importers to the new paths, keeping imported names identical:
   - `api/revisions.py`: `from spoilerless.app.revisions.service import revert_revision_work, RevisionInvalidAction` (Task 2 adds the catch) and `from spoilerless.app.revisions.repository import REVISION_GET_QUERY`.
   - `graph/candidates.py`: whatever it imports from `spoilerless.app.revisions` (check: log_revision / RevisionRepository) -> `from spoilerless.app.revisions.repository import ...` (or .service if it imports revert).
   - `repository/change_set.py` and `repository/user_content.py`: same treatment (they import revision logging from the package).
   - `tests/test_revisions.py`: update any direct `from spoilerless.app.revisions import ...` to the submodule paths.
5. Git-rename awareness: `git mv` is NOT required (keep plain file moves); ensure `git status` shows the new files and the shrunken __init__.py.
  </action>
  <acceptance_criteria>
- `grep -rn "from spoilerless.app.revisions import\|from spoilerless.app import revisions" spoilerless/app spoilerless/tests` returns NOTHING (no package-root imports remain).
- `grep -c "class RevisionRepository" spoilerless/app/revisions/repository.py` == 1 and `grep -c "async def revert_revision_work" spoilerless/app/revisions/service.py` == 1.
- `wc -l spoilerless/app/revisions/__init__.py` < 10 (docstring only).
- `unset PYTHONPATH && uv run python -c "from spoilerless.app.main import app"` exits 0 (app imports cleanly after the split).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Introduce domain exceptions and register the sentinels</name>
  <files>spoilerless/app/revisions/service.py, spoilerless/app/api/exceptions.py, spoilerless/app/api/revisions.py</files>
  <read_first>spoilerless/app/api/exceptions.py (FULL — module docstring documents the context-varying-message precedent at lines 12-14), spoilerless/app/revisions/service.py (the moved revert_revision_work), spoilerless/app/api/revisions.py (revert_revision route lines 146-177)</read_first>
  <action>
1. In `revisions/service.py`, define six exception classes (plain Exception subclasses carrying the message): `RevisionNotFound`, `RevisionForbidden`, `RevisionCannotRevertCreate`, `RevisionCannotRevertCanonical`, `RevisionAlreadyExists`, `RevisionInvalidAction` (init takes `message: str`; `__str__` returns it).
2. Replace the 10 `raise http_error(...)` calls with raises per Table A, byte-identical envelope:
   | # | current line | envelope today | exception to raise |
   |:-:|:---|:---|:---|
   | 1 | 184 | 404 RESOURCE_NOT_FOUND "Resource not found." | RevisionNotFound |
   | 2 | 189 | 422 CANNOT_REVERT_CREATE "Cannot revert a Creation revision." | RevisionCannotRevertCreate |
   | 3 | 199 | 422 INVALID_ACTION "Cannot revert revision with an unknown resource type." | RevisionInvalidAction(same message) |
   | 4 | 204 | 422 INVALID_ACTION "Cannot revert revision with an unknown target type." | RevisionInvalidAction(same message) |
   | 5 | 218 | 404 RESOURCE_NOT_FOUND "Resource not found." | RevisionNotFound |
   | 6 | 222 | 409 CANNOT_REVERT_CANONICAL "Cannot revert a canonical or candidate resource." | RevisionCannotRevertCanonical |
   | 7 | 232 | 403 FORBIDDEN "This resource belongs to another user." | RevisionForbidden |
   | 8 | 270 | 403 FORBIDDEN "This resource belongs to another user." | RevisionForbidden |
   | 9 | 283 | 409 RESOURCE_ALREADY_EXISTS "This resource has already been re-created." | RevisionAlreadyExists |
   | 10 | 321 | 422 INVALID_ACTION f"Cannot revert revision with action: {action.value}" | RevisionInvalidAction(same message) |
   Remove the `http_error` import from service.py (audit: no other http_error call remains in the module).
3. In `api/exceptions.py`, add to `_SENTINEL_SPECS` the 5 uniform entries: (RevisionNotFound, 404, "RESOURCE_NOT_FOUND", "Resource not found."), (RevisionForbidden, 403, "FORBIDDEN", "This resource belongs to another user."), (RevisionCannotRevertCreate, 422, "CANNOT_REVERT_CREATE", "Cannot revert a Creation revision."), (RevisionCannotRevertCanonical, 409, "CANNOT_REVERT_CANONICAL", "Cannot revert a canonical or candidate resource."), (RevisionAlreadyExists, 409, "RESOURCE_ALREADY_EXISTS", "This resource has already been re-created."). Import them from `spoilerless.app.revisions.service`.
4. In `api/revisions.py` revert route, wrap the `execute_write` call in exactly ONE try/except: `except RevisionInvalidAction as exc: raise http_error(422, "INVALID_ACTION", str(exc))`. Route body otherwise unchanged.
  </action>
  <acceptance_criteria>
- `grep -n "http_error" spoilerless/app/revisions/service.py` returns NOTHING (data layer unseamed from HTTP errors).
- `grep -n "RevisionInvalidAction" spoilerless/app/api/revisions.py` shows the import + exactly one `except RevisionInvalidAction` block.
- `grep -c "RevisionNot" spoilerless/app/api/exceptions.py` >= 5 registry entries; the only message-varying code (INVALID_ACTION) is handled by the route catch, matching the documented ChangeSetConflict precedent (exceptions.py:12-14).
- `unset PYTHONPATH && uv run python -c "from spoilerless.app.main import app"` exits 0.
  </acceptance_criteria>
</task>

<task type="test">
  <name>Task 3: Envelope parity tests and registry pinning</name>
  <files>spoilerless/tests/test_revisions.py, spoilerless/tests/test_error_handlers.py</files>
  <read_first>spoilerless/tests/test_revisions.py (existing revert envelope assertions — assert-status/code/message style), spoilerless/tests/test_error_handlers.py (module docstring may pin registry contents/size — extend deliberately, do not break)</read_first>
  <action>
Follow scratch-series + teardown discipline (never `series_dexter`, never user `ae8a41b7-db96-40e8-b6c2-2e3c69aedb11`; drift-agnostic asserts; create user content as the ACTING scratch user, revert as owner/non-owner/admin as required). Add to `test_revisions.py`:
1. `test_revert_envelope_byte_parity_all_codes`: one parametrized test covering the 6 codes — 404 RESOURCE_NOT_FOUND (missing revision), 422 CANNOT_REVERT_CREATE (revert a CREATED revision), 422 INVALID_ACTION (unknown action via a crafted revision row, or the existing path that triggers it), 409 CANNOT_REVERT_CANONICAL (revert UPDATED canonical resource), 403 FORBIDDEN (revert another user's resource as non-admin), 409 RESOURCE_ALREADY_EXISTS (revert DELETED revision whose resource was re-created) — asserting status AND `detail.code` AND `detail.message` byte-identical to Table A.
2. `test_revert_success_logs_reverted_revision`: revert succeeds and logs a REVERTED revision with the acting user's id (assert on the scratch revision's own id, never global counts).
If `test_error_handlers.py` pins the registry (e.g. asserts the exact _SENTINEL_SPECS tuple), extend the pin deliberately to include the 5 new revision entries — do not weaken the existing assertions.
  </action>
  <acceptance_criteria>
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_revisions.py -q` passes (old + new tests).
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_error_handlers.py -q` passes.
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_change_set_revision.py spoilerless/tests/test_user_content_api.py spoilerless/tests/test_candidate_review.py -q` passes (revision logging via the change_set/user_content/candidates importers unchanged).
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_openapi_contract.py -q` passes (no route surface change).
  </acceptance_criteria>
  <validation>
Run the four pytest invocations above sequentially (never two pytest processes in parallel against the shared AuraDB).
  </validation>
</task>

</tasks>

<verification>
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_revisions.py spoilerless/tests/test_error_handlers.py -q` → all pass (envelope byte-parity + registry).
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_change_set_revision.py spoilerless/tests/test_user_content_api.py spoilerless/tests/test_candidate_review.py spoilerless/tests/test_openapi_contract.py -q` → pass (importers unchanged, OpenAPI locked).
- Full backend suite (10-chunk runner) green; scratch series + teardown only; never `series_dexter`.
</verification>

## Artifacts this phase produces

- `spoilerless/app/revisions/repository.py` (new): `RevisionRepository` (class), `REVISION_CREATE_QUERY`, `REVISION_GET_QUERY`.
- `spoilerless/app/revisions/service.py` (new): `revert_revision_work`, `_REVERT_LABEL_ALLOWLIST`, `_IMMUTABLE_FIELDS`, domain exceptions `RevisionNotFound`, `RevisionForbidden`, `RevisionCannotRevertCreate`, `RevisionCannotRevertCanonical`, `RevisionAlreadyExists`, `RevisionInvalidAction`.
- `spoilerless/app/revisions/__init__.py`: reduced to a package docstring (no shim).
- `spoilerless/app/api/exceptions.py`: +5 `_SENTINEL_SPECS` entries.
- `spoilerless/app/api/revisions.py`: imports from submodules; revert route gains one `except RevisionInvalidAction` catch.
- `spoilerless/app/graph/candidates.py`, `spoilerless/app/repository/change_set.py`, `spoilerless/app/repository/user_content.py`: import paths updated.
- `spoilerless/tests/test_revisions.py`: parametrized envelope-parity test + REVERTED-log test.
- `spoilerless/tests/test_error_handlers.py`: registry pin extended (only if it pins contents).

## Flagged assumptions (unresolved probe edges)

The phase edge probe surfaced 15 edges (6 unclassified, 2 boundary, 2 precision, 2 idempotency, 3 concurrency) against the 12-CONTEXT.md requirement texts; all remained **UNRESOLVED** (specless fallback protocol: never auto-resolve, never dismiss). Edges assigned to this plan:

- **[E-UNC-2] unclassified edge — UNRESOLVED (probe):** revision-log visibility vs revert authorization interplay (a revision row visible at the boundary but whose resource is no longer owned). Current behavior (owner check against the CURRENT resource or the stored snapshot, fail-closed to admin for unowned) is preserved exactly by the exception conversion; the edge remains open. Executor must not add new authorization logic.

Remaining unresolved edges are flagged in their best-fitting plans: E-BND-1/E-BND-2/E-PRC-1/E-UNC-1 → 12-10; E-PRC-2/E-CON-1/E-UNC-3 → 12-12; E-CON-2/E-IDP-1/E-UNC-4 → 12-13; E-UNC-5 → 12-14; E-IDP-2/E-CON-3/E-UNC-6 → 12-15.