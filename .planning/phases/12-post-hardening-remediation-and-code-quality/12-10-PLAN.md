---
phase: 12-post-hardening-remediation-and-code-quality
plan: 10
type: execute
wave: 2
depends_on: [02]
files_modified: [
  "spoilerless/app/api/share.py",
  "spoilerless/app/api/boundary.py",
  "spoilerless/app/api/candidates.py",
  "spoilerless/tests/test_share_api.py",
  "spoilerless/tests/test_security_boundary.py",
  ".planning/STATE.md"
]
autonomous: true
requirements: [THERMO-P1-01, THERMO-P3-01, THERMO-P3-04]
estimate: {tokens: 14000, raw_tokens: 14000, tasks: 3, confidence: high}
must_haves:
  truths:
    - "create_share_link resolves its boundary through ONE call to resolve_effective_boundary(service, progress_service, series_id, user, payload.visible_until_order) — the inline clamp (progress lookup + min + effective_view_order + resolve_boundary + 422) at api/share.py:59-77 is deleted and `effective_view_order` is no longer imported anywhere in api/share.py."
    - "Share creation envelope is byte-identical: no progress record -> order 1; order past persisted progress -> response visible_until_order equals the clamped value; non-persisted order -> 422 INVALID_VISIBLE_UNTIL_ORDER exactly 'visible_until_order must identify a persisted episode order.'"
    - "The candidates omitted-boundary guard is a single shared helper (require_boundary in api/boundary.py) used at every route that must 422 on a missing boundary; no inline duplicated guard blocks remain."
    - "OpenAPI surface unchanged: POST /api/share and all candidates routes keep exact signatures, params, and response models (test_openapi_contract.py + test_frontend_contract_doc.py still green)."
  artifacts:
    - path: "spoilerless/app/api/boundary.py"
      provides: "require_boundary guard + updated module docstring listing share in the D-01 gated-route set"
      exports: ["resolve_effective_boundary", "require_boundary"]
  key_links:
    - from: "spoilerless/app/api/share.py"
      to: "spoilerless/app/api/boundary.py"
      via: "Single clamp call in create_share_link"
      pattern: "resolve_effective_boundary"
    - from: "spoilerless/app/api/candidates.py"
      to: "spoilerless/app/api/boundary.py"
      via: "require_boundary guard import"
      pattern: "require_boundary"
  prohibitions:
    - "Do not change the share route signature, response model, error envelope strings, or the 422 code INVALID_VISIBLE_UNTIL_ORDER."
    - "Do not bypass resolve_effective_boundary or reintroduce any inline boundary clamp in api/share.py."
    - "Do not alter the candidates None-guard 422 envelope (code INVALID_REQUEST, message byte-identical)."
    - "Security breadcrumb: no OWASP/injection/GDPR item is minted here — this is a behavioral-preserving consolidation; existing fail-closed boundary semantics must remain byte-identical."
---

<objective>
Close the last boundary-resolution bypass found by the architecture-deepening research (Candidate C2, 12-RESEARCH.md §3.2): `create_share_link` in `api/share.py` re-implements the clamp logic inline (progress lookup → min → `effective_view_order` → `resolve_boundary` → 422) instead of calling the single fail-closed authority `resolve_effective_boundary`, and imports `spoiler.policy` directly. Replace the inline block with one resolver call (envelope byte-identical), extract the duplicated candidates omitted-boundary guard into `require_boundary` in `api/boundary.py`, and pin the clamp/422 behavior with tests.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-CONTEXT.md
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-RESEARCH.md (Candidate C2, §3.2)
@spoilerless/app/api/share.py
@spoilerless/app/api/boundary.py
@spoilerless/app/api/candidates.py
@spoilerless/app/spoiler/policy.py
@spoilerless/tests/test_share_api.py
@spoilerless/tests/test_security_boundary.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Replace the inline share clamp with one resolve_effective_boundary call</name>
  <files>spoilerless/app/api/share.py, spoilerless/app/api/boundary.py</files>
  <read_first>spoilerless/app/api/share.py (lines 39-92), spoilerless/app/api/boundary.py (full file — 12-02 already re-typed it), spoilerless/app/spoiler/policy.py (effective_view_order signature)</read_first>
  <action>
In `spoilerless/app/api/share.py`:
1. Import `resolve_effective_boundary` from `spoilerless.app.api.boundary` (file already imports `api.deps`; add the boundary import).
2. In `create_share_link`, DELETE the inline clamp block (current lines 59-77: `requested = payload.visible_until_order` … `raise http_error(422, "INVALID_VISIBLE_UNTIL_ORDER", "visible_until_order must identify a persisted episode order.")`) and replace it with exactly one call:
   `effective = await resolve_effective_boundary(service, progress_service, payload.series_id, user, payload.visible_until_order)`
3. Pass `visible_until_order=effective` to `share_repo.create(...)` (the `requested` variable no longer exists).
4. Delete the now-unused imports: `from spoilerless.app.spoiler.policy import effective_view_order` and — once `get_share_graph` no longer needs them in 12-12 (NOT this plan) — leave `GraphService`, `VISIBLE_NODE_LABELS`, `USER_RELATIONSHIP_TYPES`, `ProgressServiceDependency` untouched. This plan ONLY removes `effective_view_order` and adds the boundary import.
5. Keep `service = GraphService(database)` as-is: the migration to a deps-level `GraphServiceDependency` is Candidate C1 (plan 12-12) and must not be pre-empted here.
6. In `spoilerless/app/api/boundary.py`: update the module docstring so the D-01 gated-route list explicitly includes `share` (create_share_link) alongside graph/candidates/notes/custom nodes/relationships/revisions/episodes/visualization/expand/path/export.
  </action>
  <acceptance_criteria>
- `grep -n "effective_view_order" spoilerless/app/api/share.py` returns nothing (import and usage both gone).
- `grep -n "resolve_effective_boundary" spoilerless/app/api/share.py` shows exactly one call site, inside `create_share_link`, with argument order `(service, progress_service, payload.series_id, user, payload.visible_until_order)`.
- No `min(`, `view_as_of_order`, or `requested_view` identifiers remain in `create_share_link`.
- `grep -n "share" spoilerless/app/api/boundary.py` shows the module docstring mentioning the share route in the gated-route list.
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_share_api.py -q` passes with zero failures (existing creation/clamp tests unchanged).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Extract require_boundary and replace the duplicated candidates guards</name>
  <files>spoilerless/app/api/boundary.py, spoilerless/app/api/candidates.py</files>
  <read_first>spoilerless/app/api/candidates.py (current lines 55-70, 185-245 — three byte-identical `visible_until_order is None` guards at lines ~61, ~189, ~235), spoilerless/app/api/boundary.py</read_first>
  <action>
1. In `spoilerless/app/api/boundary.py`, add:
   `def require_boundary(visible_until_order: int | None) -> int:` — raises `http_error(422, "INVALID_REQUEST", "visible_until_order is required to read candidates — an omitted boundary must never default to every visibility level.")` when `visible_until_order is None`, else returns the value. Reuse the same `http_error` import the module already has after 12-02.
2. In `spoilerless/app/api/candidates.py`, replace EVERY inline `if visible_until_order is None: raise http_error(422, "INVALID_REQUEST", ...)` block (grep `INVALID_REQUEST` — currently 3 sites: ~line 61, ~line 189, ~line 235) with `visible_until_order = require_boundary(visible_until_order)` immediately before the `effective = await resolve_effective_boundary(...)` call. Import `require_boundary` from `spoilerless.app.api.boundary` (the module already imports `resolve_effective_boundary`).
3. If a future route needs the same guard, it must import `require_boundary` — do not re-add an inline block.
  </action>
  <acceptance_criteria>
- `grep -n "def require_boundary" spoilerless/app/api/boundary.py` returns the new definition.
- `grep -n "INVALID_REQUEST" spoilerless/app/api/candidates.py` returns ZERO inline raise blocks (the string survives only inside boundary.py's require_boundary).
- `grep -n "require_boundary" spoilerless/app/api/candidates.py` shows one import + N call sites (3), one per guarded route.
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_candidate_ingest.py spoilerless/tests/test_candidate_review.py -q` passes — the omitted-boundary 422 envelope (code INVALID_REQUEST, exact message above) is unchanged.
  </acceptance_criteria>
</task>

<task type="test">
  <name>Task 3: Pin clamp/422 behavior in tests and update the wave inventory docs</name>
  <files>spoilerless/tests/test_share_api.py, spoilerless/tests/test_security_boundary.py, .planning/STATE.md</files>
  <read_first>spoilerless/tests/test_share_api.py (existing create-share tests + scratch-series fixture pattern), spoilerless/tests/test_security_boundary.py, spoilerless/app/domain/share.py (ShareCreateRequest.visible_until_order is required, gt=0)</read_first>
  <action>
Use the repo's scratch-series discipline (create a scratch series + scratch user progress in the test, tear down everything in fixture teardown; NEVER `series_dexter`, NEVER the dev user `ae8a41b7-db96-40e8-b6c2-2e3c69aedb11`; drift-agnostic asserts on ids/fixture values, never global counts). In `spoilerless/tests/test_share_api.py` add:
1. `test_create_share_clamps_to_persisted_progress`: actor with progress `view_as_of_order=2, watched_through_order=2` posts `visible_until_order=999` → 201; response body `visible_until_order == 2`.
2. `test_create_share_no_progress_fails_closed_to_one`: actor with NO progress record posts `visible_until_order=999` → 201 with response `visible_until_order == 1`.
3. `test_create_share_non_persisted_order_422`: actor with progress at order 1 posts `visible_until_order=5` (no persisted episode 5 in the scratch series) → 422, detail message EXACTLY `visible_until_order must identify a persisted episode order.`, code `INVALID_VISIBLE_UNTIL_ORDER`.
4. `test_list_and_revoke_share_unchanged`: existing list/revoke assertions keep passing untouched (guard against accidental signature drift).
Then update `.planning/STATE.md` line ~83 (D-01 gated-route record in the Decisions table): append `share` to the gated-route enumeration — this is a doc-string touch; do NOT commit STATE.md (phase-closeout commit owns it).
  </action>
  <acceptance_criteria>
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_share_api.py -q` passes: old tests + the 4 new tests green.
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_security_boundary.py -q` passes (clamping suite untouched by the share change).
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_openapi_contract.py spoilerless/tests/test_frontend_contract_doc.py -q` passes — the 52 ops / 39 templates contract is unchanged (no route signature/param/response_model edits were made).
- `git diff --stat` shows ONLY: spoilerless/app/api/share.py, spoilerless/app/api/boundary.py, spoilerless/app/api/candidates.py, spoilerless/tests/test_share_api.py, spoilerless/tests/test_security_boundary.py (if touched), .planning/STATE.md.
  </acceptance_criteria>
  <validation>
Run `unset PYTHONPATH && uv run python scripts/run_backend_tests.py --chunk graph` (or the three pytest invocations above) and confirm all green; never run two pytest processes in parallel on the shared AuraDB.
  </validation>
</task>

</tasks>

<verification>
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_share_api.py spoilerless/tests/test_security_boundary.py -q` → all pass.
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_openapi_contract.py spoilerless/tests/test_frontend_contract_doc.py -q` → pass (locked OpenAPI surface).
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_candidate_ingest.py spoilerless/tests/test_candidate_review.py -q` → pass (guard extraction envelope-preserving).
- Full backend suite (10-chunk runner) must stay green; scratch series + teardown only; never `series_dexter`; never user `ae8a41b7-db96-40e8-b6c2-2e3c69aedb11`.
</verification>

## Artifacts this phase produces

- `spoilerless/app/api/boundary.py`: adds `require_boundary(visible_until_order: int | None) -> int` (exports: `require_boundary`); module docstring now lists `share` in the D-01 gated-route set. `resolve_effective_boundary` unchanged (re-typed by 12-02).
- `spoilerless/app/api/share.py`: `create_share_link` now calls `resolve_effective_boundary` (single clamp authority); no remaining `effective_view_order` import.
- `spoilerless/app/api/candidates.py`: `require_boundary` replaces 3 duplicated inline None-guards.
- `spoilerless/tests/test_share_api.py`: 4 new tests pinning clamp-to-progress, fail-closed-to-1, 422 envelope parity, and unchanged list/revoke.
- `.planning/STATE.md`: D-01 gated-route record gains `share` (uncommitted doc touch; closeout commit owns it).

## Flagged assumptions (unresolved probe edges)

The phase edge probe surfaced 15 edges (6 unclassified, 2 boundary, 2 precision, 2 idempotency, 3 concurrency) against the 12-CONTEXT.md requirement texts; all remained **UNRESOLVED** (no SPEC.md exists — specless fallback protocol). They are surfaced here, never auto-resolved, never dismissed. Edges assigned to this plan:

- **[E-BND-1] boundary edge — UNRESOLVED (probe):** create-side boundary requests (share creation in particular) must never widen the creator's spoiler-safe window, including under absent/missing progress records. This plan treats it by preserving the exact existing clamp math through the shared resolver and pinning it with the Task 3 tests; the edge itself remains open for a future authoritative resolution. Executor must not silently "improve" the clamp.
- **[E-BND-2] boundary edge — UNRESOLVED (probe):** precedence between clamping and 422-on-non-persisted-order (which value wins when the requested order is both above progress AND unpersisted). Current behavior (clamp first, then 422 if the CLAMPED order is unpersisted) is preserved byte-identically; do not reorder the two phases.
- **[E-PRC-1] precision edge — UNRESOLVED (probe):** which order value a share snapshot records (client-requested vs effective) in response and persisted fields. Current behavior: the effective (clamped) value is persisted and echoed; preserved exactly.
- **[E-UNC-1] unclassified edge — UNRESOLVED (probe):** share-token expiration vs boundary-change interaction (a stale token outliving the creator's boundary change). Out of scope for this behavioral-preserving plan; no behavior change is made to token validity windows.

Remaining unresolved edges are flagged in their best-fitting plans: E-BND-* none remaining; E-PRC-2/E-CON-1/E-UNC-3 → 12-12; E-CON-2/E-IDP-1/E-UNC-4 → 12-13; E-UNC-5 → 12-14; E-IDP-2/E-CON-3/E-UNC-6 → 12-15; E-UNC-2 → 12-11.