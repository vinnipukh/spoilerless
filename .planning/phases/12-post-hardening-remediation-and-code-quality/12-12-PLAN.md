---
phase: 12-post-hardening-remediation-and-code-quality
plan: 12
type: execute
wave: 3
depends_on: [02, 06, 11]
files_modified: [
  "spoilerless/app/services/graph.py",
  "spoilerless/app/api/graph.py",
  "spoilerless/app/api/share.py",
  "spoilerless/app/api/deps.py",
  "spoilerless/app/api/candidates.py",
  "spoilerless/app/api/revisions.py",
  "spoilerless/app/api/series.py",
  "spoilerless/app/api/user_content.py",
  "spoilerless/app/api/progress.py",
  "spoilerless/app/domain/graph.py",
  "spoilerless/app/domain/visualization.py",
  "spoilerless/tests/test_graph_api.py",
  "spoilerless/tests/test_share_api.py",
  "spoilerless/tests/test_visualization_cache.py",
  "spoilerless/tests/test_visualization_projection.py",
  "spoilerless/tests/test_deps.py"
]
autonomous: true
requirements: [THERMO-P2-03, THERMO-P2-01]
estimate: {tokens: 16000, raw_tokens: 16000, tasks: 3, confidence: high}
must_haves:
  truths:
    - "GraphService gains one deep method `read_visible_graph(series_id: str, effective: int, user_id: str | None) -> GraphResponse` owning cache-aside (get_cached_graph/set_cached_graph) + fetch_graph + write-through; get_graph and get_share_graph both reduce to series-check -> resolve_effective_boundary -> read_visible_graph, and the byte-parallel cache blocks in api/graph.py and api/share.py are deleted."
    - "resolve_effective_boundary stays the single D-01 authority in api/boundary.py — NOT moved into the service (deliberate deviation per 12-RESEARCH.md §3.1); expand and export routes remain no-cache and untouched."
    - "VISIBLE_NODE_LABELS + USER_RELATIONSHIP_TYPES live in domain/graph.py; VisualizationView + ExpansionKey live in domain/visualization.py; api/share.py has no `from spoilerless.app.api.graph import` import."
    - "api/deps.py exports exactly ONE GraphServiceDependency and ONE ProgressServiceDependency; the 5x/6x duplicated factory aliases (and duplicated DatabaseDependency aliases) in the routers are deleted; GraphService.find_path(...) wrapper replaces the `service._database` private-attr call at api/graph.py:413-420."
    - "OpenAPI surface unchanged: 52 ops / 39 templates locked (test_openapi_contract.py + test_frontend_contract_doc.py green); cache keys, TTL (300s), and Redis-error swallowing (T-08-06-02) preserved."
  artifacts:
    - path: "spoilerless/app/services/graph.py"
      provides: "Deep graph-read entry with cache-aside and find_path seam"
      exports: ["GraphService.read_visible_graph", "GraphService.find_path"]
    - path: "spoilerless/app/domain/graph.py"
      provides: "Read-path contract constants"
      exports: ["GraphNode", "GraphEdge", "GraphClaim", "GraphResponse", "VISIBLE_NODE_LABELS", "USER_RELATIONSHIP_TYPES"]
  key_links:
    - from: "spoilerless/app/api/graph.py"
      to: "spoilerless/app/services/graph.py"
      via: "read_visible_graph call"
      pattern: "read_visible_graph"
    - from: "spoilerless/app/api/share.py"
      to: "spoilerless/app/api/deps.py"
      via: "GraphServiceDependency import (api-graph import dropped)"
      pattern: "from spoilerless.app.api.deps import"
    - from: "spoilerless/app/api/graph.py"
      to: "spoilerless/app/domain/visualization.py"
      via: "VisualizationView/ExpansionKey moved literals"
      pattern: "VisualizationView"
  prohibitions:
    - "Do not move resolve_effective_boundary into the services layer; do not change any route signature, response model, cache key, TTL, or error envelope."
    - "Do not let read_visible_graph and any leftover route copy of the cache block diverge — the deletion test: after this plan, cache-aside exists in exactly ONE place."
    - "Do not add an epoch to graph cache keys (separate perf decision, out of scope per 12-RESEARCH.md §4.3)."
    - "Security breadcrumb: no OWASP/injection/GDPR item is minted here — this consolidation preserves the fail-closed boundary call order (series check BEFORE boundary, boundary BEFORE data) exactly."
---

<objective>
Make the graph read path one deep module (Candidate C1, 12-RESEARCH.md §3.1): concentrate the 3x-repeated read block (series check → boundary → cache-aside → fetch_graph → write-through) into `GraphService.read_visible_graph`, move the router-level contract constants into the domain layer, centralize the GraphService/ProgressService DI factories in `api/deps.py`, and kill the `service._database` private-attr leak with a `find_path` wrapper. Boundaries are NOT moved into the service — `resolve_effective_boundary` remains the single D-01 authority in the api layer.
</objective>

<context>
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-RESEARCH.md (Candidate C1, §3.1 + sequencing §2)
@spoilerless/app/services/graph.py
@spoilerless/app/api/graph.py
@spoilerless/app/api/share.py
@spoilerless/app/api/deps.py
@spoilerless/app/api/candidates.py
@spoilerless/app/api/revisions.py
@spoilerless/app/api/series.py
@spoilerless/app/api/user_content.py
@spoilerless/app/api/progress.py
@spoilerless/app/domain/graph.py
@spoilerless/app/domain/visualization.py
@spoilerless/app/graph/ontology.py
@spoilerless/app/retrieval/tools.py (find_path signature)
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add GraphService.read_visible_graph and collapse both route copies</name>
  <files>spoilerless/app/services/graph.py, spoilerless/app/api/graph.py, spoilerless/app/api/share.py</files>
  <read_first>spoilerless/app/services/graph.py (current fetch_graph), spoilerless/app/api/graph.py (lines 106-145 get_graph + the get_visualization cache block 191-274), spoilerless/app/api/share.py (get_share_graph lines 95-145), spoilerless/app/cache/graph_cache.py (get_cached_graph/set_cached_graph signatures + swallow semantics)</read_first>
  <action>
1. In `spoilerless/app/services/graph.py`:
   - Add imports: `get_cached_graph`, `set_cached_graph` from `spoilerless.app.cache.graph_cache`; `VISIBLE_NODE_LABELS`, `USER_RELATIONSHIP_TYPES` from `spoilerless.app.domain.graph` (Task 2 moves them there — do Task 2's domain move in the same change or first, so this import resolves); `GraphResponse` already imported.
   - Add:
     `async def read_visible_graph(self, series_id: str, effective: int, user_id: str | None) -> GraphResponse:` — body: `cached = await get_cached_graph(series_id, effective, user_id)`; if not None return `GraphResponse.model_validate(cached)`; else `result = await self.fetch_graph(series_id, effective, node_labels=VISIBLE_NODE_LABELS, user_relationship_types=USER_RELATIONSHIP_TYPES, effective_view_order=effective)`; then `await set_cached_graph(series_id, effective, user_id, result.model_dump(mode="json"))`; return result. Preserve the doc comment "Cache-aside (INFRA-02)... write-through on miss (best-effort; swallows Redis errors)".
   - Do NOT add boundary resolution here (stays in the api layer).
2. In `spoilerless/app/api/graph.py` `get_graph`: delete the cache-aside block (current lines 127-145); route becomes: series check -> `effective = await resolve_effective_boundary(...)` -> `user_id = user["id"] if user is not None else None` -> `return await service.read_visible_graph(series_id, effective, user_id)`. The 404 SERIES_NOT_FOUND check stays FIRST, before boundary resolution.
3. In `spoilerless/app/api/share.py` `get_share_graph`: delete the cache block (current lines 129-144) and the direct `get_cached_graph/set_cached_graph` imports; call `await service.read_visible_graph(series_id, effective, user_id)` with `user_id = None`. The 404 series-exists check stays.
4. `get_visualization` keeps its own viz-cache block (different key space `viz:` + epoch) — do NOT collapse it into read_visible_graph; expand/export remain no-cache.
  </action>
  <acceptance_criteria>
- `grep -n "get_cached_graph\|set_cached_graph" spoilerless/app/api/graph.py spoilerless/app/api/share.py` returns NOTHING (cache internals only inside services/graph.py now — except the viz-cache imports api/graph.py legitimately keeps for the visualization route).
- `grep -n "read_visible_graph" spoilerless/app/api/graph.py spoilerless/app/api/share.py spoilerless/app/services/graph.py` shows 1 definition + exactly 2 route call sites.
- `grep -n "def read_visible_graph" spoilerless/app/services/graph.py` shows the signature `(self, series_id: str, effective: int, user_id: str | None) -> GraphResponse`.
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_graph_api.py spoilerless/tests/test_share_api.py spoilerless/tests/test_visualization_cache.py -q` passes (hit/miss/write-through and no-op-Redis behavior unchanged).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Move contract constants to the domain layer and centralize DI factories</name>
  <files>spoilerless/app/domain/graph.py, spoilerless/app/domain/visualization.py, spoilerless/app/api/deps.py, spoilerless/app/api/graph.py, spoilerless/app/api/share.py, spoilerless/app/api/candidates.py, spoilerless/app/api/revisions.py, spoilerless/app/api/series.py, spoilerless/app/api/user_content.py, spoilerless/app/api/progress.py</files>
  <read_first>spoilerless/app/api/graph.py (lines 41-97: constants + factory aliases), spoilerless/app/api/deps.py (DatabaseDependency at line 27), spoilerless/app/domain/graph.py, spoilerless/app/domain/visualization.py (EXPANSION_*/GRAPHRAG_FOCUS_VIEW_TYPE already here), spoilerless/app/graph/ontology.py (stdlib+yaml only — safe for domain import)</read_first>
  <action>
1. `spoilerless/app/domain/graph.py`: add `VISIBLE_NODE_LABELS: list[str] = ["Series", "Episode", "Character", "Event", "Location", "Organization", "Object"]` and `USER_RELATIONSHIP_TYPES: list[str] = sorted(load_ontology().user_safe_relationship_types)` (import `load_ontology` from `spoilerless.app.graph.ontology` — verified no cycle: ontology imports stdlib+yaml only). Keep them as module constants with the existing doc comments about the D-01/D-14 contract.
2. `spoilerless/app/domain/visualization.py`: move `VisualizationView = Literal[...]` (6 views) and `ExpansionKey = Literal[...]` (7 keys) here, next to `PROJECTION_VERSION`/`EXPANSION_*`; import `Literal` from typing; update the domain module docstring to note these are the locked OpenAPI enum vocabularies (D-29/D-21).
3. `spoilerless/app/api/deps.py`: add `GraphServiceDependency` (factory `get_graph_service(database: DatabaseDependency) -> GraphService`) and `ProgressServiceDependency` (factory `get_progress_service(database: DatabaseDependency) -> ProgressService`); import the services. Keep `DatabaseDependency` (already line 27) as the single alias.
4. Routers — delete their local `get_graph_service`/`get_progress_service`/`GraphServiceDependency`/`ProgressServiceDependency`/`DatabaseDependency` alias definitions and import them from `spoilerless.app.api.deps`: `api/graph.py`, `api/candidates.py`, `api/revisions.py`, `api/series.py`, `api/user_content.py`, `api/progress.py`. Keep the `_resolve_effective_boundary` back-compat alias in graph.py.
5. `api/graph.py`: remove `VISIBLE_NODE_LABELS`/`USER_RELATIONSHIP_TYPES` definitions and import them from `spoilerless.app.domain.graph`; import `VisualizationView`/`ExpansionKey` from `spoilerless.app.domain.visualization`.
6. `api/share.py`: replace `from spoilerless.app.api.graph import (USER_RELATIONSHIP_TYPES, VISIBLE_NODE_LABELS, ProgressServiceDependency)` with `from spoilerless.app.domain.graph import USER_RELATIONSHIP_TYPES, VISIBLE_NODE_LABELS` (if still referenced by name after Task 1 — otherwise drop entirely) + `from spoilerless.app.api.deps import ProgressServiceDependency`; replace `service = GraphService(database)` with a `GraphServiceDependency` parameter in `create_share_link` and `get_share_graph` (signature shape changes are DI-only: the OpenAPI contract does NOT pin dependency params). Delete the `from spoilerless.app.services.graph import GraphService` import if unused.
7. `api/graph.py` lines ~413-420: replace `find_path(service._database, ...)` with `await service.find_path(...)` using the new wrapper.
  </action>
  <acceptance_criteria>
- `grep -rn "def get_graph_service\|def get_progress_service" spoilerless/app/api/*.py | grep -v deps.py` returns NOTHING (factories defined once, in deps.py).
- `grep -rn "DatabaseDependency = Annotated" spoilerless/app/api/*.py | grep -v deps.py` returns NOTHING (single alias in deps.py:27).
- `grep -rn "from spoilerless.app.api.graph import" spoilerless/app/api` returns NOTHING (share->graph api seam gone).
- `grep -n "service._database" spoilerless/app/api/graph.py` returns NOTHING; `grep -n "def find_path" spoilerless/app/services/graph.py` shows the wrapper delegating to `retrieval.tools.find_path` with `self._database`.
- `grep -n "VISIBLE_NODE_LABELS\|USER_RELATIONSHIP_TYPES" spoilerless/app/domain/graph.py` shows both constants; `grep -n "VisualizationView\|ExpansionKey" spoilerless/app/domain/visualization.py` shows both Literals (aliases imported into api/graph.py).
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_deps.py -q` passes (deps module imports cleanly); `unset PYTHONPATH && uv run python -c "from spoilerless.app.main import app"` exits 0.
  </acceptance_criteria>
</task>

<task type="test">
  <name>Task 3: Contract and behavior regression suite</name>
  <files>spoilerless/tests/test_graph_api.py, spoilerless/tests/test_share_api.py, spoilerless/tests/test_visualization_projection.py, spoilerless/tests/test_security_boundary.py, spoilerless/tests/test_openapi_contract.py, spoilerless/tests/test_frontend_contract_doc.py</files>
  <read_first>spoilerless/tests/test_graph_api.py (existing live-graph tests + scratch fixtures), spoilerless/tests/test_share_api.py, spoilerless/tests/test_visualization_projection.py, spoilerless/tests/test_security_boundary.py</read_first>
  <action>
No new test files: this plan is a behavioral-preserving consolidation, so the regression evidence IS the existing suites staying green (asserts are already drift-agnostic against scratch ids). Run and record, in order (never two pytest processes in parallel):
1. `unset PYTHONPATH && uv run pytest spoilerless/tests/test_graph_api.py spoilerless/tests/test_share_api.py -q` — graph + share read paths through the new deep method (cache hit/miss/write-through, 404 series-check-first, boundary clamp unchanged).
2. `unset PYTHONPATH && uv run pytest spoilerless/tests/test_visualization_cache.py spoilerless/tests/test_visualization_projection.py -q` — viz cache block untouched, projection behavior unchanged.
3. `unset PYTHONPATH && uv run pytest spoilerless/tests/test_security_boundary.py -q` — boundary behavior through the new call shape is identical (anonymous 999 -> 1 with 200).
4. `unset PYTHONPATH && uv run pytest spoilerless/tests/test_openapi_contract.py spoilerless/tests/test_frontend_contract_doc.py -q` — 52 ops / 39 templates still locked.
If any suite fails, the failure is a behavior drift introduced by Tasks 1-2 (the suites did not change) — fix the source, never weaken the test.
  </action>
  <acceptance_criteria>
- All four pytest invocations pass with zero modifications to the test files listed.
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_deps.py -q` passes.
- `git diff --stat` touches ONLY the source files in files_modified (+ any test file only if a signature assert needed a deliberate, commented update — default: none).
  </acceptance_criteria>
  <validation>
After the focused suites, run `unset PYTHONPATH && uv run python scripts/run_backend_tests.py --chunk graph` and confirm green before closing.
  </validation>
</task>

</tasks>

<verification>
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_graph_api.py spoilerless/tests/test_share_api.py spoilerless/tests/test_visualization_cache.py spoilerless/tests/test_visualization_projection.py spoilerless/tests/test_security_boundary.py spoilerless/tests/test_deps.py spoilerless/tests/test_openapi_contract.py spoilerless/tests/test_frontend_contract_doc.py -q` → all pass, no test-file edits.
- `unset PYTHONPATH && uv run python -c "from spoilerless.app.main import app"` exits 0.
- Full backend suite (10-chunk runner) green afterwards; scratch series + teardown only; never `series_dexter`.
</verification>

## Artifacts this phase produces

- `spoilerless/app/services/graph.py`: `GraphService.read_visible_graph(series_id, effective, user_id) -> GraphResponse` (deep cache-aside + fetch + write-through entry) and `GraphService.find_path(...)` wrapper (delegates to `retrieval/tools.py::find_path` with `self._database`).
- `spoilerless/app/domain/graph.py`: `VISIBLE_NODE_LABELS`, `USER_RELATIONSHIP_TYPES` constants.
- `spoilerless/app/domain/visualization.py`: `VisualizationView`, `ExpansionKey` literals (moved).
- `spoilerless/app/api/deps.py`: `GraphServiceDependency`, `ProgressServiceDependency` (single factories; `DatabaseDependency` stays sole alias).
- `spoilerless/app/api/graph.py`, `api/share.py`, `api/candidates.py`, `api/revisions.py`, `api/series.py`, `api/user_content.py`, `api/progress.py`: alias deletions + import rewiring; share.py drops the api→api import.
- No new test files; existing suites are the regression gate.

## Flagged assumptions (unresolved probe edges)

The phase edge probe surfaced 15 edges (6 unclassified, 2 boundary, 2 precision, 2 idempotency, 3 concurrency) against the 12-CONTEXT.md requirement texts; all remained **UNRESOLVED** (specless fallback protocol: never auto-resolve, never dismiss). Edges assigned to this plan:

- **[E-PRC-2] precision edge — UNRESOLVED (probe):** which order value appears in the graph response's `effective_view_order` vs `visible_until_order` fields under clamping. `fetch_graph`/`read_visible_graph` pass `effective_view_order=effective` exactly as today (response `visible_until_order` = the clamped effective value); the consolidation must not alter this pairing.
- **[E-CON-1] concurrency edge — UNRESOLVED (probe):** concurrent readers performing write-through on the same cache key (double-fetch, no locking). Today both routes would fetch and `set_cached_graph` last-writer-wins; preserved as-is — this plan adds no locking and no fail-closed behavior change.
- **[E-UNC-3] unclassified edge — UNRESOLVED (probe):** series-deleted-after-cache-write interplay (a cached graph outliving the series row). Existing 404-series-check-first ordering (checked before boundary/cache) is preserved exactly.

Remaining unresolved edges are flagged in their best-fitting plans: E-BND-1/E-BND-2/E-PRC-1/E-UNC-1 → 12-10; E-UNC-2 → 12-11; E-CON-2/E-IDP-1/E-UNC-4 → 12-13; E-UNC-5 → 12-14; E-IDP-2/E-CON-3/E-UNC-6 → 12-15.