---
phase: 12-post-hardening-remediation-and-code-quality
plan: 14
type: execute
wave: 3
depends_on: [08, 13]
files_modified: [
  "frontend/src/lib/graph/sceneElements.ts",
  "frontend/src/components/graph/graphElements.ts",
  "frontend/src/lib/visualizationAdapter.ts",
  "frontend/src/components/graph/GraphCanvas.tsx",
  "frontend/src/lib/visualizationAdapter.test.ts",
  "frontend/src/components/graph/graphElements.test.ts",
  "frontend/src/components/graph/GraphCanvas.test.tsx"
]
autonomous: true
requirements: [THERMO-P0-02, THERMO-P0-03]
estimate: {tokens: 15000, raw_tokens: 15000, tasks: 3, confidence: high}
must_haves:
  truths:
    - "One neutral adapter module (frontend/src/lib/graph/sceneElements.ts) exists with a single SceneElementDefinition-shaped model, per-source normalize (fromGraph / fromVisualization), one shared enrich, and one parents-first emit; GraphCanvas's `activeVisualization ? toCytoscapeElements(...) : graphToElements(...)` branch collapses into one call."
    - "Cluster policy is one explicit function (clusterFor) with per-source input policy: DTO group membership when a groups list is present (visualization path), else episode band `Ep #N` from visible_from_order, else `Main` (graph path). NO episode bands are added to ungrouped visualization nodes — the visualization path's visuals are byte-identical (no new compound boxes)."
    - "The dead `(node as Record<string, unknown>).subplot ?? .cluster` cast in graphElements.ts is DELETED with zero behavior change: neither spoiler/filter.py NODES_QUERY, graph/ontology.py, nor the backend GraphNode model emit subplot/cluster (the episode-band fallback always fired); GraphNode/GraphResponse types are untouched."
    - "Exact data-key contracts stay pinned: NODE_DATA_KEYS/GROUP_DATA_KEYS/EDGE_DATA_KEYS in visualizationAdapter (or their moved equivalents) and the graphElements element shapes are unchanged; visualizationAdapter.test.ts (T10-LEAK-04) and graphElements.test.ts pass with ZERO pin changes unless a deliberate, commented exception is justified."
    - "apiUrl(image_url) handling of both current paths is preserved identically (Character-only portrait rule); all 44 Vitest suites + `npm run build` stay green."
  artifacts:
    - path: "frontend/src/lib/graph/sceneElements.ts"
      provides: "Neutral Cytoscape element adapter (normalize + enrich + emit + clusterFor)"
      exports: ["SceneElementDefinition", "fromGraph", "fromVisualization", "clusterFor"]
  key_links:
    - from: "frontend/src/components/graph/GraphCanvas.tsx"
      to: "frontend/src/lib/graph/sceneElements.ts"
      via: "Single adapter call in the elements useMemo"
      pattern: "fromVisualization"
    - from: "frontend/src/lib/graph/sceneElements.ts"
      to: "frontend/src/components/graph/graphElements.ts"
      via: "Graph-path element conversion delegates"
      pattern: "graphToElements"
  prohibitions:
    - "Do not change the visualization path's rendered output (no new cluster parents, no decoration changes) — any key-set change in the pinned tests must be deliberate, commented with justification, and reviewed before landing."
    - "Do not alter the reconciler (cytoscapeReconciler.ts) — it is vocabulary-agnostic and stays untouched."
    - "Do not loosen tsc strictness or edit types/graph.ts GraphNode/GraphResponse shapes for this plan."
    - "Security breadcrumb: no OWASP/injection/GDPR item is minted here — data keys are benign presentation flags; the T10-LEAK-04 comment is the traceability contract for any key change."
---

<objective>
Unify the two Cytoscape element adapters (Candidate C5, 12-RESEARCH.md §3.5): `graphToElements(graph, mode)` (graphElements.ts:27-157) and `toCytoscapeElements(dto)` (visualizationAdapter.ts:71-134) duplicate normalize/enrich/emit logic with divergent decoration policies. Create one neutral `sceneElements.ts` module with per-source normalization and one explicit cluster policy, collapse GraphCanvas's adapter branch, delete the dead `subplot`/`cluster` cast (zero behavior change), and keep the exact-shape pin tests as the parity gate.
</objective>

<context>
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-RESEARCH.md (Candidate C5, §3.5)
@frontend/src/components/graph/graphElements.ts (FULL: cluster derivation lines 77-121, simple/areaScale/claimStatus decorations, isolated-node pruning 68-72, overviewProjection 38-42)
@frontend/src/lib/visualizationAdapter.ts (FULL: NODE_DATA_KEYS/GROUP_DATA_KEYS/EDGE_DATA_KEYS 44-69, toCytoscapeElements 71-134, group-prefixed parents 33/79-94)
@frontend/src/components/graph/GraphCanvas.tsx (elements useMemo branch ~511-520)
@frontend/src/lib/visualizationAdapter.test.ts (T10-LEAK-04 shape pins)
@frontend/src/components/graph/graphElements.test.ts
@frontend/src/components/graph/cytoscapeReconciler.ts (untouched)
@frontend/src/types/graph.ts (GraphNode/GraphResponse — NOT modified by this plan)
</context>

<tasks>

<task type="execute">
  <name>Task 1: Build the neutral sceneElements module</name>
  <files>frontend/src/lib/graph/sceneElements.ts, frontend/src/components/graph/graphElements.ts, frontend/src/lib/visualizationAdapter.ts</files>
  <read_first>frontend/src/components/graph/graphElements.ts (FULL — cluster derivation, decorations, pruning, overviewProjection), frontend/src/lib/visualizationAdapter.ts (FULL — key pin arrays, group parents, debugLabels)</read_first>
  <action>
Create `frontend/src/lib/graph/sceneElements.ts`:
1. `export type SceneElementDefinition = { group: 'nodes' | 'edges'; data: Record<string, unknown>; classes?: string }` (or the union shape both adapters already converge on — match the existing ElementDefinition usage).
2. One cluster policy function: `export function clusterFor(node: { visible_from_order?: number | null }, groups?: { id: string; label: string }[]): { id: string; label: string } | null`:
   - When `groups` is non-null (visualization path): the node's DTO group membership (1:1, `group:`-prefixed parent ids exactly as toCytoscapeElements does today).
   - When `groups` is null/undefined (graph path): episode band `Ep #N` from `visible_from_order` (string interpolation identical to graphElements.ts:93-100), fallback `Main`.
   - Document the single policy constant in a comment: per-source selection is INPUT-driven (groups list present vs absent), never a second policy branch — so an ungrouped visualization node NEVER gets an episode band (visual parity).
3. `export function fromGraph(graph: GraphResponse, mode: GraphMode, opts?: { debugLabels?: boolean }): SceneElementDefinition[]` — full graph-path behavior: overviewProjection when mode requires it (graphElements.ts:38-42), isolated-node pruning (68-72), `simple` dots (no-image AND degree < 3, :90), `areaScale: 3` for `Ep #1` in full mode (:134), `claimStatus` on edges (:151), clusters via `clusterFor(node, undefined)`, parents-first emission order.
4. `export function fromVisualization(dto: VisualizationDTO, opts?: { debugLabels?: boolean }): SceneElementDefinition[]` — full viz-path behavior: group-parents from DTO groups (1:1), displayTier/order/episodeId/relationClass/debugLabel keys, NO simple/areaScale/claimStatus decorations, data keys exactly NODE_DATA_KEYS/GROUP_DATA_KEYS/EDGE_DATA_KEYS, parents-first emission order, `debugLabels: view_type === 'full'` option plumbed through.
5. Add a shared `enrich` step internally (one function applying per-path decorations), even if callers only see fromGraph/fromVisualization — the point is ONE place where decoration policy lives.
Keep `graphElements.ts` and `visualizationAdapter.ts` exporting their current functions (they re-export/chain into sceneElements or stay as internal implementation the module imports); if graphElements.ts's exports become unused by GraphCanvas after Task 2, keep the functions (they are unit-tested) but allow their re-export to flow through sceneElements.
  </action>
  <acceptance_criteria>
- `grep -n "export function clusterFor\|export function fromGraph\|export function fromVisualization\|export type SceneElementDefinition" frontend/src/lib/graph/sceneElements.ts` shows all four exports.
- `grep -n "subplot\|cluster ??\|(node as Record" frontend/src/components/graph/graphElements.ts` returns NOTHING (dead cast removed — Task 2 or here, same commit).
- The `Ep #N` interpolation string and `Main` fallback exist in EXACTLY one place (sceneElements.ts): `grep -rn "Ep #" frontend/src/lib/graph frontend/src/components/graph | wc -l` <= expected (clusterFor only; graphElements.ts may retain a thin re-export).
- No behavior delta yet — `cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/graph/graphElements.test.ts src/lib/visualizationAdapter.test.ts --reporter=dot` still passes at this intermediate step (pins unchanged).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Collapse the GraphCanvas adapter branch and delete the dead cast</name>
  <files>frontend/src/components/graph/GraphCanvas.tsx, frontend/src/components/graph/graphElements.ts</files>
  <read_first>frontend/src/components/graph/GraphCanvas.tsx (elements useMemo lines ~509-521, incl. lastVisualizationRef handling; image_url/apiUrl consumption site), frontend/src/components/graph/graphElements.ts (lines 90-101 dead cast region)</read_first>
  <action>
1. In `GraphCanvas.tsx` the elements useMemo: replace the `if (activeVisualization) { return toCytoscapeElements(...) } return graphToElements(graph, mode)` branch with ONE call: `return activeVisualization ? sceneElements.fromVisualization(activeVisualization, { debugLabels: activeVisualization.metadata.view_type === 'full' }) : sceneElements.fromGraph(graph, mode)` (import `sceneElements` from `../../lib/graph/sceneElements`). Keep `lastVisualizationRef` retention logic exactly as-is (the `visualization ?? lastVisualizationRef.current` behavior and the `activeVisualization = null` branch — lines ~505-512 — must not change).
2. Update GraphCanvas's imports: drop `toCytoscapeElements`/`graphToElements` direct imports if nothing else uses them; keep any other imports from those modules unchanged.
3. In `graphElements.ts`: DELETE the dead cast at lines 93-100 — the cluster key derivation becomes `const rawCluster = undefined` → always the episode-band fallback (`Ep #N` from visible_from_order, else `Main`); keep the function structure and comments updated to state the fallback always fires (no backend source emits subplot/cluster — verified: spoiler/filter.py NODES_QUERY, graph/ontology.py, backend GraphNode pydantic extras-ignored). `types/graph.ts` GraphNode/GraphResponse stay untouched.
4. Preserve `apiUrl(image_url)` handling exactly as today wherever it lives (adapter or canvas consumption) — the Character-only portrait prefix rule must not change.
  </action>
  <acceptance_criteria>
- `grep -n "toCytoscapeElements\|graphToElements" frontend/src/components/graph/GraphCanvas.tsx` returns NOTHING; `grep -n "sceneElements.from\|fromVisualization\|fromGraph" frontend/src/components/graph/GraphCanvas.tsx` shows the single call.
- `grep -n "subplot\|cluster" frontend/src/components/graph/graphElements.ts` returns NOTHING (dead cast gone; the file may still export other symbols).
- `cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/graph/GraphCanvas.test.tsx src/components/graph/graphElements.test.ts --reporter=dot` passes.
  </acceptance_criteria>
</task>

<task type="test">
  <name>Task 3: Parity gate — pinned shape suites, full vitest, and strict build</name>
  <files>frontend/src/lib/visualizationAdapter.test.ts, frontend/src/components/graph/graphElements.test.ts, frontend/src/components/graph/GraphCanvas.test.tsx</files>
  <read_first>frontend/src/lib/visualizationAdapter.test.ts (exact-shape pins NODE_DATA_KEYS/GROUP_DATA_KEYS/EDGE_DATA_KEYS — T10-LEAK-04 contract comment), frontend/src/components/graph/graphElements.test.ts, frontend/src/components/graph/GraphCanvas.test.tsx</read_first>
  <action>
1. Run the pin suites: `cd frontend && NODE_ENV=test CI=1 npx vitest run src/lib/visualizationAdapter.test.ts src/components/graph/graphElements.test.ts src/components/graph/cytoscapeReconciler.test.ts --reporter=dot`. If any exact-shape assertion fails, the source drifted — fix the source (the unified module must emit byte-identical key sets), do NOT edit pins. The ONLY acceptable pin edit is a deliberate visual change with justification in the T10-LEAK-04 comment (default: NO pin changes — viz path visuals are frozen).
2. Full gate: `cd frontend && NODE_ENV=test CI=1 npm run test` (all 44 suites) then `cd frontend && npm run build` (tsc strict — the dead-cast deletion is only type-safe if no other code referenced the cast fields; the strict build proves it).
  </action>
  <acceptance_criteria>
- visualizationAdapter.test.ts, graphElements.test.ts, cytoscapeReconciler.test.ts pass with ZERO assertions edited.
- `cd frontend && NODE_ENV=test CI=1 npm run test` exits 0 (44 suites).
- `cd frontend && npm run build` exits 0 (tsc strict + vite build; no TS errors from the cast deletion or the adapter branch change).
- `git diff --stat` shows no edits to types/graph.ts or cytoscapeReconciler.ts.
  </acceptance_criteria>
  <validation>
Run pin suites first, then the full test gate, then the build — in that order; the build is the final arbiter (09-07 blind spot).
  </validation>
</task>

</tasks>

<verification>
- `cd frontend && NODE_ENV=test CI=1 npm run test` → all 44 suites green, pin suites untouched.
- `cd frontend && npm run build` → exits 0.
- `grep -rn "subplot" frontend/src` → no matches outside docs/comments.
- Manual sanity (optional): Overview clusters still render `Ep #N`/`Main` compounds; visualization projections render identical groups as before the unification.
</verification>

## Artifacts this phase produces

- `frontend/src/lib/graph/sceneElements.ts` (new): `SceneElementDefinition`, `clusterFor(node, groups?)`, `fromGraph(graph, mode, opts?)`, `fromVisualization(dto, opts?)` (+ internal shared `enrich`).
- `frontend/src/components/graph/graphElements.ts`: dead `subplot`/`cluster` cast deleted; may thin to re-export/chain into sceneElements (its unit-tested exports kept).
- `frontend/src/lib/visualizationAdapter.ts`: unchanged in behavior; may re-export through sceneElements (pin tests unchanged).
- `frontend/src/components/graph/GraphCanvas.tsx`: adapter branch collapsed to one call; imports updated.
- No test-file changes (pins stay byte-identical).

## Flagged assumptions (unresolved probe edges)

The phase edge probe surfaced 15 edges (6 unclassified, 2 boundary, 2 precision, 2 idempotency, 3 concurrency) against the 12-CONTEXT.md requirement texts; all remained **UNRESOLVED** (specless fallback protocol: never auto-resolve, never dismiss). Edges assigned to this plan:

- **[E-UNC-5] unclassified edge — UNRESOLVED (probe):** compound↔flat scene switches under the unified adapter (reconciler cascade-removal interactions). The reconciler is vocabulary-agnostic and untouched; the adapters emit the same parent/child structure as today, so topology reconciliation behavior is unchanged. The edge stays open; the dedicated topology regression matrix (react-cytoscape-topology-aware-reconciliation reference) remains the guard.

Remaining unresolved edges are flagged in their best-fitting plans: E-BND-1/E-BND-2/E-PRC-1/E-UNC-1 → 12-10; E-UNC-2 → 12-11; E-PRC-2/E-CON-1/E-UNC-3 → 12-12; E-CON-2/E-IDP-1/E-UNC-4 → 12-13; E-IDP-2/E-CON-3/E-UNC-6 → 12-15.