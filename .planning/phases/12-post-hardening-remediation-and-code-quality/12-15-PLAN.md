---
phase: 12-post-hardening-remediation-and-code-quality
plan: 15
type: execute
wave: 3
depends_on: [02, 12]
files_modified: [
  "spoilerless/app/services/graph.py",
  "spoilerless/app/api/candidates.py",
  "spoilerless/app/api/change_set.py",
  "spoilerless/app/api/user_content.py",
  "spoilerless/app/api/revisions.py",
  "spoilerless/app/cache/graph_cache.py",
  "spoilerless/tests/test_visualization_cache.py",
  "spoilerless/tests/test_revisions.py"
]
autonomous: true
requirements: [THERMO-P2-03, THERMO-P3-09]
estimate: {tokens: 14000, raw_tokens: 14000, tasks: 3, confidence: high}
must_haves:
  truths:
    - "One deep facade `invalidate_series_cache(series_id: str) -> None` exists in services/graph.py owning the epoch-bump-BEFORE-delete sequence (graph_revision:{series_id} INCR, then scan_iter deletes of graph:{sid}:* + viz:{sid}:*) with the same best-effort Redis-error swallowing as invalidate_series today (T-08-06-02); no api router imports spoilerless.app.cache.graph_cache for invalidation anymore."
    - "POST /api/series/{series_id}/revisions/{revision_id}/revert now invalidates the series cache after execute_write — closing the LIVE staleness omission (revert mutates visible content but today leaves graph/viz entries valid for up to the 300s TTL)."
    - "All 12 remembered invalidation call sites are replaced by the facade call (api/candidates.py x4, api/change_set.py x2, api/user_content.py x6); no new mutation path bypasses it."
    - "Epoch semantics unchanged: bump-before-delete ordering is preserved (D-30 race separation), no epoch added to graph keys, TTL/key shapes untouched, over-invalidation remains safe by construction (T-08-06-01)."
    - "The deliberate boundary is documented: full impossibility-of-forgetting would need a write coordinator wrapping execute_write — explicitly out of scope (documented in graph_cache.py docstring, per 12-RESEARCH.md §3.6)."
  artifacts:
    - path: "spoilerless/app/services/graph.py"
      provides: "Deep cache-invalidation facade"
      exports: ["invalidate_series_cache"]
    - path: "spoilerless/app/cache/graph_cache.py"
      provides: "Docstring call-site inventory for invalidation contract"
      exports: ["invalidate_series", "get_cached_graph", "set_cached_graph"]
  key_links:
    - from: "spoilerless/app/api/revisions.py"
      to: "spoilerless/app/services/graph.py"
      via: "Revert invalidation after execute_write"
      pattern: "invalidate_series_cache"
    - from: "spoilerless/app/api/candidates.py"
      to: "spoilerless/app/services/graph.py"
      via: "All ingest/review invalidation call sites"
      pattern: "invalidate_series_cache"
  prohibitions:
    - "Never reorder the epoch bump before the key deletes (D-30 race separation); never make invalidation fail closed on Redis errors (cache is a performance layer — swallow)."
    - "Do not change TTLs, key shapes, or add an epoch to graph keys (separate perf decision, out of scope)."
    - "Do not add a write-coordinator abstraction — the facade + documented inventory is the deliberate boundary of this plan."
    - "Security breadcrumb: no OWASP/injection/GDPR item is minted here — reverting a canonical resource is already 409-guarded; this plan only fixes cache freshness, never authorization."
---

<objective>
Make cache invalidation structural (Candidate C6, 12-RESEARCH.md §3.6): concentrate the 12 remembered `invalidate_series` call sites behind one deep `invalidate_series_cache` facade in the services layer (so the api layer stops importing cache internals), fix the verified LIVE omission — `POST /revisions/{id}/revert` mutates visible graph content but never invalidates, leaving up to 300s of stale responses — and pin the epoch/deletion semantics with the existing _FakeRedis harness.
</objective>

<context>
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-RESEARCH.md (Candidate C6, §3.6 + corrected evidence §1: 12 call sites; revert omission at api/revisions.py:152-177)
@spoilerless/app/cache/graph_cache.py (FULL — invalidate_series at lines 138-160, viz epoch keys 178-190)
@spoilerless/app/services/graph.py (post-12-12: read_visible_graph deep entry)
@spoilerless/app/api/candidates.py (invalidate call sites at ~153, 292, 324, 364)
@spoilerless/app/api/change_set.py (~116, 189)
@spoilerless/app/api/user_content.py (~168, 205, 221, 234, 270, 286)
@spoilerless/app/api/revisions.py (revert_revision 146-177)
@spoilerless/tests/test_visualization_cache.py (_FakeRedis harness)
@spoilerless/tests/test_revisions.py
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add the deep invalidate_series_cache facade</name>
  <files>spoilerless/app/services/graph.py, spoilerless/app/cache/graph_cache.py</files>
  <read_first>spoilerless/app/cache/graph_cache.py (invalidate_series full body lines 138-160 + the docstring contract lines 1-21), spoilerless/app/services/graph.py (post-12-12 file)</read_first>
  <action>
1. In `spoilerless/app/services/graph.py` add (importing `invalidate_series` from `spoilerless.app.cache.graph_cache`):
   `async def invalidate_series_cache(self, series_id: str) -> None:` — body: `await invalidate_series(series_id)` with the same best-effort swallow semantics (no try/except change: cache.graph_cache.invalidate_series already swallows Redis errors internally — verify and preserve). Docstring: "Deep invalidation seam for every content-mutating write (D-30: epoch bump before key deletes; T-08-06-01 over-invalidation is safe; T-08-06-02 errors swallowed)."
2. In `spoilerless/app/cache/graph_cache.py`:
   - Extend the module docstring with the call-site inventory (the contract is discoverable): every api-layer mutation route that must invalidate — candidates ingest/review routes (4 sites), change_set confirm/reject routes (2), user_content write routes (6), revision revert (added by this plan) — and the note that new mutation paths must call the service facade, not raw cache internals, and that full impossibility-of-forgetting (a write coordinator wrapping execute_write) is deliberately out of scope.
   - Do NOT modify `invalidate_series` itself (epoch math, ordering, swallow semantics unchanged).
  </action>
  <acceptance_criteria>
- `grep -n "def invalidate_series_cache" spoilerless/app/services/graph.py` shows the deep method delegating to `invalidate_series`.
- `grep -n "invalidate_series" spoilerless/app/cache/graph_cache.py` shows the inventory docstring entry AND the unchanged `def invalidate_series` (epoch-bump-then-delete order intact).
- `unset PYTHONPATH && uv run python -c "from spoilerless.app.main import app"` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Replace the 12 call sites and fix the revert omission</name>
  <files>spoilerless/app/api/candidates.py, spoilerless/app/api/change_set.py, spoilerless/app/api/user_content.py, spoilerless/app/api/revisions.py</files>
  <read_first>spoilerless/app/api/candidates.py (invalidate sites ~153, 292, 324, 364 + the import at line 16), spoilerless/app/api/change_set.py (~116, 189 + import line 24), spoilerless/app/api/user_content.py (~168, 205, 221, 234, 270, 286 + import line 10), spoilerless/app/api/revisions.py (revert_revision 146-177)</read_first>
  <action>
1. In `api/candidates.py`, `api/change_set.py`, `api/user_content.py`: replace `from spoilerless.app.cache.graph_cache import invalidate_series` with `from spoilerless.app.services.graph import GraphService` (if not already imported via deps — after 12-12 the routers use GraphServiceDependency; add an `invalidate_series_cache` usage via a `GraphServiceDependency`-typed service parameter only where the route already has one, or construct `GraphService(database)` consistently with the route's existing pattern) and replace every `await invalidate_series(series_id)` call with `await service.invalidate_series_cache(series_id)` (naming the service variable to match the route's existing DI name — `graph_service`, `service`, or the deps-provided instance).
   - Exact call sites (verify line numbers at HEAD with `grep -n "invalidate_series" spoilerless/app/api/*.py`): candidates.py 4 sites (ingest + review routes), change_set.py 2 sites (confirm + reject), user_content.py 6 sites (note/custom-node/custom-relationship writes).
2. In `api/revisions.py` `revert_revision`: after `result = await database.execute_write(revert_revision_work, command)` add `await service.invalidate_series_cache(series_id)` (obtain the service via the route's existing database dependency — construct `GraphService(database)` matching the file's current factory pattern, or reuse the GraphServiceDependency from 12-12 if the route was migrated) with a comment: "WRITE-FRESHNESS (C6): revert re-creates/restores visible content — the series cache must not serve pre-revert state for up to the 300s TTL." Keep the command build and response validation untouched.
  </action>
  <acceptance_criteria>
- `grep -rn "from spoilerless.app.cache.graph_cache import invalidate_series" spoilerless/app/api` returns NOTHING (no api router imports cache internals for invalidation).
- `grep -rn "invalidate_series_cache" spoilerless/app/api | wc -l` == 13 total call sites (12 replaced + 1 new revert site).
- `grep -n "invalidate_series_cache" spoilerless/app/api/revisions.py` shows exactly one call, placed AFTER `execute_write(revert_revision_work, command)` and BEFORE the return.
- `unset PYTHONPATH && uv run python -c "from spoilerless.app.main import app"` exits 0.
  </acceptance_criteria>
</task>

<task type="test">
  <name>Task 3: Pin invalidation semantics and the revert regression</name>
  <files>spoilerless/tests/test_visualization_cache.py, spoilerless/tests/test_revisions.py</files>
  <read_first>spoilerless/tests/test_visualization_cache.py (the _FakeRedis harness — get_redis monkeypatch pattern, invalidate_series tests), spoilerless/tests/test_revisions.py (existing revert tests + scratch fixture)</read_first>
  <action>
1. In `spoilerless/tests/test_visualization_cache.py`, extend with (using the existing `_FakeRedis` harness — no live Redis, no Neo4j):
   - `test_invalidate_series_facade_epoch_bump_and_delete`: call `GraphService(fake_db).invalidate_series_cache(series_id)` (or the service-level function) with the fake redis pointed at `graph_cache.get_redis`; assert `graph_revision:{sid}` incremented exactly once, `graph:{sid}:*` and `viz:{sid}:*` keys deleted, and that the epoch bump happened BEFORE the deletes (record the operation order in the fake, D-30 race separation).
   - `test_invalidate_series_cache_swallows_redis_errors`: fake redis raising on delete — the facade returns normally (T-08-06-02, no fail-closed).
2. In `spoilerless/tests/test_revisions.py`, add the revert-freshness regression (scratch series + teardown; never `series_dexter`; acting user is the scratch user, never `ae8a41b7-db96-40e8-b6c2-2e3c69aedb11`):
   - `test_revert_invalidates_series_cache`: using the same _FakeRedis harness pattern (or a monkeypatched `invalidate_series_cache` spy on the service), create a note, revert its revision via the API, and assert the series invalidation was invoked for the scratch series id (spy assertion, drift-agnostic) — the behavioral contract: after revert, a subsequent graph read must not hit a pre-revert cache entry.
3. Behavior-unchanged suites must stay green without edits: `unset PYTHONPATH && uv run pytest spoilerless/tests/test_candidate_ingest.py spoilerless/tests/test_candidate_review.py spoilerless/tests/test_change_set_api.py spoilerless/tests/test_user_content_api.py -q` (invalidation is a side effect; these assert write behavior).
  </action>
  <acceptance_criteria>
- The two new test_visualization_cache.py tests pass (epoch-bump-before-delete ordering asserted through the fake's operation log; swallow-on-error asserted).
- `test_revert_invalidates_series_cache` passes with the spy assertion on the scratch series id.
- All four behavior-unchanged suites pass with ZERO edits.
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_revisions.py -q` passes (existing + new tests).
  </acceptance_criteria>
  <validation>
Run the suites sequentially (never two pytest processes in parallel against the shared AuraDB); finish with `unset PYTHONPATH && uv run python scripts/run_backend_tests.py --chunk cache` if the chunk name exists, else the full 10-chunk runner.
  </validation>
</task>

</tasks>

<verification>
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_visualization_cache.py spoilerless/tests/test_revisions.py -q` → all pass (epoch ordering, swallow semantics, revert regression).
- `unset PYTHONPATH && uv run pytest spoilerless/tests/test_candidate_ingest.py spoilerless/tests/test_candidate_review.py spoilerless/tests/test_change_set_api.py spoilerless/tests/test_user_content_api.py -q` → green, unedited.
- Full backend suite (10-chunk runner) green; scratch series + teardown only; never `series_dexter`.
</verification>

## Artifacts this phase produces

- `spoilerless/app/services/graph.py`: `invalidate_series_cache(series_id) -> None` deep facade (delegates to cache.graph_cache.invalidate_series; epoch-bump-before-delete + swallow semantics).
- `spoilerless/app/cache/graph_cache.py`: docstring call-site inventory (invalidation contract + deliberate no-write-coordinator boundary).
- `spoilerless/app/api/candidates.py`, `api/change_set.py`, `api/user_content.py`: 12 call sites migrated to the facade; cache-import lines removed.
- `spoilerless/app/api/revisions.py`: revert route now invalidates after `execute_write` (the real omission fix).
- `spoilerless/tests/test_visualization_cache.py`: +2 tests (ordering, swallow-on-error).
- `spoilerless/tests/test_revisions.py`: +1 revert-invalidation regression (spy assertion on scratch series id).

## Flagged assumptions (unresolved probe edges)

The phase edge probe surfaced 15 edges (6 unclassified, 2 boundary, 2 precision, 2 idempotency, 3 concurrency) against the 12-CONTEXT.md requirement texts; all remained **UNRESOLVED** (specless fallback protocol: never auto-resolve, never dismiss). Edges assigned to this plan:

- **[E-IDP-2] idempotency edge — UNRESOLVED (probe):** repeated/duplicate invalidation of the same series (double-bump). Today the epoch INCR makes over-invalidation safe by construction (T-08-06-01); the facade must not add deduplication or guards — a second bump is harmless and stays harmless.
- **[E-CON-3] concurrency edge — UNRESOLVED (probe):** epoch bump racing concurrent writers and readers (a reader embedding the just-bumped epoch while a delete lags). The D-30 ordering (bump before deletes) is the existing mitigation; preserved byte-for-byte — do not "improve" the race window.
- **[E-UNC-6] unclassified edge — UNRESOLVED (probe):** cache keys written by a transaction that later rolls back (invalidation fired but the write aborted — a benign miss, never a stale serve). Current behavior preserved; the facade adds no cleanup for aborted writes.

Remaining unresolved edges are flagged in their best-fitting plans: E-BND-1/E-BND-2/E-PRC-1/E-UNC-1 → 12-10; E-UNC-2 → 12-11; E-PRC-2/E-CON-1/E-UNC-3 → 12-12; E-CON-2/E-IDP-1/E-UNC-4 → 12-13; E-UNC-5 → 12-14.