---
phase: 12-post-hardening-remediation-and-code-quality
plan: 08
type: execute
wave: 2
depends_on: [07]
files_modified: [
  "frontend/src/App.tsx",
  "frontend/src/hooks/useWorkspaceScene.ts",
  "frontend/src/hooks/useWorkspaceNavigation.ts",
  "frontend/src/hooks/useVisualization.ts",
  "frontend/src/components/layout/ResizableRail.tsx",
  "frontend/src/components/layout/AppIcons.tsx",
  "frontend/src/components/graph/GraphCanvas.tsx",
  "frontend/src/components/graph/useCytoscapeBridge.ts",
  "frontend/src/components/graph/useCytoscapeLayout.ts",
  "frontend/src/components/graph/autoZoomHold.ts",
  "frontend/src/components/dialogs/CreateCustomNodeDialog.tsx",
  "frontend/src/components/dialogs/CreateRelationshipDialog.tsx",
  "frontend/src/components/detail/DetailPanel.tsx",
  "frontend/src/components/detail/CharacterPortrait.tsx",
  "frontend/src/components/detail/tabs/OverviewTab.tsx",
  "frontend/src/components/detail/tabs/NotesTab.tsx",
  "frontend/src/components/detail/tabs/ClaimsTab.tsx",
  "frontend/src/components/detail/tabs/EvidenceTab.tsx"
]
autonomous: true
requirements: [
  THERMO-P0-02,
  THERMO-P0-03,
  THERMO-P0-04,
  THERMO-P2-05,
  THERMO-P2-07
]
estimate: {tokens: 25000, raw_tokens: 25000, tasks: 4, confidence: high}
must_haves:
  truths:
    - "frontend/src/App.tsx is reduced from 1,198 lines to < 350 lines with zero render-phase state mutations."
    - "frontend/src/components/graph/GraphCanvas.tsx is reduced from 1,120 lines to < 350 lines with Cytoscape lifecycle encapsulated in dedicated hooks."
    - "frontend/src/components/detail/DetailPanel.tsx is reduced from 1,049 lines to < 250 lines with separate tab subcomponents."
    - "All 1,000-line maintainability ceiling violations across frontend files are permanently resolved."
    - "ResizableRail is a shared pointer-capture drag-rail component used by both timeline and chat sheets."
    - "All 44 Vitest test suites (404+ tests) pass with zero behavioral or visual regressions."
  artifacts:
    - path: "frontend/src/hooks/useWorkspaceScene.ts"
      provides: "Unified hook for visualization DTO fetching, expansions, and derived scene merging"
    - path: "frontend/src/components/layout/ResizableRail.tsx"
      provides: "Unified resizable rail primitive"
    - path: "frontend/src/components/graph/useCytoscapeBridge.ts"
      provides: "Cytoscape event listener, hover, and selection dispatching bridge"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/hooks/useWorkspaceScene.ts"
      via: "Declarative scene data consumption"
---

<objective>
Execute the "Code Judo" structural simplification of the frontend architecture:
1. Decompose monolithic `App.tsx` (1,198 lines -> <350 lines) by extracting `useWorkspaceScene` (DTO fetching, expansions, derived merges, in-flight race condition guards), `useWorkspaceNavigation`, `useVisualization`, and `AppIcons.tsx`. Eliminate render-phase `setState` mutations.
2. Decompose `GraphCanvas.tsx` (1,120 lines -> <350 lines) by extracting `useCytoscapeBridge`, `useCytoscapeLayout`, `CreateCustomNodeDialog.tsx`, and scoping `autoZoomHold` state safely.
3. Decompose `DetailPanel.tsx` (1,049 lines -> <250 lines) by extracting `CreateRelationshipDialog.tsx`, `CharacterPortrait.tsx`, and tab components (`OverviewTab`, `NotesTab`, `ClaimsTab`, `EvidenceTab`). Delete artificial `setTimeout` loading hacks.
4. Extract unified `ResizableRail.tsx` primitive replacing duplicate pointer capture math in `App.tsx` and `ChatSheet.tsx`.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-CONTEXT.md
@frontend/src/App.tsx
@frontend/src/components/graph/GraphCanvas.tsx
@frontend/src/components/detail/DetailPanel.tsx
@frontend/src/components/chat/ChatSheet.tsx
</context>

<tasks>

<task type="execute">
  <name>Task 1: Extract unified ResizableRail and AppIcons primitives</name>
  <files>
    frontend/src/components/layout/ResizableRail.tsx,
    frontend/src/components/layout/AppIcons.tsx,
    frontend/src/components/chat/ChatSheet.tsx
  </files>
  <action>
1. Create `frontend/src/components/layout/ResizableRail.tsx`:
   - Abstract pointer down, move, up handlers with `setPointerCapture` and fallback window event listeners for jsdom compatibility.
   - Support orientation (`horizontal` vs `vertical`), min/max bounds, default width/height, and `onResize(newDimension)`.
   - Implement keyboard accessible arrow keys for step resizing.
2. Refactor `frontend/src/components/chat/ChatSheet.tsx` to use `ResizableRail`.
3. Create `frontend/src/components/layout/AppIcons.tsx` to hold raw SVG icon definitions and branding graphics extracted from `App.tsx`.
  </action>
</task>

<task type="execute">
  <name>Task 2: Extract DetailPanel subcomponents and dialogs</name>
  <files>
    frontend/src/components/detail/DetailPanel.tsx,
    frontend/src/components/dialogs/CreateRelationshipDialog.tsx,
    frontend/src/components/detail/CharacterPortrait.tsx,
    frontend/src/components/detail/tabs/OverviewTab.tsx,
    frontend/src/components/detail/tabs/NotesTab.tsx,
    frontend/src/components/detail/tabs/ClaimsTab.tsx,
    frontend/src/components/detail/tabs/EvidenceTab.tsx
  </files>
  <action>
1. Extract `CreateRelationshipDialog` to `frontend/src/components/dialogs/CreateRelationshipDialog.tsx`.
2. Extract `CharacterPortrait` to `frontend/src/components/detail/CharacterPortrait.tsx`.
3. Extract each tab into `frontend/src/components/detail/tabs/`:
   - `OverviewTab.tsx`: Summary, entity meta, quick actions, backlinks link.
   - `NotesTab.tsx`: Note list, `NoteItem`, and `NoteEditor`.
   - `ClaimsTab.tsx`: Claim list and filtering.
   - `EvidenceTab.tsx`: Evidence chains and citations.
4. In `DetailPanel.tsx`:
   - Delete the artificial `setTimeout(() => setResolved(true), 0)` hack; replace with pure synchronous `useMemo` for claims and evidence.
   - Import tab subcomponents; keep `DetailPanel.tsx` as a lean (< 250 lines) tab shell container.
  </action>
</task>

<task type="execute">
  <name>Task 3: Decompose GraphCanvas.tsx and encapsulate Cytoscape lifecycles</name>
  <files>
    frontend/src/components/graph/GraphCanvas.tsx,
    frontend/src/components/graph/useCytoscapeBridge.ts,
    frontend/src/components/graph/useCytoscapeLayout.ts,
    frontend/src/components/graph/autoZoomHold.ts,
    frontend/src/components/dialogs/CreateCustomNodeDialog.tsx
  </files>
  <action>
1. Extract `CreateCustomNodeDialog` to `frontend/src/components/dialogs/CreateCustomNodeDialog.tsx`.
2. Create `frontend/src/components/graph/useCytoscapeBridge.ts`:
   - Encapsulate Cytoscape canvas instance attachment, viewport event listeners (`tap`, `mouseover`, `mouseout`, `cxttap`), and hover card placement state.
3. Create `frontend/src/components/graph/useCytoscapeLayout.ts`:
   - Encapsulate layout execution (`cose-bilkent`, `concentric`, `preset`), position caching, and expansion placement algorithms (`localPlacementFor`).
4. Update `autoZoomHold.ts` to support instance-scoped or ref-based hold state instead of an unbounded global mutable singleton.
5. In `GraphCanvas.tsx`:
   - Coordinate the bridge and layout hooks; render canvas container, overlays (`NodeHoverCard`, `GraphStatus`, `GraphLegend`), and dialogs cleanly (< 350 lines).
  </action>
</task>

<task type="execute">
  <name>Task 4: Decompose App.tsx and extract scene and navigation hooks</name>
  <files>
    frontend/src/App.tsx,
    frontend/src/hooks/useWorkspaceScene.ts,
    frontend/src/hooks/useWorkspaceNavigation.ts,
    frontend/src/hooks/useVisualization.ts
  </files>
  <action>
1. Create `frontend/src/hooks/useVisualization.ts`:
   - Standardize projection loading on `useFetchState`.
2. Create `frontend/src/hooks/useWorkspaceScene.ts`:
   - Encapsulate active view resolution (`topTab` + `evidenceMode` -> `VisualizationViewType`).
   - Load base visualization DTO.
   - Manage expansion records with an in-flight series/episode verification guard to eliminate race condition contamination (THERMO-P2-07).
   - Derive `mergedVisualization` and `newlyRevealedIds` using pure `useMemo` computations.
   - Eliminate all render-phase `setState` mutations (`if (graphData && activeGraph !== graphData) setActiveGraph(graphData)`).
3. Create `frontend/src/hooks/useWorkspaceNavigation.ts`:
   - Manage tab selection, story sub-modes, command palette state, and keyboard hotkeys.
4. Refactor `App.tsx`:
   - Consume `useWorkspaceScene`, `useWorkspaceNavigation`, `useWatchProgress`, and `useAuth`.
   - Render `AppShell`, `GraphCanvas`, `DetailPanel`, `ChatSheet`, `TimelineView`, `CommandPalette`, and modals declaratively (< 350 lines).
  </action>
</task>

<task type="test">
  <name>Task 5: Run full frontend test suite</name>
  <files>frontend/src/App.test.tsx, frontend/src/components/graph/GraphCanvas.test.tsx</files>
  <action>
Execute `npm run test` (Vitest) and `npm run build` to verify all 44 test suites pass and TypeScript compiles with zero errors.
  </action>
</task>

</tasks>
