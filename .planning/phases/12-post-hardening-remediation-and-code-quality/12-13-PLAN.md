---
phase: 12-post-hardening-remediation-and-code-quality
plan: 13
type: execute
wave: 3
depends_on: [08]
files_modified: [
  "frontend/src/hooks/useSceneState.ts",
  "frontend/src/hooks/useSceneState.test.ts",
  "frontend/src/App.tsx",
  "frontend/src/hooks/useWorkspaceScene.ts",
  "frontend/src/components/graph/GraphCanvas.tsx",
  "frontend/src/components/graph/GraphFilterPanel.tsx",
  "frontend/src/components/graph/filterState.ts",
  "frontend/src/components/graph/focusReducer.ts",
  "frontend/src/lib/graph/highlight.ts",
  "frontend/src/lib/graph/positionCache.ts",
  "frontend/src/components/graph/GraphCanvas.test.tsx"
]
autonomous: true
requirements: [THERMO-P0-02, THERMO-P0-03]
estimate: {tokens: 16000, raw_tokens: 16000, tasks: 3, confidence: high}
must_haves:
  truths:
    - "The scene reducer (useSceneState, D-24) is the SINGLE authority for filters, focus, and expansions on the graph canvas: GraphCanvas holds no local filterState useState and no focusReducer useReducer; GraphFilterPanel stays controlled but reads scene slices and emits dispatches (SET_NODE_KIND_FILTER / SET_EDGE_CLASS_FILTER / SET_ALL_FILTERS)."
    - "filterState.ts and focusReducer.ts are DELETED. positionCache (getCachedPositions/setCachedPositions/__resetPositionCacheForTests, bounded Map) lives in frontend/src/lib/graph/positionCache.ts; applyFocusToCytoscape is imported from lib/graph/highlight directly (no focusReducer re-export)."
    - "Default filter behavior is preserved exactly: all node kinds and all edge families visible on a fresh scene (seeded maps derived from NODE_TYPES + EDGE_TYPE_TO_FAMILY), AND the absent-key semantics are pinned by a test: a filter map key absent from the record reads as visible."
    - "Expansions are no longer double-booked: the parallel expansionRecords useState is gone; mergedVisualization derives from scene.expansions + scene.expansionHistory via useMemo; no dispatches or setState calls execute during render (React 19 rule)."
    - "All 44 Vitest suites (404+ tests) pass and `npm run build` (tsc strict) passes with zero regressions; useSceneState.test.ts is extended with the new filter/focus/expansion contracts."
  artifacts:
    - path: "frontend/src/lib/graph/positionCache.ts"
      provides: "Relocated bounded position cache with test-reset seam"
      exports: ["getCachedPositions", "setCachedPositions", "__resetPositionCacheForTests"]
    - path: "frontend/src/hooks/useSceneState.ts"
      provides: "Scene authority extended to filters/focus/expansions (existing exports unchanged)"
      exports: ["useSceneState", "SceneState", "SceneAction", "INITIAL_SCENE_STATE", "isServerSafeId"]
  key_links:
    - from: "frontend/src/hooks/useSceneState.ts"
      to: "frontend/src/components/graph/GraphCanvas.tsx"
      via: "scene + dispatchScene props"
      pattern: "dispatchScene"
    - from: "frontend/src/components/graph/GraphFilterPanel.tsx"
      to: "frontend/src/hooks/useSceneState.ts"
      via: "Scene-filter action dispatches"
      pattern: "SET_NODE_KIND_FILTER"
  prohibitions:
    - "Do not reintroduce render-phase setState/dispatch (the P0 finding 12-08 eliminates): all dispatches happen in event handlers or effects; mergedVisualization is a useMemo."
    - "Do not change the JSON-serializability contract (D-24/T10-FOCUS-04) of SceneState — no functions, no cy references, no DOM in state."
    - "Do not alter filter-application visuals or the focus-animation entry behavior; GraphCanvas keeps applying `.filtered-out` marking — only the state authority moves."
    - "Security breadcrumb: no OWASP/injection/GDPR item is minted here — the server-safe-id focus charset (T10-FOCUS-02) and the absent-key-visible default are preserved and pinned, never loosened."
---

<objective>
Make scene state ONE reducer (Candidate C4, 12-RESEARCH.md §3.4): after 12-08's decomposition, the scene state is still split-brained — GraphCanvas keeps parallel `filterState` useState + `focusReducer` useReducer, App/useWorkspaceScene double-books expansions (`setExpansionRecords` + `dispatchScene`), and `filterState.ts` also owns the module-level positionCache. Fold filters and focus into the existing `useSceneState` reducer (its `SET_NODE_KIND_FILTER`/`SET_EDGE_CLASS_FILTER`/`SET_ALL_FILTERS`/`SET_FOCUS`/`CLEAR_FOCUS`/`ADD_EXPANSION` actions already exist), relocate the position cache, delete both state modules, and pin the new contracts with tests.
</objective>

<context>
@.planning/phases/12-post-hardening-remediation-and-code-quality/12-RESEARCH.md (Candidate C4, §3.4 + semantics trap)
@frontend/src/hooks/useSceneState.ts (FULL)
@frontend/src/hooks/useSceneState.test.ts
@frontend/src/App.tsx (post-12-08 layout: scene wiring in App.tsx or useWorkspaceScene.ts)
@frontend/src/components/graph/GraphCanvas.tsx (lines ~505-620, ~820-880, ~1050-1060)
@frontend/src/components/graph/GraphFilterPanel.tsx (props at lines 11-16)
@frontend/src/components/graph/filterState.ts
@frontend/src/components/graph/focusReducer.ts
@frontend/src/lib/graph/highlight.ts
@frontend/src/components/graph/GraphCanvas.test.tsx (line 7 imports the reset seam)
@frontend/src/components/graph/relationshipStyles.ts (EDGE_TYPE_TO_FAMILY)
@frontend/src/lib/nodeTypes.ts (NODE_TYPES)
</context>

<tasks>

<task type="execute">
  <name>Task 1: Fold filters into the scene reducer and rewire GraphFilterPanel</name>
  <files>frontend/src/hooks/useSceneState.ts, frontend/src/hooks/useSceneState.test.ts, frontend/src/App.tsx, frontend/src/hooks/useWorkspaceScene.ts, frontend/src/components/graph/GraphCanvas.tsx, frontend/src/components/graph/GraphFilterPanel.tsx</files>
  <read_first>frontend/src/hooks/useSceneState.ts (SceneState.nodeKindFilters/edgeClassFilters + SET_*_FILTER actions + INITIAL_SCENE_STATE), frontend/src/components/graph/GraphCanvas.tsx (lines 595-610: allNodeTypes/allEdgeFamilies/filterState useState; lines 820-880: filter application), frontend/src/components/graph/GraphFilterPanel.tsx (controlled props + EDGE_TYPE_TO_FAMILY derivation), frontend/src/lib/nodeTypes.ts (NODE_TYPES), frontend/src/components/graph/relationshipStyles.ts (EDGE_TYPE_TO_FAMILY)</read_first>
  <action>
1. In `frontend/src/components/graph/GraphCanvas.tsx`:
   - DELETE `const [filterState, setFilterState] = useState<FilterState>(() => initialFilterState(allNodeTypes, allEdgeFamilies))` (line ~601) and the `allNodeTypes`/`allEdgeFamilies` useMemos (lines ~599-600, families hardcoded `['CHARACTER','STRUCTURAL','EPISODE','USER']` — the drift source).
   - Receive `scene` + `dispatchScene` via props (12-08 already wires the scene through the workspace; extend the prop types with the filter slices).
   - Filter application stays in GraphCanvas: the `Object.entries(filterState.nodeTypes)` loop (lines ~827+) now reads `scene.nodeKindFilters` with absent-key = visible (`entry visible !== false`), and the `filterState.edgeFamilies` reads become `scene.edgeClassFilters`; `.filtered-out` marking and timelines unchanged.
2. In the scene owner (App.tsx or useWorkspaceScene.ts — post-12-08 home of the scene): seed the reducer with initialized filter maps so default-all-visible is preserved WITHOUT changing the hook's `{}` default:
   - Node kinds: `Object.fromEntries(NODE_TYPES.map((nt) => [nt.type, true]))`.
   - Edge families: `Array.from(new Set(Object.values(EDGE_TYPE_TO_FAMILY)))` mapped to `true` (kills the hardcoded 4-family list; matches GraphFilterPanel.tsx:19's own derivation).
   - Seed via the existing `useSceneState({ nodeKindFilters: ..., edgeClassFilters: ... })` initial-state parameter (the hook already supports it — useSceneState.test.ts:31 uses it).
3. `frontend/src/components/graph/GraphFilterPanel.tsx`: keep the component CONTROLLED — change props from `filterState: FilterState` + `onToggleNodeType/onToggleEdgeFamily/onSetAll` to scene slices + dispatch callbacks that emit `SET_NODE_KIND_FILTER`/`SET_EDGE_CLASS_FILTER`/`SET_ALL_FILTERS` with the same toggle semantics. Other panel visuals (role=switch rows, All/None, 44px rows) untouched.
4. In `frontend/src/hooks/useSceneState.ts`: no action changes needed for filters (actions exist). If the reducer's filter records need absent-key-visible handling in the derived reads, add a pure helper `isFilterEnabled(filters: Record<string, boolean>, key: string): boolean` (absent => true) and export it for GraphCanvas; keep SceneState JSON-serializable.
  </action>
  <acceptance_criteria>
- `grep -rn "filterState" frontend/src/components/graph/GraphCanvas.tsx frontend/src/components/graph/GraphFilterPanel.tsx frontend/src/App.tsx frontend/src/hooks/useWorkspaceScene.ts` returns NOTHING (identifier fully retired from consumers).
- `grep -n "useState<FilterState>\|initialFilterState" frontend/src/components/graph/GraphCanvas.tsx` returns NOTHING.
- `grep -n "SET_NODE_KIND_FILTER\|SET_EDGE_CLASS_FILTER\|SET_ALL_FILTERS" frontend/src/components/graph/GraphFilterPanel.tsx frontend/src/App.tsx` shows dispatch wiring.
- `grep -rn "'CHARACTER', 'STRUCTURAL', 'EPISODE', 'USER'" frontend/src` returns NOTHING (hardcoded family list gone; EDGE_TYPE_TO_FAMILY-derived).
- `cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/graph/GraphFilterPanel.test.tsx src/components/graph/GraphCanvas.test.tsx src/hooks/useSceneState.test.ts --reporter=dot` passes.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Fold focus and expansions into the scene reducer; delete both state modules</name>
  <files>frontend/src/components/graph/GraphCanvas.tsx, frontend/src/components/graph/focusReducer.ts, frontend/src/components/graph/filterState.ts, frontend/src/lib/graph/positionCache.ts, frontend/src/lib/graph/highlight.ts, frontend/src/App.tsx, frontend/src/hooks/useWorkspaceScene.ts, frontend/src/components/graph/GraphCanvas.test.tsx</files>
  <read_first>frontend/src/components/graph/GraphCanvas.tsx (line ~605 useReducer(focusReducer, initialFocusState()); the reveal/focus application path), frontend/src/components/graph/focusReducer.ts (re-exports applyFocusToCytoscape at line 5), frontend/src/lib/graph/highlight.ts (line 5 comment references focusReducer), frontend/src/components/graph/filterState.ts (lines 62-107 positionCache), frontend/src/App.tsx (post-12-08 expansion wiring: expand/undo/collapse handlers and mergedVisualization useMemo, previously lines ~380-417; note useSceneState's ADD_EXPANSION record param was built for this)</read_first>
  <action>
1. Focus:
   - In `GraphCanvas.tsx`, DELETE `const [focusState, dispatchFocus] = useReducer(focusReducer, initialFocusState())` (line ~605). Replace `dispatchFocus({ type: 'FOCUS_NODE', id })`/`CLEAR_FOCUS` with `dispatchScene({ type: 'SET_FOCUS', nodeIds: [id], edgeIds: [] })`/`{ type: 'CLEAR_FOCUS' }`; read focus from `scene.focus` (SceneFocus `{ nodeIds, edgeIds } | null`).
   - Change the `applyFocusToCytoscape` import in GraphCanvas from the `./focusReducer` re-export to a direct `from '../../lib/graph/highlight'` import (check the exact relative path at HEAD; do not alias through focusReducer).
   - In `lib/graph/highlight.ts`, fix the comment that references focusReducer (line ~5) to reference the scene reducer instead.
2. Expansions (App.tsx / useWorkspaceScene.ts, post-12-08 layout):
   - DELETE the parallel `expansionRecords` useState (and its setter) that double-books with `dispatchScene` at the expand/undo/collapse handlers.
   - `mergedVisualization` (currently a useMemo with `expansionRecords` in the deps) now derives from `scene.expansions` + `scene.expansionHistory` only; it must remain a `useMemo` — no render-phase dispatch. The expand handler dispatches `ADD_EXPANSION` with `nodeIds` + `record` (the ExpansionRecord the fetch produced); undo dispatches `UNDO_EXPANSION`; collapse dispatches `COLLAPSE_EXPANSION` with the anchorId.
   - The in-flight race guard from 12-08 (expansion responses validated against the current scene before dispatch) stays in place.
3. Position cache relocation:
   - Create `frontend/src/lib/graph/positionCache.ts` with `getCachedPositions`, `setCachedPositions` (bounded Map, MAX_CACHED_POSITION_KEYS = 20, key shapes `${seriesId}:${visibleUntilOrder}:${mode}` and `${seriesId}:${viewKey}`), and `__resetPositionCacheForTests` — moved verbatim from filterState.ts (lines 62-107) with the same doc comments (PROB-09/#74 bound, D-23 viewKey override).
   - Update GraphCanvas.tsx and `GraphCanvas.test.tsx` line 7 (`import { __resetPositionCacheForTests } from './filterState'` -> `from '../../lib/graph/positionCache'`). Verify with `grep -rn "filterState\|positionCache" frontend/src --include="*.ts" --include="*.tsx"` that filterState.ts has zero importers left.
   - DELETE `frontend/src/components/graph/filterState.ts` and `frontend/src/components/graph/focusReducer.ts` (verify `git ls-files` first; both are tracked).
  </action>
  <acceptance_criteria>
- `grep -rn "focusReducer\|initialFocusState\|FOCUS_NODE" frontend/src` returns NOTHING (focusReducer fully removed; the FOCUS action vocabulary is SET_FOCUS/CLEAR_FOCUS).
- `grep -rn "filterState" frontend/src` returns NOTHING; `grep -n "__resetPositionCacheForTests" frontend/src/lib/graph/positionCache.ts frontend/src/components/graph/GraphCanvas.test.tsx` shows the relocated export + the updated import.
- `grep -n "expansionRecords" frontend/src/App.tsx frontend/src/hooks/useWorkspaceScene.ts` returns NOTHING; `grep -n "useMemo" frontend/src/hooks/useWorkspaceScene.ts` shows mergedVisualization still derived via useMemo with `scene.expansions` deps.
- `cd frontend && NODE_ENV=test CI=1 npx vitest run src/hooks/useSceneState.test.ts src/components/graph/GraphCanvas.test.tsx --reporter=dot` passes.
  </acceptance_criteria>
</task>

<task type="test">
  <name>Task 3: Extend useSceneState tests and run the full frontend gate</name>
  <files>frontend/src/hooks/useSceneState.test.ts</files>
  <read_first>frontend/src/hooks/useSceneState.test.ts (existing JSON round-trip / D-24 / server-safe-id tests), frontend/src/hooks/useSceneState.ts (reducer actions)</read_first>
  <action>
Extend `useSceneState.test.ts` (existing tests untouched) with:
1. `filters`: dispatch `SET_NODE_KIND_FILTER`/`SET_EDGE_CLASS_FILTER`/`SET_ALL_FILTERS` and assert the records update; assert absent-key-visible: `isFilterEnabled({}, 'Character') === true` and `isFilterEnabled({ Character: false }, 'Character') === false`.
2. `focus`: `SET_FOCUS`/`CLEAR_FOCUS` round-trip; JSON round-trip of the full state still passes with a non-null focus (D-24/T10-FOCUS-04 serializability contract unchanged).
3. `expansions`: `ADD_EXPANSION` with a record pushes `expansionHistory` and `expansions`; `UNDO_EXPANSION` removes exactly the recorded additionIds; `COLLAPSE_EXPANSION` removes records for the anchor (semantics already implemented — the tests pin them for the new wiring consumers).
Then run the FULL gate: `cd frontend && NODE_ENV=test CI=1 npm run test` (all 44 suites) and `cd frontend && npm run build` (tsc strict + vite build — this catches test-file TS errors the vitest run misses, per the 09-07 build blind spot). Fix any type errors in the touched files, not by weakening tests.
  </action>
  <acceptance_criteria>
- All new useSceneState tests pass; the existing useSceneState test file's original assertions are unchanged.
- `cd frontend && NODE_ENV=test CI=1 npm run test` exits 0 (44 suites, 404+ tests).
- `cd frontend && npm run build` exits 0 (tsc strict across referenced projects — no TS18048/TS2339-style errors in the touched files).
  </acceptance_criteria>
  <validation>
Run `npm run test` BEFORE `npm run build`; if the build surfaces type errors in test files (the known blind spot), fix the source/tests and re-run both.
  </validation>
</task>

</tasks>

<verification>
- `cd frontend && NODE_ENV=test CI=1 npm run test` → all 44 suites green.
- `cd frontend && npm run build` → exits 0.
- `grep -rn "filterState\|focusReducer\|expansionRecords" frontend/src` → no matches.
- Manual sanity (optional, live browser): filters default-all-on, focus animation on tap, expansion undo removes exactly the added nodes.
</verification>

## Artifacts this phase produces

- `frontend/src/lib/graph/positionCache.ts` (new): `getCachedPositions`, `setCachedPositions`, `__resetPositionCacheForTests`.
- `frontend/src/hooks/useSceneState.ts`: optional new pure export `isFilterEnabled(filters, key)`; reducer otherwise unchanged (actions already existed).
- `frontend/src/hooks/useSceneState.test.ts`: new filter/focus/expansion contract tests.
- `frontend/src/components/graph/GraphCanvas.tsx`: filter/focus state removed (props-in); `.filtered-out` application reads scene slices.
- `frontend/src/components/graph/GraphFilterPanel.tsx`: prop shape changed to scene slices + dispatch callbacks.
- `frontend/src/App.tsx` / `frontend/src/hooks/useWorkspaceScene.ts`: expansion single-booking via scene dispatches; mergedVisualization derived from scene.
- `frontend/src/lib/graph/highlight.ts`: comment updated (focusReducer reference removed).
- Deleted files: `frontend/src/components/graph/filterState.ts`, `frontend/src/components/graph/focusReducer.ts`.

## Flagged assumptions (unresolved probe edges)

The phase edge probe surfaced 15 edges (6 unclassified, 2 boundary, 2 precision, 2 idempotency, 3 concurrency) against the 12-CONTEXT.md requirement texts; all remained **UNRESOLVED** (specless fallback protocol: never auto-resolve, never dismiss). Edges assigned to this plan:

- **[E-CON-2] concurrency edge — UNRESOLVED (probe):** in-flight expansion/visualization responses arriving after a scene switch (the 12-08 race guard covers mismatch rejection; the deeper question of partial-apply vs drop stays open). This plan keeps the 12-08 guard exactly as built; do not add new merge logic.
- **[E-IDP-1] idempotency edge — UNRESOLVED (probe):** reducer dispatch idempotency under React 19 StrictMode double-invocation. The reducer is pure (no side effects), which is the invariant this plan preserves; no dispatch deduplication is added.
- **[E-UNC-4] unclassified edge — UNRESOLVED (probe):** scene position cache key collision between the graph path and the visualization path (both use `${seriesId}:` prefixed keys; the viewKey override was D-23's resolution). The bounded-Map semantics relocate verbatim; no key-shape change is made.

Remaining unresolved edges are flagged in their best-fitting plans: E-BND-1/E-BND-2/E-PRC-1/E-UNC-1 → 12-10; E-UNC-2 → 12-11; E-PRC-2/E-CON-1/E-UNC-3 → 12-12; E-UNC-5 → 12-14; E-IDP-2/E-CON-3/E-UNC-6 → 12-15.