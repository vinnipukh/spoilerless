---
phase: 09-feature-expansion-full-audit-remediation
plan: 14
type: execute
wave: 7
depends_on: ["09-01", "09-13"]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/graph/layoutConfig.ts
  - frontend/src/components/graph/filterState.ts
  - frontend/src/components/graph/focusReducer.ts
  - frontend/src/components/graph/GraphFilterPanel.tsx
  - frontend/src/types/cytoscape-fcose.d.ts
  - frontend/src/components/graph/GraphCanvas.tsx
  - frontend/src/components/graph/graphElements.ts
  - frontend/src/components/graph/graphStylesheet.ts
  - frontend/src/components/graph/GraphControls.tsx
  - frontend/src/components/graph/GraphLegend.tsx
  - frontend/src/components/graph/GraphCanvas.test.tsx
autonomous: true
requirements: [PROB-32]
must_haves:
  truths:
    - "The flat cose-bilkent pass is replaced by a cluster-aware cytoscape-fcose layout driven by compound parent nodes (subplot/cluster tag or Event.sequence_in_episode bands) — the #57 hairball is solved structurally, not just filtered (D-03)"
    - "Node-type/edge-type filter toggles, zoom-based label culling (labels hidden below zoom 0.8, all shown above 1.4), a focus/neighborhood mode over the existing .faded/.selected-dominant classes, and deterministic positions cached per (seriesId, visibleUntilOrder) all ship (D-04)"
    - "GraphCanvas.tsx is not grown — layout config, filter state, and the focus reducer are extracted into three new modules (D-06)"
    - "GraphCanvas.test.tsx's toHaveLength(11) assertion is count-independent (D-05) — the toy fixture stays, the assertion changes"
  artifacts:
    - path: "frontend/src/components/graph/layoutConfig.ts"
      provides: "fcose registration + layoutOptionsFor('fcose'|'cose-bilkent'|'cose')"
    - path: "frontend/src/components/graph/filterState.ts"
      provides: "Filter toggles + per-(seriesId, boundary) position cache"
    - path: "frontend/src/components/graph/focusReducer.ts"
      provides: "Focus/neighborhood reducer over .faded/.selected-dominant"
    - path: "frontend/src/components/graph/GraphFilterPanel.tsx"
      provides: "Node-type + edge-family filter chips panel (FEAT-11.4 filters)"
  key_links:
    - from: "graphElements.ts graphToElements"
      to: "node data parent: clusterId"
      via: "cluster key = subplot/cluster tag when carried, else visible_from_order band via the episodes prop"
      pattern: "parent"
    - from: "GraphCanvas.tsx runLayout"
      to: "layoutConfig.ts layoutOptionsFor('fcose')"
      via: "the existing imperative layout path (effect keyed on graph), randomize: false + cached positions"
      pattern: "runLayout"
  prohibitions:
    - id: "NO-GOD-FILE-GROWTH"
      statement: "Layout config, filter state, and focus logic must live in the extracted modules — not appended to GraphCanvas.tsx (D-06)"
    - id: "NO-FUSE-JS"
      statement: "No new dependency beyond cytoscape-fcose@2.2.0 — fuse.js is forbidden (UI-SPEC registry safety)"
    - id: "NO-COUNT-LOCKED-ASSERTS"
      statement: "GraphCanvas tests must not assert exact element counts against the enriched seed — count-independent only (D-05)"
---

<objective>
The graph-density overhaul (PROB-32, #57) — the LAST frontend plan by
sequencing constraint: replace the flat cose-bilkent layout with a
cluster-aware cytoscape-fcose compound layout, ship the FEAT-11.4 filter
toggles, zoom-based label culling, focus/neighborhood mode, deterministic
positions, and edge opacity falloff — with layout config, filter state, and
the focus reducer EXTRACTED out of the 530-line god-file (D-06). This is the
only new dependency in the phase: cytoscape-fcose@2.2.0 (package-legitimacy
gate PASSED; the ONLY allowed new dependency per the zero-cost constraint).

Purpose: At real content density (32 Characters / 39 Events / 17 Objects /
5 Orgs / 22 Locations / 132 Claims → ~90 visible nodes at boundary 1) the
canvas must separate clusters, reduce visual noise, and stay deterministic
across boundary round-trips.
Output: fcose cluster layout + filter panel + culling + focus reducer +
deterministic positions + count-independent tests.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md (§Pattern 4 fcose, §Code Examples: fcose registration, §Pitfall 2)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md (§GraphCanvas.tsx; §graphElements.ts; §graphStylesheet.ts; §GraphControls.tsx)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md (§10.2 PROB-32 + D-03/D-04 contract)
@.planning/REQUIREMENTS.md (PROB-32)
@docs/PROBLEMS.md (#57)
@frontend/src/components/graph/GraphCanvas.tsx
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install cytoscape-fcose + extract layoutConfig + cluster parent nodes</name>
  <read_first>
    frontend/package.json (current deps; cytoscape-cose-bilkent present),
    frontend/src/components/graph/GraphCanvas.tsx (lines 33-87 — the
    layoutOptionsFor + try/catch registration block; the imperative runLayout
    path keyed on the graph effect),
    frontend/src/components/graph/graphElements.ts (graphToElements ~25-66 —
    node data {id, label, nodeType, origin, imageUrl?}),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Code Examples: fcose options — quality:'default', randomize:false,
    nodeRepulsion:8000, idealEdgeLength:100, padding:48, animate:
    prefersReducedMotion ? false : 'end'),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md
    (§10.2 locked fcose options + cluster parent style)
  </read_first>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/types/cytoscape-fcose.d.ts, frontend/src/components/graph/layoutConfig.ts, frontend/src/components/graph/graphElements.ts, frontend/src/components/graph/GraphCanvas.tsx</files>
  <action>
    Install cytoscape-fcose pinned to 2.2.0 (the phase's ONLY new dependency —
    package-legitimacy gate PASSED, iVis-at-Bilkent): `npm install
    cytoscape-fcose@2.2.0` in frontend/. Create frontend/src/types/cytoscape-fcose.d.ts
    mirroring the existing types/cytoscape-cose-bilkent.d.ts shim pattern.

    Extract layoutConfig.ts from GraphCanvas.tsx's layout block (lines 33-87):
    module-level fcose registration inside the existing try/catch fallback
    pattern (cytoscape.use(fcose); on failure fall back to 'cose' like the
    cose-bilkent guard), and layoutOptionsFor(name: 'fcose' | 'cose-bilkent' |
    'cose') where the fcose branch returns the LOCKED options: name 'fcose',
    quality 'default', randomize false, nodeRepulsion 8000, idealEdgeLength
    100, edgeElasticity 0.45, padding 48, animate prefersReducedMotion ? false
    : 'end'. Keep the existing cose-bilkent/cose branches byte-equivalent.

    Cluster parents: in graphElements.ts, attach `parent: clusterId` to node
    data — cluster key = subplot/cluster tag when the payload carries one,
    else an episode band derived from visible_from_order via the episodes prop
    (e.g. "S01E01"); emit one compound parent element per cluster id with
    data {id: 'cluster:' + key, label: episode code}. In GraphCanvas.tsx,
    delete the old cose-bilkent import/registration block and route the
    existing imperative runLayout path through the extracted layoutConfig
    (the test-double guard — `if (typeof cy.layout !== 'function') return` —
    MUST be kept).
    Commit with message "feat(09-14): cytoscape-fcose + layoutConfig extraction + cluster parents (PROB-32)".
  </action>
  <verify>
    <automated>cd frontend && node -e "console.log(require('cytoscape-fcose/package.json').version)" | grep -q "2.2.0"</automated>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/graph/GraphCanvas.test.tsx 2>&1 | tail -4</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - cytoscape-fcose@2.2.0 is in package.json (pinned, exact version)
    - layoutConfig.ts exports the fcose branch with the locked options; GraphCanvas imports it; the test-double guard survives
    - graphElements emits parent: clusterId + compound parent elements
    - vitest + npm run build green
  </acceptance_criteria>
  <done>fcose is installed and the cluster-aware layout foundation is extracted.</done>
</task>

<task type="auto">
  <name>Task 2: Filter panel + zoom label culling + focus reducer + edge opacity falloff</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md
    (§10.2 — GraphFilterPanel floating top-center below the search bar,
    bg-card ring-1 ring-border rounded-md shadow-sm p-2 w-64; "Nodes" chips
    (Character/Event/Location/Organization/Object/Episode with NodeSwatch dot,
    rounded-full bg-muted px-2.5 py-1 text-xs), "Relationships" chips from
    EDGE_TYPE_TO_FAMILY with EdgeSwatch; hidden type → chip opacity-40; "All"/
    "None" text-xs footer buttons; culling thresholds zoom < 0.8 hide labels
    (cluster + selected labels stay), > 1.4 show all; focus toggle =
    MousePointer2 icon, active bg-accent text-accent-foreground; edge opacity
    0.6 at zoom < 1.0),
    frontend/src/components/graph/GraphLegend.tsx (NODE_TYPES + EDGE_TYPE_TO_FAMILY exports),
    frontend/src/components/graph/GraphCanvas.tsx (~346-397 the .faded/.selected-dominant logic)
  </read_first>
  <files>frontend/src/components/graph/filterState.ts, frontend/src/components/graph/focusReducer.ts, frontend/src/components/graph/GraphFilterPanel.tsx, frontend/src/components/graph/graphStylesheet.ts, frontend/src/components/graph/GraphControls.tsx, frontend/src/components/graph/GraphCanvas.tsx</files>
  <action>
    Create filterState.ts: module-level helpers for node-type/edge-family
    visibility toggles (a plain record of boolean toggles + toggle/all/none
    ops) and a positions cache keyed by `${seriesId}:${visibleUntilOrder}`
    (getCachedPositions/setCachedPositions — the useGraph.ts key-string
    precedent).

    Create focusReducer.ts: a reducer over the existing class logic — given
    (cy, focusedId) applies .selected-dominant to the focused element,
    .faded to everything outside its closed neighborhood (1-hop), and clears
    on empty-canvas tap or an explicit clear action (the GraphFocusIndicator
    Clear pattern).

    Create GraphFilterPanel.tsx per UI-SPEC: floating panel top-center with
    the Nodes chips (NodeSwatch dot + type label), Relationships chips
    (EDGE_TYPE_TO_FAMILY + EdgeSwatch), opacity-40 for hidden types, and
    "All"/"None" footer buttons. Toggles are instant class toggles
    (.filtered-out { display: none } in the stylesheet), never a relayout.

    graphStylesheet.ts: add the compound-parent style block (background
    var(--elevated) #1E2740, border var(--border) width 1, radius 8, label
    episode code text-muted-foreground 10px, padding 24 — children keep all
    existing per-type shapes/colors), .filtered-out { display: none }, zoom
    label culling as a stylesheet function on cy.zoom() + cy.on('zoom')
    re-render (thresholds 0.8/1.4; cluster + selected labels always visible),
    and edge opacity falloff (non-hovered edges at rest 0.6 below zoom 1.0,
    hovered/active stay 1).

    GraphCanvas.tsx: wire the filter panel, focus toggle (GraphControls
    MousePointer2 button, active bg-accent), and culling — all through the
    extracted modules; GraphLegend gains a cluster row when cluster tags are
    active.
    Commit with message "feat(09-14): filter panel + zoom culling + focus reducer + edge falloff (PROB-32/D-04)".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/graph/GraphCanvas.test.tsx 2>&1 | tail -4</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - GraphFilterPanel renders the chips + All/None contract; toggles are class-only (no relayout)
    - Culling hides labels below zoom 0.8 (cluster + selected always visible) and shows all above 1.4
    - Focus mode applies .faded/.selected-dominant via the reducer and clears on empty-tap/Clear
    - Edge opacity 0.6 at zoom < 1.0 (hovered/active stay 1); vitest + build green
  </acceptance_criteria>
  <done>The density controls (filters, culling, focus, falloff) are live through the extracted modules.</done>
</task>

<task type="auto">
  <name>Task 3: Deterministic positions + count-independent test assertions (D-05)</name>
  <read_first>
    frontend/src/components/graph/GraphCanvas.test.tsx (the ~line 200
    toHaveLength(11) assertion locked to the old 11-node seed fixture),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Assumption A8 — keep the toy fixture, make assertions count-independent),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md
    (§Interaction Contract: deterministic layout — randomize:false + positions
    cached per (seriesId, visibleUntilOrder); Reset re-runs from cache, never
    a random seed)
  </read_first>
  <files>frontend/src/components/graph/GraphCanvas.tsx, frontend/src/components/graph/GraphCanvas.test.tsx, frontend/src/components/graph/GraphControls.tsx</files>
  <action>
    Deterministic positions (D-04): after each successful fcose layout run,
    capture node positions and persist them via filterState's positions cache
    keyed by `${seriesId}:${visibleUntilOrder}`; on a boundary round-trip or
    re-render, restore cached positions instead of re-randomizing (fcose runs
    with randomize:false, so the cache is the determinism guarantee across
    fetches). GraphControls' Reset re-runs the layout FROM THE CACHE — never a
    random seed.

    Count-independent tests (D-05, Assumption A8): in GraphCanvas.test.tsx,
    replace the toHaveLength(11) assertion (locked to the old 11-node seed)
    with count-independent checks — assert structure/behavior (e.g. elements
    map 1:1 to the fixture's node ids, cluster parents are present for the
    fixture's episode bands, filter toggles change visibility classes, focus
    mode applies .faded) rather than exact counts. Keep the toy fixture
    untouched.
    Commit with message "feat(09-14): deterministic cached positions + count-independent canvas tests (PROB-32/D-04/D-05)".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/graph/GraphCanvas.test.tsx 2>&1 | tail -4</automated>
    <automated>rg -n "toHaveLength\(11\)" frontend/src/components/graph/GraphCanvas.test.tsx | wc -l</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
    <automated>cd frontend && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Positions are cached per (seriesId, visibleUntilOrder) and restored on boundary round-trips; Reset re-runs from cache
    - No toHaveLength(11)-style exact-count assertions remain in GraphCanvas.test.tsx (rg zero)
    - vitest + npm run build + npm run lint (0/0) green
  </acceptance_criteria>
  <done>The canvas is deterministic across boundary round-trips and the tests are count-independent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layout engine <-> canvas | fcose registration fallback must not crash on load |
| Filter/focus state <-> cytoscape classes | Class-only toggles, no data mutation |
| Position cache <-> boundary | Cache keyed by boundary — a boundary change must never restore stale positions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-14-01 | Denial of Service | fcose registration failure blanking the canvas | medium | mitigate | try/catch registration with 'cose' fallback (existing pattern, kept) |
| T-09-14-02 | Tampering | Filter state mutating element data | low | mitigate | Class toggles only (.filtered-out display none); no data changes |
| T-09-14-03 | Spoofing | Stale positions across boundary change | low | mitigate | Cache key includes visibleUntilOrder; restore only on exact key match |
| T-09-14-04 | Tampering | Exact-count tests re-locking to seed | low | mitigate | Count-independent assertions + rg gate |
| T-09-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | ONLY cytoscape-fcose@2.2.0 (legitimacy gate PASSED); fuse.js forbidden |
</threat_model>

<verification>
Run GraphCanvas.test.tsx + the full frontend suite, npm run build, npm run
lint (0/0). Grep gates: `rg -n "fuse" frontend/package.json frontend/src`
zero; `rg -n "toHaveLength\(11\)"` zero; `rg -n "cose-bilkent"` in
GraphCanvas.tsx shows no leftover import block (only the fallback union in
layoutConfig). Confirm package.json has cytoscape-fcose@2.2.0 pinned.
</verification>

<success_criteria>
The #57 hairball is solved: cluster-aware fcose layout with compound parents,
filter toggles, zoom culling, focus mode, and deterministic positions — all
through extracted modules, with count-independent tests and the full frontend
gate green. PROB-32 (and with it FEAT-11.4 graph filters) is complete.
</success_criteria>

## Artifacts this phase produces

- `frontend/package.json` + `package-lock.json` (MODIFIED) —
  cytoscape-fcose@2.2.0 (the phase's only new dependency)
- `frontend/src/types/cytoscape-fcose.d.ts` (NEW) — type shim
- `frontend/src/components/graph/layoutConfig.ts` (NEW) — fcose registration +
  locked options (D-03)
- `frontend/src/components/graph/filterState.ts` (NEW) — toggles + position
  cache (D-04)
- `frontend/src/components/graph/focusReducer.ts` (NEW) — focus/neighborhood
  reducer (D-04)
- `frontend/src/components/graph/GraphFilterPanel.tsx` (NEW) — filter chips
  (FEAT-11.4)
- `frontend/src/components/graph/graphElements.ts` (MODIFIED) — parent:
  clusterId + compound parents
- `frontend/src/components/graph/graphStylesheet.ts` (MODIFIED) — cluster
  parents, .filtered-out, culling fn, edge opacity falloff
- `frontend/src/components/graph/GraphCanvas.tsx` (MODIFIED) — wiring only
  (D-06 extraction)
- `frontend/src/components/graph/GraphControls.tsx` (MODIFIED) — focus toggle
- `frontend/src/components/graph/GraphLegend.tsx` (MODIFIED) — cluster row
- `frontend/src/components/graph/GraphCanvas.test.tsx` (MODIFIED) —
  count-independent assertions (D-05)

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-14-SUMMARY.md` when done
</output>
