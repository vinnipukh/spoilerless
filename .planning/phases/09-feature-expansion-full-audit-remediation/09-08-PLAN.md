---
phase: 09-feature-expansion-full-audit-remediation
plan: 08
type: execute
wave: 2
depends_on: ["09-01", "09-03", "09-04"]
files_modified:
  - spoilerless/tests/test_candidate_ingest.py
  - spoilerless/tests/test_candidate_review.py
  - spoilerless/tests/test_seed_idempotency.py
  - spoilerless/tests/conftest.py
  - spoilerless/scripts/zombie_sweep.py
  - spoilerless/app/graph/setup.py
  - spoilerless/app/graph/seed.py
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - docs/DEPLOYMENT.md
  - docs/RUNBOOK.md
  - spoilerless/tests/test_database.py
  - spoilerless/tests/test_ontology.py
  - spoilerless/tests/test_series_service.py
  - spoilerless/tests/test_api_series.py
  - spoilerless/tests/test_deps.py
  - spoilerless/tests/test_config.py
  - spoilerless/tests/test_main_lifespan.py
autonomous: true
requirements: [PROB-06, PROB-18, PROB-20, PROB-22]
must_haves:
  truths:
    - "The backend suite runs against a disposable/scratch Neo4j — candidate tests are scratch-series-scoped with teardown fixtures, seed tests are order/state-independent, and nothing mutates the live application graph (PROB-06/#14/#15, D-07/D-08)"
    - "A one-time zombie sweep script exists with a mandatory dry-run mode, never touches the protected dev user, and CI gains a DB-pollution gate (PROB-22/#46)"
    - "Core modules — db driver, ontology loader, series service, api/series, deps, config, lifespan/health — have direct unit tests (PROB-18/#40)"
    - "A startup schema check (or documented reseed path) exists so seed drift like the episode-props 01N52 storm cannot hide again (PROB-20/#44)"
    - "Carry-over 09-07 (dependency scanning, artifact publication, staged promotion, branch protection) and 09-08 (incident/rollback runbook) land as code/docs artifacts; platform wiring is folded into the final operator wave"
  artifacts:
    - path: "spoilerless/scripts/zombie_sweep.py"
      provides: "Dry-run-first zombie AppUser/Session sweep script (PROB-22)"
    - path: "docs/RUNBOOK.md"
      provides: "Incident/rollback runbook (09-08 carry-over)"
    - path: ".github/workflows/release.yml"
      provides: "Staged-promotion workflow skeleton (09-07 carry-over)"
  key_links:
    - from: "spoilerless/tests/test_candidate_ingest.py"
      to: "scratch series teardown fixture"
      via: "series_scratch_* id + teardown deleting series rows AND origin='candidate' residue AND UserSeriesProgress rows"
      pattern: "series_scratch"
    - from: ".github/workflows/ci.yml backend job"
      to: "DB-pollution gate"
      via: "post-suite residue check + dependency scan step + artifact upload"
      pattern: "pip-audit|upload-artifact"
  prohibitions:
    - id: "NEVER-LIVE-DB-SUITE"
      statement: "The full backend suite must never run against live AuraDB — CI fresh container or local docker only (Pitfall 1)"
    - id: "NEVER-DELETE-REAL-USER"
      statement: "The zombie sweep must never delete the protected dev user id; only rows with no progress/chat/ownership ties (D-07)"
    - id: "NO-EXACT-SEED-SETS"
      statement: "Seed-idempotency assertions must be order/state-independent or additive — exact-set counts against a live DB are forbidden"
    - id: "NO-UNSTAGED-ENV"
      statement: "No .env or config.json value may be committed by this plan's commits"
---

<objective>
Make the test suite trustworthy and the CI pipeline complete: candidate and
seed tests move to scratch series with teardown fixtures (#14/#15/#46, D-07),
seed assertions become state-independent, a dry-run-first zombie sweep script
is created (execution against live AuraDB is gated to the final operator wave
per VALIDATION.md), CI gains a DB-pollution gate plus the carry-over 09-07
code artifacts (dependency scan, artifact upload, staged-promotion workflow,
branch-protection checklist), the incident/rollback runbook (09-08) lands, the
core modules get direct unit tests (#40), and a startup schema check closes
the seed-drift loop (#44).

Purpose: A red or polluting suite cannot gate a public release; the deferred
OPS carry-overs contribute their code/docs artifacts now, with platform wiring
in the operator wave.
Output: A deterministic, scratch-isolated backend suite; sweep tooling;
hardened CI; core-module tests; schema-drift check; runbook.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md (§Pattern 1 scratch-series, §Pitfall 1/2, §Wave 2 guidance, §Environment Availability)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md (§Scratch-series + teardown test isolation; §graph/seed.py)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-VALIDATION.md (§Manual-Only Verifications — sweep/reseed are operator sign-off)
@.planning/REQUIREMENTS.md (PROB-06/18/20/22)
@docs/PROBLEMS.md (#14, #15, #40, #44, #46)
@spoilerless/tests/test_retrieval_tools.py (scratch-series precedent ~74-75)
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scratch-series isolation + teardown fixtures + state-independent seed assertions</name>
  <read_first>
    spoilerless/tests/test_retrieval_tools.py (~74-75 — the existing
    scratch_series context manager precedent: yield a series_scratch_* id,
    teardown DETACH DELETEs everything with that series_id),
    spoilerless/tests/test_candidate_ingest.py (SERIES_ID = "series_dexter" —
    the #46 pollution source),
    spoilerless/tests/test_candidate_review.py (same live-series writes),
    spoilerless/tests/test_seed_idempotency.py (exact count + exact
    constraint-set assertions — the #14 red tests),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md
    (§Scratch-series + teardown — teardown must ALSO delete UserSeriesProgress
    rows and origin='candidate' nodes)
  </read_first>
  <files>spoilerless/tests/test_candidate_ingest.py, spoilerless/tests/test_candidate_review.py, spoilerless/tests/test_seed_idempotency.py, spoilerless/tests/conftest.py</files>
  <action>
    Move test_candidate_ingest.py and test_candidate_review.py off the live
    series_dexter id onto a scratch series id (e.g. series_scratch_candidates
    and series_scratch_review), following the test_retrieval_tools.py
    asynccontextmanager pattern: yield the scratch id, and in the finally
    block delete (a) all nodes with that series_id, (b) any origin='candidate'
    residue (MATCH (n) WHERE n.origin = 'candidate' DETACH DELETE n — the #14
    root cause), and (c) any UserSeriesProgress rows created by the fixtures
    (progress rows carry series_id but no visible_from_order and trip the
    seed-integrity audit — the runbook's documented full-suite contamination
    path). Never touch real user rows and never run against AuraDB (local
    docker / CI container only, D-07/D-08).

    Rework test_seed_idempotency.py: replace exact-count assertions against
    the live DB with order/state-independent assertions — assert relative
    invariants (idempotent re-run produces identical counts to the first run
    on a FRESH scratch database; constraint set is a SUPERSET check that
    tolerates additive constraints like the upcoming ShareToken constraint
    from plan 09-12; provenance/visibility invariants rather than absolute
    numbers). The three currently-red tests must pass deterministically.

    If conftest.py needs a shared scratch-series fixture or a suite-level
    residue guard, add it there (module-scoped, teardown-always).
    Commit with message "test(09-08): scratch-series isolation + state-independent seed assertions (PROB-06/22)".
  </action>
  <verify>
    <automated>uv run pytest spoilerless/tests/test_candidate_ingest.py spoilerless/tests/test_candidate_review.py spoilerless/tests/test_seed_idempotency.py -x -q 2>&1 | tail -3</automated>
    <automated>rg -n 'series_dexter' spoilerless/tests/test_candidate_ingest.py spoilerless/tests/test_candidate_review.py | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Candidate tests use scratch series ids and teardown deletes series rows + candidate residue + progress rows
    - Seed-idempotency assertions are state-independent/additive (superset check) and pass deterministically on a fresh scratch DB
    - No candidate/seed test writes to the live series_dexter graph or real user rows
  </acceptance_criteria>
  <done>The #14/#15/#46 pollution class is eliminated; the three formerly-red seed tests pass deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Zombie sweep script (dry-run first) + CI DB-pollution gate + carry-over 09-07/09-08 artifacts</name>
  <read_first>
    docs/PROBLEMS.md (#46 — 3,855 zombie AppUser rows, 21/21 expired sessions,
    5 orphaned; the never-delete id),
    spoilerless/app/repository/session.py (sweep Cypher shape),
    .github/workflows/ci.yml (backend job — fresh Neo4j service container),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Assumptions A4, §Wave 2: sweep script with dry-run; carry-over 09-07/09-08
    folding guidance)
  </read_first>
  <files>spoilerless/scripts/zombie_sweep.py, .github/workflows/ci.yml, .github/workflows/release.yml, docs/DEPLOYMENT.md, docs/RUNBOOK.md</files>
  <action>
    Zombie sweep script (PROB-22): create spoilerless/scripts/zombie_sweep.py
    as a standalone python script (the runbook's ad-hoc AuraDB audit pattern:
    AsyncGraphDatabase.driver with the same TLS normalization as
    graph/database.py — neo4j:// + encrypted=True + TrustCustomCAs(certifi)).
    Modes: --dry-run (default, mandatory first) prints counts of (a) AppUser
    rows with NO progress/chat/ownership ties, (b) expired and orphaned
    Session nodes; --execute performs the deletions AFTER the dry-run count is
    shown. HARD RULES baked into the script: never delete the protected dev
    user id (a module constant matching the runbook's never-delete list);
    only delete AppUser rows with no outgoing/incoming ownership edges; delete
    sessions only where expires_at < now OR revoked_at IS NOT NULL OR the
    owner no longer exists. The script must print the exact counts before and
    after. Execution against live AuraDB is NOT part of this plan — it is the
    final operator wave's checkpoint-gated step (plan 09-18); this plan only
    creates and locally verifies the script.

    CI DB-pollution gate (PROB-22): add a step to the backend job in
    .github/workflows/ci.yml AFTER the pytest run that fails if residue
    remains — e.g. a Cypher count of origin='candidate' nodes or
    series_scratch_* rows over the CI container after the suite, using the
    same uv invocation the job already uses. Also add the 09-07 code
    artifacts: a dependency-scan step (pip-audit on the lockfile or
    equivalent zero-cost scanner; npm audit in the frontend job) and an
    artifact-upload step (upload test reports/coverage on failure).

    Carry-over 09-07 staged promotion + branch protection: create
    .github/workflows/release.yml as a skeleton staged-promotion workflow
    (draft → release-candidate → release triggers, gated on the CI workflow
    passing) and document the branch-protection settings checklist in
    docs/DEPLOYMENT.md (operator applies them in the GitHub UI during the
    final wave — GitHub settings have no repo-local CLI path).

    Carry-over 09-08 runbook: create docs/RUNBOOK.md — incident detection
    (UptimeRobot alert, /health degraded), diagnosis ladder (the runbook's
    three live-DB counts for chat-dead, structured-log grep points),
    rollback procedure (Render redeploy previous deploy, Vercel rollback,
    graph restore via reseed), and the on-call contact flow. Keep it
    executable by a future operator; no dashboards platform wiring in this
    plan.
    Commit with message "feat(09-08): zombie sweep script + CI pollution gate + dep scan + runbook (PROB-22, 09-07/09-08)".
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('spoilerless/scripts/zombie_sweep.py').read())"</automated>
    <automated>rg -n 'dry-run|dry_run|--execute' spoilerless/scripts/zombie_sweep.py</automated>
    <automated>rg -n 'pip-audit|npm audit|upload-artifact' .github/workflows/ci.yml</automated>
    <automated>test -f docs/RUNBOOK.md && test -f .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - zombie_sweep.py parses, defaults to dry-run, has an --execute mode, and hard-codes the never-delete protection
    - ci.yml has a post-suite pollution gate, a dependency-scan step, and an artifact-upload step
    - release.yml (staged-promotion skeleton) and docs/RUNBOOK.md exist; DEPLOYMENT.md documents the branch-protection checklist
    - No live AuraDB mutation was executed by this plan (script creation only)
  </acceptance_criteria>
  <done>Sweep tooling, CI gates, and the 09-07/09-08 code artifacts exist and are locally verified.</done>
</task>

<task type="auto">
  <name>Task 3: Direct unit tests for core modules + startup schema check</name>
  <read_first>
    spoilerless/app/graph/database.py (Neo4jDatabase — open/execute_write/execute_query
    semantics, the $query-param collision class),
    spoilerless/app/graph/ontology.py (load_ontology — now cached per 09-05),
    spoilerless/app/services/series.py, spoilerless/app/api/series.py,
    spoilerless/app/api/deps.py, spoilerless/app/core/config.py (get_settings
    lru_cache — monkeypatch attributes, never replace),
    spoilerless/app/main.py (lifespan + /health),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Validation Architecture — the Phase Requirements → Test Map row for PROB-18)
  </read_first>
  <files>spoilerless/tests/test_database.py, spoilerless/tests/test_ontology.py, spoilerless/tests/test_series_service.py, spoilerless/tests/test_api_series.py, spoilerless/tests/test_deps.py, spoilerless/tests/test_config.py, spoilerless/tests/test_main_lifespan.py, spoilerless/app/graph/setup.py, spoilerless/app/graph/seed.py</files>
  <action>
    PROB-18/#40 direct unit tests — one new test file per module:
    - test_database.py: driver-construction normalization (the neo4j+s:// →
      neo4j:// + encrypted + TrustCustomCAs path), execute_write/execute_query
      semantics with a fake/scratch driver, and a regression asserting a bound
      parameter named "query" cannot collide with the driver method's first
      positional arg (the documented TypeError class — assert the error shape
      or that the app code avoids the name; use the scratch Neo4j for the
      happy path).
    - test_ontology.py: load_ontology returns the expected structure, is
      cached (call twice, same object / no re-read), and a missing ontology
      file raises the documented error (import-time crash protection).
    - test_series_service.py: series list/get behavior against scratch data.
    - test_api_series.py: route-level list/get with boundary handling
      (post-09-04 anonymous rules).
    - test_deps.py: require_current_user 401 path, require_admin 403 path,
      request.state.user stamping.
    - test_config.py: Settings defaults (session_cookie_secure default True,
      redis_url empty default, ALLOWED_EMAILS parsing), lru_cache behavior
      (monkeypatch attributes on the shared instance).
    - test_main_lifespan.py: lifespan startup/shutdown (rate-limiter guard on
      empty redis_url, the session-sweep task from 09-04 starts/stops), and
      the /health endpoint's 200 (database connected) and 503 (degraded)
      shapes — including the renamed service field from 09-01.

    PROB-20/#44 startup schema check: add a lightweight schema-drift check to
    the setup path (spoilerless/app/graph/setup.py or seed.py): after
    setup_database, verify the expected episode properties (the
    synopsis_visible_from_order / image_visible_from_order fields whose
    absence caused the 01N52 storm) exist on seeded Episode nodes and print a
    warning or fail with a clear message when drift is detected, so a stale
    live DB cannot hide again. The live AuraDB reseed itself is the final
    operator wave's gated step (plan 09-18); this plan ships the check + a
    test that it passes on a freshly seeded scratch DB.
    Commit with message "test(09-08): core module direct tests + startup schema check (PROB-18/20)".
  </action>
  <verify>
    <automated>uv run pytest spoilerless/tests/test_database.py spoilerless/tests/test_ontology.py spoilerless/tests/test_series_service.py spoilerless/tests/test_api_series.py spoilerless/tests/test_deps.py spoilerless/tests/test_config.py spoilerless/tests/test_main_lifespan.py -x -q 2>&1 | tail -3</automated>
    <automated>uv run pytest spoilerless/tests/test_seed_idempotency.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Seven new test files exist covering database/ontology/series service/api series/deps/config/lifespan-health
    - The $query-collision regression and the ontology import-time crash class are asserted
    - The startup schema check exists and passes on a fresh scratch seed; the 01N52 drift class is detected by it
  </acceptance_criteria>
  <done>Core modules have direct tests and seed drift is detectable at startup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite <-> shared Neo4j | Scratch series only; teardown always |
| Sweep script <-> live AuraDB | Dry-run mandatory; never-delete list enforced; execution gated to operator wave |
| CI workflow <-> repo secrets | No secret values in workflow files |
| Core driver layer <-> tests | The $query-param collision class must be regression-locked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08-01 | Tampering | Suite pollution of live graph (#14/#15/#46) | critical | mitigate | Scratch-series + teardown fixtures + CI pollution gate; never AuraDB |
| T-09-08-02 | Availability | Sweep deleting real users (A4) | high | mitigate | Dry-run-first script, never-delete constant, ownership-tie predicate, operator-wave execution |
| T-09-08-03 | Tampering | Dependency supply chain (09-07) | high | mitigate | pip-audit/npm audit scan steps in CI |
| T-09-08-04 | Information Disclosure | Schema drift hiding (01N52 storm, #44) | medium | mitigate | Startup schema check + fresh-seed test |
| T-09-08-05 | Spoofing | Driver-layer param collision class | medium | mitigate | test_database.py regression asserting the documented error shape |
| T-09-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | Only pip-audit/npm audit tooling run in CI — no new runtime dependencies |
</threat_model>

<verification>
Run the seven new core test files plus test_candidate_ingest,
test_candidate_review, test_seed_idempotency on the scratch Neo4j. Grep
gates: zero 'series_dexter' references in candidate tests; zombie_sweep.py
has dry-run + never-delete constant; ci.yml has pollution gate + dep scan +
artifact upload. Confirm no .env file staged in this plan's commits.
</verification>

<success_criteria>
The suite is deterministic and scratch-isolated; sweep tooling and CI gates
exist; core modules are directly tested; schema drift is detectable; the
09-07/09-08 carry-over code artifacts are in the tree — with all live-DB
destructive execution deferred to the operator wave.
</success_criteria>

## Artifacts this phase produces

- `spoilerless/tests/test_candidate_ingest.py` + `test_candidate_review.py`
  (MODIFIED) — scratch-series ids + teardown fixtures
- `spoilerless/tests/test_seed_idempotency.py` (MODIFIED) — state-independent/
  additive assertions
- `spoilerless/tests/conftest.py` (MODIFIED) — shared scratch fixture if needed
- `spoilerless/scripts/zombie_sweep.py` (NEW) — dry-run-first sweep script
  with never-delete protection
- `.github/workflows/ci.yml` (MODIFIED) — DB-pollution gate, pip-audit/npm
  audit steps, artifact upload
- `.github/workflows/release.yml` (NEW) — staged-promotion skeleton
- `docs/DEPLOYMENT.md` (MODIFIED) — branch-protection checklist
- `docs/RUNBOOK.md` (NEW) — incident/rollback runbook (09-08)
- `spoilerless/app/graph/setup.py` / `seed.py` (MODIFIED) — startup
  schema-drift check (PROB-20)
- NEW test files: test_database.py, test_ontology.py, test_series_service.py,
  test_api_series.py, test_deps.py, test_config.py, test_main_lifespan.py

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-08-SUMMARY.md` when done
</output>
