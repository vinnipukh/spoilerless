---
phase: 09-feature-expansion-full-audit-remediation
plan: 13
type: execute
wave: 3
depends_on: ["09-01", "09-12"]
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/detail/DetailPanel.tsx
  - frontend/src/components/chat/ChatSheet.tsx
  - frontend/src/components/graph/GraphControls.tsx
  - frontend/src/components/graph/GraphLegend.tsx
  - frontend/src/components/episode/EpisodeSelector.tsx
  - frontend/src/components/detail/BacklinksTab.tsx
  - frontend/src/components/graph/NodeHoverCard.tsx
  - frontend/src/components/detail/RevisionHistoryPanel.tsx
  - frontend/src/components/detail/RevisionHistoryPanel.test.tsx
  - frontend/src/components/graph/GraphCanvas.tsx
  - frontend/src/components/ErrorBoundary.tsx
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/components/graph/GraphCanvas.test.tsx
autonomous: true
requirements: [FEAT-10, FEAT-11, PROB-21]
must_haves:
  truths:
    - "The graph and detail panel are usable on mobile viewports — touch pan/zoom/tap native, topBar wraps, panels full-width on <md, 44px targets (FEAT-10)"
    - "FEAT-11 second-brain touches ship: backlinks panel (reverse relationships + mentions), hover preview card (md-only), per-node properties (from existing payload), and version history as real before→after values in RevisionHistoryPanel"
    - "A root-level error boundary plus a per-panel boundary around chat prevents one uncaught render error from blanking the app; the committed debug log in GraphCanvas is removed (PROB-21/#45)"
  artifacts:
    - path: "frontend/src/components/ErrorBoundary.tsx"
      provides: "Root + per-panel error boundaries (PROB-21)"
    - path: "frontend/src/components/detail/BacklinksTab.tsx"
      provides: "Incoming-edge + note-mention backlinks tab (FEAT-11.1)"
    - path: "frontend/src/components/graph/NodeHoverCard.tsx"
      provides: "120ms-delay hover preview card (FEAT-11.2)"
  key_links:
    - from: "DetailPanel.tsx"
      to: "BacklinksTab.tsx + properties dl"
      via: "new Backlinks tab (ListTree icon) + Overview properties from existing payload fields"
      pattern: "BacklinksTab"
    - from: "GraphCanvas.tsx"
      to: "NodeHoverCard.tsx"
      via: "node mouseover handler → renderedPosition() + 12px offset, dismiss on mouseout/Esc, hidden md:block"
      pattern: "renderedPosition"
  prohibitions:
    - id: "NO-NEW-DATA-FIELDS"
      statement: "FEAT-11 properties surface only what the payload already carries — no new backend fields (UI-SPEC 10.13.5)"
    - id: "NO-BLANK-APP"
      statement: "An uncaught render error must never unmount the whole root — error boundary at root + chat panel (PROB-21)"
    - id: "NO-DEBUG-LOGS"
      statement: "Committed debug console.log statements in GraphCanvas are removed (PROB-21/#45)"
---

<objective>
Finish the frontend wave with three interlocking workstreams: mobile
responsiveness (FEAT-10), the FEAT-11 "second-brain" touches (backlinks,
hover preview card, per-node properties, real before/after revision diffs),
and the PROB-21 error-boundary hardening (#45) — one uncaught render error
must never blank the app again. All changes stay inside the locked design
system (UI-SPEC tokens, 44px targets, reduced-motion rules).

Purpose: The app becomes phone-usable, detail-rich, and crash-resilient before
the graph-density overhaul lands on top (plan 09-14).
Output: Mobile breakpoint pass, four second-brain surfaces, and root + chat
error boundaries with the debug log removed.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md (§10.12 FEAT-10, §10.13 FEAT-11, §Copywriting, §Interaction Contract)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md (§GraphControls.tsx; §DetailPanel.tsx; §RevisionHistoryPanel.tsx)
@.planning/REQUIREMENTS.md (FEAT-10/11, PROB-21)
@docs/PROBLEMS.md (#45)
@frontend/src/components/detail/DetailPanel.tsx
@frontend/src/components/graph/GraphCanvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile responsive pass (FEAT-10)</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md
    (§10.12 — topBar flex-wrap gap-2; wordmark text-2xl hidden sm:block;
    SeriesSelect/EpisodeSelector full row width on <md; DetailPanel sheet
    w-full sm:w-[420px], tabs overflow-x-auto; ChatSheet full-width on mobile;
    GraphControls expandable chip on <md or bottom-4 left-4 clear of legend;
    legend defaults closed on <md; pb-[env(safe-area-inset-bottom)] on
    bottom-anchored controls; no pointer-type detection, md: classes only),
    frontend/src/components/layout/AppShell.tsx,
    frontend/src/components/detail/DetailPanel.tsx,
    frontend/src/components/chat/ChatSheet.tsx,
    frontend/src/components/graph/GraphControls.tsx,
    frontend/src/components/graph/GraphLegend.tsx,
    frontend/src/components/episode/EpisodeSelector.tsx
  </read_first>
  <files>frontend/src/components/layout/AppShell.tsx, frontend/src/components/detail/DetailPanel.tsx, frontend/src/components/chat/ChatSheet.tsx, frontend/src/components/graph/GraphControls.tsx, frontend/src/components/graph/GraphLegend.tsx, frontend/src/components/episode/EpisodeSelector.tsx</files>
  <action>
    Apply the UI-SPEC 10.12 breakpoint pass with Tailwind md: classes only (no
    pointer-type detection, no JS media queries):
    - AppShell: topBar left cluster gains flex-wrap gap-2; wordmark stays
      text-2xl; subtitle-sized controls hidden sm:block; SeriesSelect and
      EpisodeSelector render at full row width on <md.
    - DetailPanel: sheet w-full sm:w-[420px]; tabs overflow-x-auto.
    - ChatSheet: full-width on mobile per the same sheet treatment.
    - GraphControls: on <md collapse to a single expandable chip (Menu icon)
      revealing the existing 44px icon stack — or keep the stack but raise
      bottom-4 left-4 clear of the legend; add pb-[env(safe-area-inset-bottom)]
      to bottom-anchored controls (GraphControls, legend, share footer if
      present).
    - GraphLegend: defaults closed on <md (Collapsible, existing component).
    - EpisodeSelector: verify-only — existing md:hidden Select takes over on
      <md; no change unless the pass reveals a breakage (then fix per the
      locked pill contract).
    - Canvas: Cytoscape touch pan/zoom/tap is native — no pointer-events
      workarounds; verify tap targets stay >= 44px.
    Verify with the full frontend suite + build; no visual contract changes
    above md.
    Commit with message "feat(09-13): mobile responsive pass (FEAT-10)".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npm run test 2>&1 | tail -4</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - AppShell topBar wraps on <md; DetailPanel/ChatSheet full-width on <md with scrollable tabs
    - GraphControls reachable on <md (expandable chip or raised stack) with safe-area inset; legend defaults closed
    - All above-md visuals unchanged; full suite + build green
  </acceptance_criteria>
  <done>The app is usable on mobile viewports per the FEAT-10 contract.</done>
</task>

<task type="auto">
  <name>Task 2: FEAT-11 second-brain surfaces — backlinks, hover card, properties, revision diff</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md
    (§10.13 — BacklinksTab rows: edge-type chip + source label + Open ghost
    button + "Mentioned in note" rows, empty "No backlinks yet"/"Nothing else
    points to this node."; NodeHoverCard w-56 bg-card p-3 at
    cy.getElementById(id).renderedPosition() + 12px, 120ms delay, hidden
    md:block, label + type badge + "Revealed in {episode code}" + first claim
    snippet <=2 lines; properties dl grid-cols-[auto_1fr] dt text-xs
    text-muted-foreground dd font-medium: Node type, Origin, Revealed in,
    Episode, Claims count, Notes count, Image source — NO new data fields;
    RevisionHistoryPanel diffFields renders Before/After VALUES in a text-xs
    two-line block inside a Collapsible),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md
    (§DetailPanel.tsx tabs; §RevisionHistoryPanel.tsx diffFields)
  </read_first>
  <files>frontend/src/components/detail/BacklinksTab.tsx, frontend/src/components/graph/NodeHoverCard.tsx, frontend/src/components/detail/DetailPanel.tsx, frontend/src/components/detail/RevisionHistoryPanel.tsx, frontend/src/components/detail/RevisionHistoryPanel.test.tsx, frontend/src/components/graph/GraphCanvas.tsx, frontend/src/components/graph/GraphCanvas.test.tsx</files>
  <action>
    Create BacklinksTab.tsx per UI-SPEC 10.13.1: rows for incoming edges
    (edges whose target equals the selected id): edge-type chip (rounded
    bg-muted px-1.5 py-0.5 text-xs) + source node label (font-medium) + "Open"
    ghost button; plus notes containing the node label (substring) as
    "Mentioned in note" rows; row click selects the source node via the
    existing onSelect path; empty state with the locked copy. Add the
    "Backlinks" tab (ListTree icon) to DetailPanel's tab row.

    Create NodeHoverCard.tsx per UI-SPEC 10.13.2: Card (bg-card ring-1
    ring-border rounded-md shadow-sm p-3 w-56) positioned at
    cy.getElementById(id).renderedPosition() + 12px offset, hidden md:block,
    120ms show delay, dismiss on mouseout/Esc: label text-sm font-medium,
    node-type badge, "Revealed in {episode code}" text-xs
    text-muted-foreground, first claim snippet (<=2 lines truncated). Wire the
    existing node mouseover handler in GraphCanvas to it (md-only; touch
    devices keep tap = select with no card).

    DetailPanel Overview tab: add the properties definition list (Node type,
    Origin, Revealed in from visible_from_order, Episode from episode_id,
    Claims count, Notes count, Image source) — surfacing ONLY payload fields.

    RevisionHistoryPanel: upgrade diffFields to render actual before→after
    VALUES ("Before: {old}" text-muted-foreground / "After: {new}"
    text-foreground, text-xs two-line block) inside an expandable Collapsible
    row instead of "field changed" text; update its test to assert the values
    render.
    Commit with message "feat(09-13): second-brain touches — backlinks, hover card, properties, revision diffs (FEAT-11)".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/detail/RevisionHistoryPanel.test.tsx src/components/graph/GraphCanvas.test.tsx 2>&1 | tail -4</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - BacklinksTab + properties dl + hover card render per contract with locked copy; no new data fields
    - RevisionHistoryPanel shows real before/after values; its test asserts them
    - GraphCanvas hover wiring is md-only; vitest + build green
  </acceptance_criteria>
  <done>All four second-brain surfaces are live per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Error boundaries + debug-log removal (PROB-21)</name>
  <read_first>
    docs/PROBLEMS.md (#45 — React 19 unmounts the entire root on an uncaught
    render error; the app has zero error boundaries; the committed module-load
    console.log in GraphCanvas),
    frontend/src/main.tsx (createRoot render site),
    frontend/src/App.tsx (composition root),
    frontend/src/components/chat/ChatPanel.tsx (the per-panel boundary target)
  </read_first>
  <files>frontend/src/components/ErrorBoundary.tsx, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/components/graph/GraphCanvas.tsx</files>
  <action>
    Create frontend/src/components/ErrorBoundary.tsx: a class component
    implementing componentDidCatch + getDerivedStateFromError rendering a
    fallback UI (the app's Alert/destructive pattern with a reload button —
    keep copy generic per the design system; no blank screen). Wrap the root
    render in main.tsx (or App) with the boundary, and wrap ChatPanel with its
    own per-panel boundary so a chat render error cannot take down the rest of
    the app (the #45 scenario). In GraphCanvas.tsx remove the committed
    debug console.log module-load statement (and any other stray debug logs
    the grep finds) — do not touch the legitimate try/catch registration
    logging that matches the cose-bilkent fallback pattern.
    Tests: a GraphCanvas/App-level test rendering a throwing child inside the
    boundary asserts the fallback UI appears instead of a crashed root; the
    module-load log is absent (rg gate).
    Commit with message "feat(09-13): root + chat error boundaries, debug log removal (PROB-21/#45)".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/App.test.tsx 2>&1 | tail -3</automated>
    <automated>rg -n "GC-MODULE|console\.log" frontend/src/components/graph/GraphCanvas.tsx | wc -l</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - ErrorBoundary wraps the root and ChatPanel; a throwing child renders the fallback, not a blank page
    - The module-load console.log and any other debug logs are gone from GraphCanvas (rg zero)
    - App.test.tsx + build green
  </acceptance_criteria>
  <done>The app is crash-resilient per PROB-21 and debug noise is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render tree <-> uncaught errors | One component crash must not unmount the root |
| Hover card <-> canvas coordinates | md-only surface, dismissible |
| Backlinks/properties <-> payload | Existing fields only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-13-01 | Denial of Service | Blank app on render error (#45) | high | mitigate | Root + chat error boundaries; fallback UI; test |
| T-09-13-02 | Information Disclosure | Properties/backlinks inventing data | low | mitigate | Payload fields only (prohibition); no new backend fields |
| T-09-13-03 | Tampering | Debug log leaking internals (#45) | low | mitigate | Removal + rg gate |
| T-09-13-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies |
</threat_model>

<verification>
Run the full frontend suite, npm run build, npm run lint (0/0). Grep gates:
`rg -n "console\.log" frontend/src/components/graph/GraphCanvas.tsx` zero;
ErrorBoundary imported in main.tsx/App.tsx and wrapping ChatPanel.
</verification>

<success_criteria>
Mobile usability, all four second-brain surfaces, and crash resilience are
live per UI-SPEC/PROB-21 with the full frontend gate green.
</success_criteria>

## Artifacts this phase produces

- Mobile pass (FEAT-10): AppShell, DetailPanel, ChatSheet, GraphControls,
  GraphLegend, EpisodeSelector (verify) — md: breakpoint + safe-area changes
- `frontend/src/components/detail/BacklinksTab.tsx` (NEW) — FEAT-11.1
- `frontend/src/components/graph/NodeHoverCard.tsx` (NEW) — FEAT-11.2
- `frontend/src/components/detail/DetailPanel.tsx` (MODIFIED) — Backlinks tab
  + properties dl (FEAT-11.1/11.5)
- `frontend/src/components/detail/RevisionHistoryPanel.tsx` (+test, MODIFIED)
  — real before/after values (FEAT-11.6)
- `frontend/src/components/graph/GraphCanvas.tsx` (MODIFIED) — hover-card
  wiring, debug log removed
- `frontend/src/components/ErrorBoundary.tsx` (NEW) — PROB-21
- `frontend/src/main.tsx` / `frontend/src/App.tsx` (MODIFIED) — boundary wrap
- GraphCanvas.test.tsx (MODIFIED) — boundary fallback test

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-13-SUMMARY.md` when done
</output>
