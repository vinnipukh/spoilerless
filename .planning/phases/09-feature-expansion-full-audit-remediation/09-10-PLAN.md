---
phase: 09-feature-expansion-full-audit-remediation
plan: 10
type: execute
wave: 3
depends_on: ["09-01", "09-09"]
files_modified:
  - frontend/src/components/timeline/TimelineView.tsx
  - frontend/src/components/timeline/TimelineEventRow.tsx
  - frontend/src/components/series/SeriesDashboard.tsx
  - frontend/src/App.tsx
  - frontend/src/components/layout/HeaderNavAction.tsx
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/timeline/TimelineView.test.tsx
  - frontend/src/components/series/SeriesDashboard.test.tsx
autonomous: true
requirements: [FEAT-02, FEAT-04]
must_haves:
  truths:
    - "A timeline view shows the visible story's events in chronological order as a tab alongside the graph — rows sorted by visible_from_order then episode order, click selects the node and frames it in the graph view (FEAT-02)"
    - "A series dashboard lists all available series with per-series progress as a Dialog entry point that AUGMENTS (does not replace) the existing dropdown (FEAT-04)"
    - "Both views respect the spoiler boundary — they render only what the already-filtered payload contains"
  artifacts:
    - path: "frontend/src/components/timeline/TimelineView.tsx"
      provides: "Tabbed chronological event view (FEAT-02)"
    - path: "frontend/src/components/series/SeriesDashboard.tsx"
      provides: "Series list + progress dialog (FEAT-04)"
  key_links:
    - from: "TimelineView row click"
      to: "App.tsx graph view"
      via: "same onSelect path as graph selection — select the node and switch the view so it is framed"
      pattern: "onSelect"
    - from: "SeriesDashboard 'Open series'"
      to: "setSelectedSeriesId + watchProgress boundary reset"
      via: "existing series/progress flow — dropdown stays unchanged"
      pattern: "setSelectedSeriesId"
  prohibitions:
    - id: "NO-REPLACE-DROPDOWN"
      statement: "The series dropdown is kept — the dashboard augments it (CONTEXT discretion: augment, don't replace)"
    - id: "NO-ROUTER"
      statement: "View switching uses the state-driven view union — no router dependency is added"
    - id: "NO-SPOILER-SURFACE"
      statement: "Timeline and dashboard render only already-filtered payload data"
---

<objective>
Ship the two entry-point views: the tabbed timeline (FEAT-02) and the series
dashboard (FEAT-04, augmenting the existing dropdown per CONTEXT's stated
discretion). Both are state-driven additions to the no-router App.tsx view
union, rendered strictly from already-filtered payloads.

Purpose: Give users a chronological reading path through the visible story and
a per-series progress overview without replacing the muscle-memory dropdown.
Output: TimelineView + TimelineEventRow, SeriesDashboard, HeaderNavAction icon
support, and the extended view union — vitest-covered.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md (§10.4 FEAT-02, §10.6 FEAT-04, §Interaction Contract, §Copywriting)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md (§App.tsx; §HeaderNavAction.tsx; §SeriesDashboard analogs)
@.planning/REQUIREMENTS.md (FEAT-02/04)
@frontend/src/App.tsx
@frontend/src/components/layout/HeaderNavAction.tsx
@frontend/src/hooks/useGraph.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timeline view (FEAT-02, tabbed)</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md
    (§10.4 — full-canvas ScrollArea, vertical rail border-l-2 border-border
    pl-4 ml-3, sticky episode group headers bg-background/90 backdrop-blur
    font-heading text-sm text-muted-foreground, event cards bg-card ring-1
    ring-border rounded-md p-3 text-sm with size-2.5 rounded-full bg-accent
    dot + label + episode badge + claims count; empty copy "The timeline is
    empty" / "Advance your watch progress to reveal more events."),
    frontend/src/hooks/useGraph.ts (payload: events with visible_from_order,
    episode data via the episodes prop),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md
    (§App.tsx — the view union precedent at line 71)
  </read_first>
  <files>frontend/src/components/timeline/TimelineView.tsx, frontend/src/components/timeline/TimelineEventRow.tsx, frontend/src/components/timeline/TimelineView.test.tsx</files>
  <action>
    Create TimelineEventRow.tsx: a button row (min-h-[44px]) per UI-SPEC 10.4 —
    left dot size-2.5 rounded-full bg-accent (filled + ring-accent when
    selected), label font-medium, episode badge (rounded bg-muted px-1.5
    py-0.5 text-xs text-muted-foreground), claims count "N claims" text-xs
    text-muted-foreground. Row click calls the passed onSelect.

    Create TimelineView.tsx: a full-canvas ScrollArea rendering events sorted
    by visible_from_order then episode order (stable secondary: label), grouped
    under sticky episode headers ("S01E01" style from the episodes prop);
    vertical rail per the contract; empty state with the locked copy. Rows are
    keyboard-navigable (↑/↓/Enter).

    Wire in App.tsx: extend the view union from 'graph' | 'settings' to
    'graph' | 'timeline' | 'settings' (line ~71); add the CalendarClock
    HeaderNavAction trigger in the topBar (default view stays graph); on a
    timeline row click, select the node via the existing onSelect path AND
    switch the view to 'graph' so the node is framed.
    Tests: chronological sort; group headers; row click selects + switches
    view; empty state copy; keyboard navigation.
    Commit with message "feat(09-10): timeline view tab (FEAT-02)".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/timeline/TimelineView.test.tsx 2>&1 | tail -4</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - TimelineView renders events in visible_from_order/episode-order with sticky group headers and the exact card contract
    - Row click selects the node and switches to the graph view (state-driven, no router)
    - Empty state uses the locked copy; ↑/↓/Enter work
    - vitest + npm run build green
  </acceptance_criteria>
  <done>The tabbed timeline view is live and keyboard-navigable.</done>
</task>

<task type="auto">
  <name>Task 2: Series dashboard (FEAT-04, augments the dropdown)</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md
    (§10.6 — Dialog max-w-lg, header "Series" font-heading text-xl, series
    cards bg-card ring-1 ring-border rounded-lg p-4 with title font-heading
    text-base, episode-count line text-xs text-muted-foreground, progress bar
    h-1.5 w-full rounded-full bg-muted with bg-primary fill,
    watchedThroughOrder/episodeCount %, "Open series" bg-primary button
    min-h-[44px], ring-accent on the current series; empty copy "No series
    available" / "Add a series to get started."),
    frontend/src/App.tsx (series state: selectedSeriesId, watchProgress flow),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md
    (§byok.ts localStorage shape — dashboard needs no persistence; Dialog
    primitives precedent from SettingsPage)
  </read_first>
  <files>frontend/src/components/series/SeriesDashboard.tsx, frontend/src/components/series/SeriesDashboard.test.tsx, frontend/src/App.tsx, frontend/src/components/layout/AppShell.tsx</files>
  <action>
    Create SeriesDashboard.tsx: a Dialog (max-w-lg) listing all available
    series as cards per UI-SPEC 10.6 — title, episode count, progress bar
    (width = watchedThroughOrder/episodeCount percent, bg-primary fill), and
    an "Open series" primary button; the currently-open series card gets
    ring-accent; ↑/↓/Enter over cards; empty state with the locked copy.

    Wire in App.tsx: dashboard open state + a LayoutGrid HeaderNavAction
    trigger beside the existing SeriesSelect (the dropdown itself is KEPT
    unchanged — augment, don't replace, per CONTEXT's stated discretion);
    "Open series" → setSelectedSeriesId(series.id) + close the dialog + reset
    the episode selector to that series' watched boundary through the existing
    watchProgress flow. AppShell topBar slot accepts the trigger.
    Tests: renders series with progress bars; Open series switches the
    selected series and closes; empty state; keyboard navigation; the dropdown
    still renders (regression).
    Commit with message "feat(09-10): series dashboard dialog (FEAT-04)".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/series/SeriesDashboard.test.tsx 2>&1 | tail -4</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - SeriesDashboard renders the exact card/progress/button contract; current series ring-accent
    - Open series switches selection + resets boundary via the existing flow; dropdown remains functional
    - Empty state copy locked; vitest + npm run build green
  </acceptance_criteria>
  <done>The dashboard augments the dropdown as a full series-entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Timeline/dashboard <-> graph payload | Renders only already-filtered data |
| Dashboard Open-series <-> progress flow | Must reuse the existing boundary flow |
| App view union <-> nav triggers | No router; state-driven only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10-01 | Information Disclosure | Timeline/dashboard leaking future content | high | mitigate | Both render only the boundary-filtered payload; no new data calls |
| T-09-10-02 | Elevation of Privilege | Dashboard bypassing boundary reset | low | mitigate | Open series reuses setSelectedSeriesId + watchProgress flow verbatim |
| T-09-10-03 | Tampering | Router dependency creep | low | mitigate | State-driven view union only; prohibition gate |
| T-09-10-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies |
</threat_model>

<verification>
Run the two new vitest files + the full frontend suite + npm run build +
npm run lint (0/0). Confirm App.tsx's view union contains 'timeline' and the
dropdown still renders in SeriesSelect tests.
</verification>

<success_criteria>
The timeline tab and the series dashboard are live per UI-SPEC, both
payload-local and state-driven, with the dropdown preserved and the full
frontend gate green.
</success_criteria>

## Artifacts this phase produces

- `frontend/src/components/timeline/TimelineView.tsx` (NEW) — tabbed
  chronological view (FEAT-02)
- `frontend/src/components/timeline/TimelineEventRow.tsx` (NEW) — event row
  with dot/badge/claims count
- `frontend/src/components/series/SeriesDashboard.tsx` (NEW) — series
  list + progress dialog (FEAT-04)
- `frontend/src/App.tsx` (MODIFIED) — view union 'graph' | 'timeline' |
  'settings', dashboard state + triggers, row-click → select + switch
- `frontend/src/components/layout/HeaderNavAction.tsx` (MODIFIED) — icon-swap
  support for CalendarClock/LayoutGrid triggers
- `frontend/src/components/layout/AppShell.tsx` (MODIFIED) — topBar slots
- Tests: TimelineView.test.tsx, SeriesDashboard.test.tsx (NEW)

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-10-SUMMARY.md` when done
</output>
