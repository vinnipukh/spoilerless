---
phase: 09-feature-expansion-full-audit-remediation
plan: 17
type: execute
wave: 5
depends_on: ["09-16"]
files_modified: []
autonomous: false
requirements: [AUTH-03, SEC-03, INFRA-02]
user_setup:
  - service: render
    why: "Two live env vars: ADMIN_EMAILS (operator supplies the admin email value) and REDIS_URL (Upstash rediss:// URL from Phase 8 08-01 user_setup)"
  - service: google
    why: "Two test accounts for the live admin-role check — one admin-listed, one non-admin (expects 403)"
must_haves:
  truths:
    - "Candidate review (approve/reject/edit) and ChangeSet confirm are admin-gated LIVE: an admin session succeeds and a non-admin session receives 403 in the existing error envelope (AUTH-03, 09-03 — 08-UAT #6 skipped-by-choice, now closed)"
    - "Rapid repeated login attempts against the live backend return 429 via the multi-worker-safe Redis-backed limiter, then the check stops — a bounded scripted verification, not a load test (SEC-03, 09-04, RESEARCH threat pattern)"
    - "Graph GETs are served from the Upstash cache (repeat GET fast) and a write invalidates the (series_id, effective_boundary, user_id) key — cache-aside + invalidation verified live (INFRA-02, 09-04 — 08-UAT #9 pattern)"
  artifacts: []
  key_links:
    - from: "Render env ADMIN_EMAILS"
      to: "api/deps.py RequireAdminDependency"
      via: "operator sets the env var; the allowlisted email resolves to role=admin at login; the admin gate stays fail-closed when unset (Phase 8 default)"
      pattern: "ADMIN_EMAILS"
    - from: "Render env REDIS_URL"
      to: "rate limiter + graph cache-aside"
      via: "Upstash rediss:// URL from 08-01 user_setup; rate limiting is disabled-by-design when empty, so live 429 + cache behavior only exists once set"
      pattern: "REDIS_URL"
  prohibitions:
    - id: "NO-UNBOUNDED-LOAD"
      statement: "The 429 verification is a bounded scripted check (a handful of rapid attempts, then STOP) — never a load test; AuraDB/Upstash free-tier cooldown awareness (RESEARCH Pitfall 1)"
    - id: "NO-ADMIN-BY-BYPASS"
      statement: "The live check must prove the non-admin 403 path, not just the admin success path — AUTH-03 is fail-closed and the 403 is the evidence"
    - id: "NO-REDIS-URL-IN-REPO"
      statement: "REDIS_URL exists only as a Render platform env var — never written into the repository (INFRA-05)"
---
<objective>
Final OPERATOR wave, task 2 of the wave-5 set (after 09-16's push/CI/rename):
close the two Phase 8 carry-overs that are pure live-stack verification —
09-03 (ADMIN_EMAILS on Render + live admin-role check: candidate
approve/reject/edit + ChangeSet confirm admin-gated, non-admin 403) and 09-04
(REDIS_URL on Render + live 429 rate-limit + graph-cache hit/invalidation
verification). Both were deferred by operator choice in Phase 8
(08-VERIFICATION gaps 2; 08-UAT #6 skipped, #8/#9 passed pre-Redis-config) and
are recorded as manual-only rows in 09-VALIDATION.

Purpose: Prove the access-control and caching/rate-limit guarantees against
the deployed stack with real env configuration — the code-level gates already
pass in CI; this wave confirms the configured production behaves the same.
Output: ADMIN_EMAILS and REDIS_URL configured on Render; live admin-role,
429, and cache behavior verified and recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md (§Environment Availability — Upstash Redis URL from 08-01 user_setup; §Security Domain — threat patterns: rate-limit bypass, zombie sweep; §Wave / Sequencing Guidance — final-wave items 2/3)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-VALIDATION.md (§Manual-Only Verifications — rows 2 and 3)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-CONTEXT.md (D-14 carry-overs 09-03/09-04; Claude's discretion — exact ADMIN_EMAILS value operator-supplied)
@.planning/phases/08-production-deployment-automated-ci-cd/08-VERIFICATION.md (gap 2 — admin-role live check skipped; UAT #8/#9 passed)
@.planning/REQUIREMENTS.md (AUTH-03, SEC-03, INFRA-02)
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set ADMIN_EMAILS on Render + live admin-role verification (09-03)</name>
  <read_first>
    .planning/phases/08-production-deployment-automated-ci-cd/08-VERIFICATION.md
    (gap 2 — gate logic verified by automated tests: test_admin_*.py 403
    paths, RequireAdminDependency in api/deps.py; only the live check was
    deferred),
    .planning/REQUIREMENTS.md (AUTH-03 — admin role enforced on candidate
    review approve/reject/edit and ChangeSet approval; non-admin gets a clear
    403),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-CONTEXT.md
    (Claude's discretion — exact allowlisted value is operator-supplied)
  </read_first>
  <files>.planning/phases/09-feature-expansion-full-audit-remediation/09-17-SUMMARY.md</files>
  <action>
    Operator supplies the admin email value (per CONTEXT discretion), then
    Render dashboard -> service (spoilerless-api after 09-16) -> Environment ->
    add ADMIN_EMAILS as a comma-separated list of allowlisted admin emails.
    Trigger the deploy (auto-deploy on env change or manual deploy) and
    confirm the service comes back healthy (curl https://api.spoilerless.net/health).

    Live admin-role verification against app.spoilerless.net +
    api.spoilerless.net (browser-based, UAT-style):
    1. Log in with the admin-listed Google account; open candidate review and
       perform approve/reject/edit on a scratch candidate row (create one via
       the candidate UI/API first if the queue is empty — scratch-scoped,
       teardown after); open ChangeSet confirm and confirm a scratch
       ChangeSet. Each action must succeed with no 403.
    2. Log in with a second, NON-admin Google account (allowlisted but NOT in
       ADMIN_EMAILS); repeat the same approve/reject/edit + ChangeSet confirm
       actions. Each must return 403 in the existing error envelope
       (AUTH_* code) — the fail-closed proof.
    3. Tear down the scratch candidate/ChangeSet rows created for the check
       (admin session) so the live DB carries no check residue (D-08).
    Record every action + observed status in 09-17-SUMMARY.md. Code-level
    baseline stays covered by the existing admin-gate tests (they pass in CI
    — this task adds the configured-production evidence).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" https://api.spoilerless.net/health</automated>
    <automated>uv run pytest spoilerless/tests/test_admin.py spoilerless/tests/test_candidate_review.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - ADMIN_EMAILS set on Render and the service redeployed healthy
    - Admin session: approve/reject/edit + ChangeSet confirm succeed live
    - Non-admin session: the same actions return 403 (recorded)
    - Scratch rows created for the check were torn down; evidence in the SUMMARY
  </acceptance_criteria>
  <done>AUTH-03 is proven live with a configured admin list; 09-03 is closed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set REDIS_URL on Render + live 429 and graph-cache verification (09-04)</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Environment Availability — Upstash Redis URL from 08-01 user_setup;
    §Security Domain — 'Rate-limit bypass during 09-04 verification' threat
    pattern: bounded scripted checks, then stop; AuraDB/Upstash free-tier
    cooldown awareness),
    .planning/REQUIREMENTS.md (SEC-03 — login, chat-send, content-write
    endpoints rate-limited per user/IP via a multi-worker-safe store, 429 in
    the existing error envelope; INFRA-02 — Upstash cache-aside keyed by
    (series_id, effective_boundary, user_id), invalidated on writes),
    .planning/phases/08-production-deployment-automated-ci-cd/08-VERIFICATION.md
    (UAT #8 429 pass, UAT #9 cache pass — repeat for the configured env)
  </read_first>
  <files>.planning/phases/09-feature-expansion-full-audit-remediation/09-17-SUMMARY.md</files>
  <action>
    Render dashboard -> service -> Environment -> add REDIS_URL = the Upstash
    rediss:// URL from the Phase 8 08-01 user_setup (never write the URL into
    the repo — INFRA-05; the value exists only as a platform env var).
    Deploy and confirm health.

    Live verification (bounded, scripted — then STOP per RESEARCH):
    1. 429 rate limit: issue a handful of rapid login attempts against
       /api/auth/google (or the equivalent live login POST) from one
       client/IP — e.g. a short shell loop of 6-10 requests. The responses
       must transition to 429 in the existing error envelope once the
       Redis-backed per-user/IP limit trips (multi-worker-safe, not the
       in-memory per-process dict). Stop immediately after the 429 is
       observed — this is not a load test. Wait out the window, then confirm
       a normal login succeeds again (the limiter recovers).
    2. Graph cache hit/invalidation: GET the graph endpoint twice for the
       same (series, boundary, user) — the repeat GET must be served fast
       (cache hit; Upstash). Perform a write that changes the underlying
       cached data (e.g. a scratch note/claim create or progress update via
       the UI), then GET again — the response must reflect the change
       (invalidation fired). The Redis-outage degrade-to-Neo4j path is
       already covered by 08-UAT #9; no outage simulation needed here.
    Record observed status codes, response timings, and cache behavior in
    09-17-SUMMARY.md, including the Upstash console hit-count evidence if
    visible.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" https://api.spoilerless.net/health</automated>
    <automated>for i in $(seq 1 8); do curl -s -o /dev/null -w "%{http_code} " -X POST https://api.spoilerless.net/api/auth/google; done; echo</automated>
  </verify>
  <acceptance_criteria>
    - REDIS_URL set on Render; service redeployed healthy; URL never entered the repo
    - 429 observed after rapid login attempts, then normal login recovers after the window (bounded check)
    - Repeat graph GET is cache-fast; a write invalidates and the next GET reflects it
    - Evidence (codes, timings, Upstash hits) recorded in the SUMMARY
  </acceptance_criteria>
  <done>SEC-03 and INFRA-02 are proven live with Redis configured; 09-04 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render env ADMIN_EMAILS <-> admin gate | Misconfiguration silently locks or unlocks admin endpoints |
| Live login flow <-> rate limiter | A bounded check can accidentally become a load test on the free tier |
| Graph GET <-> Upstash cache | Cache serving stale data after a write is a correctness break |
| Platform env <-> repository | REDIS_URL/ADMIN_EMAILS values must never land in git |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-17-01 | Spoofing | ADMIN_EMAILS mis-set (wrong email, empty) | medium | mitigate | Operator-supplied value; verify the admin session succeeds AND non-admin 403s |
| T-09-17-02 | Denial of Service | 429 check escalating into a load test | high | mitigate | Bounded 6-10 request loop, stop on first 429 (RESEARCH threat pattern) |
| T-09-17-03 | Tampering | Cache serving stale graph data | medium | mitigate | Write-then-read invalidation check as part of the live verification (INFRA-02) |
| T-09-17-04 | Information Disclosure | REDIS_URL/ADMIN_EMAILS in the repo | high | mitigate | Platform-env-only (INFRA-05); grep gate on the working tree before commit |
| T-09-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs in this plan — browser + curl + existing tests only |
</threat_model>

<verification>
Health curl after each env change; the bounded 429 loop; the admin-gate
pytest baseline; the write-then-read cache check. Grep gates: `git grep -i
'rediss://'` and `git grep -i 'ADMIN_EMAILS'` must return ZERO hits in tracked
files (platform-env-only). Confirm the SUMMARY records the configured values
(redacted URL), observed codes, and cache evidence.
</verification>

<success_criteria>
ADMIN_EMAILS and REDIS_URL are configured on Render; the live admin-role
check proves admin success + non-admin 403 (AUTH-03); the live 429 and
cache-hit/invalidation checks prove the Redis-backed limiter and cache-aside
(SEC-03, INFRA-02); carry-overs 09-03 and 09-04 are closed with recorded
evidence.
</success_criteria>

## Artifacts this phase produces

- `09-17-SUMMARY.md` (NEW) — admin-role live evidence, 429/cache verification
  evidence, redacted env notes
- No product code changes — Render env configuration + live verification only

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-17-SUMMARY.md` when done
</output>
