---
phase: 09-feature-expansion-full-audit-remediation
plan: 02
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - spoilerless/tests/test_google_verifier.py
  - spoilerless/tests/test_auth.py
  - frontend/src/api/progress.test.ts
  - frontend/src/api/progress.ts
  - spoilerless/tests/test_openapi_contract.py
autonomous: true
requirements: [PROB-14, PROB-15, PROB-23]
must_haves:
  truths:
    - "A real behavioral test exercises ProductionGoogleVerifier with a garbage token over a MockTransport and asserts the documented error mapping — the NameError class (#42) can never ship again (PROB-23)"
    - "The Google-verifier exception handling already fixed in the tree (auth.py lazy import binding google in function scope) is verified by a test, not just by inspection (PROB-14)"
    - "Frontend progress payloads are locked by transport-level wire-shape tests that do NOT mock the API client — the #43 shipping-green class is closed (PROB-15/23)"
  artifacts:
    - path: "spoilerless/tests/test_google_verifier.py"
      provides: "ProductionGoogleVerifier behavioral test (garbage token + httpx.MockTransport)"
    - path: "frontend/src/api/progress.test.ts"
      provides: "Wire-shape contract tests for the three progress payload shapes via globalThis.fetch stub"
  key_links:
    - from: "frontend/src/api/progress.ts updateProgress"
      to: "backend domain/progress.py ProgressUpdateRequest._exactly_one_boundary_field"
      via: "wire-shape tests assert the request body never carries both boundary fields"
      pattern: "globalThis.fetch"
  prohibitions:
    - id: "NO-CLIENT-MOCK"
      statement: "Wire-shape tests must replace globalThis.fetch — vi.mock of the API client module is forbidden (Pitfall 4)"
    - id: "NO-LIVE-LLM"
      statement: "No test may spend LLM API money — the verifier test uses httpx.MockTransport, never a live Google endpoint"
---

<objective>
Create the two cheapest, highest-value regression nets for the phase's two
most dangerous shipped-green bug classes, per RESEARCH Wave 0: (1) a real
behavioral test for `ProductionGoogleVerifier` — PROBLEMS #42/#47 — proving
that a verification failure maps to the documented GoogleVerificationError /
GoogleTransportError contract and can never again surface as a NameError
disguised as a 503; and (2) transport-level wire-shape contract tests for the
frontend progress payload builder (PROBLEMS #43/#47, PROB-15) asserting the
exact three request-body shapes the backend validator accepts, WITHOUT mocking
the API client (the pattern that enshrined the 08-01 chat-422 and progress-422
bugs). PROB-14's code fix is already verified in the tree; this plan's task is
the regression lock.

Purpose: These two tests exist before any remediation plan modifies the auth
or progress code paths, so the fixes in 09-03/09-04/09-07 are proven against a
red-capable net.
Output: Two new test files (backend + frontend) green on the scratch stack.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md (§Wave 0 Gaps, §Pattern 2, §Pitfall 4)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md (§backend/tests/test_google_verifier.py, §Wire-shape FE tests)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-VALIDATION.md (§Wave 0 Requirements)
@spoilerless/app/services/auth.py
@frontend/src/api/progress.ts
@frontend/src/api/chat.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Behavioral test for ProductionGoogleVerifier (garbage token + MockTransport)</name>
  <read_first>
    spoilerless/app/services/auth.py (ProductionGoogleVerifier.verify ~lines 52-93 —
    the lazy "from google.auth.transport import requests" import binding google in
    function scope, the except clause referencing google.auth.exceptions.TransportError),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md
    (§backend/tests/test_google_verifier.py — monkeypatch get_settings() attributes,
    never replace the lru_cached instance),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Pattern 2 zero-cost verification, §Wave 0 Gaps)
  </read_first>
  <files>spoilerless/tests/test_google_verifier.py, spoilerless/tests/test_auth.py</files>
  <action>
    Create spoilerless/tests/test_google_verifier.py: a unit test that
    constructs ProductionGoogleVerifier directly (per its constructor
    signature — read auth.py for exact args; likely a client-id/audience
    parameter) and drives it with a garbage token over an httpx.MockTransport
    that returns a canned error response (e.g. 400 {"error_description":
    "Invalid value for id_token"}), asserting:
    - the raised exception is the documented verifier error type (read
      auth.py's exception classes — assert the exact class/contract, e.g.
      GoogleVerificationError for an invalid-token 400 and
      GoogleTransportError for a transport-level failure), and
    - the failure is NOT a NameError (the #42 regression: assert the
      exception is not an instance of NameError, and assert the route-level
      mapping would produce the documented AUTH_* error, not a 503
      AUTH_SERVICE_UNAVAILABLE, by asserting the exception type directly).
    Cover at least three cases: garbage/opaque token (invalid), transport
    failure (MockTransport raising httpx.TransportError), and a valid-token
    happy path if the verifier's success path is testable without a real
    Google cert fetch (if the success path requires real network/certs,
    document that and skip it — the regression value is the failure paths).
    Config pitfall: get_settings() is lru_cached — if the verifier reads
    settings (e.g. GOOGLE_CLIENT_ID), monkeypatch attributes on the shared
    instance, never replace it.

    In spoilerless/tests/test_auth.py: replace the existing
    test_auth_module_imports-only reference to ProductionGoogleVerifier
    (#47's exact complaint — the verifier is currently only imported, never
    exercised) with nothing if the new file supersedes it, or leave the import
    test if it still adds value. Ensure the new test file is collected by
    pytest (testpaths = spoilerless/tests).

    Commit with message "test(09-02): behavioral ProductionGoogleVerifier test (garbage token + MockTransport)".
  </action>
  <verify>
    <automated>uv run pytest spoilerless/tests/test_google_verifier.py -x -q 2>&1 | tail -3</automated>
    <automated>uv run pytest spoilerless/tests/test_auth.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_google_verifier.py asserts the verifier raises the documented exception class for a garbage token over MockTransport
    - The test explicitly asserts the raised exception is not a NameError (the #42 regression class)
    - At least one transport-failure case (MockTransport raising httpx.TransportError) maps to the documented transport error type
    - Both pytest invocations pass with zero network access to Google
  </acceptance_criteria>
  <done>The verifier's failure paths are locked by a behavioral test; the #42 NameError class is regression-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Wire-shape contract tests for the progress payload builder (no client mock)</name>
  <read_first>
    frontend/src/api/progress.ts (updateProgress body builder — the per-intent
    payload contract: forward confirm sends watched_through_order +
    view_as_of_order; view-only sends view_as_of_order ALONE; plain sends
    visible_until_order alone — NEVER both boundary fields),
    frontend/src/api/chat.test.ts (the transport-level fetch-stub pattern:
    replace globalThis.fetch with a stub, assert the captured request body),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Pitfall 4: FE wire-shape tests that mock the API client enshrine contract bugs)
  </read_first>
  <files>frontend/src/api/progress.test.ts, frontend/src/api/progress.ts</files>
  <action>
    Create frontend/src/api/progress.test.ts: transport-level wire-shape tests
    that replace globalThis.fetch with a stub (copy the chat.test.ts pattern
    exactly — a jest.fn capturing (url, init) and returning a canned Response),
    call updateProgress for each of the three intents, and assert the captured
    request body:
    1. forward confirm intent → body contains watched_through_order AND
       view_as_of_order and does NOT contain visible_until_order;
    2. view-only intent → body contains view_as_of_order ALONE (never the
       legacy confirm alias visible_until_order);
    3. plain/legacy intent → body contains visible_until_order alone.
    The assertions must be against the JSON-parsed body of the captured
    fetch call — never assert against a mocked updateProgress return (no
    vi.mock of '@/api/progress' anywhere — that is the #43 shipping-green
    class). Also assert the request URL and credentials: 'include' shape
    matches apiFetch conventions.
    If any of the three assertions FAILS against the current builder (the
    08-04 fix may have landed partially), fix frontend/src/api/progress.ts's
    body construction so each intent produces exactly the shape the backend
    ProgressUpdateRequest._exactly_one_boundary_field validator accepts — the
    plan's acceptance is the three green wire-shape tests, however the builder
    must be shaped to pass them.
    Run with NODE_ENV=test CI=1 vitest so jsdom + the canonical CI env apply.
    Commit with message "test(09-02): progress payload wire-shape contract tests (fetch-level, no client mock)".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/api/progress.test.ts 2>&1 | tail -5</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - progress.test.ts replaces globalThis.fetch (no vi.mock of the API client) and asserts all three payload shapes
    - The view-only test asserts visible_until_order is ABSENT from the body
    - The forward-confirm test asserts visible_until_order is ABSENT and watched_through_order + view_as_of_order are PRESENT
    - vitest run is green and npm run build (tsc -b) passes — test-file type errors would red the Vercel deploy (Pitfall 5)
  </acceptance_criteria>
  <done>The three progress payload shapes are locked at the transport level; a regression to the #43 class fails CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code <-> production verifier | The behavioral test drives ProductionGoogleVerifier exactly as the login route does |
| Frontend fetch stub <-> progress endpoint contract | Wire-shape tests assert the exact body the backend validator accepts |
| Test suite <-> live services | Zero network: MockTransport for Google, jsdom fetch stub for the FE — never live AuraDB/Google/LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02-01 | Spoofing | Google ID-token verification failure path (#42) | high | mitigate | Behavioral test asserts documented error type and explicitly asserts NOT a NameError; no live Google call |
| T-09-02-02 | Tampering | Progress payload wire shape (#43) | high | mitigate | Transport-level fetch-stub tests for all three intent shapes; client-module mocking forbidden in the file |
| T-09-02-03 | Information Disclosure | Test accidentally hitting live services | medium | mitigate | MockTransport everywhere; vitest jsdom has no network by default; zero-cost constraint documented in plan |
| T-09-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs in this plan |
</threat_model>

<verification>
`uv run pytest spoilerless/tests/test_google_verifier.py -x` and
`uv run pytest spoilerless/tests/test_auth.py -x` pass; frontend
`NODE_ENV=test CI=1 npx vitest run src/api/progress.test.ts` passes and
`npm run build` is green. Confirm no test file imports or calls
`vi.mock('@/api/progress')` (rg check) and no test touches the network.
</verification>

<success_criteria>
Both regression nets exist and are green: the verifier's failure modes are
locked behaviorally, and the three progress payload shapes are locked at the
transport level without mocking the API client — the two most expensive
shipped-green bug classes of the audit are now CI-failing on recurrence.
</success_criteria>

## Artifacts this phase produces

- `spoilerless/tests/test_google_verifier.py` (NEW) — behavioral tests:
  garbage-token → documented verifier error (never NameError); transport
  failure → documented transport error; valid-token path documented/skipped
  if it needs real Google certs
- `spoilerless/tests/test_auth.py` (MODIFIED) — verifier no longer
  import-only; superseded import assertion removed or kept per value
- `frontend/src/api/progress.test.ts` (NEW) — fetch-stub wire-shape tests for
  the three progress intents (forward-confirm / view-only / plain)
- `frontend/src/api/progress.ts` (MODIFIED only if a shape fails) — per-intent
  body builder corrected to match `_exactly_one_boundary_field`

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-02-SUMMARY.md` when done
</output>
