---
phase: 09-feature-expansion-full-audit-remediation
plan: 16
type: execute
wave: 7
depends_on: ["09-15"]
files_modified:
  - .git/config
autonomous: false
requirements: [PROB-08, OPS-01, REBRAND-01, PROB-11]
user_setup:
  - service: github
    why: "Repo rename (hdgrafcehennemi -> spoilerless) needs owner/settings access on the remote (vinnipukh/hdgrafcehennemi, verified reachable)"
  - service: render
    why: "Dashboard rename of the live service hdgrafcehennemi-api (render.yaml name: change creates a NEW service per blueprint semantics — operator dashboard action)"
  - service: uptimerobot
    why: "Dashboard rename of the /health monitor to match the renamed product"
must_haves:
  truths:
    - "origin/main is identical to local main after the push — `git rev-parse origin/main` equals `git rev-parse HEAD` and `git log origin/main..HEAD` is empty, so no unpushed work is the only copy of the project (PROB-11, #29)"
    - "GitHub Actions runs backend pytest + frontend build/lint green on main — the 09-02 carry-over CI re-run is closed (OPS-01, PROB-08, 08-VERIFICATION gap 1)"
    - "The GitHub repo, Render service, and UptimeRobot monitor carry the renamed product identity, and the local remote points at the new URL (REBRAND-01 operator steps, RESEARCH A6: rename AFTER the 09-02 push)"
    - "D-13: the 09-01 carry-over (UptimeRobot external monitor) is DONE — verified live at 08-UAT #11 pass; only its monitor rename remains in this operator wave, no monitor-setup plan is needed"
    - "D-14: the remaining Phase 8 carry-overs are in scope — 09-02 CI re-run (this plan), 09-03 admin live check (09-17), 09-04 REDIS_URL live verification (09-17), 09-07 full CI/CD and 09-08 observability artifacts (09-08)"
  artifacts: []
  key_links:
    - from: "local main"
      to: "origin/main"
      via: "git push origin main, then `git rev-parse origin/main` == `git rev-parse HEAD` (PROB-11 remote-sync truth)"
      pattern: "git rev-parse"
    - from: "local remote origin"
      to: "renamed GitHub repo URL"
      via: "git remote set-url origin <new-url> immediately after the GitHub Settings rename, then git ls-remote reachability"
      pattern: "remote set-url"
  prohibitions:
    - id: "NO-UNPUSHED-ONLY-COPY"
      statement: "The phase must not close with unpushed commits — origin/main must equal local main at the recorded SHA (PROB-11/#29)"
    - id: "NO-RENAME-BEFORE-PUSH"
      statement: "The GitHub repo rename happens AFTER the push to the old URL — pushing to the old remote still works until renamed (RESEARCH A6)"
    - id: "NO-STALE-REMOTE"
      statement: "After the rename, the local origin URL must point at the new repo — a stale hdgrafcehennemi remote URL is forbidden (REBRAND-01)"
---
<objective>
Final OPERATOR wave, task 1 of the wave-5 set (09-16 -> 09-17 -> 09-18): close
the two remaining Phase 8 carry-overs that need the operator's accounts —
push local main to origin and confirm GitHub Actions green on main (09-02),
and rename the live service identities (REBRAND-01 operator steps: GitHub
repo rename + git remote set-url, Render service rename, UptimeRobot monitor
rename) — then prove the PROB-11 remote-sync truth so the repo can never
silently regress to a single-copy state.

Purpose: Every autonomous phase-9 commit lands on origin/main behind a green
CI gate, the product identity rename completes on the platforms (code-side
rename shipped in 09-01), and the 'no unpushed work is the only copy' truth
(#29) is verified at the exact post-push SHA.
Output: origin/main == local main (CI green), renamed GitHub/Render/
UptimeRobot identities, PROB-11 verified and recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md (§Runtime State Inventory — live service config; §Open Questions #3 — 09-02 exact scope; §Wave / Sequencing Guidance — final wave items 1/6; §Assumptions A5/A6)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-VALIDATION.md (§Manual-Only Verifications — row 1: CI re-run green on main)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-CONTEXT.md (D-12 REBRAND scope, D-14 carry-over 09-02)
@.planning/phases/08-production-deployment-automated-ci-cd/08-VERIFICATION.md (gap 1 — CI re-run pending, 08-UAT #4)
@.planning/REQUIREMENTS.md (PROB-08, PROB-11, OPS-01)
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Push local main to origin + confirm GitHub Actions green on main (09-02)</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Open Questions #3 — VERIFIED: ci-smoke-test branch does NOT exist
    locally or on the remote; the eslint/React-Compiler fixes are already in
    local main; local main was 4 commits ahead of origin/main at ~288743e
    when RESEARCH was written — phase-9 commits have advanced it since),
    .planning/phases/08-production-deployment-automated-ci-cd/08-VERIFICATION.md
    (gap 1: backend 12/12 on clean container + frontend lint 0 errors were
    verified locally; the Actions re-run on main was never confirmed)
  </read_first>
  <files>.git/config, .github/workflows/ci.yml, .planning/phases/09-feature-expansion-full-audit-remediation/09-16-SUMMARY.md</files>
  <action>
    Git-state check FIRST (never a blind push): run git status --short (clean,
    no .env staged), git log --oneline -8, git rev-parse HEAD, and
    git rev-parse origin/main to record the pre-push delta; confirm with
    git branch -a that the ci-smoke-test branch no longer exists anywhere
    (RESEARCH Open Q3) — the eslint config + React-Compiler-era lint fixes
    from Phase 8 live on local main and are what CI will now judge. Confirm
    origin is the verified remote (git ls-remote origin -h refs/heads/main
    reachable — vinnipukh/hdgrafcehennemi per RESEARCH).

    Push: git push origin main. Then confirm the GitHub Actions workflow
    (.github/workflows/ci.yml — backend pytest job + frontend build/lint job,
    OPS-01) goes GREEN on main: prefer gh CLI (gh run list --branch main
    --limit 5 then gh run watch), else the Actions web UI. If any job fails,
    close the failure (a follow-up fix task may be dispatched) and re-run
    until both jobs are green — do not proceed past this gate with CI red.
    Record the run IDs + job results in the SUMMARY (09-16-SUMMARY.md),
    including the pre-push and post-push SHAs.
    Commit nothing in this plan's tasks unless a fix is required (then commit
    with a message citing "09-16" and re-push).
  </action>
  <verify>
    <automated>git status --short | grep -v '\.planning/' ; git rev-parse HEAD origin/main</automated>
    <automated>gh run list --branch main --limit 5 2>/dev/null || echo "gh CLI unavailable — use Actions web UI"</automated>
    <automated>test "$(git rev-parse origin/main)" = "$(git rev-parse HEAD)" && echo SYNCED || echo AHEAD</automated>
  </verify>
  <acceptance_criteria>
    - git push origin main succeeded; origin/main == local main at the recorded SHA
    - GitHub Actions backend pytest + frontend build/lint both green on main (run IDs recorded)
    - Any new failure was closed and re-run to green before the gate passed
  </acceptance_criteria>
  <done>origin/main carries all phase-9 commits behind a green CI gate; 09-02 is closed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: REBRAND operator steps — GitHub repo rename + remote set-url + Render + UptimeRobot renames</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Runtime State Inventory — live service config row: Render service
    hdgrafcehennemi-api, GitHub repo vinnipukh/hdgrafcehennemi, UptimeRobot
    monitor, Cloudflare app./api. subdomains; §Assumptions A5/A6; final-wave
    item 6),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-CONTEXT.md
    (D-12: rename every user-visible reference to spoilerless; git history
    untouched; runtime/deploy names updated)
  </read_first>
  <files>.git/config, .planning/phases/09-feature-expansion-full-audit-remediation/09-16-SUMMARY.md</files>
  <action>
    Rename the GitHub repository: GitHub UI -> repo Settings -> General ->
    rename hdgrafcehennemi -> spoilerless (or the operator's chosen final
    name per D-12; git history is untouched — this is a platform rename).
    Immediately update the local remote so nothing silently targets the old
    URL: git remote set-url origin https://github.com/&lt;owner&gt;/spoilerless.git
    (the actual owner/name from git remote -v), then prove reachability with
    git ls-remote origin -h refs/heads/main. Also fix any reference to the old
    URL that surfaced post-wave-0 (e.g. root index.html GITHUB_REPOSITORY_URL
    and .planning/ doc pointers that name the new repo URL).

    Render: rename the live service hdgrafcehennemi-api -> spoilerless-api in
    the dashboard (RESEARCH A5: a render.yaml name: change creates a NEW
    service under blueprint semantics, so the dashboard rename — not the
    blueprint — is the operator action; confirm exactly one service exists
    afterwards and the api.spoilerless.net route keeps resolving).

    UptimeRobot: rename the /health monitor to match the renamed product
    (monitor URL stays https://api.spoilerless.net/health; keep the 5-min
    interval and email alert — OPS-02 must keep working through the rename).
    Record all four renames (repo URL, remote URL, Render service name,
    monitor name) + timestamps in the SUMMARY.
  </action>
  <verify>
    <automated>git remote -v | grep -i spoilerless</automated>
    <automated>git ls-remote origin -h refs/heads/main</automated>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" https://api.spoilerless.net/health</automated>
  </verify>
  <acceptance_criteria>
    - GitHub repo renamed; git remote -v shows the new URL; git ls-remote on the new remote succeeds
    - Render service renamed with exactly one service and api.spoilerless.net still healthy (200)
    - UptimeRobot monitor renamed and still polling /health
  </acceptance_criteria>
  <done>The product identity rename is complete on all operator-owned platforms.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: PROB-11 remote-sync truth — origin/main == local main, recorded</name>
  <read_first>
    .planning/REQUIREMENTS.md (PROB-11 — default branch pushed to and kept in
    sync with a real, accessible remote; resolves #29),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Runtime State Inventory — remote verified reachable)
  </read_first>
  <files>.planning/phases/09-feature-expansion-full-audit-remediation/09-16-SUMMARY.md</files>
  <action>
    After Task 1 (push) and Task 2 (rename + remote set-url), fetch the
    renamed remote and prove the remote-sync truth end-to-end: git fetch
    origin, then assert `git rev-parse origin/main` equals `git rev-parse
    HEAD` AND `git log --oneline origin/main..HEAD` is empty (no unpushed
    commits) AND the reverse (git log HEAD..origin/main) is also empty (no
    un-pulled commits — the remote carries nothing local lacks). Record the
    verified SHA + both log-line counts in the SUMMARY, and confirm the
    renamed repo's main resolves to the same SHA via the web UI or
    git ls-remote origin -h refs/heads/main.
    This is the #29 truth: the project's only copy is never local.
  </action>
  <verify>
    <automated>git fetch origin && test "$(git rev-parse origin/main)" = "$(git rev-parse HEAD)" && echo REMOTE-SYNCED</automated>
    <automated>git log --oneline origin/main..HEAD | wc -l ; git log --oneline HEAD..origin/main | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - origin/main SHA == local HEAD SHA, both directions of the commit log empty
    - The renamed remote's main resolves to the same SHA
    - SHA + counts recorded in 09-16-SUMMARY.md
  </acceptance_criteria>
  <done>PROB-11 (#29) is verified and recorded — the remote is the true source of record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main <-> origin/main | Unpushed commits are the only copy until the push lands (#29) |
| git remote <-> renamed GitHub repo | A stale URL silently splits the remote-sync truth |
| Render blueprint <-> live service | A name: change creating a second service breaks api.spoilerless.net (A5) |
| UptimeRobot monitor <-> /health | The rename must not kill the OPS-02 alert path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-16-01 | Spoofing | Push of unverified state | high | mitigate | Git-state check first (status/log/rev-parse); Actions gate green before the gate passes |
| T-09-16-02 | Tampering | Stale remote URL after repo rename | high | mitigate | git remote set-url + ls-remote reachability immediately after rename (PROB-11) |
| T-09-16-03 | Availability | Render rename creating a duplicate service (A5) | medium | mitigate | Dashboard rename path per RESEARCH A5; verify exactly one service + /health 200 |
| T-09-16-04 | Information Disclosure | Unverified remote-sync truth (#29) | high | mitigate | PROB-11 rev-parse equality gate in both directions, recorded in SUMMARY |
| T-09-16-05 | Tampering | UptimeRobot monitor lost in rename | low | mitigate | Monitor URL unchanged (api.spoilerless.net/health); rename only the label |
| T-09-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs in this plan — operator platform actions and git only |
</threat_model>

<verification>
Run the automated gates in each task (git rev-parse equality, git remote -v,
git ls-remote, /health curl). Grep gates: none on product files (no repo
content changes). Confirm the SUMMARY records pre/post push SHAs, Actions run
IDs, the four renames, and the final verified SHA.
</verification>

<success_criteria>
Local main is on origin/main behind a green GitHub Actions run, the
GitHub/Render/UptimeRobot identities carry the renamed product name with the
local remote pointing at the new URL, and PROB-11's remote-sync truth is
recorded — the phase's code is safe on the remote before the live-service
verifications of 09-17.
</success_criteria>

## Artifacts this phase produces

- `.git/config` (MODIFIED) — origin remote URL after set-url
- `09-16-SUMMARY.md` (NEW) — SHAs, Actions run IDs, rename evidence, PROB-11
  verification record
- No product code changes — this wave is operator actions + evidence

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-16-SUMMARY.md` when done
</output>
