---
phase: 09-feature-expansion-full-audit-remediation
plan: 18
type: execute
wave: 9
depends_on: ["09-17"]
files_modified: []
autonomous: false
requirements: [PROB-20, PROB-22]
user_setup:
  - service: neo4j-auradb
    why: "Live AuraDB credentials (NEO4J_* platform env / operator shell env) for the one-shot reseed + zombie sweep — destructive, operator-sign-off-gated"
  - service: render
    why: "NEO4J_* env already configured on the service; operator executes the reseed/sweep against the SAME live DB the app uses"
must_haves:
  truths:
    - "The live AuraDB matches the current seed script after the reseed — episode queries no longer emit the 01N52 'property key does not exist' warnings (PROB-20, #44)"
    - "Zombie :AppUser rows (~3,855 with no progress/chat/ownership ties) and expired/orphaned :Session nodes (21/21 expired, 5 orphaned) are swept from the live DB, and the protected dev user ae8a41b7-db96-40e8-b6c2-2e3c69aedb11 provably survives (PROB-22, #46, D-07)"
    - "A read-only integrity audit (scripts/aura_graph_integrity.sh pattern) reports clean before/after counts for each destructive op — no destructive run happens without dry-run evidence + operator sign-off (RESEARCH A3/A4, Pitfall 1)"
  artifacts: []
  key_links:
    - from: "live AuraDB"
      to: "spoilerless/app/graph/setup.py setup_database (idempotent MERGE-based seed)"
      via: "uv run spoilerless-setup (post-09-01 console entry; module form uv run python -m spoilerless.app.graph.setup) against AuraDB after sign-off — MERGE preserves user content"
      pattern: "spoilerless-setup"
    - from: "spoilerless/scripts/zombie_sweep.py --execute"
      to: "live AuraDB"
      via: "--dry-run counts shown first, operator approves, then --execute; never-delete module constant protects ae8a41b7-..."
      pattern: "--dry-run"
  prohibitions:
    - id: "NEVER-DELETE-DEV-USER"
      statement: "The sweep must never delete the real dev user ae8a41b7-db96-40e8-b6c2-2e3c69aedb11 — hard-coded never-delete constant in the script (PROB-22/D-07)"
    - id: "NO-UNSIGNED-DESTRUCTIVE"
      statement: "No destructive live-DB operation (reseed or sweep) runs without the dry-run/count evidence and the operator's explicit sign-off (RESEARCH A3/A4)"
    - id: "NO-SWEEP-LOOPS"
      statement: "Live-DB mutations are one-shot scripted ops — never run in a loop and never from the full test suite; the suite targets scratch/CI Neo4j, never AuraDB (RESEARCH Pitfall 1)"
    - id: "NO-USER-DATA-LOSS"
      statement: "The reseed is MERGE-based and must preserve user content (progress rows, chat sessions, user notes) — verify with spot checks after the run (D-08)"
---
<objective>
Final OPERATOR wave, task 3 of the wave-5 set (after 09-16/09-17): execute
the two destructive live-DB operations that the autonomous waves deliberately
built tooling for but did NOT run — PROB-20's live AuraDB reseed (dry-run
audit -> operator sign-off -> one-shot idempotent MERGE reseed) and PROB-22's
zombie sweep execution (dry-run counts -> operator sign-off -> --execute,
never touching the real dev user). Both are gated as blocking human-action
checkpoints because they mutate the shared live database (RESEARCH Pitfall 1:
AuraDB flood-blocking + suspension; A3/A4: overwrite/delete risk).

Purpose: Close #44 (seed drift — the 01N52 episode-property storm) and #46
(the 3,855-zombie landfill + expired sessions) against the real AuraDB, with
read-only integrity audit before and after each op and the protected dev user
provably intact.
Output: Live AuraDB reseeded to the current seed; zombie rows swept; before/
after audit evidence recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md (§Runtime State Inventory — stored-data row: 3,855 zombie AppUser, 21/21 expired Session, 5 orphaned, never-delete id; §Assumptions A3/A4; §Pitfall 1 — one-shot ops, no loops, AuraDB flood/suspension; §Wave / Sequencing Guidance — final-wave items 4/5)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md (§scripts — zombie_sweep.py dry-run pattern; reseed path: setup_database via graph/setup.py CLI, gated behind the read-only aura_graph_integrity.sh audit)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-CONTEXT.md (D-07 — one-time sweep, never delete ae8a41b7-...; D-08 — tests never mutate real user rows)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-08-PLAN.md (created spoilerless/scripts/zombie_sweep.py + the startup schema check; live execution explicitly deferred to THIS plan)
@.planning/REQUIREMENTS.md (PROB-20 — #44; PROB-22 — #46)
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: PROB-20 live AuraDB reseed — read-only audit -> sign-off -> one-shot MERGE reseed</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Assumptions A3 — reseed is safe via the idempotent MERGE-based setup
    script, gated behind the read-only scripts/aura_graph_integrity.sh audit;
    §Pitfall 1 — one-shot, explicit approval, never in a loop),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md
    (reseed path: setup_database (seed.py:377) via graph/setup.py CLI —
    database.open() -> verify_connection() -> setup_database -> close();
    gate behind the read-only audit),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-08-PLAN.md
    (Task 3 — the startup schema-drift check that detects the 01N52 class:
    synopsis_visible_from_order / image_visible_from_order on Episode nodes)
  </read_first>
  <files>.planning/phases/09-feature-expansion-full-audit-remediation/09-18-SUMMARY.md</files>
  <action>
    PRE-CHECK (read-only): run the integrity audit — scripts/aura_graph_integrity.sh
    (the read-only audit companion per RESEARCH/PATTERNS; if it is not present
    in the tree, use the runbook's documented read-only count Cypher from
    docs/RUNBOOK.md instead) — and record: series_dexter node/relationship
    counts, Episode property presence for the 01N52 fields
    (synopsis_visible_from_order, image_visible_from_order), and the user
    content rows (progress rows, chat sessions, notes) that MUST survive the
    reseed. Confirm the NEO4J_* env in the operator shell points at AuraDB
    (never local docker).

    SIGN-OFF: review the pre-check output and the MERGE semantics of
    spoilerless/app/graph/setup.py (idempotent MERGE-based seed that preserves
    user content per RESEARCH A3). The operator explicitly approves the reseed
    before any write.

    RUN (one-shot): uv run spoilerless-setup (the post-09-01 console entry;
    equivalent module form: uv run python -m spoilerless.app.graph.setup)
    against the live AuraDB. Run it ONCE — never in a loop (Pitfall 1). If
    AuraDB returns Unauthorized/AuthError mid-run (flood-blocking or
    suspension, Pitfall 1), STOP, wait out the cooldown, and retry once —
    do not hammer the DB.

    POST-CHECK: rerun the read-only audit; verify episode queries that
    previously emitted the 01N52 'property key does not exist' warnings now
    return clean; spot-check that the user content rows from the pre-check
    (progress, chat sessions, notes) survived (D-08). Record before/after
    counts + warnings + spot-checks in 09-18-SUMMARY.md.
  </action>
  <verify>
    <automated>test -f scripts/aura_graph_integrity.sh && bash -n scripts/aura_graph_integrity.sh && echo AUDIT-SCRIPT-OK || echo "audit script absent — use docs/RUNBOOK.md read-only counts"</automated>
    <automated>rg -n "01N52|synopsis_visible_from_order|image_visible_from_order" spoilerless/app/graph/seed.py</automated>
    <automated>uv run python -c "import spoilerless.app.graph.setup; print('setup-import-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-check audit recorded (counts + user-content rows + AuraDB env confirmed)
    - Operator sign-off documented BEFORE the run
    - Reseed executed once against AuraDB; post-check audit clean
    - Episode queries emit no 01N52 warnings; user content spot-checks pass
  </acceptance_criteria>
  <done>The live AuraDB matches the current seed; #44 is closed with audit evidence.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: PROB-22 zombie sweep execution — dry-run counts -> sign-off -> --execute (never the dev user)</name>
  <read_first>
    .planning/phases/09-feature-expansion-full-audit-remediation/09-08-PLAN.md
    (Task 2 — zombie_sweep.py contract: --dry-run default prints counts of
    (a) AppUser rows with NO progress/chat/ownership ties, (b) expired and
    orphaned Session nodes; --execute performs deletions after the count is
    shown; hard-coded never-delete module constant; deletes only AppUser rows
    with no ownership edges and sessions that are expired/revoked/orphaned;
    prints exact counts before and after),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Runtime State Inventory — ~3,855 zombies, 21/21 expired sessions, 5
    orphaned; the protected id ae8a41b7-db96-40e8-b6c2-2e3c69aedb11;
    §Assumptions A4 — sweep script must print a dry-run count first)
  </read_first>
  <files>.planning/phases/09-feature-expansion-full-audit-remediation/09-18-SUMMARY.md</files>
  <action>
    DRY-RUN (mandatory first): run uv run python spoilerless/scripts/zombie_sweep.py
    --dry-run against the live AuraDB (same driver/TLS normalization as
    graph/database.py). It must print the deletion-candidate counts: (a)
    AppUser rows with no progress/chat/ownership ties (expect ~3,855), (b)
    expired + orphaned Session nodes (expect 21 expired / 5 orphaned). Verify
    the never-delete protection in the script source covers
    ae8a41b7-db96-40e8-b6c2-2e3c69aedb11 and that the protected id is NOT in
    the candidate set (rg the constant + eyeball the dry-run output). Also
    run the read-only integrity audit (scripts/aura_graph_integrity.sh, or
    the docs/RUNBOOK.md read-only counts if absent) for the pre-sweep baseline.

    SIGN-OFF: review the dry-run counts with the operator; confirm every
    candidate is test-created (no real-user markers); explicit approval to
    execute.

    RUN (one-shot): uv run python spoilerless/scripts/zombie_sweep.py --execute
    against the live AuraDB — ONCE, never in a loop (Pitfall 1). The script
    prints before/after counts. If AuraDB flood-blocks mid-run, stop and wait
    out the cooldown before any retry.

    POST: rerun the read-only integrity audit; prove the protected dev user
    survived (a direct read-only Cypher MATCH on
    ae8a41b7-db96-40e8-b6c2-2e3c69aedb11 must return the row); confirm zombie
    counts dropped (~3,855 -> 0 candidates; sessions swept). Regression:
    run the seed-idempotency suite against the SCRATCH/CI Neo4j (never
    AuraDB — Pitfall 1) — uv run pytest spoilerless/tests/test_seed_idempotency.py -x.
    Record everything in 09-18-SUMMARY.md.
  </action>
  <verify>
    <automated>rg -n "ae8a41b7-db96-40e8-b6c2-2e3c69aedb11" spoilerless/scripts/zombie_sweep.py</automated>
    <automated>uv run python spoilerless/scripts/zombie_sweep.py --dry-run 2>&1 | tail -10</automated>
    <automated>uv run pytest spoilerless/tests/test_seed_idempotency.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run counts printed and recorded; protected id verified absent from candidates and hard-coded in the script
    - Operator sign-off documented before --execute
    - Sweep executed once; before/after counts recorded; audit clean after
    - Protected dev user ae8a41b7-... provably survives (MATCH returns the row)
    - Seed-idempotency tests green against scratch/CI Neo4j after the sweep
  </acceptance_criteria>
  <done>The zombie landfill is swept from the live DB with the dev user intact; #46 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell <-> live AuraDB | One-shot destructive scripts cross into the shared production DB |
| Sweep script <-> user rows | The never-delete id and ownership-tie predicate separate zombies from real users |
| Reseed script <-> live graph | MERGE semantics must not overwrite user content (A3) |
| Full test suite <-> AuraDB | The suite must never target the live DB (Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-18-01 | Availability | Sweep deleting real user rows (A4) | critical | mitigate | Dry-run-first script, never-delete constant for ae8a41b7-..., ownership-tie predicate, operator sign-off, post-MATCH proof |
| T-09-18-02 | Tampering | Reseed overwriting user content (A3) | high | mitigate | MERGE-based setup script + pre/post audit + user-content spot-checks (D-08) |
| T-09-18-03 | Availability | AuraDB flood-block/suspension mid-op (Pitfall 1) | medium | mitigate | One-shot ops, stop on Unauthorized, cooldown before any retry — no loops |
| T-09-18-04 | Tampering | Suite residue re-polluting the live DB post-sweep | medium | mitigate | Seed-idempotency + CI pollution gate target scratch/CI Neo4j only |
| T-09-18-05 | Information Disclosure | Audit/evidence leaving raw AuraDB credentials | low | mitigate | Use platform env; record counts only, never connection strings |
| T-09-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs — the sweep/reseed scripts already exist from 09-08 |
</threat_model>

<verification>
Run the dry-run sweep (read-only) and the read-only integrity audit before
and after each destructive op; rg the never-delete constant; MATCH the
protected user post-sweep; run seed-idempotency against scratch/CI Neo4j.
Grep gates: no credentials in the SUMMARY; `git status --short` shows no repo
file changes from this plan (the plan modifies the live DB, not the tree).
</verification>

<success_criteria>
The live AuraDB is reseeded to the current seed (no 01N52 warnings, user
content intact) and the zombie AppUser/session landfill is swept with the
protected dev user provably present — both ops executed once, audit-gated,
sign-off-documented, with before/after evidence in the SUMMARY. PROB-20 and
PROB-22 are closed, completing the phase's operator wave.
</success_criteria>

## Artifacts this phase produces

- `09-18-SUMMARY.md` (NEW) — pre/post audit counts, dry-run counts, sign-off
  records, reseed + sweep evidence, protected-user MATCH proof
- No product code changes — the destructive tooling (zombie_sweep.py, setup
  schema check) shipped in 09-08; this plan executes it against live AuraDB

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-18-SUMMARY.md` when done
</output>
