---
phase: 09-feature-expansion-full-audit-remediation
plan: 07
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - frontend/src/hooks/useWatchProgress.ts
  - frontend/src/hooks/useWatchProgress.test.ts
  - frontend/src/components/episode/EpisodeSelector.test.tsx
  - frontend/src/components/graph/GraphCanvas.tsx
  - frontend/src/components/graph/graphStylesheet.ts
  - frontend/src/App.tsx
  - frontend/src/components/graph/GraphCanvas.test.tsx
  - frontend/src/hooks/useChatSessions.ts
  - frontend/src/hooks/useNotes.ts
  - frontend/src/hooks/useRevisions.ts
  - frontend/src/components/detail/DetailPanel.tsx
  - frontend/src/App.test.tsx
  - frontend/eslint.config.js
autonomous: true
requirements: [PROB-07, PROB-08, PROB-31, FEAT-03]
must_haves:
  truths:
    - "Clicking a locked episode ALWAYS opens the unlock dialog (forward) or loads the episode view-only (backward) — the silent no-ops at useWatchProgress.ts requestChange lines 133/139 are gone, and the mount-time hydration race cannot clobber a user's click (PROB-31, #56)"
    - "Advancing watch progress highlights nodes/edges newly revealed by that episode with a temporary glow that auto-clears — FEAT-03 ships on top of the fixed hook"
    - "npm run lint reports 0 errors, including the real react-hooks/refs stale-ref bugs in useChatSessions/useNotes/useRevisions — fixed by moving ref writes out of render bodies, not by scoping more rules to warn (PROB-08, #16)"
    - "App.test.tsx's end-to-end test passes deterministically in isolation AND in the full suite (PROB-07, #17)"
  artifacts:
    - path: "frontend/src/hooks/useWatchProgress.ts"
      provides: "requestChange with no silent return branches + hydration race guard"
    - path: "frontend/src/components/graph/graphStylesheet.ts"
      provides: ".newly-revealed overlay + pulse keyframes (FEAT-03)"
  key_links:
    - from: "App.tsx"
      to: "GraphCanvas.tsx newlyRevealedIds"
      via: "client-side diff of pre/post-advance id sets around watchProgress.confirmedOrder"
      pattern: "newlyRevealedIds"
    - from: "useWatchProgress.ts requestChange"
      to: "ConfirmAdvanceModal unlock flow"
      via: "forward locked-episode clicks always route to the dialog; view-only awaits its POST and refetches on failure"
      pattern: "ConfirmAdvanceModal"
  prohibitions:
    - id: "NO-SILENT-NOOP"
      statement: "requestChange must never silently return — every click either opens the dialog, loads the episode, or surfaces an error (PROB-31)"
    - id: "NO-NEW-LINT-EXEMPTIONS"
      statement: "Lint cleanup must FIX the stale-ref bugs — scoping additional rules to warn is forbidden (RESEARCH anti-pattern)"
    - id: "NO-WIRE-MOCK"
      statement: "Regression tests assert the wire shape via a fetch stub, never vi.mock of the progress API client (Pitfall 4)"
---

<objective>
Frontend correctness and quality wave: fix the user-reported episode-selector
no-op bug (#56) with a regression test, ship the FEAT-03 newly-revealed
highlight on top of the fixed hook, eliminate the 28 lint errors including the
real React 19 stale-ref bugs (#16), and make the flaky App end-to-end test
deterministic (#17). PROB-31 must land BEFORE the wave-3 feature plans because
FEAT-03 and the command palette's episode switching both depend on a correct
requestChange.

Purpose: The most user-visible frontend defects (dead episode clicks,
re-scrambling state) and the suite-trust defects (lint debt, flaky e2e) are
cleared before the feature wave builds on top.
Output: A deterministic, lint-clean frontend where locked-episode interactions
always work and advances visibly reveal new content.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md (§Pitfall 4, §Anti-Patterns: fixing lint by scoping)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md (§useWatchProgress.ts; §Frontend build/lint gates)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md (§10.1 PROB-31, §10.5 FEAT-03)
@.planning/REQUIREMENTS.md (PROB-07/08/31, FEAT-03)
@docs/PROBLEMS.md (#16, #17, #56)
@frontend/src/hooks/useWatchProgress.ts
@frontend/src/components/graph/GraphCanvas.tsx
@frontend/eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix useWatchProgress silent no-ops + hydration race, then ship FEAT-03 reveal highlight</name>
  <read_first>
    frontend/src/hooks/useWatchProgress.ts (requestChange ~131-151 — line 133
    `if (nextOrder === currentView) return` silent no-op; line 139-146 view-only
    branch swallowing failures; mount hydration effect ~104-129 with [] deps
    clobbering clicks),
    frontend/src/components/episode/ConfirmAdvanceModal.tsx (the unlock flow
    the fix must always route to),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md
    (§10.1 PROB-31 — no visual change to EpisodeSelector; §10.5 FEAT-03 —
    .newly-revealed overlay #7C3AED, 4000ms, reduced-motion static),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md
    (§useWatchProgress.ts — exact no-op lines + wire-shape regression pattern)
  </read_first>
  <files>frontend/src/hooks/useWatchProgress.ts, frontend/src/hooks/useWatchProgress.test.ts, frontend/src/components/episode/EpisodeSelector.test.tsx, frontend/src/components/graph/GraphCanvas.tsx, frontend/src/components/graph/graphStylesheet.ts, frontend/src/App.tsx, frontend/src/components/graph/GraphCanvas.test.tsx</files>
  <action>
    PROB-31/#56 fixes in useWatchProgress.ts:
    (a) line ~133: remove the silent return — when nextOrder equals currentView,
    reconcile currentView to the selector's displayed value (or surface the
    no-op via the same dialog/load path) so a click is never swallowed;
    (b) the view-only branch (~139-146): a forward click above watchedThroughOrder
    must ALWAYS open the ConfirmAdvanceModal unlock flow; a backward click must
    load the episode view-only AND await the view-only POST, refetching the
    graph on failure (or optimistically refetching) so the UI never shows
    "nothing happened";
    (c) hydration race (~104-129): serialize mount-time getProgress against
    user clicks — skip hydration if a click already occurred, or merge backend
    values without clobbering a newer local change.
    Keep EpisodeSelector's visual contract byte-identical (UI-SPEC 10.1 — no
    visual files touched) and keep locked pills focusable.
    Regression tests (wire-shape, no client mock): (1) clicking a locked
    episode above watchedThroughOrder with a FAILING view-only POST still
    opens the unlock dialog; (2) clicking a locked episode below
    watchedThroughOrder loads the episode and refetches on POST failure; (3)
    a click during mount hydration is not clobbered by the late hydration
    response; (4) EpisodeSelector.test.tsx asserts no no-op regression.

    FEAT-03 (on top of the fixed hook): in App.tsx, compute the set-diff of
    node ids between the pre-advance and post-advance graph payloads when
    watchProgress.confirmedOrder changes (both payloads are available in the
    confirm flow per FEATURE-RESEARCH §2) and pass the diff as a
    `newlyRevealedIds` prop to GraphCanvas. In GraphCanvas.tsx, accept the new
    optional prop (defaulted exactly like focusedElementIds/revealElementIds so
    existing tests compile) and apply a `.newly-revealed` class for 4000ms
    then auto-clear — WITHOUT re-running the layout (same guard as the
    existing reveal effect). In graphStylesheet.ts, add `.newly-revealed`:
    overlay-color #7C3AED, overlay-opacity 0.45, overlay-padding 10, plus a
    2-cycle pulse keyframe (0.45→0.15→0.45); under prefers-reduced-motion the
    glow is static with no pulse and 0ms transitions.
    GraphCanvas.test.tsx: add a highlight-effect test (class applied on
    newlyRevealedIds, cleared after the timer) using fake timers.
    Commit with message "feat(09-07): progress-hook no-op fixes + FEAT-03 newly-revealed highlight".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/hooks/useWatchProgress.test.ts src/components/episode/EpisodeSelector.test.tsx src/components/graph/GraphCanvas.test.tsx 2>&1 | tail -5</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - requestChange has no silent return branches; forward locked clicks always open the unlock dialog; view-only awaits + refetches on failure
    - The hydration race regression test passes (click not clobbered by late hydration)
    - newlyRevealedIds flows App → GraphCanvas; the class applies and auto-clears on a 4000ms timer without relayout; reduced-motion is static
    - Targeted vitest + npm run build green
  </acceptance_criteria>
  <done>Episode clicks always act, advances visibly reveal new content, and both are regression-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale-ref lint errors (real React 19 bugs) — 0 errors, no new exemptions</name>
  <read_first>
    frontend/src/hooks/useChatSessions.ts, frontend/src/hooks/useNotes.ts,
    frontend/src/hooks/useRevisions.ts (writing fetchKeyRef.current in the
    render body — the react-hooks/refs stale-ref correctness bug under React
    19 double-render),
    frontend/src/components/detail/DetailPanel.tsx and
    frontend/src/components/graph/GraphCanvas.tsx (preserve-manual-memoization
    findings),
    frontend/eslint.config.js (~28-39 — the 3 React-Compiler-era rules ALREADY
    scoped to warn; do NOT add more exemptions),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Anti-Patterns: fixing lint by scoping more rules to warn is forbidden)
  </read_first>
  <files>frontend/src/hooks/useChatSessions.ts, frontend/src/hooks/useNotes.ts, frontend/src/hooks/useRevisions.ts, frontend/src/components/detail/DetailPanel.tsx, frontend/src/components/graph/GraphCanvas.tsx, frontend/eslint.config.js</files>
  <action>
    Fix the real bugs, not the config: move every `fetchKeyRef.current` write
    out of render bodies into effects (useEffect) in useChatSessions.ts,
    useNotes.ts, and useRevisions.ts — the correct React 19 pattern that makes
    the ref value stable across double-render. Fix the
    preserve-manual-memoization findings in DetailPanel.tsx and GraphCanvas.tsx
    by removing the unnecessary memoization or making it conform to the rule
    (delete the violation rather than suppressing it). Fix any remaining
    no-explicit-any findings in test files by typing the values. Do NOT add
    new rules to the warn-scope block in eslint.config.js (RESEARCH
    anti-pattern — PROB-08/09-06 must FIX, not exempt).
    Verify `npm run lint` reports 0 errors AND 0 warnings on the whole tree.
    Commit with message "fix(09-07): stale-ref bugs fixed in effects, lint 0 errors (PROB-08/#16)".
  </action>
  <verify>
    <automated>cd frontend && npm run lint 2>&1 | tail -5</automated>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/hooks/useChatSessions.test.ts src/hooks/useNotes.test.ts src/hooks/useRevisions.test.ts 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - npm run lint exits with 0 errors (and 0 warnings) — no new warn-scope rules added
    - fetchKeyRef.current writes live inside effects in all three hooks
    - The three hook test files still pass
  </acceptance_criteria>
  <done>Lint is a clean gate; the stale-ref class of bugs is fixed in code, not exempted in config.</done>
</task>

<task type="auto">
  <name>Task 3: Make the App end-to-end test deterministic</name>
  <read_first>
    frontend/src/App.test.tsx ("runs select → confirm → fetch → render →
    inspect end-to-end" — passes 15/15 in isolation, fails in the full run),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Pitfall 6, PROBLEMS #17 — order/timing-dependent e2e),
    .planning/codebase/TESTING.md (jsdom + NODE_ENV=test CI=1 conventions)
  </read_first>
  <files>frontend/src/App.test.tsx</files>
  <action>
    Make the end-to-end flow deterministic: identify the order/timing
    dependence (fetch resolution order, timers, or cross-file state in
    jsdom). Apply the documented fixes: mock timers/requestAnimationFrame
    where the flow depends on them, ensure complete isolation (localStorage/
    sessionStorage cleared in beforeEach — the runbook pitfall that jsdom
    localStorage persists across tests in a file), and stub fetch responses
    with explicit resolution order so the select→confirm→fetch→render→inspect
    sequence cannot interleave with other tests' pending work. The test must
    pass in isolation AND in the full suite run — verify with the full
    `NODE_ENV=test CI=1 npm run test` invocation, not just the single file.
    Commit with message "test(09-07): deterministic App e2e (PROB-07/#17)".
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/App.test.tsx 2>&1 | tail -3</automated>
    <automated>cd frontend && NODE_ENV=test CI=1 npm run test 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - App.test.tsx passes in isolation
    - The FULL frontend suite passes (no order-dependent failure) — the #17 flake is gone
    - No test relies on implicit cross-file jsdom state
  </acceptance_criteria>
  <done>The full frontend suite is deterministic and gate-trustworthy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click <-> episode selector state | Silent-swallow branches cross here |
| Mount hydration <-> user-initiated state | Race between backend response and local click |
| Component render <-> ref writes | React 19 double-render stale-ref class |
| Test suite <-> suite ordering | Order/timing-dependent e2e must not gate releases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07-01 | Tampering | Locked-episode no-op (#56) | high | mitigate | requestChange no silent returns; unlock-dialog regression with failing POST; hydration serialization |
| T-09-07-02 | Spoofing | Stale-ref state under double-render (#16) | medium | mitigate | fetchKeyRef.current moved into effects; lint gate 0 errors |
| T-09-07-03 | Denial of Service | Flaky e2e gate (#17) | medium | mitigate | Timers/storage isolation; full-suite verification |
| T-09-07-04 | Information Disclosure | Wire-shape regression mock blindness | medium | mitigate | Fetch-stub assertions only, no client-module mocks |
| T-09-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies |
</threat_model>

<verification>
Run the targeted vitest files (useWatchProgress, EpisodeSelector, GraphCanvas,
the three hook files, App.test) then the FULL frontend suite
(`NODE_ENV=test CI=1 npm run test`) plus `npm run build` and `npm run lint`
(0 errors, 0 warnings). Confirm eslint.config.js gained no new warn-scope
rules (`git diff frontend/eslint.config.js` shows no rule-scoping additions).
</verification>

<success_criteria>
Episode clicks always open/load (regression-locked), advances reveal new
content with the FEAT-03 glow, lint is 0/0 without new exemptions, and the
full frontend suite is deterministic — the frontend is a trustworthy base for
the wave-3 feature plans.
</success_criteria>

## Artifacts this phase produces

- `frontend/src/hooks/useWatchProgress.ts` (MODIFIED) — no silent returns,
  view-only awaits + refetches, hydration/click serialization
- `frontend/src/hooks/useWatchProgress.test.ts` (MODIFIED) — locked-click +
  failing-POST regression, hydration-race regression
- `frontend/src/components/episode/EpisodeSelector.test.tsx` (MODIFIED) —
  no-op regression assertions (visual contract untouched)
- `frontend/src/App.tsx` (MODIFIED) — newlyRevealedIds diff computation
- `frontend/src/components/graph/GraphCanvas.tsx` (MODIFIED) — newlyRevealedIds
  prop + 4000ms auto-clear effect (no relayout)
- `frontend/src/components/graph/graphStylesheet.ts` (MODIFIED) —
  `.newly-revealed` overlay + pulse keyframes (reduced-motion static)
- `frontend/src/components/graph/GraphCanvas.test.tsx` (MODIFIED) —
  highlight-effect test (fake timers)
- `frontend/src/hooks/useChatSessions.ts`, `useNotes.ts`, `useRevisions.ts`
  (MODIFIED) — ref writes in effects
- `frontend/src/components/detail/DetailPanel.tsx` (MODIFIED) — memoization
  violation fixed
- `frontend/src/App.test.tsx` (MODIFIED) — deterministic e2e
- `frontend/eslint.config.js` (MODIFIED only if a legit fix is required — no
  new warn exemptions)

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-07-SUMMARY.md` when done
</output>
