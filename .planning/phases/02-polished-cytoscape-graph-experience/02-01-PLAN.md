---
phase: 02-polished-cytoscape-graph-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/test/fixtures/graphResponse.ts
  - frontend/src/types/graph.ts
  - frontend/src/types/series.ts
  - frontend/src/types/cytoscape-cose-bilkent.d.ts
  - frontend/src/api/client.ts
  - frontend/src/api/series.ts
  - frontend/src/api/graph.ts
  - frontend/src/hooks/useSeries.ts
  - frontend/src/hooks/useEpisodes.ts
  - frontend/src/hooks/useGraph.ts
  - frontend/src/hooks/useWatchProgress.ts
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/episode/SeriesSelect.tsx
  - frontend/src/components/episode/EpisodeSelector.tsx
  - frontend/src/components/episode/ConfirmAdvanceModal.tsx
  - frontend/src/components/graph/GraphCanvas.tsx
  - frontend/src/components/graph/graphElements.ts
  - frontend/src/components/detail/DetailPanel.tsx
  - frontend/src/App.tsx
  - frontend/src/App.css
  - frontend/src/App.test.tsx
  - .planning/phases/02-polished-cytoscape-graph-experience/02-UI-SPEC.md
autonomous: false
requirements:
  - UI-01
  - UI-02
security_enforcement: true
asvs_level: 1

must_haves:
  truths:
    - "On initial mount, no /api/graph request fires and the app shows the D-01 empty state until the user selects both a series and an episode (no GraphResponse render, SeriesSelect and EpisodeSelector both start unselected)."
    - "frontend/src/App.tsx contains no Vite-starter markup (no react.svg/vite.svg/hero.png references, no counter button) and instead composes AppShell + SeriesSelect + EpisodeSelector + ConfirmAdvanceModal + GraphCanvas + DetailPanel wired to getSeries()/getEpisodes()/getGraph()."
    - "Changing the selected episode (forward OR backward relative to the currently confirmed visible_until_order) opens ConfirmAdvanceModal before any new /api/graph request is issued; canceling issues no request and leaves state unchanged."
    - "Refreshing the page within the same tab restores the last confirmed series and visible_until_order from sessionStorage without opening ConfirmAdvanceModal."
    - "EpisodeSelector always lists a set of options exactly equal to the persisted episodes returned by GET /api/series/{id}/episodes (currently 3: S01E01-03); selecting the last available episode (order 3) sets visible_until_order to exactly 3, and there is no selector path that can request an order beyond the fetched episode list."
    - "useWatchProgress computes the requested visible_until_order directly from the selected episode's own episode_order integer field with no client-side arithmetic transformation, and classifies a progress change as forward/backward via a direct integer comparison against the currently-confirmed order."
    - "With exactly 3 persisted episodes (S01E01-03) returned by GET /api/series/{id}/episodes, EpisodeSelector never needs scroll/wrap overflow handling (UI-SPEC UI Considerations - overflow/episode selector)."
  artifacts:
    - frontend/src/types/graph.ts - GraphNode/GraphEdge/GraphClaim/GraphSource/GraphEvidence/GraphResponse types mirroring backend/app/domain/graph.py
    - frontend/src/types/series.ts - SeriesResponse/EpisodeResponse types mirroring backend/app/domain/series.py
    - frontend/src/api/client.ts - apiFetch<T>() + ApiError mirroring backend/app/core/errors.py {detail:{code,message}} shape
    - frontend/src/hooks/useWatchProgress.ts - sessionStorage-backed watch-progress state with hydration guard (D-02) and forward/backward change detection (D-03)
    - frontend/src/App.tsx - replaces the Vite starter with the composed product layout
    - frontend/vite.config.ts - Vitest test config (jsdom environment, globals, setupFiles) added alongside existing Vite/React/Tailwind plugin config
    - frontend/src/App.test.tsx - first end-to-end component test exercising the full select-confirm-render-select flow
  key_links:
    - useWatchProgress to ConfirmAdvanceModal - every live progress change (forward or backward) must route through the modal before committing to sessionStorage/state
    - sessionStorage hydration to ConfirmAdvanceModal - hydration on mount must bypass the modal-opening code path entirely (Pitfall 5)
    - App.tsx selection state to GraphCanvas/DetailPanel - the fetched GraphResponse and click-selected element must flow from App.tsx down without a second network round trip
  prohibitions:
    - statement: "Frontend must never auto-select a series or episode on mount, or fire a /api/graph request before the user has deliberately chosen both (D-01)."
      status: resolved
      verification: explicit
    - statement: "sessionStorage hydration on mount must never re-trigger ConfirmAdvanceModal; only a live, user-initiated progress change may open it (D-02, Pitfall 5)."
      status: resolved
      verification: explicit
    - statement: "A malformed or tampered sessionStorage value must never crash the app on mount; JSON.parse must be guarded and fall back to the D-01 empty state."
      status: resolved
      verification: explicit
---

<objective>
Replace the untouched Vite/React starter with the first real, production-quality vertical slice of the HD Graf Cehennemi frontend: pick a series, pick an episode, confirm the watch-progress change, fetch the spoiler-safe graph, render it in Cytoscape, and inspect a selected element in a detail panel. This plan also stands up the test tooling (Vitest + React Testing Library) that does not exist anywhere in the repo yet, gated behind a package-legitimacy human-verify checkpoint for the three packages the audit flagged `SUS`.

This is the phase's tracer: it touches every layer (types, API client, hooks, sessionStorage-backed state, Cytoscape rendering, detail panel, top-level composition) with the thinnest correct implementation of each layer. Layout polish (cose-bilkent, full stylesheet, selection highlight/fade), the Overview/Claims/Evidence tab split vs. the structural-edge minimal card (D-06/D-07), and loading/error states are deliberately deferred to Plan 02 and Plan 03 as horizontal expansion on top of this proven skeleton.

Purpose: prove the full request -> confirm -> fetch -> render -> inspect path works end-to-end against the live backend before investing further in visual polish, so any architectural problem surfaces on this task's own context budget rather than after several more plans have built on a broken foundation.
Output: A running product frontend (no Vite-starter remnants) that satisfies UI-01 and the mechanical core of UI-02, plus the test infrastructure later plans depend on.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-polished-cytoscape-graph-experience/02-CONTEXT.md
@.planning/phases/02-polished-cytoscape-graph-experience/02-RESEARCH.md
@.planning/phases/02-polished-cytoscape-graph-experience/02-PATTERNS.md
@.planning/phases/02-polished-cytoscape-graph-experience/02-UI-SPEC.md
@backend/app/domain/graph.py
@backend/app/domain/series.py
@backend/app/core/errors.py
@frontend/src/App.tsx
@frontend/src/components/ui/select.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/sheet.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy check - vitest, @testing-library/jest-dom, jsdom</name>
  <what-built>02-RESEARCH.md's Package Legitimacy Audit flagged three new devDependencies this phase needs as `SUS` (`too-new` heuristic on an actively-maintained, high-download package, not a confirmed slopsquat): `vitest` (82.3M weekly downloads, github.com/vitest-dev/vitest), `@testing-library/jest-dom` (55.6M weekly downloads, github.com/testing-library/jest-dom), `jsdom` (85.9M weekly downloads, github.com/jsdom/jsdom). No installs have happened yet - this checkpoint gates Task 2's `npm install`.</what-built>
  <how-to-verify>
    For each of the three packages, open its npmjs.com page and confirm: (1) the linked GitHub repository matches the canonical org named above, (2) weekly download count is in the tens-of-millions range, (3) the latest published version's changelog/tag looks like a normal maintenance release, not a suspicious first-ever publish. URLs: https://www.npmjs.com/package/vitest, https://www.npmjs.com/package/@testing-library/jest-dom, https://www.npmjs.com/package/jsdom.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installing all three, or name which package(s) failed verification and why.</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end "select series to inspect element" - one path only</name>
  <files>
    frontend/package.json, frontend/vite.config.ts, frontend/src/test/setup.ts, frontend/src/test/fixtures/graphResponse.ts,
    frontend/src/types/graph.ts, frontend/src/types/series.ts, frontend/src/types/cytoscape-cose-bilkent.d.ts,
    frontend/src/api/client.ts, frontend/src/api/series.ts, frontend/src/api/graph.ts,
    frontend/src/hooks/useSeries.ts, frontend/src/hooks/useEpisodes.ts, frontend/src/hooks/useGraph.ts, frontend/src/hooks/useWatchProgress.ts,
    frontend/src/components/layout/AppShell.tsx, frontend/src/components/episode/SeriesSelect.tsx, frontend/src/components/episode/EpisodeSelector.tsx, frontend/src/components/episode/ConfirmAdvanceModal.tsx,
    frontend/src/components/graph/graphElements.ts, frontend/src/components/graph/GraphCanvas.tsx, frontend/src/components/detail/DetailPanel.tsx,
    frontend/src/App.tsx, frontend/src/App.css, frontend/src/App.test.tsx,
    .planning/phases/02-polished-cytoscape-graph-experience/02-UI-SPEC.md
  </files>
  <read_first>
    frontend/src/App.tsx
    frontend/src/main.tsx
    frontend/vite.config.ts
    frontend/package.json
    frontend/src/components/ui/select.tsx
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/sheet.tsx
    frontend/src/lib/utils.ts
    backend/app/domain/graph.py
    backend/app/domain/series.py
    backend/app/core/errors.py
    .planning/phases/02-polished-cytoscape-graph-experience/02-UI-SPEC.md
    .planning/phases/02-polished-cytoscape-graph-experience/02-RESEARCH.md
    .planning/phases/02-polished-cytoscape-graph-experience/02-PATTERNS.md
  </read_first>
  <behavior>
    - Mount with nothing selected: no fetch to /api/series/{id}/graph fires; empty-state copy renders.
    - Select series then episode S01E01, confirm the modal: exactly one fetch to /api/series/series_dexter/graph?visible_until_order=1 fires; canceling the modal fires none.
    - Refresh (re-mount with sessionStorage pre-populated): state restores without opening the modal.
    - Change episode again (forward to S01E02 or backward to S01E01): modal reopens before any new fetch.
    - Click a rendered node: DetailPanel shows that node's label; click empty canvas: DetailPanel returns to the "Select a node to see details." placeholder.
  </behavior>
  <action>
    1. Install dependencies in frontend/: `npm install cytoscape-cose-bilkent` (runtime, OK-verdict); `npm install -D @types/react-cytoscapejs vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom` (devDependencies; the three SUS-flagged ones are now approved by Task 1). Add `"test": "vitest"` to package.json `scripts`.
    2. Add a `test` block to the existing `defineConfig` in frontend/vite.config.ts (keep `plugins`/`resolve.alias` untouched): `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./src/test/setup.ts']`. Add `/// <reference types="vitest/config" />` at the top of the file.
    3. Create frontend/src/test/setup.ts importing '@testing-library/jest-dom'.
    4. Create frontend/src/types/cytoscape-cose-bilkent.d.ts: `declare module 'cytoscape-cose-bilkent'` exporting a default `Ext` (from cytoscape's own types) - no upstream .d.ts exists (`@types/cytoscape-cose-bilkent` is confirmed `SLOP`/does-not-exist on the registry).
    5. Create frontend/src/types/graph.ts and frontend/src/types/series.ts mirroring backend/app/domain/graph.py and backend/app/domain/series.py field-for-field. Type `origin` as `string` - the wire value is literally `'canonical'` in this project, never `'curated'`; do not write `'curated'` anywhere. `GraphEdge.claim_id` is `string | null`.
    6. Create frontend/src/api/client.ts exporting `ApiErrorDetail` (`{code: string, message: string}`), an `ApiError` class extending `Error` with a `code` field, and `apiFetch<T>(url: string): Promise<T>` that throws `ApiError` populated from the response body's `detail` field on any non-2xx status (covers 404 `series_not_found`, 422 `invalid_visible_until_order`, 503 `database_unavailable`/`database_error`).
    7. Create frontend/src/api/series.ts exporting `getSeries()` (`GET /api/series`) and `getEpisodes(seriesId)` (`GET /api/series/{seriesId}/episodes`), both via `apiFetch`.
    8. Create frontend/src/api/graph.ts exporting `getGraph(seriesId, visibleUntilOrder)` (`GET /api/series/{seriesId}/graph?visible_until_order={visibleUntilOrder}`), via `apiFetch`. Per D-08, these three functions are the ONLY backend calls this phase makes - no new endpoint, no server-side persistence of watch progress; `visible_until_order` is always recomputed client-side (useWatchProgress) and resent in full on every call, never stored by the backend.
    9. Create frontend/src/hooks/useSeries.ts, useEpisodes.ts, useGraph.ts following a `{status:'idle'|'loading'|'error'|'success'}` state-union shape over `getSeries`/`getEpisodes`/`getGraph` (see 02-PATTERNS.md's `useGraph.ts` example).
    10. Create frontend/src/hooks/useWatchProgress.ts implementing D-01/D-02/D-03: `STORAGE_KEY = 'hdgraf.watchProgress'`. On mount, read `sessionStorage.getItem(STORAGE_KEY)`, `JSON.parse` inside try/catch, validate `seriesId` is a non-empty string and `visibleUntilOrder` is a positive integer, and hydrate directly into state via a code path that never opens ConfirmAdvanceModal - any parse/validation failure falls back to the D-01 empty (`null`) state. Expose `requestChange(seriesId, nextOrder)` for live user interaction: it must set a `pendingChange` field (read by ConfirmAdvanceModal) rather than committing immediately. Expose `confirmChange()` (commits to state + `sessionStorage.setItem`) and `cancelChange()` (discards `pendingChange`, no state mutation). Classify direction as `'forward'` when `nextOrder > currentOrder` and `'backward'` when `nextOrder < currentOrder`, via direct integer comparison only.
    11. Create frontend/src/components/episode/SeriesSelect.tsx and EpisodeSelector.tsx using the shadcn `Select`/`SelectTrigger`/`SelectValue`/`SelectContent`/`SelectItem` composition from frontend/src/components/ui/select.tsx. `EpisodeSelector`'s `SelectItem` list must be generated directly from the `EpisodeResponse[]` array returned by `useEpisodes` - no hardcoded episode list, no synthetic "next episode" beyond what was fetched.
    12. Create frontend/src/components/episode/ConfirmAdvanceModal.tsx using the shadcn `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription`/`DialogFooter` composition. Forward-direction copy (locked, 02-UI-SPEC.md Copywriting Contract): title "Unlock S01E0X?", body "You're about to see new characters, events, and relationships from S01E0X. This can't be undone. Continue?", cancel button "Cancel", confirm button "Yes, unlock episode". Add a backward-direction copy variant (Claude's-discretion addition per CONTEXT.md, since D-03 extends confirmation to backward moves and the locked copy is forward-only): title "Rewatch S01E0X?", body "You're about to move your watch progress back to S01E0X. Continue?", same "Cancel"/"Yes, unlock episode" buttons, same `--warning` (#F59E0B) color treatment as forward (never `--destructive`). Append this backward-copy variant as a new row under 02-UI-SPEC.md's `## Copywriting Contract` table (do not edit the existing forward-case row), noted inline as `(Claude's-discretion addition, CONTEXT.md backward-copy variant)`.
    13. Create frontend/src/components/graph/graphElements.ts exporting a pure mapping function from `GraphResponse.nodes`/`edges` to Cytoscape `ElementDefinition[]`, carrying `nodeType`/`origin`/`claimId` in each element's `data()`. This function must read only fields already present on `GraphResponse` and must never filter/exclude any node or edge by `visible_from_order` - the backend has already filtered.
    14. Create frontend/src/components/graph/GraphCanvas.tsx: call `cytoscape.use(coseBilkent)` once at module scope (import registered now for Plan 02, not yet used as the active layout). Render `<CytoscapeComponent>` with `layout={{name: 'cose', fit: true, padding: 24}}` and a minimal stylesheet (`node` selector: `label: 'data(label)'`; `edge` selector: `label: 'data(label)'`) - the `cose-bilkent` layout swap and full type/origin stylesheet are Plan 02's scope. Accept an `onSelect(element)` prop; wire `cy.on('tap', 'node', ...)`, `cy.on('tap', 'edge', ...)`, and a background-tap-to-clear handler that call it (D-05 - nothing on canvas is inert to clicks; neighbor highlight/fade is Plan 02's scope).
    15. Create frontend/src/components/detail/DetailPanel.tsx: a single shadcn `Sheet`/`SheetContent`/`SheetHeader`/`SheetTitle` panel (no `Tabs` yet - the Overview/Claims/Evidence split (D-07) and the separate `StructuralEdgeCard` (D-06) are Plan 03's scope) rendering the selected node's `label`/`type` or selected edge's `type`/`source`/`target`, and the locked placeholder copy "Select a node to see details." when nothing is selected.
    16. Create frontend/src/components/layout/AppShell.tsx using `Card`/`Separator` primitives as a simple top-bar + content-area wrapper.
    17. Rewrite frontend/src/App.tsx: remove all Vite-starter markup/imports (`reactLogo`, `viteLogo`, `heroImg`, the counter `<button>`, the `App.css` starter import). Compose `AppShell` > `SeriesSelect` + `EpisodeSelector` (driven by `useSeries`/`useEpisodes`) > `ConfirmAdvanceModal` (open when `useWatchProgress().pendingChange` is non-null) > `GraphCanvas` (driven by `useGraph(seriesId, confirmedOrder)`, mounted only once both a series and a confirmed order exist, per D-01) > `DetailPanel` (driven by `GraphCanvas`'s `onSelect`). Empty frontend/src/App.css of Vite-starter rules.
    18. Create frontend/src/test/fixtures/graphResponse.ts exporting a hand-written `GraphResponse` object mirroring the live S01E01 shape from 02-RESEARCH.md `## Code Examples` (11 nodes: 6 Character, 1 Event, 2 Location, 1 Episode, 1 Series; 6 edges of types OCCURRED_IN/PART_OF/WORKS_WITH/FAMILY_OF; 4 claims with one `status: "candidate"`/`valid_until_order: 1`; 1 source; 3 evidence).
    19. Create frontend/src/App.test.tsx (Vitest + RTL + @testing-library/user-event): stub `global.fetch` to resolve with fixture series/episodes/graph payloads keyed by URL. Write the RED test first (assert the empty state and the full select-confirm-fetch-render-select flow from `<behavior>` above) before completing steps 1-18, then confirm it goes GREEN once App.tsx is wired.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/App.test.tsx</automated>
    <automated>cd frontend && npm run build</automated>
    <automated>cd frontend && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/App.tsx contains no occurrence of `reactLogo`, `viteLogo`, `heroImg`, or `className="counter"`.
    - frontend/src/types/graph.ts exports GraphNode, GraphEdge, GraphClaim, GraphSource, GraphEvidence, GraphResponse with fields matching backend/app/domain/graph.py.
    - frontend/src/hooks/useWatchProgress.ts exposes `requestChange`, `confirmChange`, `cancelChange`, and `pendingChange`.
    - `cd frontend && npm run test -- --run src/App.test.tsx` exits 0.
    - `cd frontend && npm run build` exits 0.
    - `cd frontend && npm run lint` exits 0.
    - .planning/phases/02-polished-cytoscape-graph-experience/02-UI-SPEC.md contains a new backward-copy-variant row under `## Copywriting Contract` alongside (not replacing) the existing forward-case row.
    - Manually running `npm run dev` (frontend on :5173) against the live backend (:8000), selecting Dexter then S01E01, and confirming the modal renders a Cytoscape canvas with 11 elements and opens DetailPanel on node click.
  </acceptance_criteria>
  <done>The full select-series to select-episode to confirm to fetch to render to inspect-element path works end-to-end against the live backend and is covered by one passing automated component test; build and lint are clean; the change is committed.</done>
  <reversibility rating="reversible">D-01/D-02/D-03/D-04 are all client-side UX/state gating choices with no data migration cost - easy to narrow or reverse in a later phase without touching the backend contract.</reversibility>
</task>

</tasks>

## Artifacts This Phase Produces

Symbols/files newly created by this plan (excluded from drift verification as pre-existing):

- `frontend/src/types/graph.ts` - `GraphNode`, `GraphEdge`, `GraphClaim`, `GraphSource`, `GraphEvidence`, `GraphResponse` types
- `frontend/src/types/series.ts` - `SeriesResponse`, `EpisodeResponse` types
- `frontend/src/types/cytoscape-cose-bilkent.d.ts` - ambient module declaration
- `frontend/src/api/client.ts` - `ApiErrorDetail`, `ApiError`, `apiFetch<T>()`
- `frontend/src/api/series.ts` - `getSeries()`, `getEpisodes()`
- `frontend/src/api/graph.ts` - `getGraph()`
- `frontend/src/hooks/useSeries.ts` - `useSeries()`
- `frontend/src/hooks/useEpisodes.ts` - `useEpisodes()`
- `frontend/src/hooks/useGraph.ts` - `useGraph()`
- `frontend/src/hooks/useWatchProgress.ts` - `useWatchProgress()` (`requestChange`, `confirmChange`, `cancelChange`, `pendingChange`)
- `frontend/src/components/layout/AppShell.tsx` - `AppShell`
- `frontend/src/components/episode/SeriesSelect.tsx` - `SeriesSelect`
- `frontend/src/components/episode/EpisodeSelector.tsx` - `EpisodeSelector`
- `frontend/src/components/episode/ConfirmAdvanceModal.tsx` - `ConfirmAdvanceModal`
- `frontend/src/components/graph/graphElements.ts` - graph-to-Cytoscape-elements mapping function
- `frontend/src/components/graph/GraphCanvas.tsx` - `GraphCanvas`
- `frontend/src/components/detail/DetailPanel.tsx` - `DetailPanel` (minimal, unified - expanded in Plan 03)
- `frontend/src/test/setup.ts`, `frontend/src/test/fixtures/graphResponse.ts`, `frontend/src/App.test.tsx` - test infrastructure and first fixture
- `frontend/src/App.tsx` (rewritten) - product root composition, Vite starter fully removed

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser <-> FastAPI backend | All three consumed endpoints cross this boundary; backend is same-repo, CORS-locked to http://localhost:5173 |
| Browser sessionStorage <-> React state | Client-persisted watch-progress value re-read on every mount; not a security boundary, but an availability boundary (malformed value must not crash the app) |
| npm registry <-> frontend/package.json | New dependencies installed this task; three flagged SUS by the legitimacy gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | npm install (vitest, @testing-library/jest-dom, jsdom) | high | mitigate | Task 1 blocking human-verify checkpoint confirms each package's npm page/GitHub org/download count before Task 2 installs; SLOP-verdict `@types/cytoscape-cose-bilkent` and `msw` are never installed |
| T-02-02 | Denial of Service | frontend/src/hooks/useWatchProgress.ts sessionStorage read on mount | medium | mitigate | JSON.parse wrapped in try/catch; shape-validated (seriesId non-empty string, visibleUntilOrder positive integer) before trusting; any failure falls back to D-01 empty state instead of throwing |
| T-02-03 | Information Disclosure | frontend/src/components/graph/graphElements.ts, GraphCanvas.tsx | high | mitigate | Never re-filter nodes/edges by `visible_from_order` client-side; the mapping function passes through exactly what `GraphResponse` already contains - the backend (Phase 1, verified) remains the sole visibility authority |
| T-02-04 | Tampering | frontend/src/components/detail/DetailPanel.tsx rendering node/edge label text | low | accept | React JSX text interpolation auto-escapes; no `dangerouslySetInnerHTML` is used anywhere in this task's components (full evidence/claim text rendering arrives in Plan 03, which repeats this same control) |
| T-02-05 | Spoofing | CORS origin allowlist (backend/app/main.py hardcodes http://localhost:5173) | low | accept | No backend change in this phase (D-08); if Vite's dev server ever starts on a different port, this manifests as a CORS error, not an app bug - documented in 02-RESEARCH.md Pitfall 4 |

</threat_model>

<verification>
- `cd frontend && npm run test -- --run` passes (App.test.tsx is the only suite so far)
- `cd frontend && npm run build` and `npm run lint` both exit 0
- Manual: `npm run dev` (frontend) + running backend at :8000 - selecting Dexter then S01E01 shows a populated Cytoscape canvas with no prior confirmation-modal flash; changing to S01E02 opens the modal before any new network request (verify via browser devtools Network tab)
</verification>

<success_criteria>
UI-01 is satisfied: the Vite starter is fully replaced and the app loads series/episode/graph data from the backend. The mechanical core of UI-02 is satisfied: watch-progress changes (forward and backward) are gated behind a confirmation modal, and sessionStorage restores state without re-prompting. Visual polish (UI-03's cose-bilkent layout/stylesheet) and the detail-panel tab/branch split (UI-04) are intentionally not yet complete - they are Plan 02 and Plan 03.
</success_criteria>

<output>
Create `.planning/phases/02-polished-cytoscape-graph-experience/02-01-SUMMARY.md` when done
</output>

