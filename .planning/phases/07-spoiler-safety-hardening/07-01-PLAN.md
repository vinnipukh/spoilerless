---
phase: 07-spoiler-safety-hardening
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SPOILER-THREAT-MODEL.md
  - docs/SPOILER-DEFERRED-DESIGN.md
  - docs/SPOILER-TERMINOLOGY.md
  - .planning/phases/07-spoiler-safety-hardening/07-AUDIT.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - id: D-19
      statement: "docs/SPOILER-THREAT-MODEL.md exists and documents every direct leak class (future node, relationship, Claim, Evidence, Source text, chat message) and every indirect leak class named in D-19 (episode title, synopsis, runtime, image, poster, cast ordering, actor appearance count, character status, first/last appearance, search suggestion, autocomplete, hidden result count, node degree, path existence, graph layout, citation title, external-link label, chat-session title, ChangeSet summary, error message, timing-sensitive alternate response, cache key/stale cache), each row carrying enforcement layer, backend query/service, frontend behavior, test coverage, and fail-closed rule."
    - id: D-19
      statement: "docs/SPOILER-THREAT-MODEL.md ends with a regression matrix whose every row maps one leak class to a concrete test file/command and a pass/fail gate."
    - id: D-01
      statement: "The audit records the current stack as Neo4j Community + FastAPI + official driver + React/TypeScript/Vite + Cytoscape.js + Google auth + HttpOnly sessions and explicitly marks every source-plan stack item (Memgraph, GQLAlchemy, Next.js, JWT, Redis, second graph DB, frontend rewrite, unrestricted Cypher) as rejected."
    - id: D-04
      statement: "The audit/design section specifies the exact contract of the central visibility-policy service (validate_visibility_order, is_visible, effective_view_order, require_visible_resource, filter_public_metadata, mask_episode_metadata, assert_visibility_invariants) as the single owner of visible_from_order semantics, to be implemented in 07-02."
    - id: D-02
      statement: "docs/SPOILER-TERMINOLOGY.md locks visible_from_order as the single canonical reveal-point property name and lists the rejected competing names."
    - id: D-03
      statement: "docs/SPOILER-TERMINOLOGY.md states the fail-closed visibility rule (resource visible iff visible_from_order IS NOT NULL and <= effective_view_order; missing visibility fails closed) and forbids any coalesce-default on visible_from_order for story-sensitive data."
    - id: D-05
      statement: "docs/SPOILER-TERMINOLOGY.md defines watched_through_order, view_as_of_order, effective_view_order = min(view_as_of_order, watched_through_order) and the invariant 1 <= view_as_of_order <= watched_through_order, matching the D-05 progress model."
    - id: D-17
      statement: "docs/SPOILER-DEFERRED-DESIGN.md documents the future Person/ACTED_AS/APPEARS_IN model with the episodes_seen_so_far invariant and states it is not built this phase."
    - id: D-18
      statement: "docs/SPOILER-DEFERRED-DESIGN.md documents future invariants for reviews (spoiler_up_to_order), ratings (watched-only), trivia (visible_from_order), and recommendations (no future-cast/plot/title leaks) without any placeholder tables or UI."
    - id: D-24
      statement: "The audit is organized to feed the D-24 implementation order (audit/terminology/design first, then migration, metadata, relationships, search, media, chat, regression)."
  artifacts:
    - docs/SPOILER-THREAT-MODEL.md
    - docs/SPOILER-TERMINOLOGY.md
    - docs/SPOILER-DEFERRED-DESIGN.md
    - .planning/phases/07-spoiler-safety-hardening/07-AUDIT.md
  key_links:
    - "07-AUDIT.md leak inventory -> docs/SPOILER-THREAT-MODEL.md rows -> regression matrix rows -> concrete test files (backend/tests/test_progress_api.py, test_graph_api.py, test_retrieval_tools.py, test_chat_api.py, test_change_set_api.py)"
    - "docs/SPOILER-TERMINOLOGY.md -> backend/app/spoiler/policy.py contract (implemented 07-02)"
---

<objective>
Audit every leak channel in the existing repo, lock the visibility terminology, produce the
D-19/DOCS-01 threat-model document with a regression matrix, document deferred-feature future
invariants (D-17/D-18/DOCS-02), and specify — not implement — the central visibility-policy
service contract that 07-02 implements.

Purpose: every later plan in this phase (07-02..07-08) executes against a locked vocabulary
(watched_through_order / view_as_of_order / effective_view_order / visible_from_order), a
documented leak inventory, and a single agreed policy-service contract, so no later plan
re-derives semantics. D-19/DOCS-01 and D-17/D-18/DOCS-02 are fully delivered here; D-04's
contract is defined here and implemented in 07-02.
Output: docs/SPOILER-THREAT-MODEL.md, docs/SPOILER-TERMINOLOGY.md,
docs/SPOILER-DEFERRED-DESIGN.md, .planning/phases/07-spoiler-safety-hardening/07-AUDIT.md.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md
@backend/app/spoiler/filter.py
@backend/app/retrieval/tools.py
@backend/app/api/series.py
@backend/app/api/graph.py
@backend/app/api/progress.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repository leak-channel audit — inventory every direct and indirect leak surface</name>
  <read_first>
    backend/app/spoiler/filter.py, backend/app/retrieval/tools.py, backend/app/api/series.py,
    backend/app/api/graph.py, backend/app/api/progress.py, backend/app/api/user_content.py,
    backend/app/api/chat.py, backend/app/api/change_set.py, backend/app/domain/graph.py,
    backend/app/domain/series.py, frontend/src/hooks/useWatchProgress.ts,
    frontend/src/components/episode/EpisodeSelector.tsx
  </read_first>
  <files>backend/app/spoiler/filter.py, backend/app/retrieval/tools.py, backend/app/api/series.py, backend/app/api/graph.py, backend/app/api/progress.py, backend/app/api/user_content.py, backend/app/api/chat.py, backend/app/api/change_set.py, backend/app/domain/graph.py, backend/app/domain/series.py, frontend/src/hooks/useWatchProgress.ts, frontend/src/components/episode/EpisodeSelector.tsx</files>
  <action>
    Produce `.planning/phases/07-spoiler-safety-hardening/07-AUDIT.md` (new file): a table with
    one row per leak channel. Columns: channel, current behavior (with real symbol names),
    enforcement layer today, gap vs the D-03 fail-closed rule, owning plan (07-02..07-08).
    Cover at minimum the D-19 direct classes (future node, relationship, Claim, Evidence,
    Source text, chat message) and indirect classes (episode title, synopsis, runtime, image,
    character status, appearance count, search suggestion, autocomplete, hidden result count,
    node degree, path existence, graph layout, citation title, external-link label, chat-session
    title, ChangeSet summary, error message, timing-sensitive alternate response, cache key).
    For each API surface, record the real route, query constant (e.g. SERIES_EPISODES_QUERY,
    NODES_QUERY, VISIBLE_CLAIMS_QUERY, EVIDENCE_QUERY, SOURCES_QUERY, SEARCH_ENTITIES_QUERY,
    GRAPH_SUMMARY_COUNTS_QUERY, USER_NOTES_QUERY), and response field. Confirm the current
    `visible_until_order` boundary plumbing (api/progress.py, api/graph.py, services/graph.py
    resolve_boundary, services/chat.py _resolve_or_create_progress, api/user_content.py
    Boundary query params) and record which surfaces are boundary-aware today and which are not
    (the episodes list route GET /api/series/{series_id}/episodes is expected to be a gap).
    Record the D-01 stack verdict explicitly: current stack kept, source-plan stack items
    rejected. Use `grep -rn "visible_until_order" backend/app` and
    `grep -rn "visible_from_order" backend/app` from the repo root as evidence; paste the
    per-file counts into the audit's evidence section.
  </action>
  <verify>
    <automated>grep -c "owning plan" .planning/phases/07-spoiler-safety-hardening/07-AUDIT.md; grep -c "gap" .planning/phases/07-spoiler-safety-hardening/07-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-spoiler-safety-hardening/07-AUDIT.md` exists and its table has rows for every direct and indirect leak class listed in D-19.
    - Every row names a real symbol: route path or query constant or response field that exists in `backend/app` at HEAD (verified by grep before writing).
    - The audit names the episodes-list route as a boundary-unaware surface if SERIES_EPISODES_QUERY lacks a boundary parameter (verified by reading backend/app/spoiler/filter.py).
    - The audit's stack section records the D-01 keep/reject verdict verbatim by concept: keep Neo4j/FastAPI/driver/React/Vite/Cytoscape/Google/HttpOnly; reject Memgraph/GQLAlchemy/Next.js/JWT/Redis/second graph DB/frontend rewrite/unrestricted Cypher.
    - `grep -c "owning plan" <audit>` and `grep -c "gap" <audit>` both return a nonzero count.
  </acceptance_criteria>
  <done>The full leak surface is inventoried with real symbols, gap ownership assigned to plans 07-02..07-08, and the D-01 stack verdict recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Threat-model document + regression matrix + terminology lock (DOCS-01)</name>
  <read_first>
    .planning/phases/07-spoiler-safety-hardening/07-AUDIT.md,
    .planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/07-spoiler-safety-hardening/07-AUDIT.md, .planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md, .planning/REQUIREMENTS.md</files>
  <action>
    Create `docs/SPOILER-THREAT-MODEL.md` (new): a security document in the D-19 shape. One
    section per leak class, each with the required rows: enforcement layer (which service or
    repository or frontend component), backend query/service (real symbol), frontend behavior,
    test coverage (real test file), and fail-closed rule (what happens when the guard is
    missing). Include the regression matrix as a final table: leak class -> enforcement ->
    test file/command -> pass gate. The matrix must reference the canonical backend invocation
    `unset PYTHONPATH && source .venv/Scripts/activate && pytest backend/tests/<file> -k ...`
    and the frontend invocation `cd frontend && NODE_ENV=test CI=1 npx vitest run <pattern>`.
    Include the D-25 statement that completion is never claimed while any public API response
    still contains a future entity name, relationship, Claim, Evidence, Source label, citation,
    episode synopsis, spoiler-sensitive title, media URL, count, path, chat message, or
    ChangeSet detail. Create `docs/SPOILER-TERMINOLOGY.md` (new) locking: visible_from_order as
    the single canonical reveal-point property (D-02), the fail-closed rule with no
    coalesce-default on visible_from_order for story-sensitive data (D-03), the
    watched_through_order / view_as_of_order / effective_view_order definitions and the
    invariant 1 <= view_as_of_order <= watched_through_order with effective_view_order =
    min(view_as_of_order, watched_through_order) (D-05), and publication-order authority with
    the note that episode-code strings and season-number strings are never compared (D-09).
    Do not implement any code in this task; contracts are prose and signatures only.
  </action>
  <verify>
    <automated>grep -c "regression matrix" docs/SPOILER-THREAT-MODEL.md; grep -c "effective_view_order" docs/SPOILER-TERMINOLOGY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/SPOILER-THREAT-MODEL.md` contains a "Regression Matrix" heading and a table whose rows reference real backend/tests/ files.
    - Every D-19 indirect leak class appears as a heading or table row (grep -c on a representative sample: title, synopsis, autocomplete, degree, citation).
    - `docs/SPOILER-TERMINOLOGY.md` defines watched_through_order, view_as_of_order, and effective_view_order with the min() rule and the 1 <= view_as_of_order <= watched_through_order invariant.
    - `docs/SPOILER-TERMINOLOGY.md` states the D-03 fail-closed rule and explicitly forbids any coalesce-default of visible_from_order for story-sensitive data (grep -c "fails closed" >= 1).
    - `docs/SPOILER-TERMINOLOGY.md` names visible_from_order as the single canonical reveal-point property (grep -c "visible_from_order" >= 3).
    - `git diff --check` reports no whitespace errors on the three new docs.
  </acceptance_criteria>
  <done>DOCS-01 is delivered: a D-19-shaped threat model with a regression matrix, plus the locked terminology doc that later plans reference verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Deferred-feature invariants doc + central policy-service contract design (DOCS-02, D-04)</name>
  <read_first>
    .planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md,
    .planning/phases/07-spoiler-safety-hardening/07-AUDIT.md,
    backend/app/domain/user_content.py,
    backend/app/spoiler/filter.py
  </read_first>
  <files>.planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md, .planning/phases/07-spoiler-safety-hardening/07-AUDIT.md, backend/app/domain/user_content.py, backend/app/spoiler/filter.py</files>
  <action>
    Create `docs/SPOILER-DEFERRED-DESIGN.md` (new) documenting each deferred feature with future
    invariants and no placeholder tables or UI, per D-17 and D-18: Person/ACTED_AS/APPEARS_IN
    model (appearances count only visible episodes via an episodes_seen_so_far-style field,
    never total planned count, never last appearance), reviews (spoiler_up_to_order, hidden
    above the reader's effective boundary), ratings (watched episodes only, no future quality
    signals), trivia (visible_from_order), recommendations (no future cast/plot/title/
    relationship leaks), awards, external wiki integration. Then append a section
    "Central visibility-policy service contract" to `docs/SPOILER-TERMINOLOGY.md` specifying
    the exact D-04 surface for the module `backend/app/spoiler/policy.py` that 07-02 will
    implement: validate_visibility_order(order) raising on order < 1 or non-persisted order;
    is_visible(record, effective_view_order) failing closed on a null visible_from_order;
    effective_view_order(view_as_of_order, watched_through_order) returning the min with the
    invariant enforced; require_visible_resource(record, effective_view_order) raising a
    resource-hidden error; filter_public_metadata(record, effective_view_order) dropping
    spoiler-sensitive fields; mask_episode_metadata(episode, effective_view_order) producing
    the D-21 display shape (id, code, display_title, is_unlocked, is_current_view);
    assert_visibility_invariants(record). State that no competing reveal-point names are
    introduced anywhere in backend/app or frontend/src (D-02) and that the module follows the
    existing package layout (backend/app/spoiler/) with no new framework (D-01).
  </action>
  <verify>
    <automated>grep -c "episodes_seen_so_far" docs/SPOILER-DEFERRED-DESIGN.md; grep -c "def effective_view_order" docs/SPOILER-TERMINOLOGY.md; grep -c "mask_episode_metadata" docs/SPOILER-TERMINOLOGY.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/SPOILER-DEFERRED-DESIGN.md` has one section per deferred feature (Person model, reviews, ratings, trivia, recommendations) with future invariants and no tables/UI stubs.
    - `docs/SPOILER-DEFERRED-DESIGN.md` contains the episodes_seen_so_far invariant and the "never last appearance" rule (grep -c "episodes_seen_so_far" >= 1, grep -c "last appearance" >= 1).
    - `docs/SPOILER-TERMINOLOGY.md` gains a "Central visibility-policy service contract" section naming all six D-04 functions (grep -c each of validate_visibility_order, is_visible, effective_view_order, require_visible_resource, filter_public_metadata, mask_episode_metadata, assert_visibility_invariants >= 1).
    - `git diff --check` reports no whitespace errors.
  </acceptance_criteria>
  <done>DOCS-02 is delivered and the D-04 policy-service contract is fully specified for 07-02 to implement without reinterpretation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> implementation | Documentation claims (enforcement layer, fail-closed rule) become contract; later plans execute against them |
| API response -> client | Any future-episode/story data rendered or returned is a leak; masking must be backend-side per D-08 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | docs/SPOILER-THREAT-MODEL.md | high | mitigate | Every leak class documented with enforcement layer + regression matrix row so later plans close it (D-19) |
| T-07-02 | Information Disclosure | backend/app/spoiler/filter.py SERIES_EPISODES_QUERY | high | mitigate | Audit flags boundary-unaware surfaces; 07-03 adds backend masking (D-08) |
| T-07-03 | Tampering | npm/pip installs | high | mitigate | No new packages this phase; any future install requires slopcheck + blocking human checkpoint |
</threat_model>

<verification>
`grep -c "regression matrix" docs/SPOILER-THREAT-MODEL.md` and `grep -c "Central visibility-policy service contract" docs/SPOILER-TERMINOLOGY.md` both >= 1; `git diff --check` clean; `ls docs/SPOILER-THREAT-MODEL.md docs/SPOILER-TERMINOLOGY.md docs/SPOILER-DEFERRED-DESIGN.md .planning/phases/07-spoiler-safety-hardening/07-AUDIT.md` lists all four files.
</verification>

<success_criteria>
All leak classes are inventoried with real symbols and gap ownership; the threat model + regression matrix (DOCS-01) and deferred-feature invariants (DOCS-02) exist; the terminology doc locks visible_from_order, the fail-closed rule, and the watched/view/effective progress model; the D-04 policy-service contract is specified for 07-02.
</success_criteria>

<output>
Create `.planning/phases/07-spoiler-safety-hardening/07-01-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces (this plan's contribution)

New files: `docs/SPOILER-THREAT-MODEL.md`, `docs/SPOILER-TERMINOLOGY.md`,
`docs/SPOILER-DEFERRED-DESIGN.md`,
`.planning/phases/07-spoiler-safety-hardening/07-AUDIT.md`. No code symbols (the
`backend/app/spoiler/policy.py` contract is specified here, implemented in 07-02).
