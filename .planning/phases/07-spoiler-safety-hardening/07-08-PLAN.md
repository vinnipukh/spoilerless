---
phase: 07-spoiler-safety-hardening
plan: 8
type: execute
wave: 6
depends_on: ["07-07"]
files_modified:
  - docs/SPOILER-THREAT-MODEL.md
  - docs/API.md
  - docs/ARCHITECTURE.md
  - .planning/phases/07-spoiler-safety-hardening/07-08-ACCEPTANCE.md
autonomous: false
requirements: [DOCS-01, DOCS-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - id: D-25
      statement: "The full backend pytest suite runs from the repo root with the canonical invocation and matches the documented baseline failure/error NAME set exactly (5 failed / 7 errors: test_seed_idempotency.py x3, test_extraction_models.py::TestSchemaArtifact x2, test_candidate_ingest.py x4 errors, test_candidate_review.py x3 errors) with zero new failures; the contract files (test_openapi_contract.py, test_frontend_contract_doc.py) are green."
    - id: D-25
      statement: "Frontend verification passes: full vitest suite green, `tsc --noEmit` clean, production build succeeds, `git diff --check` clean, and `npm run lint` reports 0 NEW errors versus the 28 pre-existing baseline."
    - id: D-23
      statement: "Manual browser acceptance is executed via the dev-login flow (POST /api/auth/dev gated by AUTH_DEV_CODE — no Google needed): watch 3 -> view 1 shows Episode-1-safe graph/chat, selector states render watched/viewed/locked, unlock confirmation copy shows, stale ChangeSet confirm fails closed, media fallback renders, search suggests nothing hidden."
    - id: D-25
      statement: "Completion is claimed only after a public-API sweep proves no response contains a future entity name, relationship, Claim, Evidence, Source label, citation, episode synopsis, spoiler-sensitive title, media URL, count, path, chat message, or ChangeSet detail."
    - id: D-24
      statement: "The regression run exercises every plan 07-01..07-07 deliverable in order, confirming the phase shipped as a coherent whole with no unrelated visual changes mixed in."
  artifacts:
    - .planning/phases/07-spoiler-safety-hardening/07-08-ACCEPTANCE.md
  key_links:
    - "docs/SPOILER-THREAT-MODEL.md regression matrix -> executed test commands -> 07-08-ACCEPTANCE.md results"
    - "AUTH_DEV_CODE dev-login -> manual acceptance flows (browser) -> recorded results"
---

<objective>
Close the phase with the full verification gate (D-25), the D-23 remainder (frontend tests +
regression rows), a manual browser acceptance run via the dev-login flow, and final
documentation updates (regression matrix finalization in docs/SPOILER-THREAT-MODEL.md, API/
ARCHITECTURE doc drift fixes if found). This plan is the release gate: nothing is complete
while any public API response still contains a future story element.

Purpose: plans 07-01..07-07 delivered the hardening; 07-08 proves it end-to-end against the
shared live stack and produces the acceptance record the milestone gate reads.
Output: .planning/phases/07-spoiler-safety-hardening/07-08-ACCEPTANCE.md with the executed
regression results, the manual acceptance checklist results, and the finalized docs.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md
@docs/SPOILER-THREAT-MODEL.md
@docs/API.md
@docs/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full backend regression — baseline name-set match + spoiler suites (D-25, D-23)</name>
  <read_first>
    backend/tests/conftest.py, backend/app/graph/setup.py,
    .planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md
  </read_first>
  <files>backend/tests/conftest.py, backend/app/graph/setup.py, .planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md</files>
  <action>
    From the repo root, run the full backend suite with the canonical invocation
    `unset PYTHONPATH && source .venv/Scripts/activate && pytest backend/tests -q` and record
    the FAILED/ERROR name list. Accept the run only if the failure/error NAME SET is exactly
    the documented baseline (test_seed_idempotency.py x3, test_extraction_models.py::
    TestSchemaArtifact x2, test_candidate_ingest.py x4 errors, test_candidate_review.py x3
    errors) — any other failure name is a phase regression and must be root-caused and fixed
    before proceeding (do not fix the pre-existing baseline names; they are out of scope). Then
    run the spoiler-relevant suites explicitly:
    `unset PYTHONPATH && source .venv/Scripts/activate && pytest backend/tests/test_spoiler_policy.py backend/tests/test_progress_api.py backend/tests/test_episode_ordering.py backend/tests/test_episode_masking.py backend/tests/test_graph_api.py backend/tests/test_retrieval_tools.py backend/tests/test_retrieval_pipeline.py backend/tests/test_user_content_api.py backend/tests/test_chat_api.py backend/tests/test_chat_persistence.py backend/tests/test_citations.py backend/tests/test_prompt_injection.py backend/tests/test_conversational_tone.py backend/tests/test_change_set_api.py backend/tests/test_change_set_confirmation.py backend/tests/test_change_set_protection.py backend/tests/test_change_set_revision.py backend/tests/test_openapi_contract.py backend/tests/test_frontend_contract_doc.py -q` and record pass counts. Live-DB hygiene: if any interrupted run left pollution, reseed via `unset PYTHONPATH && source .venv/Scripts/activate && uv run --project backend python -m backend.app.graph.setup` before re-running; tests writing :AppSetting-style global nodes must backup->restore (none should — 07-01..07-07 added none); verify no test-created users/sessions/progress rows remain via a fresh-driver probe, cleaning up any stragglers under test user ids. Record every command and its result in 07-08-ACCEPTANCE.md.
  </action>
  <verify>
    <automated>unset PYTHONPATH && source .venv/Scripts/activate && pytest backend/tests -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The full-suite FAILED/ERROR name set matches the documented baseline exactly (5 failed / 7 errors, same names); passed count grows by the new phase tests.
    - All spoiler-relevant suites pass with 0 failures (recorded per-file).
    - test_openapi_contract.py and test_frontend_contract_doc.py are green (contract inventory unchanged: 33 path templates / 45 ops).
    - A post-run probe shows no leftover test users/sessions/progress rows on the shared live DB.
    - Every command + result is recorded in `.planning/phases/07-spoiler-safety-hardening/07-08-ACCEPTANCE.md`.
  </acceptance_criteria>
  <done>Backend regression proven: zero new failures, exact baseline name set, contract files green, live DB clean.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend regression — vitest, typecheck, lint gate, production build (D-25)</name>
  <read_first>
    frontend/package.json, frontend/src/test/setup.ts
  </read_first>
  <files>frontend/package.json, frontend/src/test/setup.ts</files>
  <action>
    From `frontend/`, run in order: (1) `NODE_ENV=test CI=1 npx vitest run` — the full suite
    must pass (guard against the NODE_ENV production trap: confirm `echo $NODE_ENV` is not
    production before running); (2) `npx tsc --noEmit` — zero type errors; (3)
    `npm run lint` — establish the baseline first (`git stash push -- frontend/src && npm run
    lint && git stash pop` records the 28 pre-existing errors), then gate on 0 NEW errors vs
    that baseline on files touched by plans 07-03/07-06/07-07; (4) `npm run build` —
    production build succeeds; (5) `git diff --check` — clean. Record every command and result
    in 07-08-ACCEPTANCE.md. Do not fix the 28 pre-existing lint errors (deferred debt per
    CONTEXT.md deferred items). Never commit secrets; never modify real environment files
    (D-25).
  </action>
  <verify>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `NODE_ENV=test CI=1 npx vitest run` passes with 0 failures (full suite).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` reports exactly the 28 pre-existing errors (0 NEW).
    - `npm run build` completes successfully.
    - `git diff --check` reports no whitespace errors.
    - All five results are recorded in 07-08-ACCEPTANCE.md.
  </acceptance_criteria>
  <done>Frontend regression proven: tests, types, lint gate, and production build all green.</done>
</task>

<task type="auto">
  <name>Task 3: Manual browser acceptance via dev-login + final documentation (D-25, D-23 remainder)</name>
  <read_first>
    .planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md,
    docs/SPOILER-THREAT-MODEL.md, docs/API.md, docs/ARCHITECTURE.md
  </read_first>
  <files>.planning/phases/07-spoiler-safety-hardening/07-CONTEXT.md, docs/SPOILER-THREAT-MODEL.md, docs/API.md, docs/ARCHITECTURE.md</files>
  <action>
    With the backend running (verify `curl http://127.0.0.1:8000/api/health` first; if the
    user's own uvicorn is already serving, use it — do not start a second instance), execute
    the manual acceptance checklist through the browser using the dev-login bypass: sign in
    with `POST /api/auth/dev` body {"code": "<AUTH_DEV_CODE>"} (read the code from the root
    .env via `grep '^AUTH_DEV_CODE' .env`; endpoint disabled -> 403 when unset). Walk: (a)
    unlock through 3, then select Episode 1 — the graph shows only Episode-1-safe content,
    watched progress stays 3, selector marks Episode 1 as currently viewed without
    re-confirmation (PROG-01); (b) episode selector shows masked generic titles for
    locked/future episodes and the three states (watched/viewed/locked) (D-22); (c) the
    confirmation dialog for unlocking above watched shows "Episodes 1 through N will be
    considered watched" (PROG-02); (d) chat at view 1 answers Episode-1-safe with Episode-3
    messages hidden, restored on return (CHAT-02); (e) a stale later-boundary ChangeSet
    confirm fails closed with the clear error (EDIT-02); (f) character image area shows the
    initials placeholder without revealing future characters (MEDIA-01/02); (g) search/
    autocomplete suggests nothing hidden (SEARCH-01). Record each step's observed result in
    07-08-ACCEPTANCE.md. Then finalize docs: update docs/SPOILER-THREAT-MODEL.md's regression
    matrix with the executed results; check docs/API.md and docs/ARCHITECTURE.md for drift
    (prose counts vs `app.openapi()` ground truth — 33 path templates / 45 ops) and fix only
    inaccurate sections, preserving accurate user-authored prose. All reports in English
    (D-25). Do not commit anything.
  </action>
  <verify>
    <automated>curl -s http://127.0.0.1:8000/api/health; grep -c "PASS" .planning/phases/07-spoiler-safety-hardening/07-08-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-spoiler-safety-hardening/07-08-ACCEPTANCE.md` exists with one PASS/FAIL row per checklist step (a)-(g) and every step PASS.
    - The regression matrix in docs/SPOILER-THREAT-MODEL.md reflects the executed results.
    - docs/API.md / docs/ARCHITECTURE.md route-inventory prose matches `app.openapi()` ground truth (33 path templates / 45 ops) or is explicitly marked for follow-up — no silent stale counts.
    - `grep -c "PASS" 07-08-ACCEPTANCE.md` >= 7 (all checklist steps recorded).
    - Nothing is committed (git status shows the plan/summary/docs files uncommitted for the orchestrator).
  </acceptance_criteria>
  <done>The manual acceptance checklist passes end-to-end on the live stack; the regression matrix and docs are finalized; the phase gate record exists.</done>
</task>

</tasks>

### UI/UX Constraints

This plan makes NO frontend code changes (full regression + manual browser
acceptance + documentation only), so no UI/UX rule modifications apply here.
The manual acceptance checklist in this plan RE-VERIFIES the D-22 UI behavior
shipped by 07-03 (three-state episode distinction, generic masked labels,
keyboard accessibility, unlock-confirmation copy, no layout shift) — any
violation found during browser acceptance is reported as a regression against
07-03 and is NOT fixed in this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification claims -> milestone gate | A run that merely matches counts (not NAME SETS) can mask regressions; name-set comparison is mandatory (D-25) |
| live DB <-> regression runs | Interrupted suites pollute the shared Neo4j; reseed + teardown discipline required |
| dev-login -> app | AUTH_DEV_CODE must never leak into commits or real environment files (D-25) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08-01 | Repudiation | regression recording | high | mitigate | Every command + result recorded in 07-08-ACCEPTANCE.md; name-set comparison not count-only (D-25) |
| T-07-08-02 | Information Disclosure | dev-login acceptance | medium | mitigate | AUTH_DEV_CODE read via shell grep only; never committed; never written into reports (D-25) |
| T-07-08-03 | Denial of Service | shared live DB | medium | mitigate | Reseed after interrupted runs; teardown created users/sessions/progress (runbook hygiene) |
| T-07-08-SC | Tampering | npm/pip installs | high | mitigate | No new packages this phase |
</threat_model>

<verification>
Full backend suite name-set match; spoiler suites green; frontend vitest/tsc/lint/build green; `git diff --check` clean; 07-08-ACCEPTANCE.md has >= 7 PASS rows; docs regression matrix finalized; nothing committed.
</verification>

<success_criteria>
The phase gate passes: zero new backend failures with exact baseline name-set match, frontend fully green, manual acceptance PASS on every checklist step via dev-login, regression matrix and docs finalized, and the D-25 public-API sweep confirms no future story element remains in any response.
</success_criteria>

<output>
Create `.planning/phases/07-spoiler-safety-hardening/07-08-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces (this plan's contribution)

`.planning/phases/07-spoiler-safety-hardening/07-08-ACCEPTANCE.md` (executed regression
results + manual acceptance checklist); finalized `docs/SPOILER-THREAT-MODEL.md` regression
matrix; drift-fixed `docs/API.md` / `docs/ARCHITECTURE.md` sections (prose counts only, when
inaccurate). No production code changes.
