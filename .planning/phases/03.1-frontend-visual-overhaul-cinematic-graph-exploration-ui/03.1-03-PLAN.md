---
phase: 03.1
plan: 3
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/episode/EpisodeSelector.tsx
  - frontend/src/components/episode/SeriesSelect.tsx
autonomous: true
requirements: [D-13, D-16, D-17, D-33, D-34]
user_setup: []
must_haves:
  truths:
    - "AppShell.tsx's exact prop contract (user, onLogout, topBar, children) and the z-[60] header-above-Sheet layering are unchanged after restyling"
    - "A Space Grotesk-styled app title/wordmark is visible in the top bar per D-13's Display typography role"
    - "EpisodeSelector presents a segmented/pill control at desktop/medium widths while its onSelect(episodeOrder)/value/disabled prop contract and keyboard navigation are unchanged"
    - "App.test.tsx's existing 'combobox'/'option'-role assertions for the 'Watch progress' control continue to pass unmodified, because the existing shadcn Select is still rendered (responsively hidden, not removed) as the narrow-viewport fallback per D-33"
    - "SeriesSelect keeps its exact shadcn Select structure and 'Series' aria-label, restyled only"
  artifacts:
    - "frontend/src/components/layout/AppShell.tsx with a restyled top bar/avatar block and unchanged prop contract"
    - "frontend/src/components/episode/EpisodeSelector.tsx exposing both a new segmented control and the preserved Select fallback, both wired to the same onSelect contract"
  key_links:
    - "EpisodeSelector.tsx's onSelect(episodeOrder) -> App.tsx's handleEpisodeSelect (unchanged call site, this plan does not touch App.tsx)"
    - "AppShell.tsx's --elevated hover treatment -> the --elevated/--color-elevated token pair added by plan 03.1-01 Task 1"
---

<objective>
Restyle AppShell's top bar/avatar block with the new Space Grotesk typography and --elevated hover token, and replace EpisodeSelector's bare shadcn Select with a segmented/pill control at desktop/medium widths per D-16/D-33 — while keeping the existing Select rendered (responsively hidden) as the documented narrow-viewport fallback so App.test.tsx's episode-selection assertions require no change.

Purpose: D-16's "clean episode controls" and the "polished application shell" are two of this phase's four named deliverables; D-33 explicitly requires reusing the existing Select for narrow viewports rather than inventing a new component.
Output: a restyled AppShell.tsx, a dual-rendered (segmented + Select-fallback) EpisodeSelector.tsx, and a lightly polished SeriesSelect.tsx — zero behavior-contract changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-CONTEXT.md
@.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-RESEARCH.md
@.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-UI-SPEC.md
@frontend/src/components/layout/AppShell.tsx
@frontend/src/components/episode/EpisodeSelector.tsx
@frontend/src/components/episode/SeriesSelect.tsx
@frontend/src/App.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppShell.tsx restyle — Space Grotesk wordmark, elevated-token avatar/logout hover, unchanged prop contract</name>
  <files>frontend/src/components/layout/AppShell.tsx</files>
  <read_first>frontend/src/components/layout/AppShell.tsx</read_first>
  <precondition>frontend/src/index.css already defines the --elevated CSS custom property and its --color-elevated @theme inline mapping (added by plan 03.1-01, Task 1) before this task's hover:bg-elevated Tailwind usage can resolve to a real color.</precondition>
  <action>
    In AppShell.tsx's top-bar Card, add a small app title/wordmark element (e.g. a `<span className="font-heading text-2xl">HD Graf Cehennemi</span>`) rendered before `{topBar}` inside the `flex min-w-0 items-center gap-4` container — this satisfies D-13/03.1-UI-SPEC.md's Display-role typography usage ("top bar series/episode title only") via a static app wordmark rather than widening AppShell's prop contract with new series/episode data (Claude's-discretion resolution, documented here: adding new props would touch App.tsx and violate D-16's "preserve the existing behavior contract exactly" instruction, so a self-contained static title is the safer choice). Restyle the avatar/logout block (the `flex items-center gap-3 shrink-0` div) using `hover:bg-elevated` (or an equivalent Tailwind class resolving to the new `--elevated` token) on the Logout `Button`'s wrapper or a small padded row around the avatar+name+logout group, without changing `onLogout`, `user.display_name`, or `avatarUrl(user)` logic — every conditional (`user &&`, `avatarUrl(user) ? ... : ...`, `onLogout &&`) must remain byte-identical in structure. Preserve the existing z-[60] code comment and the `relative z-[60] rounded-none` classes on the `Card` exactly as-is (D-16/D-17) — do not renumber or remove this z-index layering.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test -- --run src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/App.test.tsx` passes all 5 existing tests unmodified (this task does not touch any text/role AppShell exposes that App.test.tsx queries: "Test User", the Logout button, or the avatar image)
    - `grep -c "z-\[60\]" frontend/src/components/layout/AppShell.tsx` returns at least 1 (layering comment/class preserved)
    - `grep -c "font-heading" frontend/src/components/layout/AppShell.tsx` returns at least 1 (wordmark added)
    - `npm run build` succeeds with zero TypeScript errors
  </acceptance_criteria>
  <done>AppShell shows a Space Grotesk wordmark and an --elevated-token hover treatment on its avatar/logout block; its exact prop contract, z-index layering, and onLogout/avatar logic are unchanged; App.test.tsx passes unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: EpisodeSelector segmented control (with preserved Select fallback) + SeriesSelect visual polish</name>
  <files>frontend/src/components/episode/EpisodeSelector.tsx, frontend/src/components/episode/SeriesSelect.tsx</files>
  <read_first>frontend/src/components/episode/EpisodeSelector.tsx, frontend/src/components/episode/SeriesSelect.tsx, frontend/src/App.test.tsx</read_first>
  <precondition>The radix-ui package (already an installed dependency, version 1.6.7) is available for its ToggleGroup export; if ToggleGroup is not exported at the installed version, fall back to a custom row of the existing button.tsx variant system (Assumption A3 in 03.1-RESEARCH.md) rather than adding a new npm package.</precondition>
  <action>
    In EpisodeSelector.tsx, add a new segmented/pill control built from Radix `ToggleGroup` (imported from the already-installed `radix-ui` package) or, if that export is unavailable at the installed version, a small row of the existing `Button` component's variants — each segment represents one episode, showing `{episode.code}` at the medium breakpoint (640-1023px, per D-33's "codes only" truncation rule) and `{episode.code} — {episode.title}` at desktop (>=1024px). Wire this new control to the exact same `value`/`onSelect(episodeOrder: number)`/`disabled` props already defined on this component's `Props` type — do not change the exported `Props` shape. Wrap the new segmented control in a `hidden md:inline-flex` (or equivalent) responsive class so it only renders at medium+ widths, and give the ORIGINAL, UNCHANGED `<Select>` block (value/onValueChange/disabled, `SelectTrigger aria-label="Watch progress"`, `SelectItem` list) a `md:hidden` class so it renders only below the medium breakpoint — do NOT remove, rename, or restructure the original Select block; both controls remain simultaneously present in the DOM, toggled only by CSS display classes, so `frontend/src/App.test.tsx`'s existing `screen.findByRole('combobox', { name: 'Watch progress' })` / `screen.findByRole('option', { name: /S01E01/ })` assertions keep matching the untouched Select markup exactly as before (D-33 explicitly requires reusing this same Select for the narrow/fallback case — this dual-render approach satisfies that requirement while also making the segmented control the primary desktop/medium experience). Style the segmented control's active segment using `--accent` (`#7C3AED`) per the "active episode segment" entry in 03.1-UI-SPEC.md's Color section's accent reserved-for list, and give inactive/hover segments a `hover:bg-elevated` treatment (per the --elevated token added in plan 03.1-01). Preserve full keyboard navigation (Tab/Arrow-key support that Radix `ToggleGroup` provides natively, or, in the Button-row fallback, explicit `tabIndex`/`aria-pressed` handling) and the `disabled` prop's effect on the new control exactly mirroring its effect on the existing Select.
    In SeriesSelect.tsx, apply visual polish only (spacing/typography token classes consistent with the new design system) — do not change its `Select`/`SelectTrigger`/`SelectContent`/`SelectItem` structure, its `Props` type, or its `aria-label="Series"` convention.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test -- --run src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/App.test.tsx` passes all 5 existing tests unmodified, including the full "select -> confirm -> fetch -> render -> inspect" flow that clicks `combobox`/`option` roles for both "Series" and "Watch progress"
    - `grep -c "aria-label=\"Watch progress\"" frontend/src/components/episode/EpisodeSelector.tsx` returns at least 1 (original Select preserved verbatim)
    - `grep -c "onSelect" frontend/src/components/episode/EpisodeSelector.tsx` shows the same single `onSelect(episodeOrder: number)` prop signature as before (no widened `Props` type)
    - `npm run build` succeeds with zero TypeScript errors
  </acceptance_criteria>
  <done>A segmented/pill episode control is the primary desktop/medium experience; the original Select remains in the DOM as the CSS-hidden narrow-viewport fallback so App.test.tsx's combobox/option assertions pass unmodified; SeriesSelect is visually polished with no structural change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google-authenticated session (unchanged) -> AppShell avatar/logout visual restyle | No session/auth logic touched; only CSS/markup around the existing `useAuth`/`onLogout` call sites |
| Backend EpisodeResponse/SeriesResponse (unchanged shape) -> new segmented control markup | Already-typed, already-fetched data rendered into new pill/button markup — no new fetch, no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-06 | Spoofing / Tampering | AppShell avatar/logout restyle | low | accept | No behavior change — `onLogout`/`useAuth`/Google Sign-In session architecture is explicitly out of scope (D-02/D-17); this plan only changes surrounding classNames |
| T-03.1-07 | Tampering | EpisodeSelector's dual-rendered segmented + Select controls | low | mitigate | Both controls call the identical, already-validated `onSelect(episodeOrder)` -> `useWatchProgress`'s existing non-integer/negative/zero guards (verified in `useWatchProgress.test.ts`, untouched by this plan) — no new input-validation surface is introduced by adding a second control that funnels into the same validated contract |

No new form input surface, API call, or third-party script is introduced by this plan.
</threat_model>

<verification>
- `cd frontend && npm run test -- --run` — full existing suite green, in particular `App.test.tsx`'s combobox/option-based episode-selection flow
- `cd frontend && npm run build` — clean
- `cd frontend && npm run lint` — clean
- `git diff --stat -- backend/` — must be empty (D-01)
- `grep -rc "dangerouslySetInnerHTML" frontend/src | awk -F: '{s+=$2} END {print s+0}'` — must remain 0
</verification>

<success_criteria>
AppShell shows the new typography/hover treatment with an unchanged prop contract and z-index layering; EpisodeSelector's segmented control is the primary desktop/medium UI while the original Select survives as the CSS-hidden narrow fallback, keeping App.test.tsx's episode-selection assertions passing unmodified; SeriesSelect is visually polished only.
</success_criteria>

<output>
Create `.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-03-SUMMARY.md` when done
</output>

## Artifacts This Plan Produces

- No new files. Modifies `AppShell.tsx` (app wordmark, `--elevated` hover treatment), `EpisodeSelector.tsx` (new dual-rendered segmented control alongside the preserved Select), `SeriesSelect.tsx` (visual polish only).
- No new exported types, no new npm dependencies (Radix `ToggleGroup` is sourced from the already-installed `radix-ui` package; falls back to existing `Button` variants if unavailable), no new CSS custom properties (consumes `--elevated`/`--accent` added/already present from plan 03.1-01 and Phase 2).
