---
phase: 03.1
plan: 4
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/ui/collapsible.tsx
  - frontend/src/components/graph/GraphLegend.tsx
  - frontend/src/components/graph/GraphControls.tsx
  - frontend/src/components/graph/GraphCanvas.tsx
autonomous: true
requirements: [D-15, D-30, D-34, D-35]
user_setup: []
must_haves:
  truths:
    - "A collapsible legend (bottom-left, collapsed by default) shows Node Types and Relationships sections sourced from the same relationshipStyles.ts module the canvas stylesheet uses, so legend and canvas colors can never drift apart"
    - "Zoom-in/zoom-out/fit-to-view/reset controls (bottom-right, 44x44px hit areas, tooltip + aria-label per control) are always present regardless of graph size and never cover graph content"
    - "Reset re-runs the same runLayout(cy) function GraphCanvas already calls on [graph] change, not a second layout code path"
    - "The 3 zero-instance-today relationship groups (LOCATED_IN, provenance, revision) appear in the legend as a visually de-emphasized forward-compat sub-group, never hidden or silently omitted"
    - statement: "The expanded GraphLegend panel (8 node-type rows + 9 relationship rows) fits within a fixed max-height with internal scroll rather than page-level scroll, even though no explicit overflow test proves this yet"
      verification: backstop
    - statement: "GraphControls and GraphLegend render immediately on canvas mount with no loading state, since both are pure client-side chrome with no network dependency, though this is not yet proven by an explicit test"
      verification: backstop
    - "Planner assumption (UI-SPEC UI Considerations, unresolved row 'long-text'): legend row labels (node-type and relationship-group names) use single-line no-wrap by default; no truncation rule is implemented since the current longest name (PARTICIPATED_IN) fits comfortably — revisit only if a future, longer type/relationship name is added"
  artifacts:
    - "frontend/src/components/graph/GraphLegend.tsx (new) importing its color/shape data from relationshipStyles.ts"
    - "frontend/src/components/graph/GraphControls.tsx (new) wired to GraphCanvas.tsx's existing cy instance and runLayout function"
    - "frontend/src/components/ui/tooltip.tsx and frontend/src/components/ui/collapsible.tsx (new, official shadcn registry components)"
  key_links:
    - "GraphLegend.tsx's color/shape data -> relationshipStyles.ts's EDGE_TYPE_TO_FAMILY/FAMILY_HEX (plan 03.1-01), never a duplicated inline color list"
    - "GraphControls.tsx's zoom/fit/reset buttons -> GraphCanvas.tsx's existing cyInstanceRef and runLayout (plan 03.1-01 Task 1/3), no second cy-management path"
---

<objective>
Add a collapsible node/relationship legend and zoom-in/zoom-out/fit/reset controls as floating overlays on the graph canvas, sourcing all color/shape data from the single relationshipStyles.ts module and reusing the existing cy instance/runLayout function, per D-15/D-30 and 03.1-UI-SPEC.md's Legend and GraphControls sections.

Purpose: a legend and zoom controls are two of this phase's explicitly named UI Considerations deliverables; sourcing them from the same module the canvas stylesheet uses (rather than a second, independently-authored color list) prevents the exact drift RESEARCH.md's Pitfall 5 warns about.
Output: two new components (GraphLegend.tsx, GraphControls.tsx) and two new official shadcn primitives (tooltip, collapsible), wired into GraphCanvas.tsx's existing canvas wrapper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-CONTEXT.md
@.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-RESEARCH.md
@.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-UI-SPEC.md
@frontend/src/components/graph/GraphCanvas.tsx
@frontend/src/components/graph/relationshipStyles.ts
@frontend/src/components/detail/StructuralEdgeCard.tsx
@frontend/src/components/layout/AppShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn tooltip/collapsible primitives + build the collapsible GraphLegend overlay</name>
  <files>frontend/src/components/ui/tooltip.tsx, frontend/src/components/ui/collapsible.tsx, frontend/src/components/graph/GraphLegend.tsx, frontend/src/components/graph/GraphCanvas.tsx</files>
  <read_first>frontend/src/components/graph/GraphCanvas.tsx, frontend/src/components/graph/relationshipStyles.ts, frontend/src/components/detail/StructuralEdgeCard.tsx, frontend/components.json</read_first>
  <precondition>frontend/src/components/graph/relationshipStyles.ts and GraphCanvas.tsx's relative-positioned canvas wrapper div (the graph-canvas-backdrop container) already exist, added by plan 03.1-01 Task 1/Task 3, before this task adds an overlay child into that wrapper.</precondition>
  <action>
    Add the official shadcn `tooltip` and `collapsible` components (run `npx shadcn add tooltip collapsible` from inside `frontend/`, or the project's existing shadcn CLI invocation pattern) — both are official `@shadcn` registry blocks per 03.1-UI-SPEC.md's Registry Safety section (no vetting gate required, `components.json`'s `registries` field stays `{}`).
    Create `frontend/src/components/graph/GraphLegend.tsx` following `StructuralEdgeCard.tsx`'s small-self-contained-component shape: a single collapsible panel positioned bottom-left with a `16px` inset, containing two sections — "Node Types" (one row per real node type from the sizing/color table: shape glyph + color swatch + type name for Character/Event/Location/Organization/Episode/Series/UserNote/Object) and "Relationships" (one row per color family, sourced from `relationshipStyles.ts`'s `EDGE_TYPE_TO_FAMILY`/`FAMILY_HEX` exports — do not hardcode a second, independently-authored color list here per D-15/Pitfall 5). Render the 3 zero-instance-today groups (`LOCATED_IN`, the provenance group, the revision group) inside a visually de-emphasized "Forward-compatible" sub-section (muted opacity/smaller text) rather than omitting them. Default state is collapsed (Claude's-discretion, resolved by 03.1-UI-SPEC.md) — a real `<button>` toggle using the new shadcn `Collapsible`/`CollapsibleTrigger` with `aria-expanded` reflecting state and an `aria-label` that reads "Show legend" when collapsed and "Hide legend" when expanded (exact strings from 03.1-UI-SPEC.md's Copywriting Contract). Legend rows themselves are static (not independently focusable) — only the toggle button is a tab stop.
    In `GraphCanvas.tsx`, import and render `<GraphLegend />` as a child inside the existing `graph-canvas-backdrop` relative wrapper div (added by plan 03.1-01 Task 3), positioned `absolute bottom-4 left-4 z-10` so it never covers graph content per D-30 and sits above the background grid/glow `::before` layer without needing to touch that layer's own CSS.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with zero TypeScript errors after adding `tooltip.tsx`/`collapsible.tsx` and importing them
    - `grep -c "relationshipStyles" frontend/src/components/graph/GraphLegend.tsx` returns at least 1 (colors sourced from the shared module, not duplicated)
    - `grep -c "aria-expanded" frontend/src/components/graph/GraphLegend.tsx` returns at least 1
    - `grep -c "Forward-compat" frontend/src/components/graph/GraphLegend.tsx` (case-insensitive) returns at least 1 (the 3 zero-instance groups are represented, not omitted)
  </acceptance_criteria>
  <done>A collapsed-by-default legend renders bottom-left over the canvas, its Node Types/Relationships data is sourced entirely from relationshipStyles.ts, the 3 zero-instance-today relationship groups appear de-emphasized rather than hidden, and the toggle is keyboard-accessible with correct aria-expanded/aria-label state.</done>
</task>

<task type="auto">
  <name>Task 2: Build GraphControls (zoom in/out/fit/reset) wired to the existing cy instance and runLayout</name>
  <files>frontend/src/components/graph/GraphControls.tsx, frontend/src/components/graph/GraphCanvas.tsx</files>
  <read_first>frontend/src/components/graph/GraphCanvas.tsx, frontend/src/components/layout/AppShell.tsx</read_first>
  <precondition>GraphCanvas.tsx's cyInstanceRef and runLayout (module-scope function) already exist from plan 03.1-01, and the graph-canvas-backdrop wrapper div exists from plan 03.1-01 Task 3, before this task adds a second absolutely-positioned overlay child into that same wrapper.</precondition>
  <action>
    Create `frontend/src/components/graph/GraphControls.tsx`: a vertical stack of 4 icon-only buttons (zoom-in, zoom-out, fit-to-view, reset), each a 44x44px hit area wrapping a 16-20px `lucide-react` icon (`ZoomIn`, `ZoomOut`, `Maximize2`, `RotateCcw`) centered inside, each wrapped in the new shadcn `Tooltip` showing the same text as its `aria-label` ("Zoom in", "Zoom out", "Fit graph to view", "Reset zoom" — exact strings from 03.1-UI-SPEC.md's Copywriting Contract). This component receives a `cy: cytoscape.Core | null` accessor (implemented as a prop typed `{ cyRef: React.RefObject<cytoscape.Core | null>; onReset: () => void }` so button click handlers read `cyRef.current` at click time rather than closing over a possibly-stale/null value at render time) — do not create a second ref or a second place that tracks the live cy instance. Zoom-in/zoom-out call `cy.zoom({ level: cy.zoom() * 1.2, renderedPosition: { x: cy.width() / 2, y: cy.height() / 2 } })` / `cy.zoom({ level: cy.zoom() / 1.2, ... })` respectively, relying on the `minZoom`/`maxZoom` bounds already set on the `CytoscapeComponent` (plan 03.1-01 Task 1) to clamp the result — do not duplicate the 0.3/2.5 bounds here. Fit-to-view calls `cy.fit(undefined, 48)` (same 48px padding as the tuned layout, for visual consistency). Reset calls the `onReset` prop, which `GraphCanvas.tsx` implements as `() => { const cy = cyInstanceRef.current; if (cy) runLayout(cy) }` — reusing the exact existing `runLayout` function plan 03.1-01 already defines, never a second layout invocation path.
    In `GraphCanvas.tsx`, import and render `<GraphControls cyRef={cyInstanceRef} onReset={...} />` as a second child inside the `graph-canvas-backdrop` wrapper div, positioned `absolute bottom-4 right-4 z-10 flex flex-col gap-2` so it never overlaps `GraphLegend`'s bottom-left position or graph content per D-30.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test -- --run src/components/graph/GraphCanvas.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/components/graph/GraphCanvas.test.tsx` passes all existing + plan-03.1-01-added tests unmodified (GraphControls' new cy-instance usage must not break the existing CytoscapeComponent stub, since it only reads `cyRef.current`/calls `onReset` on click, never during initial render)
    - `grep -c "runLayout" frontend/src/components/graph/GraphCanvas.tsx` shows `runLayout` still defined exactly once at module scope and invoked from both the existing `[graph]`-keyed effect and the new `onReset` callback — no second layout function is added
    - `grep -c "aria-label=\"Zoom in\"" frontend/src/components/graph/GraphControls.tsx` and the equivalent for "Zoom out"/"Fit graph to view"/"Reset zoom" each return at least 1
    - `npm run build` succeeds with zero TypeScript errors
  </acceptance_criteria>
  <done>Four always-present, 44x44px, tooltip+aria-labeled zoom/fit/reset controls render bottom-right over the canvas without overlapping the legend or graph content; reset reuses the existing runLayout function; zoom in/out respects the existing minZoom/maxZoom bounds without redefining them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relationshipStyles.ts's exported color data -> GraphLegend.tsx's rendered swatches | Internal, already-typed module data; no external/user input reaches this component |
| User click on GraphControls buttons -> cy.zoom()/cy.fit()/runLayout() imperative calls | Purely local canvas-viewport manipulation; no network call, no data mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-08 | Tampering / Information Disclosure | GraphLegend.tsx's new markup | low | mitigate | Renders only static, already-typed color/label data from relationshipStyles.ts — no user-controlled text, no unsafe-HTML-injection API; re-verified by the repo-wide grep count in `<verification>` staying at zero |
| T-03.1-09 | Denial of Service (perf) | GraphControls' zoom/fit/reset handlers | low | mitigate | Each handler is a single imperative Cytoscape API call fired only on click (not a continuous/looped animation); reset reuses the existing, already-guarded `runLayout` function rather than introducing a second layout code path |

No new form input, API call, or third-party script execution is introduced by this plan.
</threat_model>

<verification>
- `cd frontend && npm run test -- --run` — full suite green (all files from plans 03.1-01/02/03 plus this plan's additions)
- `cd frontend && npm run build` — clean
- `cd frontend && npm run lint` — clean
- `git diff --stat -- backend/` — must be empty (D-01)
- `grep -rc "dangerouslySetInnerHTML" frontend/src | awk -F: '{s+=$2} END {print s+0}'` — must remain 0
- Manual (deferred, per 03.1-VALIDATION.md): run `npm run dev`, confirm the legend toggle and all 4 zoom/fit/reset controls are reachable via keyboard Tab order after the top bar and before the detail panel, and that neither overlay ever visually covers graph content at the smallest supported viewport
</verification>

<success_criteria>
A collapsed-by-default legend and 4 always-present zoom/fit/reset controls render as non-overlapping floating overlays on the canvas; legend colors are sourced exclusively from relationshipStyles.ts; reset reuses the existing runLayout function; zero new dangerouslySetInnerHTML uses; zero backend changes.
</success_criteria>

<output>
Create `.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-04-SUMMARY.md` when done
</output>

## Artifacts This Plan Produces

- New file: `frontend/src/components/graph/GraphLegend.tsx` — collapsible Node Types / Relationships legend overlay
- New file: `frontend/src/components/graph/GraphControls.tsx` — zoom-in/zoom-out/fit/reset control stack
- New files (official shadcn registry, no vetting gate): `frontend/src/components/ui/tooltip.tsx`, `frontend/src/components/ui/collapsible.tsx`
- No new npm dependencies beyond what `shadcn add` vendors from the already-configured official registry; no new CSS custom properties; no changes to `GraphCanvas.tsx`'s exported `SelectedNode`/`SelectedEdge`/`SelectedElement` types.
