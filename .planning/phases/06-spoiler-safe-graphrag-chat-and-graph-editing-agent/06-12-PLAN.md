---
phase: 06-spoiler-safe-graphrag-chat-and-graph-editing-agent
plan: 12
type: execute
wave: 7
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05", "06-06", "06-07", "06-08", "06-09", "06-10", "06-11"]
files_modified:
  - README.md
  - docs/API.md
  - docs/ARCHITECTURE.md
  - docs/CONFIGURATION.md
  - docs/GETTING-STARTED.md
  - docs/frontend-api-contract.md
  - .planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-MANUAL-ACCEPTANCE.md
autonomous: false
requirements: [RAG-01, RAG-02, RAG-03, RAG-04, RAG-05, RAG-06, RAG-07, RAG-08, RAG-09, RAG-10, RAG-11, RAG-12, RAG-13, RAG-14, RAG-15, RAG-16, RAG-17]

user_setup:
  - service: llm-provider
    why: "The Manual Acceptance Matrix (Task 2) requires a live OpenAI-compatible endpoint to qualitatively verify real-provider answer quality and language-following (RAG-07); automated tests use FakeLLMProvider exclusively and do not require this."
    env_vars:
      - name: LLM_ENABLED
        source: "Set to true only for the manual UAT session; leave false otherwise"
      - name: LLM_BASE_URL
        source: "Your OpenAI-compatible provider's base URL"
      - name: LLM_API_KEY
        source: "Your provider's API key — never commit this value; .env.example stays empty"
      - name: LLM_MODEL
        source: "The model identifier your provider exposes"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "cd backend && uv run pytest passes with 0 failures across the entire backend suite, including every test file created across 06-01..06-07."
    - "cd frontend && npm run test && npm run lint && npx tsc -b && npm run build all pass with 0 failures/errors."
    - "The existing series API, graph API, Notes API, authentication, session cookies, character images, seed idempotency, spoiler tests, and OpenAPI schema all pass their existing (pre-Phase-6) test assertions unmodified — zero regression."
    - "No LLM_API_KEY or other secret value appears in git history, .env.example, documentation, logs, or any committed file introduced by this phase."
    - "The Manual Acceptance Matrix (06-PRD-SOURCE.md section 18, 20 items) is executed in a live browser session and its results are recorded, not merely referenced."
  artifacts:
    - .planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-MANUAL-ACCEPTANCE.md
    - docs/ARCHITECTURE.md
    - docs/CONFIGURATION.md
---

<objective>
Close the phase: run and confirm the complete regression suite (backend + frontend, exactly the command
list from `06-PRD-SOURCE.md` section 17), update every documentation surface section 19 requires, and
execute the Manual Acceptance Matrix (section 18) in a live browser session — the phase cannot be
called complete on automated evidence alone (per PRD section 22: "Do not claim completion with mocked
browser behavior alone").

Purpose: this is the phase's own quality gate, matching `06-PRD-SOURCE.md` section 21 "Final Quality
Bar" line for line, and the documentation this phase explicitly promises the rest of the project (threat
model, spoiler-safety invariants, mutation/confirmation workflow, provider setup, local dev
instructions).
Output: full green regression, updated docs, a completed `06-MANUAL-ACCEPTANCE.md` checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-PRD-SOURCE.md
@.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-CONTEXT.md
@.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression suite — backend + frontend, exact PRD §17 command list</name>
  <read_first>backend/pyproject.toml, frontend/package.json, .env.example</read_first>
  <files>backend/tests/test_openapi_contract.py, backend/tests/test_frontend_contract_doc.py</files>
  <action>
    Run, in order: `cd backend && uv run pytest` (full backend suite — every test file across
    06-01..06-07 plus all pre-existing Phase 1-4 tests), `cd frontend && npm run test`, `cd frontend &&
    npm run lint`, `cd frontend && npx tsc -b`, `cd frontend && npm run build` — the exact command list
    `06-PRD-SOURCE.md` section 17 requires verbatim. Specifically re-verify
    `backend/tests/test_openapi_contract.py` and `backend/tests/test_frontend_contract_doc.py` pass
    (per 06-VALIDATION.md's sampling-rate note, these are "the tests most likely to be silently broken
    by a route added in an earlier wave and only caught at phase-end otherwise"). If any regression is
    found in existing series/graph/user-content/revision/auth behavior, fix it before proceeding — do
    not defer a regression fix past this task.
  </action>
  <verify>
    <automated>cd backend && uv run pytest && cd ../frontend && npm run test && npm run lint && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` reports 0 failures for the full backend suite.
    - `npm run test`, `npm run lint`, `npx tsc -b`, `npm run build` all report 0 failures/errors.
    - `test_openapi_contract.py`/`test_frontend_contract_doc.py` specifically pass, confirming the closed-inventory route count matches every route added across 06-01/06-04/06-05/06-06/06-07.
  </acceptance_criteria>
  <done>Full backend and frontend regression suite is green, including the two closed-inventory contract tests and every pre-existing Phase 1-4 test.</done>
</task>

<task type="auto">
  <name>Task 2: Documentation — README, API, ARCHITECTURE, CONFIGURATION, GETTING-STARTED</name>
  <read_first>README.md, docs/API.md, docs/ARCHITECTURE.md, docs/CONFIGURATION.md, docs/GETTING-STARTED.md, docs/frontend-api-contract.md</read_first>
  <files>docs/GETTING-STARTED.md, docs/CONFIGURATION.md, docs/ARCHITECTURE.md, docs/frontend-api-contract.md</files>
  <action>
    Update `README.md` and `docs/GETTING-STARTED.md` with a short section on enabling chat locally
    (setting `LLM_ENABLED`/`LLM_BASE_URL`/`LLM_API_KEY`/`LLM_MODEL` in a local `.env`, never committing
    real values). Update `docs/CONFIGURATION.md` documenting every new `LLM_*` variable (name, purpose,
    default, bound). Update `docs/ARCHITECTURE.md` with: the GraphRAG-lite pipeline diagram (reuse
    06-RESEARCH.md's "System Architecture Diagram" as the source), the allowlisted-retrieval-tool
    security model, the ChangeSet propose/confirm/apply two-stage flow, and the append-only Revision
    extension. Add or update a "Spoiler-Safety Invariants" section stating explicitly: the LLM never
    receives the full unfiltered graph, never receives future-episode data, never executes arbitrary
    Cypher, cannot directly mutate canonical/candidate content; writes require typed ChangeSets and
    explicit confirmation; chat history is spoiler-filtered by the same boundary as the graph; lowering
    progress hides previously generated future-boundary messages without deleting them; all graph content
    is treated as untrusted prompt data. Confirm `docs/frontend-api-contract.md` (already updated
    per-route across 06-01/06-04/06-05/06-06/06-07) is internally consistent as a single document (no
    orphaned/duplicate route entries from the incremental per-plan edits).
  </action>
  <verify>
    <automated>grep -l "LLM_ENABLED" README.md docs/CONFIGURATION.md docs/GETTING-STARTED.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ARCHITECTURE.md` contains a "Spoiler-Safety Invariants" (or equivalently named) section covering all eight bullet points listed in the action above.
    - `docs/CONFIGURATION.md` documents all eleven `LLM_*` variables from 06-01.
    - `docs/frontend-api-contract.md` has no duplicate route entries (each path template appears exactly once).
    - No documentation file contains a real API key value anywhere (grep for a plausible key pattern finds none).
  </acceptance_criteria>
  <done>README, API, ARCHITECTURE, CONFIGURATION, and GETTING-STARTED docs fully describe the new chat/ChangeSet feature, the spoiler-safety invariants, and local provider setup, with no secret values committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual Acceptance Matrix — live browser session</name>
  <precondition>Full automated regression from Task 1 is green (backend pytest, frontend test/lint/typecheck/build all passing).</precondition>
  <what-built>
    The complete spoiler-safe GraphRAG chat and graph-editing agent: persisted watch progress, ten
    allowlisted retrieval tools, streamed grounded citation-backed answers, prompt-injection-resistant
    system prompt, ChatSession/ChatMessage persistence with hide-not-delete spoiler filtering, a typed
    ChangeSet propose/confirm/apply/revert graph-editing flow with canonical/candidate protection, and a
    full chat UI integrated into the existing DetailPanel with graph-focus synchronization.
  </what-built>
  <how-to-verify>
    Create `.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-MANUAL-ACCEPTANCE.md`
    listing all 20 items from `06-PRD-SOURCE.md` section 18 "Manual Acceptance Matrix" verbatim as a
    checklist, then execute each item in a live browser session against the running local stack (backend
    + frontend, with a real LLM provider configured per this plan's `user_setup` if available — if no
    real provider is available, note which items were verified with the fake provider substituting for
    qualitative answer-quality checks, and flag those as a known limitation rather than skipping them
    silently): 1. Login succeeds. 2. Series and progress are restored. 3. Chat session can be created. 4.
    User asks a visible-content question. 5. Answer streams. 6. Citations open relevant evidence. 7.
    "Show in graph" highlights relevant nodes/edges. 8. Future-content question returns a safe
    insufficient-information response. 9. Episode 3 chat response is hidden after returning to Episode 1.
    10. Hidden response does not enter later model context. 11. User requests a valid user-origin node
    creation. 12. Preview appears without changing graph. 13. Reject leaves graph unchanged. 14. Confirm
    applies change. 15. Graph updates. 16. Refresh preserves the change and chat. 17. Canonical deletion
    is refused. 18. Logout removes access to chats. 19. Backend-off and LLM-provider-off states are
    understandable. 20. No secret or token appears in browser storage or logs. Record pass/fail and any
    notes for each item in the checklist file, then produce the phase's Final Report content (per PRD
    section 22's 22-point list: architecture, files changed, dependencies added, env vars added, final
    API contracts, provider abstraction, retrieval tools, spoiler enforcement points, chat-history
    spoiler behavior, system-prompt version/injection defenses, citation model, ChangeSet operation
    model, canonical/candidate protection, transaction/revision behavior, frontend chat UX, graph
    synchronization, backend test results, frontend test/lint/typecheck/build results, manual acceptance
    results, known limitations, security caveats, and whether the branch is safe to commit) as the closing
    section of the same file.
  </how-to-verify>
  <resume-signal>Report which of the 20 items passed, which failed (with detail), and whether the branch is safe to commit per PRD section 22's final question — or type "approved" once 06-MANUAL-ACCEPTANCE.md is complete and all 20 items pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation / manual-checklist artifacts -> committed repository | Must never contain a real secret value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Information Disclosure | README.md, docs/*, 06-MANUAL-ACCEPTANCE.md | high | mitigate | Manual-checklist item 20 explicitly checks for secret/token leakage in browser storage/logs; Task 2's acceptance criteria grep documentation for key-shaped values (Task 2, Task 3) |
</threat_model>

<verification>
`cd backend && uv run pytest && cd ../frontend && npm run test && npm run lint && npx tsc -b && npm run build`,
plus the completed `06-MANUAL-ACCEPTANCE.md` with all 20 PRD §18 items recorded pass/fail.
</verification>

<success_criteria>
Full automated regression is green with zero pre-existing-feature regression; documentation fully
describes the new feature and its spoiler-safety/mutation-safety invariants; the Manual Acceptance
Matrix is executed and recorded, not merely referenced; no secret is committed anywhere.
</success_criteria>

<output>
Create `.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-12-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces (this plan's contribution)

New file: `.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-MANUAL-ACCEPTANCE.md` (manual acceptance checklist + PRD §22 Final Report).
