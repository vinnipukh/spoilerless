---
phase: 08-production-deployment-automated-ci-cd
plan: 08
type: execute
wave: 6
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05", "08-06", "08-07"]
files_modified:
  - docs/DEPLOYMENT.md
autonomous: true
requirements: [DOCS-03]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "docs/DEPLOYMENT.md describes the real hosted target (Vercel + Render + AuraDB Free + Upstash Redis, spoilerless.net subdomains) replacing its current 'no production deployment target defined' framing"
    - "docs/DEPLOYMENT.md documents a real rollback procedure for the hosted environment, not the current generic local-process rollback"
    - "docs/DEPLOYMENT.md documents every new environment variable this phase introduced (ADMIN_EMAILS, REDIS_URL, SESSION_COOKIE_SAMESITE) alongside the pre-existing ones, by name only — never a real secret value"
  artifacts: []
  key_links: []
---

<objective>
Rewrite `docs/DEPLOYMENT.md` to describe the actual production target this
phase built — replacing its current "no production deployment target
defined in the repository" statement — including the real rollback
procedure, the new environment variables, and the CI gate now in place.

Purpose: DOCS-03's exact requirement — the deployment doc must match the
shipped reality, not describe a hypothetical future state.
Output: An accurate docs/DEPLOYMENT.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-production-deployment-automated-ci-cd/08-CONTEXT.md
@.planning/phases/08-production-deployment-automated-ci-cd/08-RESEARCH.md
@docs/DEPLOYMENT.md
@render.yaml
@frontend/vercel.json
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/DEPLOYMENT.md for the real hosted target</name>
  <read_first>docs/DEPLOYMENT.md, render.yaml, frontend/vercel.json, .github/workflows/ci.yml, .planning/phases/08-production-deployment-automated-ci-cd/08-01-SUMMARY.md, .planning/phases/08-production-deployment-automated-ci-cd/08-05-SUMMARY.md</read_first>
  <files>docs/DEPLOYMENT.md</files>
  <action>
    Rewrite docs/DEPLOYMENT.md section by section against what this phase
    actually shipped: replace "Detected Deployment Targets" / "there is
    therefore no production deployment target defined in the repository"
    with a table listing Vercel (frontend, app.spoilerless.net, Hobby
    tier), Render (backend, api.spoilerless.net, free web service tier,
    render.yaml Blueprint), Neo4j AuraDB Free (Member-role app credential,
    replacing Docker Compose in the deploy path), and Upstash Redis
    (rate-limit counters + graph query cache). Keep the existing "Local
    Deployment" section's Docker Compose instructions (still valid for
    local dev) but add a note that this recipe is no longer part of any
    production deployment path. Rewrite "Build Pipeline" to describe the
    real GitHub Actions gate (`.github/workflows/ci.yml`, both jobs, its
    pinned Neo4j service container) instead of "No CI/CD pipeline
    detected." Rewrite "Environment Setup" to list every environment
    variable now in play by name only, grouped by platform (Render:
    NEO4J_URI/NEO4J_USERNAME/NEO4J_PASSWORD, GOOGLE_CLIENT_ID,
    SESSION_COOKIE_SECURE, SESSION_COOKIE_SAMESITE, FRONTEND_ORIGINS,
    ALLOWED_EMAILS, ADMIN_EMAILS, REDIS_URL; Vercel:
    VITE_API_BASE_URL, VITE_GOOGLE_CLIENT_ID; Upstash: the REDIS_URL
    itself) — never a real value, following this file's own existing
    "never commit .env files or real credentials" convention. Replace
    "Pre-production safety gaps" (now resolved) with a short "Production
    Safety" section listing what this phase closed (secure cookie default,
    admin-gated mutation endpoints, rate limiting, structured logging,
    Member-role least-privilege DB access) and what remains explicitly
    deferred to Phase 9 (per REQUIREMENTS.md's PROB-01..21 — ownership
    binding on user-content routes, full request/response casing
    consistency, etc.) so the doc doesn't overclaim beyond what actually
    shipped. Rewrite "Rollback" with the real procedure: Render supports
    redeploying a prior successful deploy from its dashboard's deploy
    history (or a `git revert` + push to trigger a fresh auto-deploy);
    Vercel supports promoting a prior deployment from its dashboard's
    deployment list instantly (atomic, no rebuild); Neo4j/AuraDB has no
    automated backup/restore configured this phase — state that explicitly
    as a known gap rather than describing a procedure that doesn't exist.
    Rewrite "Monitoring" to include the UptimeRobot (or equivalent) monitor
    on `/health` this phase's Ops plan set up, alongside the existing
    `/health` endpoint description.
  </action>
  <verify>
    <automated>! grep -q "no production deployment target defined" docs/DEPLOYMENT.md</automated>
    <automated>grep -q "spoilerless.net" docs/DEPLOYMENT.md</automated>
    <automated>! grep -qE "NEO4J_PASSWORD\s*=\s*[A-Za-z0-9]" docs/DEPLOYMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/DEPLOYMENT.md no longer states there is no production deployment target
    - The real hosted target (Vercel, Render, AuraDB Free, Upstash Redis, spoilerless.net) is named explicitly
    - Every new environment variable this phase introduced is documented by name, with no real secret value present anywhere in the file
    - The rollback section describes Render/Vercel's actual dashboard-based rollback mechanisms, and explicitly states Neo4j backup/restore remains unconfigured rather than inventing a procedure
  </acceptance_criteria>
  <done>docs/DEPLOYMENT.md accurately describes the shipped production target, its environment variables by name, its CI gate, and a real rollback procedure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation <-> real secret values | The doc must describe env var names only, never values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08-01 | Information Disclosure | A real credential accidentally pasted into DEPLOYMENT.md while documenting env vars | medium | mitigate | Automated verify greps for a NEO4J_PASSWORD= pattern followed by a plausible value; task action explicitly instructs names only |

</threat_model>

<verification>
Manually diff docs/DEPLOYMENT.md against the actual render.yaml,
vercel.json, and .github/workflows/ci.yml this phase produced to confirm no
drift between the doc and the real config.
</verification>

<success_criteria>
docs/DEPLOYMENT.md matches the real, shipped production deployment target,
its environment variables, its CI gate, and a real rollback procedure — no
"no production target" language remains.
</success_criteria>

<output>
Create `.planning/phases/08-production-deployment-automated-ci-cd/08-08-SUMMARY.md` when done
</output>
