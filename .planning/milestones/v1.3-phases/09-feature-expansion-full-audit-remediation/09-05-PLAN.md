---
phase: 09-feature-expansion-full-audit-remediation
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-03", "09-04"]
files_modified:
  - spoilerless/app/core/errors.py
  - spoilerless/app/api/auth.py
  - spoilerless/app/main.py
  - spoilerless/app/spoiler/filter.py
  - spoilerless/app/graph/ontology.py
  - spoilerless/app/repository/settings.py
  - spoilerless/app/api/settings.py
  - spoilerless/app/api/candidates.py
  - spoilerless/tests/test_openapi_contract.py
  - frontend/src/api/client.ts
  - frontend/vite.config.ts
  - docs/DEVELOPMENT.md
autonomous: true
requirements: [PROB-09, PROB-17, PROB-19, PROB-29, PROB-30]
must_haves:
  truths:
    - "All emitted error codes use one consistent uppercase casing convention matching a single canonical registry, and the ErrorDetail.code validation pattern accepts them (PROB-09, #20)"
    - "The backend emits baseline security headers (CSP, HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy) and CORS no longer combines wildcard methods/headers with credentials (PROB-17, #38)"
    - "Small trust lies are gone: the settings docstring no longer claims a nonexistent constraint, whitespace-only API keys are rejected server-side, api/candidates.py uses a public repository method, load_ontology() is cached (PROB-19, #41)"
    - "SOURCES/EVIDENCE endpoint MATCHes carry series_id — cross-series id collisions cannot leak (PROB-29, #53)"
    - "One root .env + envDir:'..' in vite.config.ts; GOOGLE_CLIENT_ID and VITE_GOOGLE_CLIENT_ID are verified equal at startup/CI; no backend/.env duplicates remain (PROB-30, #55)"
  artifacts:
    - path: "spoilerless/app/core/errors.py"
      provides: "Uppercase error-code registry + matching validation pattern"
    - path: "frontend/vite.config.ts"
      provides: "envDir: '..' — single root .env source"
  key_links:
    - from: "frontend/src/api/client.ts ApiError normalization"
      to: "spoilerless/app/core/errors.py canonical codes"
      via: "one casing convention, no paper-over mapping"
      pattern: "error_codes|AUTH_UNAUTHENTICATED"
    - from: "main.py middleware"
      to: "every response"
      via: "CSP/HSTS/XCTO/XFO/Referrer-Policy headers on all responses"
      pattern: "X-Content-Type-Options"
  prohibitions:
    - id: "NO-WILDCARD-CREDENTIALS"
      statement: "CORS allow_methods/allow_headers wildcards combined with allow_credentials=True are forbidden (PROB-17)"
    - id: "NO-LOWERCASE-CODES"
      statement: "New error codes must match the uppercase canonical pattern; the lowercase-only regex is replaced"
    - id: "NO-REPO-PRIVATE-ACCESS"
      statement: "Route layers must not reach into repository private attributes (repo._db) — public repository methods only"
    - id: "NO-DUP-ENV"
      statement: "backend/.env must not exist; Vite must load the root .env via envDir"
---

<objective>
API-surface hardening and trust cleanup in one plan: unify the error-code
contract to one uppercase canonical registry (#20), emit baseline security
headers and narrow CORS (#38), fix the four small code lies (#41), add
series_id to the SOURCES/EVIDENCE read-path MATCHes (#53), and consolidate the
environment to one root .env with an equality check between the backend and
frontend Google client ids (#55).

Purpose: The API contract must be self-consistent and machine-readable, the
transport must carry baseline hardening headers, and env sprawl must stop
being the trigger class for the #42 audience-mismatch 503.
Output: One error-code convention, security headers + narrowed CORS, four
trust fixes, series-scoped read-path queries, and envDir-based env
consolidation — all contract-test-locked.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md (§Open Questions 2, §Security Domain V13, §Runtime State Inventory)
@.planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md (§core/errors.py + api/auth.py; §spoiler/filter.py; §Error envelope)
@.planning/REQUIREMENTS.md (PROB-09/17/19/29/30)
@docs/PROBLEMS.md (#20, #38, #41, #53, #55)
@spoilerless/app/core/errors.py
@frontend/src/api/client.ts
@frontend/vite.config.ts
@spoilerless/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: One uppercase error-code convention + contract test + frontend normalization</name>
  <read_first>
    spoilerless/app/core/errors.py (ErrorDetail.code validation pattern ~line 28
    — lowercase-only regex vs the uppercase codes the API actually emits),
    spoilerless/app/api/auth.py (~35-42 — AUTH_* codes already uppercase),
    spoilerless/tests/test_openapi_contract.py (the contract test that must
    move with the convention),
    frontend/src/api/client.ts (ApiError normalization ~16-23 — papering over
    the inconsistency),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Open Questions 2 — uppercase canonical codes direction)
  </read_first>
  <files>spoilerless/app/core/errors.py, spoilerless/app/api/auth.py, spoilerless/tests/test_openapi_contract.py, frontend/src/api/client.ts</files>
  <action>
    Per RESEARCH Open Q2's recommendation: canonical codes are UPPERCASE
    (`AUTH_UNAUTHENTICATED`, `INVALID_REQUEST`, `LLM_STREAM_FAILED`, ...).
    In core/errors.py, change the ErrorDetail.code validation pattern from the
    lowercase-only regex to the uppercase form `^[A-Z][A-Z0-9_]*$` and add a
    single canonical registry (a module-level set or enum) listing every code
    the app emits; the validation + a contract test both reference the
    registry so future codes cannot drift. Sweep api/auth.py and any other
    route emitting lowercase/legacy codes so every emitted code matches the
    uppercase pattern (pydantic validation errors — e.g. invalid_request —
    must be re-mapped or re-emitted through the registry; pick the uppercase
    form and update the envelope construction sites).

    Update frontend/src/api/client.ts's ApiError normalization so it stops
    papering over the inconsistency — normalize against the same uppercase
    convention (accept uppercase codes as-is; keep any legacy lowercase
    mapping only as a transitional alias if the backend still emits one, and
    remove the alias when the sweep completes).

    Update test_openapi_contract.py to assert: every code appearing in the
    OpenAPI error responses matches the uppercase pattern, and every route's
    documented errors reference registry codes. Run the backend contract test
    and the frontend build.
    Commit with message "feat(09-05): uppercase error-code registry + contract test + client normalization (PROB-09)".
  </action>
  <verify>
    <automated>uv run pytest spoilerless/tests/test_openapi_contract.py -x -q 2>&1 | tail -3</automated>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/api/client.test.ts 2>&1 | tail -3</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - ErrorDetail.code validation accepts only ^[A-Z][A-Z0-9_]*$
    - A canonical registry exists and the contract test asserts every OpenAPI error code is in it and uppercase
    - client.ts normalization matches the uppercase convention
    - Contract test + frontend build green
  </acceptance_criteria>
  <done>The error contract is single-case, registry-backed, and test-locked on both sides of the wire.</done>
</task>

<task type="auto">
  <name>Task 2: Baseline security headers middleware + narrowed CORS</name>
  <read_first>
    spoilerless/app/main.py (CORS middleware block ~82-88 — allow_methods/allow_headers
    wildcards combined with allow_credentials=True),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Security Domain V13 — the five headers required by PROB-17)
  </read_first>
  <files>spoilerless/app/main.py, spoilerless/tests/test_graph_api.py, spoilerless/tests/test_main_lifespan.py</files>
  <action>
    Add a middleware (or register headers in the existing middleware stack) in
    main.py that sets on EVERY response: Content-Security-Policy (a
    baseline default-src 'self' policy tuned to the app's needs — the app
    loads self-hosted fonts/scripts and hotlinked character images per PROB-10
    follow-up; pick a policy that does not break the app and document it),
    Strict-Transport-Security (max-age=31536000; includeSubDomains — only
    meaningful over HTTPS, harmless locally), X-Content-Type-Options: nosniff,
    X-Frame-Options: DENY, and Referrer-Policy: strict-origin-when-cross-origin.

    Narrow the CORS configuration: replace the wildcard allow_methods/allow_headers
    with explicit method (GET, POST, PUT, PATCH, DELETE, OPTIONS) and explicit
    header lists (the headers the app actually sends: Content-Type,
    Authorization, and the X-LLM-* BYOK headers from 08-02), keeping
    allow_credentials=True with the explicit FRONTEND_ORIGINS allowlist
    (never "*" with credentials).

    Tests: extend test_graph_api.py (or test_main_lifespan.py if it exists) —
    assert GET /health returns all five headers; assert the CORS preflight
    response for the allowed origin returns the explicit method/header lists
    and no wildcard. Run the suite.
    Commit with message "feat(09-05): security headers middleware + narrowed CORS (PROB-17/#38)".
  </action>
  <verify>
    <automated>uv run pytest spoilerless/tests/test_graph_api.py -k "health" -x -q 2>&1 | tail -3</automated>
    <automated>uv run pytest spoilerless/tests/test_main_lifespan.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Every response carries the five baseline headers (CSP, HSTS, nosniff, DENY, Referrer-Policy)
    - CORS preflight for an allowed origin lists explicit methods/headers; no wildcard with credentials
    - Health-route tests assert the headers; suite green
  </acceptance_criteria>
  <done>Baseline transport hardening is live and asserted by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Trust nits + read-path series_id + env consolidation</name>
  <read_first>
    spoilerless/app/repository/settings.py (docstring claiming a uniqueness
    constraint that no longer exists in seed.py),
    spoilerless/app/api/settings.py (whitespace-only API keys persisted raw),
    spoilerless/app/api/candidates.py (repo._db private-attribute access),
    spoilerless/app/graph/ontology.py (load_ontology ~83 uncached, called at
    import time),
    spoilerless/app/spoiler/filter.py (SOURCES_QUERY/EVIDENCE_QUERY MATCH
    without series_id ~154/183),
    docs/DEVELOPMENT.md (line ~50 command drift),
    frontend/vite.config.ts (missing envDir),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md
    (§spoiler/filter.py — add series_id to those MATCHes, keep the canonical
    visibility clause untouched)
  </read_first>
  <files>spoilerless/app/repository/settings.py, spoilerless/app/api/settings.py, spoilerless/app/api/candidates.py, spoilerless/app/graph/ontology.py, spoilerless/app/spoiler/filter.py, docs/DEVELOPMENT.md, frontend/vite.config.ts, spoilerless/app/core/config.py, .github/workflows/ci.yml</files>
  <action>
    PROB-19 nits (#41): (a) fix the repository/settings.py docstring to
    describe the ACTUAL schema (no nonexistent uniqueness constraint); (b)
    reject whitespace-only API keys server-side in api/settings.py (strip and
    422 if empty) so a direct API call cannot store an all-spaces key —
    matching the frontend's client-side trim; (c) replace the api/candidates.py
    private-attribute access with a public repository method (add the method
    to the repository and call it — never repo._db); (d) cache load_ontology()
    (functools.lru_cache or module-level memo) so worker startup stops
    re-reading YAML and a missing ontology file cannot crash the app at import
    time.

    PROB-29 (#53): in spoiler/filter.py add the series_id predicate to the
    SOURCES_QUERY and EVIDENCE_QUERY endpoint MATCHes (the bare
    MATCH (source:Source) / MATCH (evidence:EvidenceFragment) gain
    {series_id: $series_id}) — the canonical visibility clause shape in all
    six clauses stays byte-identical. Fix docs/DEVELOPMENT.md line ~50's
    setup-command drift to the runbook-canonical invocation.

    PROB-30 (#55): frontend/vite.config.ts gains envDir: '..' (Vite loads the
    root .env; only VITE_-prefixed vars are exposed to the browser);
    confirm backend/.env does not exist (delete any leftover dup); add a
    startup/CI equality check asserting GOOGLE_CLIENT_ID (backend) equals
    VITE_GOOGLE_CLIENT_ID (frontend) — implement as a CI step (a small shell
    check in .github/workflows/ci.yml) and/or a startup assertion in
    core/config.py that warns/fails on mismatch (the #42 audience-mismatch
    trigger class). Remove the dead AUTH_DEV_CODE line from root .env is an
    OPERATOR step (gitignored file) — note it for the final wave; do not
    commit any .env.
    Commit with message "feat(09-05): trust nits + series-scoped read-path MATCH + env consolidation (PROB-19/29/30)".
  </action>
  <verify>
    <automated>uv run pytest spoilerless/tests/test_graph_api.py -x -q 2>&1 | tail -3</automated>
    <automated>rg -n "_db" spoilerless/app/api/candidates.py | wc -l</automated>
    <automated>rg -n "envDir" frontend/vite.config.ts</automated>
    <automated>test ! -f backend/.env && echo "no dup env"</automated>
  </verify>
  <acceptance_criteria>
    - settings.py docstring describes the real schema; whitespace-only keys 422 server-side
    - api/candidates.py has zero private-attribute accesses; load_ontology() is cached
    - SOURCES/EVIDENCE MATCHes carry series_id; the six visibility clauses are unchanged
    - vite.config.ts has envDir: '..'; backend/.env absent; the equality check exists in CI and/or config startup
    - DEVELOPMENT.md:50 uses the runbook-canonical setup command
  </acceptance_criteria>
  <done>The small lies are gone, the read path is series-scoped, and env is single-source with a client-id equality gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client <-> API responses | Header injection / error-envelope parsing |
| Browser <-> backend CORS preflight | Wildcard-with-credentials exposure |
| Route layer <-> repository layer | Private-attribute breach (#41) |
| Root .env <-> Vite build | VITE_* exposure must stay browser-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05-01 | Spoofing | Error-code contract drift (#20) | medium | mitigate | Uppercase registry + contract test on both sides |
| T-09-05-02 | Tampering | XSS via missing CSP (#38) | high | mitigate | CSP + X-Content-Type-Options + X-Frame-Options on every response, test-asserted |
| T-09-05-03 | Information Disclosure | Wildcard CORS with credentials (#38) | high | mitigate | Explicit methods/headers + FRONTEND_ORIGINS only |
| T-09-05-04 | Elevation of Privilege | repo._db layering breach (#41) | medium | mitigate | Public repository method; grep gate zero hits |
| T-09-05-05 | Information Disclosure | Cross-series collision on SOURCES/EVIDENCE (#53) | medium | mitigate | series_id added to endpoint MATCHes |
| T-09-05-06 | Spoofing | Google audience mismatch from env drift (#55/#42) | high | mitigate | envDir consolidation + GOOGLE_CLIENT_ID equality check in CI/startup |
| T-09-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies |
</threat_model>

<verification>
Run test_openapi_contract.py, test_graph_api.py (health/headers), and the
frontend build. Grep gates: `rg -n "_db" spoilerless/app/api/candidates.py`
returns zero; `rg -n "envDir" frontend/vite.config.ts` present; `test ! -f
backend/.env`. Confirm `git status` shows no .env file staged.
</verification>

<success_criteria>
The API emits one uppercase error-code convention locked by contract tests;
every response carries the five baseline headers with narrowed CORS; the four
#41 trust lies are fixed; the read path is series-scoped; and env is
single-source with a client-id equality gate.
</success_criteria>

## Artifacts this phase produces

- `spoilerless/app/core/errors.py` (MODIFIED) — uppercase validation pattern
  + canonical code registry
- `spoilerless/app/main.py` (MODIFIED) — security-headers middleware + explicit
  CORS method/header lists
- `spoilerless/app/api/auth.py` (MODIFIED) — code sweep to uppercase registry
- `spoilerless/app/repository/settings.py` (MODIFIED) — truthful docstring
- `spoilerless/app/api/settings.py` (MODIFIED) — whitespace-only key rejection
- `spoilerless/app/api/candidates.py` (MODIFIED) — public repository method
- `spoilerless/app/graph/ontology.py` (MODIFIED) — cached load_ontology()
- `spoilerless/app/spoiler/filter.py` (MODIFIED) — series_id on SOURCES/
  EVIDENCE MATCHes
- `frontend/src/api/client.ts` (MODIFIED) — uppercase-convention normalization
- `frontend/vite.config.ts` (MODIFIED) — envDir: '..'
- `spoilerless/app/core/config.py` (MODIFIED) — client-id equality check
  (startup) and/or `.github/workflows/ci.yml` (MODIFIED) — CI equality step
- `docs/DEVELOPMENT.md` (MODIFIED) — canonical setup command at :50
- Tests: test_openapi_contract.py (MODIFIED), test_graph_api.py (header
  assertions), test_main_lifespan.py (MODIFIED/verified)

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-05-SUMMARY.md` when done
</output>
