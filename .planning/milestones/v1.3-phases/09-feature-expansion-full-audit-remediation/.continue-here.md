---
context: phase
phase: 09-feature-expansion-full-audit-remediation
task: 2
total_tasks: 4
status: in_progress
last_updated: 2026-08-05T05:51:20.802Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Budget-death partials** — the planner AND executors died at their 50-call tool budget 3× this phase (planner at 15/18 plans, 09-01 at closeout, 09-03 at Task-1 verification). Every dispatch MUST carry a tool-budget guardrail: "If you approach your tool budget, STOP, commit what is green, write SUMMARY.md marking pending tasks, and hand off." Never re-dispatch a full plan after a death — resume from the recorded continuation point. Verify disk (git log + SUMMARY.md + self-check) before trusting any self-report.
- [ ] CONSTRAINT: **Never trust research/self-report "already fixed" claims** — RESEARCH.md said #42 NameError was already fixed in tree; the 09-02 executor found it was still LIVE and fixed it (`a36676a`). Verify live before planning around a fix claim.
- [ ] CONSTRAINT: **MSYS python frontmatter edits mangle backreferences** — `re.sub` replacement `\1` in a python heredoc wrote SOH byte 0x01 into PLAN wave lines. When editing frontmatter via python: rewrite the ENTIRE line (never preserve matched eol), byte-verify with `od -c`.
- [ ] CONSTRAINT: **12 pre-existing backend test failures are PROVEN baseline** (baseline worktree at 9315a51, pre-rename HEAD, identical failures) — seed-drift from enriched S01E01 seed (harry_morgan visible_from_order=1 asserted-hidden) + candidate-origin pollution. Planned fixes: 09-08 (test isolation + drift checks) and 09-18 (reseed + sweep). Do NOT chase them in earlier plans; note and move on.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Budget-death mid-plan | Executor/planner exhausts 50 tool calls leaving uncommitted partials or missing SUMMARY | blocking | Every dispatch prompt carries STOP-commit-SUMMARY-handoff guardrail; resume from continuation notes; verify disk before trusting returns |
| Research fact drift | RESEARCH.md "already fixed"/"already gone" claims can be wrong against the live tree | blocking | Executor verifies each claim live before building on it (09-02 caught #42 live) |
| MSYS backreference mangling | python re.sub `\1` in heredoc → SOH 0x01 bytes in YAML frontmatter | blocking | Full-line rewrite in python edits; `od -c` byte verification |
| Suite-red panic | Full-suite failures look like regressions but are baseline seed-drift/pollution | advisory | Baseline worktree comparison at pre-change HEAD before classifying a failure as regression |
| git add -A sweep | Executor commits sweep pre-existing dirty files (config.json, PROBLEMS.md) | advisory | Stage explicit paths only; never `git add -A`; never commit `.planning/config.json` |

## Required Reading (in order)

1. `.planning/phases/09-feature-expansion-full-audit-remediation/09-CONTEXT.md` — locked decisions D-01..D-14 (all-57 scope, fcose, scratch-series, snapshot links, Markdown export, rename, operator-last)
2. `.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md` — technical research (20 sections; verified facts, wave guidance)
3. `.planning/phases/09-feature-expansion-full-audit-remediation/09-PATTERNS.md` — 54 files classified, 50 analogs with line-verified excerpts
4. `.planning/phases/09-feature-expansion-full-audit-remediation/09-UI-SPEC.md` — design contract (6 dims, 13 screen contracts)
5. `.planning/phases/09-feature-expansion-full-audit-remediation/09-VALIDATION.md` — Wave 0 + manual-only verification map
6. `.planning/phases/09-feature-expansion-full-audit-remediation/09-01-SUMMARY.md` + `09-02-SUMMARY.md` — rename + regression-net closeouts
7. `C:\Users\arhan\AppData\Local\hermes\skills\hdgrafcehennemi\hdgrafcehennemi\SKILL.md` — project pitfalls ($query collision, :AppUser, no second filter fork, live-DB teardown, NODE_ENV=test CI=1, npm run build = typecheck)

<current_state>
Phase 9 "Feature Expansion & Full Audit Remediation" — 2/18 plans fully complete (09-01 REBRAND rename, 09-02 verifier+wire tests), 09-03 Task 1 committed (`0f3c388`). 15 plans remain (09-03 T2-T4, 09-04..09-15 autonomous; 09-16/17/18 operator wave = STOP for user). All planning artifacts committed (18 PLAN.md, waves 0-9 DAG-synced, decision coverage 14/14). HEAD: `0f3c388`. Execute-phase chain was in --auto mode.
</current_state>

<completed_work>

- 09-01 (done, 4 commits + summary `4304f89`): `backend/` → `spoilerless/` import root via git mv (128 files, history preserved), SERVICE_NAME → `spoilerless-backend`, pyproject/docker-compose/render.yaml/CI swept, UI title → "Spoilerless", BYOK key → `spoilerless:byok-llm-settings` with read-compat migration, 13 docs renamed. Grep gates 0/0. test_revisions import sweep fixed inline (`ae0bf59`).
- 09-02 (done, 4 commits + summary `63665ce`): `a36676a` fix — **#42 NameError was LIVE, now fixed** (google bound in ProductionGoogleVerifier.verify scope); `86bcb50` behavioral verifier test (5 passed, 1 skipped); `082cb79` progress wire-shape contract tests at fetch level, no client mock (8 passed); 42 auth tests green, build green.
- 09-03 Task 1 (done, `0f3c388`, +550/-114): CurrentUserDependency on all 9 user-content mutation routes + candidate ingest + revision revert; `user_id` added (required) to NoteResponse/CustomNodeResponse/CustomRelationshipResponse; owner-scope WHERE clauses ($is_admin = true OR node.user_id = $user_id) on UPDATE/DELETE; `UserContentForbidden` → 403; `_raise_on_ownership_conflict()` (403 cross-owner / 409 non-user / 404 missing); `user_id` on all CREATE nodes; take_snapshot keys include user_id; scratch-series tests (`test-series:user-content`) for 401/403. Executor died at budget during Task-1 verification; orchestrator fixed 2 test call sites (create_note 3-arg signature, user_id on response model fixtures) → 39/39 unit tests green.
</completed_work>

<remaining_work>

- 09-03 Task 2: `created_by` stamping on direct user-content creates + NEW `spoilerless/app/spoiler/visibility.py` with `derive_visible_from_order` (max(episode order, current progress) fail-closed) shared by `repository/user_content.py` AND `repository/change_set.py` apply path (PROB-25/26) → `feat(09-03): created_by attribution + single visibility-derivation rule`
- 09-03 Task 3: real persisted `log_revision` id in candidate approve/reject responses (not fabricated sha256); `user_id` param on `log_revision` threaded through candidates/revert/change-set apply + RevisionResponse (PROB-12/33/34); `apply_revision_id` + `revert_revision_id` dual links on ChangeSet revert (PROB-27) → `feat(09-03): real persisted revision ids + actor attribution + dual revert links`
- 09-03 Task 4: 09-03-SUMMARY.md + `docs(09): summary for 09-03` + STATE/ROADMAP tracking
- Then autonomous plans in DAG wave order: 09-06 (chat/LLM), 09-07 (progress/lint/e2e), 09-04 (read-path), 09-09 (search), 09-05 (API hardening), 09-08 (test isolation), 09-10 (timeline), 09-11 (export/path), 09-12 (share), 09-13 (mobile/FEAT-11), 09-15 (docs), 09-14 (fcose LAST FE plan)
- FINAL operator wave (STOP for user, checkpoint:human-action): 09-16 (push+CI+rename), 09-17 (ADMIN_EMAILS+REDIS_URL), 09-18 (reseed+sweep)
</remaining_work>

<decisions_made>

- All 57 PROBLEMS.md findings in scope — REQUIREMENTS.md now carries PROB-01..32 (D-01)
- fcose cluster layout (D-03); scratch-series+teardown isolation, NOT Testcontainers (D-07); snapshot-at-creation share links (D-09); Markdown-only export (D-11); MIT license; rename EARLY (D-12)
- ZERO-COST: no paid services/API spend; fcose@2.2.0 sole new dep; fuse.js/jspdf forbidden; FakeLLMProvider verification
- Operator-touch work LAST (09-16/17/18, autonomous:false)
- Sequential executor dispatch (USE_WORKTREES=false) — one at a time, disk-verify each
</decisions_made>

<blockers>

- 12 pre-existing backend suite failures: PROVEN baseline at 9315a51 (seed-drift: enriched S01E01 changed harry_morgan visibility; pollution: candidate-origin residue). Planned: 09-08, 09-18. Do not chase.
- Executor budget-deaths: 3 so far. Guardrail + resume-from-continuation is the playbook.
- Local docker Neo4j container is `spoilerless-neo4j` (was hdgrafcehennemi-neo4j). If connection-refused: `docker ps` first.
</blockers>

## Infrastructure State

- Local docker Neo4j: `spoilerless-neo4j` (neo4j:2026.06.0-community), creds `neo4j`/`hdgraf-local-password`, db `neo4j` — use shell env overrides per-run, NEVER edit `.env` (AuraDB is the prod target; sibling Claude Code flips .env — always override in the command line).
- `unset PYTHONPATH` before python/uv (hermes terminal exports it, shadows venv).
- Frontend: `NODE_ENV=test CI=1 npm run test`; typecheck = `npm run build` (tsc -b); lint = `npm run lint`.
- Background suite procs all completed (proc_22b63f6f996c, proc_7a740022b546, proc_04fb115b8e2d — none running).
- `.planning/config.json` has `workflow._auto_chain_active: true` (ephemeral chain flag — never commit; a manual invoke should clear it per workflow).

## Critical Anti-Patterns (do NOT repeat these)

- [BUDGET-DEATH]: executor/planner hitting 50-call cap mid-plan → dispatch with guardrail, resume from continuation, verify disk
- [MSYS-MANGLE]: python re.sub backrefs in heredocs corrupt YAML → full-line rewrites + od -c verify
- [TRUST-DRIFT]: research "already fixed" claims wrong vs live tree → verify live (09-02 caught #42)
- [SWEEP]: git add -A grabs config.json/PROBLEMS.md → stage explicit paths

<context>
Executing Phase 9 autonomously in --auto chain mode under a zero-cost constraint with operator-touch work deferred to the final wave. Momentum pattern: dispatch one executor (sequential), disk-verify its return (git log + SUMMARY + self-check marker), mark plan complete in ROADMAP via `gsd_run query roadmap.update-plan-progress`, commit tracking, dispatch next. On budget-death: fix small tails inline, commit green partial, re-dispatch scoped continuation. On 429/503: inline retry; persistent → cron-watchdog.
</context>

<next_action>
Start with: `/gsd-execute-phase 9` (resumes automatically at the first incomplete plan = 09-03 Task 2 remainder). It will re-dispatch 09-03 continuation (Tasks 2-4) — a fresh executor reads 09-03-PLAN.md + this handoff. Then continue waves in DAG order. The operator wave (09-16/17/18) will checkpoint for the user.
</next_action>
