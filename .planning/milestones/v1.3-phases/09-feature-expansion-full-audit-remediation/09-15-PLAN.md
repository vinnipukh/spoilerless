---
phase: 09-feature-expansion-full-audit-remediation
plan: 15
type: execute
wave: 6
depends_on: ["09-01", "09-11", "09-12"]
files_modified:
  - docs/API.md
  - docs/ARCHITECTURE.md
  - docs/ROADMAP.md
  - LICENSE
  - main.py
  - frontend/README.md
  - data/dexter/seed/characters.json
  - data/dexter/images/
  - spoilerless/tests/test_frontend_contract_doc.py
autonomous: true
requirements: [DOCS-04, PROB-10]
must_haves:
  truths:
    - "docs/API.md, docs/ARCHITECTURE.md, and docs/ROADMAP.md match live behavior — route counts regenerated from the live OpenAPI, the ChangeSet capability (proposed_change_set) documented, the 'known gaps' section lists only real gaps, and no blockers that already shipped remain deferred (DOCS-04, #21-#24)"
    - "Committed junk is removed (root main.py PyCharm template, frontend/README.md Vite boilerplate), an MIT LICENSE exists, and seed character images are self-hosted in-repo or dropped instead of hotlinking Fandom (PROB-10, #25/#28)"
  artifacts:
    - path: "LICENSE"
      provides: "MIT license (user choice per 09-DISCUSSION-LOG)"
    - path: "docs/API.md"
      provides: "Route inventory regenerated from the live app.openapi() (45/33 or the post-phase-9 count)"
    - path: "data/dexter/images/"
      provides: "Self-hosted character portraits (replaces Fandom hotlinks)"
  key_links:
    - from: "docs/API.md"
      to: "live app.openapi()"
      via: "a regeneration script (or documented regen command) so counts cannot rot again"
      pattern: "openapi"
    - from: "data/dexter/seed/characters.json"
      to: "data/dexter/images/"
      via: "imageUrl fields point at in-repo assets, served by the frontend, not the third-party image host"
      pattern: "images"
  prohibitions:
    - id: "NO-STALE-ROUTE-COUNTS"
      statement: "docs/API.md must not carry hand-maintained counts that contradict the live OpenAPI (DOCS-04)"
    - id: "NO-FANDOM-HOTLINKS"
      statement: "Seed character images must not hotlink the third-party image host (PROB-10/#28)"
    - id: "NO-COMMITTED-JUNK"
      statement: "The PyCharm template and Vite boilerplate files must not exist in the tree (PROB-10/#25)"
---

<objective>
Docs alignment (DOCS-04, #21-#24) and hygiene (PROB-10, #25/#28) in one wave-4
plan, sequenced AFTER all code plans so the docs describe the shipped state:
regenerate docs/API.md from the live OpenAPI, correct ARCHITECTURE.md's
ChangeSet claim and Known-gaps section, fix ROADMAP.md's stale deferrals, add
the MIT LICENSE, delete the committed boilerplate, and self-host (or drop) the
Fandom-hotlinked seed character images.

Purpose: Documentation must match the running code, and the repo root must be
presentable — the last autonomous wave before the operator closes the phase.
Output: Accurate API/architecture/roadmap docs, LICENSE, junk removal,
self-hosted seed images.
</objective>

<execution_context>
@$HOME/AppData/Local/hermes/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Local/hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md (§Wave 4 docs guidance, §Validation Architecture: DOCS-04 test map)
@.planning/REQUIREMENTS.md (DOCS-04, PROB-10)
@docs/PROBLEMS.md (#21-#25, #28)
@docs/API.md
@docs/ARCHITECTURE.md
@data/dexter/seed/characters.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate docs/API.md from live OpenAPI + fix ARCHITECTURE.md and ROADMAP.md</name>
  <read_first>
    docs/API.md (stale hand-maintained counts — #21),
    docs/ARCHITECTURE.md (ChangeSet claim that proposed_change_set is always
    null — false since 07-07; §Known gaps listing shipped fixes — #22/#23),
    docs/ROADMAP.md (defers auth/CSRF/roles that shipped — #24),
    spoilerless/tests/test_frontend_contract_doc.py (the doc-contract test),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§Validation Architecture — DOCS-04 test map)
  </read_first>
  <files>docs/API.md, docs/ARCHITECTURE.md, docs/ROADMAP.md, spoilerless/tests/test_frontend_contract_doc.py</files>
  <action>
    Regenerate docs/API.md from the live backend OpenAPI: add (or document) a
    small regeneration command/script that dumps app.openapi() to the doc's
    route-inventory section (the doc must carry the LIVE number, and the
    doc-contract test test_frontend_contract_doc.py must assert the doc's
    counts equal the live OpenAPI so they cannot rot again). Include the
    phase-9 routes (path, export, share) in the inventory.

    Fix docs/ARCHITECTURE.md: the ChangeSet section must state that the
    pipeline emits proposed_change_set via the 12th allowlisted tool
    (propose_changeset, wired since 07-07); the Known-gaps section must be
    reduced to ONLY gaps that are still real (e.g. the evidence/sources claim
    visibility note); every shipped fix is removed; add a short subsection
    documenting the phase-9 capabilities that now exist (share links, export,
    search, timeline, dashboard, fcose graph).

    Fix docs/ROADMAP.md: the blockers paragraph must no longer defer
    auth/ownership/CSRF/roles — mark them shipped with pointers to the
    phase-9 PROB items and Phase 8 AUTH/SEC requirements.
    Commit with message "docs(09-15): regenerate API.md, correct ARCHITECTURE + ROADMAP (DOCS-04/#21-24)".
  </action>
  <verify>
    <automated>uv run pytest spoilerless/tests/test_frontend_contract_doc.py -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - docs/API.md route counts equal the live OpenAPI and the doc-contract test passes
    - ARCHITECTURE.md documents the ChangeSet capability and lists only real known gaps; phase-9 features subsection present
    - ROADMAP.md no longer defers shipped auth/CSRF/roles work
  </acceptance_criteria>
  <done>All three docs match the shipped code.</done>
</task>

<task type="auto">
  <name>Task 2: MIT LICENSE + junk removal + self-host (or drop) seed character images</name>
  <read_first>
    data/dexter/seed/characters.json (imageUrl fields hotlinking the third-party
    image host — #28),
    main.py at the repo root (the PyCharm template print_hi — #25),
    frontend/README.md (unmodified create-vite boilerplate — #25),
    .planning/phases/09-feature-expansion-full-audit-remediation/09-RESEARCH.md
    (§State of the Art: deprecated artifacts — root main.py and frontend/README.md
    are to be deleted; §Zero-cost attestation: self-hosting is a content task)
  </read_first>
  <files>LICENSE, main.py, frontend/README.md, data/dexter/seed/characters.json, data/dexter/images/</files>
  <action>
    Add LICENSE at the repo root: the MIT license text (user choice per
    09-DISCUSSION-LOG) with the current year and the project's copyright
    holder line.

    Delete committed junk: remove the root main.py (PyCharm hello-world
    template — git rm; verify it is tracked first with git ls-files) and
    frontend/README.md (untracked Vite boilerplate — verify with git ls-files
    before git rm; if untracked, plain delete). Add a brief real
    frontend/README.md only if the repo convention requires a frontend readme
    — otherwise leave the directory without the boilerplate.

    Self-host or drop the seed images: download the character portraits
    referenced by data/dexter/seed/characters.json into data/dexter/images/
    (organized per character id) and rewrite the imageUrl fields to relative
    in-repo paths served by the frontend (the existing no-referrer meta stays
    harmless), OR — if download is not feasible under the zero-cost constraint
    or rights concerns — remove the imageUrl fields entirely so the app falls
    back to the existing initial-based avatar treatment. Pick self-hosting
    where the images are available; document the choice in the commit body.
    Frontend impact: confirm the graph/detail image rendering handles both
    relative paths and absence (the existing imageUrl conditional already
    does — verify with a quick vitest run of the graph component tests).
    Commit with message "feat(09-15): MIT LICENSE, junk removal, self-hosted seed images (PROB-10/#25/#28)".
  </action>
  <verify>
    <automated>test -f LICENSE && head -3 LICENSE | grep -q "MIT"</automated>
    <automated>test ! -f main.py && test ! -f frontend/README.md</automated>
    <automated>rg -c "wikia|nocookie" data/dexter/seed/characters.json</automated>
    <automated>cd frontend && NODE_ENV=test CI=1 npx vitest run src/components/graph 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - LICENSE exists at the repo root with MIT text
    - Root main.py and frontend/README.md no longer exist in the tree
    - characters.json has zero third-party-image-host URLs — imageUrl points in-repo or is absent
    - Graph component vitest still passes with the changed image handling
  </acceptance_criteria>
  <done>The repo root is presentable and the seed images no longer hotlink.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs <-> live OpenAPI | Route counts must not drift from reality |
| Seed data <-> third-party host | No runtime dependency on the external image host |
| Repo root <-> committed artifacts | No boilerplate/junk in the tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-15-01 | Spoofing | Docs contradicting live behavior (#21-#24) | medium | mitigate | Regeneration from app.openapi + doc-contract test |
| T-09-15-02 | Information Disclosure | Fandom hotlink privacy/legal exposure (#28) | medium | mitigate | Self-host in data/dexter/images/ or drop imageUrl |
| T-09-15-03 | Tampering | License misattribution | low | mitigate | MIT text with the project's copyright line; user choice documented |
| T-09-15-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new dependencies; image download is a content task, not a package install |
</threat_model>

<verification>
Run the doc-contract test, the graph-component vitest, npm run build, and
npm run lint. Grep gates: `rg -c "wikia|nocookie" data/dexter/seed/characters.json`
returns 0; `test ! -f main.py && test ! -f frontend/README.md`; LICENSE head
contains MIT. Confirm `git status` shows no .env staged.
</verification>

<success_criteria>
API/architecture/roadmap docs match the shipped code (contract-test-locked),
the MIT license exists, junk is gone, and seed images are self-hosted or
dropped — the repo is presentable for the operator's final wave.
</success_criteria>

## Artifacts this phase produces

- `docs/API.md` (MODIFIED) — regenerated route inventory + regen command
- `docs/ARCHITECTURE.md` (MODIFIED) — ChangeSet capability, trimmed Known
  gaps, phase-9 capabilities subsection
- `docs/ROADMAP.md` (MODIFIED) — shipped blockers marked done
- `spoilerless/tests/test_frontend_contract_doc.py` (MODIFIED) — doc-vs-live
  count lock
- `LICENSE` (NEW) — MIT
- `main.py` (DELETED), `frontend/README.md` (DELETED)
- `data/dexter/seed/characters.json` (MODIFIED) — in-repo imageUrl or absent
- `data/dexter/images/` (NEW) — self-hosted portraits

<output>
Create `.planning/phases/09-feature-expansion-full-audit-remediation/09-15-SUMMARY.md` when done
</output>
