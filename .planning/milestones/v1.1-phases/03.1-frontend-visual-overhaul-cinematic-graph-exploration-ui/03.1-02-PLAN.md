---
phase: 03.1
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/detail/DetailPanel.tsx
  - frontend/src/components/detail/DetailPanel.test.tsx
  - frontend/src/components/detail/StructuralEdgeCard.tsx
autonomous: true
requirements: [D-06, D-07, D-08, D-11, D-31, D-32, D-33, D-34]
user_setup: []
must_haves:
  truths:
    - "The Overview tab shows structured, labeled metadata rows (Node Type / Name for nodes; Relationship / Claim Type / Status / Confidence for edges) instead of raw 'Type: X' / 'Label: X' / 'Predicate: X' text, with every field still displayed"
    - "Claims-tab cards carry a magenta (#D946EF) accent and Evidence-tab cards carry an orange (#FB923C) accent, without changing the underlying claim/evidence data rendered"
    - "StructuralEdgeCard keeps its distinct tab-less identity (zero Tabs/TabsList/TabsTrigger elements) after its visual polish pass — it is not merged into the tabbed DetailPanel"
    - "The detail panel narrows to a Sheet side=\"bottom\" drawer on narrow viewports and widens modestly on desktop, reusing the existing Sheet primitive with no new drawer component"
    - "No revision/history UI content is invented anywhere in either component (D-32 remains a hard no-op this phase)"
  artifacts:
    - "frontend/src/components/detail/DetailPanel.tsx with a restructured Overview tab and accented Claims/Evidence cards"
    - "frontend/src/components/detail/DetailPanel.test.tsx with its 2 literal-text assertions explicitly reviewed/updated in the same task that changed the markup they assert on"
    - "frontend/src/components/detail/StructuralEdgeCard.tsx visually polished while its existing 2 tests still pass unmodified"
  key_links:
    - "DetailPanel.tsx's resolveClaimsForSelection/resolveEvidenceForClaims -> the same already-fetched GraphResponse, unchanged (no new network round trip introduced by the restyle)"
    - "App.tsx's centralized claim_id-based branch (unchanged, not touched by this plan) -> continues routing structural edges to StructuralEdgeCard and everything else to DetailPanel"
---

<objective>
Restructure DetailPanel's Overview tab into labeled metadata rows and add magenta/orange Claims/Evidence card accents (D-07/D-31), and give StructuralEdgeCard the same visual polish while preserving its tab-less identity (D-06), per 03.1-CONTEXT.md and 03.1-UI-SPEC.md's Copywriting Contract and DetailPanel card accents sections — all without a network round trip, a field removal, or an architecture change.

Purpose: the raw "Type: Character" / "Predicate: works_at" text is 03.1-RESEARCH.md's confirmed root cause of the "avoid raw Type/Label text" complaint, and the premium detail-panel inspector is one of this phase's four named deliverables.
Output: a restyled DetailPanel.tsx and StructuralEdgeCard.tsx, with DetailPanel.test.tsx's 2 literal-text assertions explicitly and visibly updated (not silently broken).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-CONTEXT.md
@.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-RESEARCH.md
@.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-UI-SPEC.md
@frontend/src/components/detail/DetailPanel.tsx
@frontend/src/components/detail/StructuralEdgeCard.tsx
@frontend/src/components/detail/DetailPanel.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure Overview tab into labeled metadata rows, explicitly updating the 2 dependent test assertions</name>
  <files>frontend/src/components/detail/DetailPanel.tsx, frontend/src/components/detail/DetailPanel.test.tsx</files>
  <read_first>frontend/src/components/detail/DetailPanel.tsx, frontend/src/components/detail/DetailPanel.test.tsx, frontend/src/components/ui/card.tsx</read_first>
  <reversibility rating="reversible">Updates 2 existing DetailPanel.test.tsx literal-text assertions ('Type: Character', 'Predicate: works_at') to match the new structured-row markup, per 03.1-RESEARCH.md Pitfall 2 — a code-level test-assertion change, trivially revertible via git; the underlying displayed data (selectedNode.type/label, activeClaim.predicate/claim_type/status/confidence_level) is unchanged, only its markup shape and field-label copy change, so this is not a one-way data or architecture decision.</reversibility>
  <behavior>
    - For a selected Character node, the Overview tab renders a "Node Type" label next to the value "Character" and a "Name" label next to the value "Dexter Morgan" (from the S01E01 fixture) as separate, findable text nodes
    - For a selected claim-backed edge, the Overview tab renders "Relationship" / "Claim Type" / "Status" / "Confidence" labels next to their respective activeClaim field values, with none of the fields dropped
    - The no-selection placeholder text and the two locked empty-substate strings remain byte-identical
  </behavior>
  <action>
    In DetailPanel.tsx's TabsContent value="overview" block, replace the two raw &lt;p&gt;Type: {selectedNode.type}&lt;/p&gt;/&lt;p&gt;Label: {selectedNode.label}&lt;/p&gt; lines with a small structured label/value row pattern (e.g. a &lt;dl&gt; of &lt;div className="flex items-center justify-between"&gt;&lt;dt&gt;/&lt;dd&gt; pairs, or reused Card/CardContent primitives from frontend/src/components/ui/card.tsx) rendering label "Node Type" -> value {selectedNode.type}, and label "Name" -> value {selectedNode.label} — per 03.1-UI-SPEC.md's Copywriting Contract table, these are the exact new field labels (data values unchanged). Apply the identical row pattern to the edge branch: relabel "Predicate:" to "Relationship", "Claim type:" to "Claim Type", "Status:" to "Status", "Confidence:" to "Confidence", each still rendering {activeClaim.predicate} / {activeClaim.claim_type} / {activeClaim.status} / {activeClaim.confidence_level} — do not remove, rename, or reorder which underlying fields are shown, only their presentation and label copy. Preserve the exact locked strings elsewhere in the file verbatim: 'Select a node to see details.', 'No claims recorded for this node yet', 'No evidence recorded for this claim yet', and the 'Source: {source label} - {locator}' evidence line (plain hyphen, per STATE.md's locked Decision) — none of these are part of this restructure.
    In the SAME task, in DetailPanel.test.tsx, update the 2 test assertions that currently depend on the old combined-string markup: the `screen.findByText('Type: Character')` assertion (in the "renders all three tabs..." test) becomes two separate assertions — `screen.findByText('Node Type')` and `screen.findByText('Character')` (or `getByText` once resolved) — and the `screen.findByText('Predicate: works_at')` assertion (in the "renders the claim-backed edge Overview tab..." test) becomes `screen.findByText('Relationship')` plus `screen.findByText('works_at')`. This is an explicit, visible decision made in this same task per RESEARCH.md Pitfall 2 — not a silent break discovered later. Do not touch any of the other 6 tests in this file (portrait/fallback/empty-state tests are unaffected by this restructure).
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/detail/DetailPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/components/detail/DetailPanel.test.tsx` passes all 8 existing test cases (with the 2 updated assertions passing against the new markup)
    - `grep -c "Type: {selectedNode.type}" frontend/src/components/detail/DetailPanel.tsx` returns 0 (old raw text removed) and `grep -c "Node Type" frontend/src/components/detail/DetailPanel.tsx` returns at least 1
    - `grep -c "Select a node to see details." frontend/src/components/detail/DetailPanel.tsx` returns 1 (locked placeholder untouched)
  </acceptance_criteria>
  <done>Overview tab shows labeled Node Type/Name and Relationship/Claim Type/Status/Confidence rows for nodes and edges respectively, every field still present, and the 2 previously-dependent test assertions are explicitly updated in this same commit rather than left to fail later.</done>
</task>

<task type="auto">
  <name>Task 2: Claims/Evidence card accents, StructuralEdgeCard polish, responsive Sheet width breakpoints</name>
  <files>frontend/src/components/detail/DetailPanel.tsx, frontend/src/components/detail/StructuralEdgeCard.tsx</files>
  <read_first>frontend/src/components/detail/DetailPanel.tsx, frontend/src/components/detail/StructuralEdgeCard.tsx, frontend/src/components/ui/sheet.tsx</read_first>
  <action>
    Per D-07/03.1-UI-SPEC.md's DetailPanel card accents table: on each Claims-tab item wrapper div (the existing `.map((claim) => ...)` block rendering `rounded-md border border-border p-2`), add a left-border accent using the magenta hex #D946EF (e.g. an additional class producing a 4px left border with that literal color, or an inline style `borderLeftColor`/`borderLeftWidth`) without changing the border/background elsewhere on the card or the text content inside it. Apply the same treatment to each Evidence-tab item wrapper div using the orange hex #FB923C (same hex reused intentionally for the provenance edge family per D-07/Ground-Truth findings — that reuse is documented context, not something to re-derive here). Keep both accent hex values as local named constants in this file (e.g. CLAIM_ACCENT_COLOR / EVIDENCE_ACCENT_COLOR) rather than scattering the literal string at each call site.
    Per D-33/03.1-UI-SPEC.md's Responsive degradation table, on DetailPanel.tsx's `<SheetContent side="right" ...>`: widen the desktop (>=1024px) width from the current implicit `sm:max-w-sm` (384px) to `sm:max-w-md` (448px) via a `lg:` variant so the medium breakpoint (640-1023px) keeps the existing 384px default, and add a `max-sm:` (or equivalent narrow-viewport) variant switching to `side="bottom"` with `h-auto max-h-[70vh]` — implement this by conditionally choosing the `side` prop's value or, if a single Sheet instance cannot conditionally change its `side` prop cleanly, via responsive Tailwind classes on `className` that override `SheetContent`'s own `data-[side=...]` positioning classes for the narrow case; reuse the existing Sheet/SheetContent primitives exactly as they are (D-33 explicitly forbids a new drawer/sheet primitive) and do not touch the `open modal={false}` props or the `showCloseButton={false}` behavior.
    Restyle StructuralEdgeCard.tsx's visual treatment (card-like padding/spacing, an arrow icon from `lucide-react`'s `ArrowRight` between the `{sourceLabel}` and `{targetLabel}` text instead of the plain `→` character, matching the `AppShell.tsx`/`Sheet` token conventions) while keeping the exact same rendered heading (`{selected.edgeType}`) and the exact source/target label resolution logic (`resolveLabel`) — do not import `Tabs`/`TabsList`/`TabsTrigger` anywhere in this file (Phase 2 D-06's tab-less identity, restated by 03.1-CONTEXT.md D-31, must survive this polish pass). Apply the identical responsive Sheet width/breakpoint treatment described above to this file's own `<SheetContent>` for visual consistency between the two detail surfaces.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/components/detail/DetailPanel.test.tsx src/components/detail/StructuralEdgeCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run src/components/detail/StructuralEdgeCard.test.tsx` passes both existing tests unmodified, including the "renders no TabsList/TabsTrigger elements" assertion
    - `grep -c "Tabs" frontend/src/components/detail/StructuralEdgeCard.tsx` returns 0
    - `grep -c "D946EF" frontend/src/components/detail/DetailPanel.tsx` and `grep -c "FB923C" frontend/src/components/detail/DetailPanel.tsx` each return at least 1
    - `grep -c "sm:max-w-md" frontend/src/components/detail/DetailPanel.tsx` returns at least 1
  </acceptance_criteria>
  <done>Claims cards show a magenta left accent, Evidence cards show an orange left accent, StructuralEdgeCard is visually polished but still contains zero Tabs elements and passes its 2 existing tests unmodified, and both Sheets widen on desktop / drop to a bottom drawer on narrow viewports using the existing Sheet primitive only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend GraphResponse (claim/evidence/node label text) -> restyled DetailPanel/StructuralEdgeCard markup | Already-spoiler-filtered text rendered into new structured-row/card markup this plan introduces |
| Fandom CDN image (unchanged this plan) -> CharacterPortrait `<img>` | Untrusted third-party response; this plan does not touch CharacterPortrait's implementation, only re-verifies its existing mitigations survive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-04 | Tampering / Information Disclosure | New structured-row/card markup in DetailPanel.tsx | low | mitigate | React's default JSX text-node escaping is sufficient as long as no new unsafe-HTML-injection API is introduced anywhere in this restyle; verified by an unchanged-at-zero repo-wide grep count in `<verification>` below |
| T-03.1-05 | Information Disclosure | CharacterPortrait's `<img>` referrer behavior | low | accept (pre-existing control, not modified) | This plan does not edit CharacterPortrait's implementation; the existing `referrerPolicy="no-referrer"` attribute is re-verified present, not re-implemented |

No new form input, API call, or third-party script execution is introduced — this plan only restructures already-fetched, already-filtered data into new presentational markup.
</threat_model>

<verification>
- `cd frontend && npm run test -- --run` — full existing suite green (all 7 files, including this plan's 2 updated + otherwise-unmodified assertions)
- `cd frontend && npm run build` — clean
- `cd frontend && npm run lint` — clean
- `git diff --stat -- backend/` — must be empty (D-01)
- `grep -rc "dangerouslySetInnerHTML" frontend/src | awk -F: '{s+=$2} END {print s+0}'` — must remain 0 (no new unsafe-HTML API introduced by this restyle)
- `grep -c 'referrerPolicy="no-referrer"' frontend/src/components/detail/DetailPanel.tsx` — must be 1 (CharacterPortrait's existing mitigation survives untouched)
</verification>

<success_criteria>
Overview tab displays every existing field via clearly labeled rows (no raw "Type:"/"Predicate:" text); Claims/Evidence cards carry their documented magenta/orange accents; StructuralEdgeCard keeps its tab-less identity and passes its existing tests unmodified; both detail surfaces respond to viewport width using only the existing Sheet primitive; zero new dangerouslySetInnerHTML uses; zero backend changes.
</success_criteria>

<output>
Create `.planning/phases/03.1-frontend-visual-overhaul-cinematic-graph-exploration-ui/03.1-02-SUMMARY.md` when done
</output>

## Artifacts This Plan Produces

- No new files. Modifies `DetailPanel.tsx` (restructured Overview tab, Claims/Evidence card accent constants `CLAIM_ACCENT_COLOR`/`EVIDENCE_ACCENT_COLOR`, responsive Sheet width classes) and `StructuralEdgeCard.tsx` (visual polish, responsive Sheet width classes) — no new exported types, no new npm dependencies, no new CSS custom properties (accent colors are local literal constants per D-15's narrower scope, not global tokens).
- Updates 2 existing `DetailPanel.test.tsx` assertions to match the new Overview-tab markup shape (explicit, documented change — not a silent regression).
