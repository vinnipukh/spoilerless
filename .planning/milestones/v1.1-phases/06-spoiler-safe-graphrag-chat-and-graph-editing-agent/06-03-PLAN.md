---
phase: 06-spoiler-safe-graphrag-chat-and-graph-editing-agent
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/app/repository/progress.py
  - backend/app/services/progress.py
  - backend/app/api/progress.py
  - backend/tests/test_progress_api.py
autonomous: true
requirements: [RAG-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /api/series/{series_id}/progress for one authenticated user never returns another user's UserSeriesProgress record, even when both users share the same series_id."
    - "Requesting another user's progress produces the same generic not-found-style response as requesting progress for a series the current user has never watched — no distinguishing error."
    - "Missing/invalid progress fails safely: retrieval requests behave as if the visible set is empty, never a 500 or an error message revealing series existence."
    - "A progress update persists and is immediately readable via GET by the same user."
    - "Setting requested progress equal to the current persisted value succeeds identically to a genuine change (idempotent), never rejected as a no-op error."
    - "visible_until_order supplied on the request body of POST /api/series/{series_id}/progress cannot cause the persisted GraphRAG-relevant value to diverge from what a subsequent GET returns for the same user - the write path and read path agree."
    - { statement: "Concurrent progress-update requests for the same (user, series) resolve via the MERGE-based upsert's atomic semantics with no torn/partial state under simulated concurrent calls.", verification: backstop }
  prohibitions:
    - statement: "The system must never trust a frontend- or model-supplied visible_until_order — every GraphRAG/retrieval/mutation request resolves the boundary from persisted server-side UserSeriesProgress, never from a query parameter or body field on a new chat/tool/progress-consuming endpoint."
      status: kept
      verification: unverified
  artifacts:
    - backend/app/repository/progress.py
    - backend/app/services/progress.py
    - backend/app/api/progress.py
  key_links:
    - "backend/app/api/progress.py -> backend/app/api/deps.py::require_current_user -> backend/app/repository/progress.py (user_id-scoped MATCH)"
---

<objective>
Complete RAG-01's ownership and fail-closed guarantees on top of 06-01's tracer-level `UserSeriesProgress`
persistence: cross-user access rejection, missing/invalid-progress fail-closed behavior, and update
idempotency — the full RAG-01 requirement, not just the happy path the tracer proved.

Purpose: `UserSeriesProgress` is the single source of truth every retrieval tool and the ChangeSet
pipeline resolve against. A gap here (e.g. one user reading another's progress, or a missing-progress
request returning a 500 that leaks series existence) undermines every later plan's spoiler guarantee.
Output: a fully ownership-scoped, fail-closed `ProgressRepository`/`ProgressService`/`api/progress.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-CONTEXT.md
@.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-RESEARCH.md
@.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-user rejection, fail-closed missing progress, idempotent update</name>
  <read_first>
    backend/app/repository/progress.py, backend/app/services/progress.py, backend/app/api/progress.py,
    backend/app/repository/user_content.py, backend/tests/test_progress_api.py, backend/tests/test_auth.py
  </read_first>
  <behavior>
    - Test: authenticated User A's GET /api/series/{series_id}/progress never returns User B's `UserSeriesProgress` node even when a Cypher `MATCH` without a `user_id` filter would find one for the same `series_id` — the query itself is `user_id`-scoped, not filtered post-fetch in Python.
    - Test: GET for a series the current user has never watched returns the identical response shape/status as GET for a series that does not exist at all.
    - Test: `ProgressService.resolve` for a user/series with no `UserSeriesProgress` row raises `ProgressNotFoundError`, and every caller of `resolve` (in 06-01's pipeline) treats that as an empty visible set, never propagates a raw 500.
    - Test: POST progress with `visible_until_order` equal to the currently persisted value returns 200 with the same value, not a 409/400.
    - Test: POST progress from an unauthenticated request returns 401 via `require_current_user`, not a different error shape.
  </behavior>
  <files>backend/app/repository/progress.py, backend/app/services/progress.py, backend/app/api/progress.py, backend/tests/test_progress_api.py</files>
  <action>
    Confirm and, if needed, tighten `backend/app/repository/progress.py::get`/`upsert` so every Cypher
    `MATCH`/`MERGE` includes `user_id: $user_id` directly in the node pattern (never a separate
    post-fetch ownership check in Python) — mirroring the mandatory-ownership pattern this codebase has
    no prior example of (06-RESEARCH.md Q7's "critical gap" finding: this is the first ownership-enforced
    resource in the codebase, built fresh). Confirm `backend/app/services/progress.py::ProgressService.
    resolve` raises `ProgressNotFoundError` (not a generic exception) on a missing row, and that
    `backend/app/api/progress.py`'s GET handler catches it and returns a generic empty/404-style
    response identical in shape to "series not found" (reuse the existing `http_error` shared envelope,
    same status code and a generic message, never a message that distinguishes "you have no progress
    here" from "this series does not exist"). Confirm the POST handler's upsert path is idempotent for
    an unchanged value (asserted by the existing `MERGE`-based `PROGRESS_UPSERT_QUERY` from 06-01 — no
    additional guard needed unless testing reveals a gap). Expand
    `backend/tests/test_progress_api.py` with the full RAG-01 test matrix from `06-PRD-SOURCE.md`
    section 17 "Progress": backend resolves stored progress, frontend cannot raise `visible_until_order`
    through a request, missing progress fails safely, progress update persists, cross-user rejection.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_progress_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/graph/progress.py`'s `PROGRESS_GET_QUERY` includes `user_id: $user_id` inside the node pattern itself (not appended as a Python-side filter after `execute_query` returns rows).
    - GET response for "another user's progress" and GET response for "nonexistent series" are asserted identical in status code and error `code` field.
    - `pytest tests/test_progress_api.py -x` passes with 0 failures, covering every RAG-01 PRD §17 test bullet.
  </acceptance_criteria>
  <done>Cross-user progress access is impossible at the query level (not just filtered post-fetch); missing/invalid progress fails closed with a generic response; progress updates are idempotent and immediately readable.</done>
</task>

<task type="auto">
  <name>Task 2: Concurrency backstop + full-suite regression check</name>
  <read_first>backend/app/repository/progress.py, backend/tests/test_progress_api.py</read_first>
  <files>backend/tests/test_progress_api.py</files>
  <action>
    Add a test exercising two near-simultaneous `upsert` calls for the same `(user_id, series_id)` with
    different `visible_until_order` values (using `asyncio.gather` over two repository calls in the test
    itself) and assert the resulting persisted value is one of the two submitted values (no torn/partial
    write, no exception) — a backstop-level concurrency check, not an exhaustive load test. Run the full
    backend suite once to confirm no regression in the existing `/api/series/{id}/graph`,
    `/api/user-content/*`, `/api/revisions/*`, or `/api/auth/*` endpoints from this plan's changes (this
    plan touches only new `progress.py` files, so a clean full-suite pass here is a fast, cheap
    confirmation before later plans build on it).
  </action>
  <verify>
    <automated>cd backend && uv run pytest</automated>
  </verify>
  <acceptance_criteria>
    - Concurrency backstop test passes without flaking across at least one CI run.
    - Full backend suite (`uv run pytest`) passes with 0 failures.
  </acceptance_criteria>
  <done>Concurrent-write backstop test passes; full backend suite green with no regression from this plan's changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> GET/POST /api/series/{series_id}/progress | Authenticated but potentially cross-user request |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information Disclosure / Elevation of Privilege | repository/progress.py, api/progress.py | high | mitigate | Every progress query is `user_id`-scoped at the Cypher `MATCH` level, generic response for both cross-user and nonexistent-series cases (Task 1) |
| T-06-03 | Tampering | api/progress.py | high | mitigate | `resolve()` always reads server-persisted state; no request body/query param can substitute for it in any GraphRAG-consuming code path (Task 1) |
</threat_model>

<verification>
`cd backend && uv run pytest tests/test_progress_api.py -x` then full suite `cd backend && uv run pytest`
</verification>

<success_criteria>
Cross-user progress access is provably impossible; missing/invalid progress fails closed generically;
updates are idempotent; full backend suite remains green.
</success_criteria>

<output>
Create `.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-03-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces (this plan's contribution)

No new symbols — this plan hardens `ProgressRepository`/`ProgressService`/`api/progress.py` created in 06-01.
