---
phase: 06-spoiler-safe-graphrag-chat-and-graph-editing-agent
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/app/repository/chat.py
  - backend/app/services/chat.py
  - backend/app/api/chat.py
  - backend/app/domain/chat.py
  - backend/tests/test_chat_persistence.py
  - backend/tests/test_chat_api.py
  - backend/tests/test_openapi_contract.py
  - backend/tests/test_frontend_contract_doc.py
  - docs/frontend-api-contract.md
autonomous: true
requirements: [RAG-09, RAG-10]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A ChatMessage with visible_until_order_snapshot exactly equal to the user's current progress is visible; one order above is hidden; one order below is visible."
    - "Lowering progress to exactly a message's snapshot value keeps that message visible (inclusive boundary); one step below hides it — the RAG-09 regression scenario."
    - "The Episode-3-then-Episode-1 regression: a user at Episode 3 asks a question, gets an Episode-3 answer, moves to Episode 1, reopens the chat — the Episode-3 message does not appear in the message list, session preview, session title, or citations, and does not enter LLM conversation memory on the next question."
    - "Raising progress back to Episode 3 re-reveals the previously hidden message without re-creating it (same message ID, same content)."
    - "Hidden messages are never deleted from Neo4j merely because progress decreased."
    - "A ChatSession with zero messages returns an empty message list and a session-list preview with no content, never an error."
    - "Bounded message/history length is measured in Unicode code points; Turkish message text is not truncated mid-character."
    - "Messages are returned in stable created_at-ascending order for both the API response and the LLM conversation-memory load, using the identical shared filtered query."
    - "A session-list request returning exactly one session yields a single-item list."
    - "A user with zero chat sessions for a series receives an empty list from GET .../sessions, not a 404."
    - "Sessions list newest-first (updated_at descending)."
    - "DELETE .../sessions/{session_id} for another user's session or a nonexistent session returns the identical generic 404."
    - "Bounded concurrent generations per user is enforced — a second concurrent generation request from the same user while one is in-flight is rejected with a clear error, never silently dropped or overwritten."
    - "API message/session counts never reveal the number of hidden messages beyond the current boundary."
    - { statement: "created_at/updated_at timestamps use consistent UTC ISO-8601 precision across ChatSession/ChatMessage/UserSeriesProgress; sub-second ordering under rapid successive writes is not independently stress-tested.", verification: backstop }
    - { statement: "Two concurrent message-posts in the same session from the same user are both persisted with no lost update; relative ordering is whichever the database commits first.", verification: backstop }
    - { statement: "Retrying a DELETE session request twice returns the same terminal result (204 then 404) both times, never a duplicate side effect.", verification: backstop }
  prohibitions:
    - statement: "Hidden chat messages must never be deleted merely because watch progress decreased — they are hidden, not destroyed, and become visible again if progress is raised."
      status: kept
      verification: unverified
  artifacts:
    - backend/app/repository/chat.py
    - backend/app/api/chat.py
  key_links:
    - "backend/app/repository/chat.py::list_messages_for_context / list_messages_for_response share one WHERE visible_until_order_snapshot <= $boundary filter"
    - "backend/app/api/chat.py DELETE session -> backend/app/repository/chat.py ownership two-query pattern"
---

<objective>
Harden 06-01's tracer-level chat persistence into the full RAG-09/RAG-10 requirement: the exact
Episode-3-then-Episode-1 hide-not-delete regression test, full session CRUD (including DELETE),
bounded concurrent generations, cross-user/cross-series 404 behavior, and the mandatory contract-test
updates for any new route this plan adds.

Purpose: RAG-09's critical regression scenario is explicitly named by CONTEXT.md as "must have a test" —
this is the single most spoiler-safety-load-bearing test in the chat-persistence surface. Getting the
shared-filter discipline right here (one query serving both API response and LLM context loading) is
what prevents a since-hidden answer from re-entering the model's memory.
Output: hardened `ChatRepository`/`ChatService`/`api/chat.py` with full CRUD, the RAG-09 regression
test, and updated contract-inventory tests/docs for the DELETE route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-CONTEXT.md
@.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-RESEARCH.md
@.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RAG-09 hide-not-delete regression test + boundary-exactness matrix</name>
  <read_first>
    backend/app/repository/chat.py, backend/app/services/chat.py, backend/app/domain/chat.py,
    backend/tests/test_chat_persistence.py
  </read_first>
  <behavior>
    - Test (the named regression scenario): seed a user at progress=3, create a session, post a question, receive and persist an assistant message with `visible_until_order_snapshot=3` and a citation to an Episode-3-only claim; lower progress to 1; reopen the session (GET messages) -> the Episode-3 message is absent from the list, absent from the session preview/title, and absent from any citation list; ask a new question -> the pipeline's conversation-memory load (via `list_messages_for_context`) does not include the Episode-3 message's content anywhere in what is sent to the LLM provider.
    - Test: raise progress back to 3 -> GET messages now includes the same message (same `id`, same `content`, not a duplicate).
    - Test: directly query Neo4j (via the repository, not the API) after the progress decrease -> the `ChatMessage` node still exists (not deleted).
    - Test: a message with `visible_until_order_snapshot` exactly equal to current progress is visible; one order above is hidden; one order below is visible.
  </behavior>
  <files>backend/app/repository/chat.py, backend/tests/test_chat_persistence.py</files>
  <action>
    Confirm (and complete if 06-01 left gaps) that `backend/app/repository/chat.py::list_messages_for_context`
    and `list_messages_for_response` are the same underlying parameterized query (or call one shared
    private helper) filtering `WHERE m.visible_until_order_snapshot <= $visible_until_order` — never two
    independently maintained filters (Pitfall 1 in 06-RESEARCH.md). Confirm no code path issues a Cypher
    `DELETE` against a `ChatMessage` node in response to a progress decrease — hiding is purely a
    read-time filter, never a write. Add `backend/tests/test_chat_persistence.py` implementing the exact
    regression scenario above using the real `ChatRepository`/`ProgressRepository` against live Neo4j
    (not mocked), asserting both the API-shaped result and a direct repository-level existence check.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_chat_persistence.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_chat_persistence.py` contains a test function whose body implements all five regression-scenario steps from CONTEXT.md's "Critical regression scenario" verbatim (progress to 3, ask, receive, lower to 1, reopen).
    - Grepping `backend/app/repository/chat.py` for `DELETE` finds no Cypher `DELETE` clause targeting `ChatMessage` triggered by any progress-related code path (session-delete's own DELETE, if any, is a distinct, explicitly user-initiated action, not a progress-decrease side effect).
    - `pytest tests/test_chat_persistence.py -x` passes with 0 failures.
  </acceptance_criteria>
  <done>The Episode-3-then-Episode-1 regression test passes end-to-end against real Neo4j: hidden messages are excluded from every read path (API, preview, LLM memory) but never deleted, and re-appear when progress is raised again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full session CRUD (DELETE), ownership 404s, bounded concurrency + contract updates</name>
  <read_first>
    backend/app/api/chat.py, backend/app/repository/chat.py, backend/tests/test_openapi_contract.py,
    backend/tests/test_frontend_contract_doc.py, docs/frontend-api-contract.md
  </read_first>
  <behavior>
    - Test: `DELETE /api/series/{series_id}/chat/sessions/{session_id}` for the owning user succeeds (204 or equivalent); the session's messages become inaccessible via GET afterward.
    - Test: same DELETE for another user's session, or a nonexistent session ID, both return the identical generic 404.
    - Test: a second concurrent `POST .../messages` for the same user while one is still generating is rejected with a clear, non-500 error (not silently queued or dropped).
    - Test: an unauthenticated request to any chat route returns 401 via `require_current_user`.
    - Test: a cross-series session_id (belonging to a different series than the URL's series_id) returns the same generic 404 as a nonexistent one.
  </behavior>
  <files>backend/app/api/chat.py, backend/app/services/chat.py, backend/tests/test_openapi_contract.py, docs/frontend-api-contract.md</files>
  <action>
    Add `DELETE /api/series/{series_id}/chat/sessions/{session_id}` to `backend/app/api/chat.py`, using
    the ownership two-query pattern from 06-PATTERNS.md (try the `user_id`-scoped delete first; on
    no-match, do not distinguish "not found" from "not yours" in the response — identical generic 404 in
    both cases, per RAG-03/RAG-13's "hidden and missing are indistinguishable" principle applied to
    ownership too). Add a simple per-user in-process concurrent-generation counter (this codebase has no
    existing rate-limit middleware to reuse per 06-RESEARCH.md's Security Domain table — build a minimal
    counter, e.g. a module-level `dict[str, int]` guarded appropriately, incremented on stream start and
    decremented on stream end/disconnect, rejecting a new generation when the per-user count is already
    at its bound) in `backend/app/services/chat.py`. Update
    `backend/tests/test_openapi_contract.py`/`test_frontend_contract_doc.py`'s closed-inventory
    expected sets and `docs/frontend-api-contract.md` for the new DELETE route (this is the mandatory
    same-commit contract-inventory update per 06-RESEARCH.md's Wave-0 blocker note — apply it in every
    wave that adds a route, not just 06-01).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_chat_api.py tests/test_openapi_contract.py tests/test_frontend_contract_doc.py -x</automated>
  </verify>
  <acceptance_criteria>
    - DELETE for another user's session and DELETE for a nonexistent session return the same HTTP status and the same error `code` field.
    - The concurrent-generation counter is decremented on both normal completion and client-disconnect paths (not just the happy path) — verified by a test that simulates disconnect.
    - `test_openapi_contract.py`/`test_frontend_contract_doc.py`'s closed-inventory counts include the new DELETE route.
    - `pytest tests/test_chat_api.py tests/test_openapi_contract.py tests/test_frontend_contract_doc.py -x` passes with 0 failures.
  </acceptance_criteria>
  <done>DELETE session works with generic cross-user/cross-series/missing 404s; bounded concurrent generations are enforced and correctly released on disconnect; contract-inventory tests and docs reflect the new route.</done>
</task>

<task type="auto">
  <name>Task 3: Turkish-text bounded-length + ordering + count-leakage tests</name>
  <read_first>backend/app/repository/chat.py, backend/app/api/chat.py, backend/tests/test_chat_persistence.py</read_first>
  <files>backend/tests/test_chat_persistence.py, backend/tests/test_chat_api.py</files>
  <action>
    Add tests asserting: message/history-length bounds are enforced on Python `len()` (Unicode
    code-point count) against Turkish-language message content containing non-ASCII characters (e.g.
    "İ", "ı", "ş", "ğ") without truncating mid-character; messages within a session return in stable
    `created_at`-ascending order across repeated identical GET calls; sessions list newest-first by
    `updated_at`; and a session's message-count/preview response never numerically differs between a
    user with N visible messages and one with N visible + M hidden messages of the same session (i.e.
    the count exposed to the client always reflects only currently-visible messages, never the true
    total including hidden ones).
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_chat_persistence.py tests/test_chat_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Turkish-text length-bound test passes without truncating a multi-byte character mid-sequence.
    - Count-leakage test explicitly compares a visible-only count against a true-total count and asserts they differ (proving the count is boundary-scoped, not raw).
    - `pytest tests/test_chat_persistence.py tests/test_chat_api.py -x` passes with 0 failures.
  </acceptance_criteria>
  <done>Bounded lengths respect Unicode code points for non-ASCII/Turkish text, message/session ordering is stable, and no API response numerically leaks the count of hidden messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> DELETE session | Authenticated but potentially cross-user/cross-series request |
| Progress-decrease -> chat history read path | Must hide, never delete, and must feed the same filter into LLM memory loading |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Information Disclosure | repository/chat.py | high | mitigate | Shared filtered query for context-load and API-response paths; explicit regression test for the Episode-3-then-1 scenario (Task 1) |
| T-06-04 | Information Disclosure / Elevation of Privilege | api/chat.py DELETE | high | mitigate | Ownership two-query pattern, identical generic 404 for cross-user/cross-series/missing (Task 2) |
| T-06-13 | Denial of Service | services/chat.py | medium | mitigate | Per-user concurrent-generation counter, decremented on disconnect (Task 2) |
</threat_model>

<verification>
`cd backend && uv run pytest tests/test_chat_persistence.py tests/test_chat_api.py tests/test_openapi_contract.py tests/test_frontend_contract_doc.py -x`
</verification>

<success_criteria>
The RAG-09 regression scenario passes against real Neo4j; DELETE session works with generic ownership
404s; concurrent generations are bounded; no response ever numerically leaks hidden-message counts.
</success_criteria>

<output>
Create `.planning/phases/06-spoiler-safe-graphrag-chat-and-graph-editing-agent/06-04-SUMMARY.md` when done
</output>

---

## Artifacts this phase produces (this plan's contribution)

New route: `DELETE /api/series/{series_id}/chat/sessions/{session_id}`. New internal symbol: the
per-user concurrent-generation counter in `backend/app/services/chat.py`.
