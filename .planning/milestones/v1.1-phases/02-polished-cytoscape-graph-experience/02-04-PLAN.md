---
phase: 02-polished-cytoscape-graph-experience
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - frontend/src/components/episode/ConfirmAdvanceModal.test.tsx
  - frontend/src/hooks/useWatchProgress.test.ts
  - .planning/phases/02-polished-cytoscape-graph-experience/02-VALIDATION.md
autonomous: false
requirements:
  - UI-02
  - UI-05
security_enforcement: true
asvs_level: 1

must_haves:
  truths:
    - "npm run build, npm run lint, and npm run test -- --run all exit 0 in a single local run, covering every component test created across Plans 01-04."
    - "A conversational UAT pass confirms the Definition-of-Done demo script (select Dexter/S01E01 -> inspect a character and a claim's evidence -> attempt to advance to S01E02 -> confirmation modal appears -> confirm -> newly unlocked S01E02 nodes/relationships appear) with no S01E02/S01E03 content visible before confirmation."
    - "ConfirmAdvanceModal's sessionStorage-hydration path never opens the modal on a simulated page-refresh remount, and a corrupted sessionStorage value falls back to the D-01 empty state without throwing, both proven by dedicated automated tests (not just the Plan 01 happy-path test)."
    - "Selecting the same episode that is already the current confirmed watch progress does not open ConfirmAdvanceModal or issue a new /api/graph request (no-op edge case around D-03's 'every change' language)."
  artifacts:
    - frontend/src/components/episode/ConfirmAdvanceModal.test.tsx - forward/backward/no-op copy and gating tests
    - frontend/src/hooks/useWatchProgress.test.ts - sessionStorage hydration-guard and corruption-fallback unit tests
    - .planning/phases/02-polished-cytoscape-graph-experience/02-VALIDATION.md - Per-Task Verification Map filled in with actual plan/wave/task IDs, nyquist_compliant set true
  key_links:
    - useWatchProgress.test.ts to hydration guard (Plan 01) - proves the "hydration never opens modal" truth with a dedicated unit test rather than relying on the Plan 01 App.test.tsx happy path alone
    - Full suite command to every prior plan's test file - this plan's verification is the first point where all test files run together
  prohibitions:
    - statement: "Automated checks must not be satisfied by scaffold or file presence alone - every test asserted in this plan must actually execute and pass, not merely exist (release-gate principle: file presence or scaffold status alone cannot pass a gate)."
      status: resolved
      verification: explicit
    - statement: "No component in frontend/src may use dangerouslySetInnerHTML for evidence, claim, or locator text - verified permanently via a grep audit, not just at authoring time."
      status: resolved
      verification: explicit
    - statement: "No component in frontend/src may filter or exclude graph nodes/edges by visible_from_order client-side - verified permanently via a grep audit of graphElements.ts and GraphCanvas.tsx, not just at authoring time."
      status: resolved
      verification: explicit
---

<objective>
Close the remaining Nyquist validation gaps identified in 02-RESEARCH.md's Validation Architecture and 02-VALIDATION.md's Wave 0 Requirements: dedicated tests for `useWatchProgress`'s sessionStorage hydration guard and corruption fallback (only exercised indirectly by Plan 01's happy-path `App.test.tsx` so far), a same-episode no-op edge case, and a permanent grep-based audit that the two most severe threat mitigations (no `dangerouslySetInnerHTML`, no client-side spoiler re-filtering) hold across the whole `frontend/src` tree - not just in the files that introduced them. Then run the full suite together for the first time and record the result in 02-VALIDATION.md.

Purpose: satisfy UI-05's requirement that "frontend build/lint/component checks and demo UX checks verify safe progress changes and absence of hidden-data rendering," and close the specific coverage gap the Package Legitimacy Audit / Validation Architecture flagged for `useWatchProgress`.
Output: A green full test suite, a completed 02-VALIDATION.md Per-Task Verification Map, and a documented conversational-UAT pass ready for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-polished-cytoscape-graph-experience/02-RESEARCH.md
@.planning/phases/02-polished-cytoscape-graph-experience/02-VALIDATION.md
@.planning/phases/02-polished-cytoscape-graph-experience/02-01-SUMMARY.md
@.planning/phases/02-polished-cytoscape-graph-experience/02-02-SUMMARY.md
@.planning/phases/02-polished-cytoscape-graph-experience/02-03-SUMMARY.md
@frontend/src/hooks/useWatchProgress.ts
@frontend/src/components/episode/ConfirmAdvanceModal.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dedicated useWatchProgress and ConfirmAdvanceModal edge-case tests</name>
  <files>
    frontend/src/hooks/useWatchProgress.test.ts
    frontend/src/components/episode/ConfirmAdvanceModal.test.tsx
  </files>
  <read_first>
    frontend/src/hooks/useWatchProgress.ts
    frontend/src/components/episode/ConfirmAdvanceModal.tsx
    frontend/src/App.test.tsx
    .planning/phases/02-polished-cytoscape-graph-experience/02-VALIDATION.md
  </read_first>
  <behavior>
    - `useWatchProgress` pre-seeded with a valid `sessionStorage` entry hydrates `confirmedSeriesId`/`confirmedOrder` on first render without ever setting `pendingChange`.
    - `useWatchProgress` pre-seeded with a corrupted `sessionStorage` value (invalid JSON, or a `visibleUntilOrder` of `0`/negative/non-integer/missing) falls back to the D-01 empty (`null`) state and does not throw during render.
    - Calling `requestChange(seriesId, sameOrderAsCurrent)` (reselecting the current episode) does not set `pendingChange` and does not change `confirmedOrder`.
    - `ConfirmAdvanceModal` renders the forward-direction copy ("Unlock S01E0X?"/"...new characters, events, and relationships from S01E0X...") when direction is `'forward'`, and the backward-direction copy ("Rewatch S01E0X?"/"...move your watch progress back to S01E0X...") when direction is `'backward'`.
  </behavior>
  <action>
    1. Create frontend/src/hooks/useWatchProgress.test.ts (Vitest + `@testing-library/react`'s `renderHook`): seed `sessionStorage` with a valid `{seriesId, visibleUntilOrder}` entry before rendering the hook and assert `pendingChange` is `null`/undefined immediately after mount (hydration never opens the modal); seed `sessionStorage` with an invalid JSON string and assert the hook renders without throwing and starts in the D-01 empty state; seed with `{seriesId: 'series_dexter', visibleUntilOrder: 0}` and `{seriesId: '', visibleUntilOrder: 1}` and assert both are rejected by the shape guard and fall back to empty state; call `requestChange('series_dexter', N)` where `N` equals the currently-confirmed order and assert `pendingChange` remains unset.
    2. Create frontend/src/components/episode/ConfirmAdvanceModal.test.tsx (Vitest + RTL + `@testing-library/user-event`): render with a `direction='forward'` prop/pending-change shape and assert the literal forward copy strings render; render with `direction='backward'` and assert the literal backward copy strings render; assert clicking "Cancel" calls the provided `onCancel` callback and clicking "Yes, unlock episode" calls `onConfirm`.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run src/hooks/useWatchProgress.test.ts src/components/episode/ConfirmAdvanceModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- --run src/hooks/useWatchProgress.test.ts` exits 0 and includes a passing assertion that `pendingChange` is unset after hydrating from a valid sessionStorage entry.
    - `cd frontend && npm run test -- --run src/hooks/useWatchProgress.test.ts` includes a passing assertion that a corrupted sessionStorage value does not throw and falls back to the empty state.
    - `cd frontend && npm run test -- --run src/components/episode/ConfirmAdvanceModal.test.tsx` exits 0 and asserts both the forward and backward copy variants render their exact locked/discretion strings.
  </acceptance_criteria>
  <done>useWatchProgress's hydration guard, corruption fallback, and same-episode no-op are each covered by a dedicated passing unit test independent of App.test.tsx's happy path; ConfirmAdvanceModal's forward and backward copy variants are each covered by a dedicated passing component test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full-suite verification, threat-mitigation grep audit, and demo UAT</name>
  <what-built>Every automated test file created across Plans 01-04 (App.test.tsx, GraphCanvas.test.tsx, DetailPanel.test.tsx, StructuralEdgeCard.test.tsx, useWatchProgress.test.ts, ConfirmAdvanceModal.test.tsx) run together for the first time, plus a permanent grep-based audit that the two highest-severity threat mitigations from this phase's threat models hold across the entire frontend/src tree, plus 02-VALIDATION.md's Per-Task Verification Map filled in with real task/plan/wave IDs.</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run test -- --run` and confirm every test file passes with 0 failures.
    2. Run `cd frontend && npm run build && npm run lint` and confirm both exit 0.
    3. Run a grep audit for `dangerouslySetInnerHTML` across `frontend/src` and confirm zero matches.
    4. Run a grep audit for `visible_from_order` inside `frontend/src/components/graph/graphElements.ts` and `frontend/src/components/graph/GraphCanvas.tsx` and confirm the only matches are type/data-field references (e.g. reading `node.visible_from_order` for display), never a filter/exclude condition (e.g. no `.filter(...)`/`if (... visible_from_order ...) { skip }` pattern).
    5. Manually run the Definition-of-Done demo script against `npm run dev` + the live backend: select Dexter, select S01E01, inspect a character node (Overview/Claims/Evidence tabs populate), inspect a claim-backed relationship edge, click a PART_OF/PRECEDES structural edge (minimal card, no tabs), attempt to advance to S01E02 (confirmation modal appears with forward copy), confirm, and verify new S01E02 nodes/relationships appear that were not visible before confirmation.
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe which check failed and its output.</resume-signal>
</task>

</tasks>

## Artifacts This Phase Produces

Symbols/files newly created by this plan:

- `frontend/src/hooks/useWatchProgress.test.ts` - hydration-guard, corruption-fallback, same-episode no-op unit tests
- `frontend/src/components/episode/ConfirmAdvanceModal.test.tsx` - forward/backward copy and confirm/cancel callback tests
- `.planning/phases/02-polished-cytoscape-graph-experience/02-VALIDATION.md` (updated) - Per-Task Verification Map filled in, `nyquist_compliant: true`, `wave_0_complete: true`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Whole-tree audit boundary | This plan is the first point where mitigations introduced in Plans 01-03 are verified to hold across the entire `frontend/src` tree, not just the file that introduced them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering | whole `frontend/src` tree | high | mitigate | Task 2's grep audit for `dangerouslySetInnerHTML` covers every component added across all four plans, not just DetailPanel/StructuralEdgeCard where the risk was first identified |
| T-02-13 | Information Disclosure | whole `frontend/src` tree | high | mitigate | Task 2's grep audit for `visible_from_order` filter patterns in graphElements.ts/GraphCanvas.tsx confirms no client-side re-filtering was introduced in any later plan |
| T-02-14 | Repudiation | Manual UAT step lacking an automated trace | low | accept | The Definition-of-Done demo script is inherently a cross-cutting UX flow (graph + modal + detail panel together); 02-VALIDATION.md's Manual-Only Verifications table already documents this as an accepted manual-only check, matching 02-RESEARCH.md Assumption A3 |

</threat_model>

<verification>
- `cd frontend && npm run test -- --run` (full suite across all four plans) passes with 0 failures
- `cd frontend && npm run build && npm run lint` both exit 0
- Grep audits for `dangerouslySetInnerHTML` and client-side `visible_from_order` filtering both come back clean
- Conversational UAT confirms the full Definition-of-Done demo script end-to-end
</verification>

<success_criteria>
UI-05 is satisfied: build, lint, and the full component-test suite all pass together for the first time, the two highest-severity threat mitigations are verified tree-wide (not just where first written), and a conversational UAT pass confirms the phase success criteria - the user selects Dexter and S01E01, sees only allowed Cytoscape elements, opens character/claim details and evidence, receives confirmation before advancing, and sees newly unlocked data after confirmation.
</success_criteria>

<output>
Create `.planning/phases/02-polished-cytoscape-graph-experience/02-04-SUMMARY.md` when done
</output>
